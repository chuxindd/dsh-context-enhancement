import { t as __exportAll } from "./rolldown-runtime-8H4AJuhK.js";
import { $ as ZodString, $a as $ZodCUID, $i as _positive, $n as uuid, $o as $ZodTuple, $r as _discriminatedUnion, $s as encodeAsync, $t as int, A as ZodJWT, Aa as _uuid, Ai as _mac, An as preprocess, Ao as $ZodNanoID, Ar as initializeContext, As as $ZodCheckOverwrite, At as boolean$1, B as ZodNull, Ba as $input, Bi as _nativeEnum, Bn as stringbool, Bo as $ZodPipe, Br as _check, Bs as _encode, Bt as discriminatedUnion, C as ZodExactOptional, Ca as _uint64, Cc as $constructor, Ci as _ksuid, Cn as nullish, Co as $ZodJWT, Cr as iso_exports, Cs as $ZodCheckMaxLength, Ct as _undefined$1, D as ZodIPv4, Da as _unknown, Di as _lowercase, Dn as partialRecord, Do as $ZodMAC, Dr as createToJSONSchemaMethod, Ds as $ZodCheckMinSize, Dt as base64, E as ZodGUID, Ea as _union, Ec as globalConfig, Ei as _literal, En as optional, Eo as $ZodLiteral, Er as createStandardJSONSchemaMethod, Es as $ZodCheckMinLength, Et as array, F as ZodMap, Fa as _xid, Fi as _minLength, Fn as schemas_exports, Fo as $ZodNumber, Fr as _base64, Fs as $ZodCheckStringFormat, Ft as cuid, G as ZodOptional, Ga as $ZodArray, Gi as _nonpositive, Gn as transform, Go as $ZodRecord, Gr as _coercedDate, Gs as _safeDecodeAsync, Gt as file, H as ZodNumber, Ha as globalRegistry, Hi as _never, Hn as superRefine, Ho as $ZodPreprocess, Hr as _cidrv6, Hs as _parse, Ht as email, I as ZodNaN, Ia as _xor, Ii as _minSize, In as set, Io as $ZodNumberFormat, Ir as _base64url, Is as $ZodCheckUpperCase, It as cuid2, J as ZodPreprocess, Ja as $ZodBigInt, Ji as _nullable, Jn as uint64, Jo as $ZodStringFormat, Jr as _cuid, Js as _safeParse, Jt as guid, K as ZodPipe, Ka as $ZodBase64, Ki as _normalize, Kn as tuple, Ko as $ZodSet, Kr as _coercedNumber, Ks as _safeEncode, Kt as float32, L as ZodNanoID, La as describe, Li as _multipleOf, Ln as strictObject, Lo as $ZodObject, Lr as _bigint, Ls as regexes_exports, Lt as custom, M as ZodLazy, Ma as _uuidv6, Mi as _maxLength, Mn as readonly, Mo as $ZodNonOptional, Mr as TimePrecision, Ms as $ZodCheckRegex, Mt as cidrv4, N as ZodLiteral, Na as _uuidv7, Ni as _maxSize, Nn as record, No as $ZodNull, Nr as _any, Ns as $ZodCheckSizeEquals, Nt as cidrv6, O as ZodIPv6, Oa as _uppercase, Oi as _lt, On as pipe, Oo as $ZodMap, Or as extractDefs, Os as $ZodCheckMultipleOf, Ot as base64url, P as ZodMAC, Pa as _void, Pi as _mime, Pn as refine, Po as $ZodNullable, Pr as _array, Ps as $ZodCheckStartsWith, Pt as codec, Q as ZodSet, Qa as $ZodCIDRv6, Qi as _pipe, Qn as url, Qo as $ZodTransform, Qr as _default, Qs as encode, Qt as httpUrl, R as ZodNever, Ra as meta, Ri as _nan, Rn as string$1, Ro as $ZodObjectJIT, Rr as _boolean, Rs as _decode, Rt as date$1, S as ZodEnum, Sa as _uint32, Sc as $brand, Si as _jwt, Sn as nullable, So as $ZodIntersection, Sr as ZodISOTime, Ss as $ZodCheckLowerCase, St as _null$1, T as ZodFunction, Ta as _undefined, Tc as config, Ti as _length, Tn as object, To as $ZodLazy, Tr as toJSONSchema, Ts as $ZodCheckMimeType, Tt as any, U as ZodNumberFormat, Ua as registry, Ui as _nonnegative, Un as symbol, Uo as $ZodPromise, Ur as _coercedBigint, Us as _parseAsync, Ut as emoji, V as ZodNullable, Va as $output, Vi as _negative, Vn as success, Vo as $ZodPrefault, Vr as _cidrv4, Vs as _encodeAsync, Vt as e164, W as ZodObject, Wa as $ZodAny, Wi as _nonoptional, Wn as templateLiteral, Wo as $ZodReadonly, Wr as _coercedBoolean, Ws as _safeDecode, Wt as exactOptional, X as ZodReadonly, Xa as $ZodBoolean, Xi as _optional, Xn as union, Xo as $ZodSymbol, Xr as _custom, Xs as decode, Xt as hex, Y as ZodPromise, Ya as $ZodBigIntFormat, Yi as _number, Yn as ulid, Yo as $ZodSuccess, Yr as _cuid2, Ys as _safeParseAsync, Yt as hash, Z as ZodRecord, Za as $ZodCIDRv4, Zi as _overwrite, Zn as unknown, Zo as $ZodTemplateLiteral, Zr as _date, Zs as decodeAsync, Zt as hostname, _ as ZodDefault, _a as _toLowerCase, _c as parsedType, _i as _ipv6, _n as nan, _o as $ZodIPv6, _r as ZodError, _s as $ZodCheckEndsWith, _t as _catch$1, a as ZodBigInt, aa as _regex, ac as safeEncodeAsync, ai as _file, an as ipv6, ao as $ZodDate, ar as decode$1, as as $ZodUnion, at as ZodTuple, b as ZodEmail, ba as _trim, bc as $ZodAsyncError, bi as _isoDuration, bn as never, bo as $ZodISODuration, br as ZodISODateTime, bs as $ZodCheckLengthEquals, bt as _function, c as ZodCIDRv4, ca as _slugify, cc as $ZodError, ci as _gt, cn as keyof, co as $ZodE164, cr as encodeAsync$1, cs as $ZodXID, ct as ZodURL, d as ZodCUID2, da as _stringFormat, dc as formatError, di as _includes, dn as literal, do as $ZodEnum, dr as safeDecode$1, ds as isValidBase64URL, dt as ZodUnion, ea as _promise, ec as parse, ei as _e164, en as int32, eo as $ZodCUID2, er as uuidv4, es as $ZodType, et as ZodStringFormat, f as ZodCatch, fa as _stringbool, fc as prettifyError, fi as _int, fn as looseObject, fo as $ZodExactOptional, fr as safeDecodeAsync$1, fs as isValidJWT, ft as ZodUnknown, g as ZodDate, ga as _templateLiteral, gc as joinValues, gi as _ipv4, gn as meta$1, go as $ZodIPv4, gr as safeParseAsync$1, gs as $ZodCheckBigIntFormat, gt as _ZodString, h as ZodCustomStringFormat, ha as _symbol, hc as clone, hi as _intersection, hn as map, ho as $ZodGUID, hr as safeParse$1, hs as $ZodCheck, ht as ZodXor, i as ZodBase64URL, ia as _refine, ic as safeEncode, ii as _enum, in as ipv4, io as $ZodCustomStringFormat, ir as xor, is as $ZodUndefined, it as ZodTransform, j as ZodKSUID, ja as _uuidv4, ji as _map, jn as promise, jo as $ZodNever, jr as process, js as $ZodCheckProperty, jt as check, k as ZodIntersection, ka as _url, ki as _lte, kn as prefault, ko as $ZodNaN, kr as finalize, ks as $ZodCheckNumberFormat, kt as bigint$1, l as ZodCIDRv6, la as _startsWith, lc as $ZodRealError, li as _gte, ln as ksuid, lo as $ZodEmail, lr as parse$1, ls as $ZodXor, lt as ZodUUID, m as ZodCustom, ma as _superRefine, mc as treeifyError, mi as _int64, mn as mac, mo as $ZodFunction, mr as safeEncodeAsync$1, ms as Doc, mt as ZodXID, n as ZodArray, na as _readonly, nc as safeDecode, ni as _emoji, nn as intersection, no as $ZodCodec, nr as uuidv7, ns as $ZodURL, nt as ZodSymbol, o as ZodBigIntFormat, oa as _set, oc as safeParse, oi as _float32, on as json, oo as $ZodDefault, or as decodeAsync$1, os as $ZodUnknown, ot as ZodType, p as ZodCodec, pa as _success, pc as toDotPath, pi as _int32, pn as looseRecord, po as $ZodFile, pr as safeEncode$1, ps as version, pt as ZodVoid, q as ZodPrefault, qa as $ZodBase64URL, qi as _null, qn as uint32, qo as $ZodString, qr as _coercedString, qs as _safeEncodeAsync, qt as float64, r as ZodBase64, ra as _record, rc as safeDecodeAsync, ri as _endsWith, rn as invertCodec, ro as $ZodCustom, rr as xid, rs as $ZodUUID, rt as ZodTemplateLiteral, s as ZodBoolean, sa as _size, sc as safeParseAsync, si as _float64, sn as jwt, so as $ZodDiscriminatedUnion, sr as encode$1, ss as $ZodVoid, st as ZodULID, t as ZodAny, ta as _property, tc as parseAsync, ti as _email, tn as int64, to as $ZodCatch, tr as uuidv6, ts as $ZodULID, tt as ZodSuccess, u as ZodCUID, ua as _string, uc as flattenError, ui as _guid, un as lazy, uo as $ZodEmoji, ur as parseAsync$1, us as isValidBase64, ut as ZodUndefined, v as ZodDiscriminatedUnion, va as _toUpperCase, vc as stringifyPrimitive, vi as _isoDate, vn as nanoid, vo as $ZodISODate, vr as ZodRealError, vs as $ZodCheckGreaterThan, vt as _default$1, w as ZodFile, wa as _ulid, wc as NEVER, wi as _lazy, wn as number$1, wo as $ZodKSUID, wr as allProcessors, ws as $ZodCheckMaxSize, wt as _void$1, x as ZodEmoji, xa as _tuple, xc as $ZodEncodeError, xi as _isoTime, xn as nonoptional, xo as $ZodISOTime, xr as ZodISODuration, xs as $ZodCheckLessThan, xt as _instanceof, y as ZodE164, ya as _transform, yc as util_exports, yi as _isoDateTime, yn as nativeEnum, yo as $ZodISODateTime, yr as ZodISODate, ys as $ZodCheckIncludes, yt as _enum$1, z as ZodNonOptional, za as $ZodRegistry, zi as _nanoid, zn as stringFormat, zo as $ZodOptional, zr as _catch, zs as _decodeAsync, zt as describe$1 } from "./schemas-B9RBVgB9.js";
import { o as isTaskStateSlotSource } from "./contract-C2nAZOQi.js";
import { a as createUserMessage, c as brandNumber, i as contentHasImage, l as assertNever, m as Schema, o as errorChain, r as LlmError, s as freezeMessage, t as BlockAssembler, u as deepFreeze } from "./lib-UkEFLxaM.js";
import { n as defineDomain, r as domainTable, t as DomainError } from "./lib-Bq6jN40l.js";
import { a as isCompactCheckpointSource, f as toolPairingBalancedAfter, i as isReplacementSurfaceEvent, n as reductionProvenance, o as selectToolGroups, p as toolPairingBalancedBefore, r as shadowPriceWithProvenance, s as toolResultTextLength, t as buildSurfaceSourceIndex } from "./source-index-BiRfSNPU.js";
import { Service } from "@deepseek-ai/cordis";
import { createHash, randomUUID } from "node:crypto";
import { isDeepStrictEqual } from "node:util";
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ar.js
const error$49 = () => {
	const Sizable = {
		string: {
			unit: "حرف",
			verb: "أن يحوي"
		},
		file: {
			unit: "بايت",
			verb: "أن يحوي"
		},
		array: {
			unit: "عنصر",
			verb: "أن يحوي"
		},
		set: {
			unit: "عنصر",
			verb: "أن يحوي"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "مدخل",
		email: "بريد إلكتروني",
		url: "رابط",
		emoji: "إيموجي",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "تاريخ ووقت بمعيار ISO",
		date: "تاريخ بمعيار ISO",
		time: "وقت بمعيار ISO",
		duration: "مدة بمعيار ISO",
		ipv4: "عنوان IPv4",
		ipv6: "عنوان IPv6",
		cidrv4: "مدى عناوين بصيغة IPv4",
		cidrv6: "مدى عناوين بصيغة IPv6",
		base64: "نَص بترميز base64-encoded",
		base64url: "نَص بترميز base64url-encoded",
		json_string: "نَص على هيئة JSON",
		e164: "رقم هاتف بمعيار E.164",
		jwt: "JWT",
		template_literal: "مدخل"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `مدخلات غير مقبولة: يفترض إدخال instanceof ${issue.expected}، ولكن تم إدخال ${received}`;
				return `مدخلات غير مقبولة: يفترض إدخال ${expected}، ولكن تم إدخال ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `مدخلات غير مقبولة: يفترض إدخال ${stringifyPrimitive(issue.values[0])}`;
				return `اختيار غير مقبول: يتوقع انتقاء أحد هذه الخيارات: ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return ` أكبر من اللازم: يفترض أن تكون ${issue.origin ?? "القيمة"} ${adj} ${issue.maximum.toString()} ${sizing.unit ?? "عنصر"}`;
				return `أكبر من اللازم: يفترض أن تكون ${issue.origin ?? "القيمة"} ${adj} ${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `أصغر من اللازم: يفترض لـ ${issue.origin} أن يكون ${adj} ${issue.minimum.toString()} ${sizing.unit}`;
				return `أصغر من اللازم: يفترض لـ ${issue.origin} أن يكون ${adj} ${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `نَص غير مقبول: يجب أن يبدأ بـ "${issue.prefix}"`;
				if (_issue.format === "ends_with") return `نَص غير مقبول: يجب أن ينتهي بـ "${_issue.suffix}"`;
				if (_issue.format === "includes") return `نَص غير مقبول: يجب أن يتضمَّن "${_issue.includes}"`;
				if (_issue.format === "regex") return `نَص غير مقبول: يجب أن يطابق النمط ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} غير مقبول`;
			}
			case "not_multiple_of": return `رقم غير مقبول: يجب أن يكون من مضاعفات ${issue.divisor}`;
			case "unrecognized_keys": return `معرف${issue.keys.length > 1 ? "ات" : ""} غريب${issue.keys.length > 1 ? "ة" : ""}: ${joinValues(issue.keys, "، ")}`;
			case "invalid_key": return `معرف غير مقبول في ${issue.origin}`;
			case "invalid_union": return "مدخل غير مقبول";
			case "invalid_element": return `مدخل غير مقبول في ${issue.origin}`;
			default: return "مدخل غير مقبول";
		}
	};
};
function ar_default() {
	return { localeError: error$49() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/az.js
const error$48 = () => {
	const Sizable = {
		string: {
			unit: "simvol",
			verb: "olmalıdır"
		},
		file: {
			unit: "bayt",
			verb: "olmalıdır"
		},
		array: {
			unit: "element",
			verb: "olmalıdır"
		},
		set: {
			unit: "element",
			verb: "olmalıdır"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "email address",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datetime",
		date: "ISO date",
		time: "ISO time",
		duration: "ISO duration",
		ipv4: "IPv4 address",
		ipv6: "IPv6 address",
		cidrv4: "IPv4 range",
		cidrv6: "IPv6 range",
		base64: "base64-encoded string",
		base64url: "base64url-encoded string",
		json_string: "JSON string",
		e164: "E.164 number",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Yanlış dəyər: gözlənilən instanceof ${issue.expected}, daxil olan ${received}`;
				return `Yanlış dəyər: gözlənilən ${expected}, daxil olan ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Yanlış dəyər: gözlənilən ${stringifyPrimitive(issue.values[0])}`;
				return `Yanlış seçim: aşağıdakılardan biri olmalıdır: ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Çox böyük: gözlənilən ${issue.origin ?? "dəyər"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "element"}`;
				return `Çox böyük: gözlənilən ${issue.origin ?? "dəyər"} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Çox kiçik: gözlənilən ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Çox kiçik: gözlənilən ${issue.origin} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Yanlış mətn: "${_issue.prefix}" ilə başlamalıdır`;
				if (_issue.format === "ends_with") return `Yanlış mətn: "${_issue.suffix}" ilə bitməlidir`;
				if (_issue.format === "includes") return `Yanlış mətn: "${_issue.includes}" daxil olmalıdır`;
				if (_issue.format === "regex") return `Yanlış mətn: ${_issue.pattern} şablonuna uyğun olmalıdır`;
				return `Yanlış ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Yanlış ədəd: ${issue.divisor} ilə bölünə bilən olmalıdır`;
			case "unrecognized_keys": return `Tanınmayan açar${issue.keys.length > 1 ? "lar" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `${issue.origin} daxilində yanlış açar`;
			case "invalid_union": return "Yanlış dəyər";
			case "invalid_element": return `${issue.origin} daxilində yanlış dəyər`;
			default: return `Yanlış dəyər`;
		}
	};
};
function az_default() {
	return { localeError: error$48() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/be.js
function getBelarusianPlural(count, one, few, many) {
	const absCount = Math.abs(count);
	const lastDigit = absCount % 10;
	const lastTwoDigits = absCount % 100;
	if (lastTwoDigits >= 11 && lastTwoDigits <= 19) return many;
	if (lastDigit === 1) return one;
	if (lastDigit >= 2 && lastDigit <= 4) return few;
	return many;
}
const error$47 = () => {
	const Sizable = {
		string: {
			unit: {
				one: "сімвал",
				few: "сімвалы",
				many: "сімвалаў"
			},
			verb: "мець"
		},
		array: {
			unit: {
				one: "элемент",
				few: "элементы",
				many: "элементаў"
			},
			verb: "мець"
		},
		set: {
			unit: {
				one: "элемент",
				few: "элементы",
				many: "элементаў"
			},
			verb: "мець"
		},
		file: {
			unit: {
				one: "байт",
				few: "байты",
				many: "байтаў"
			},
			verb: "мець"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "увод",
		email: "email адрас",
		url: "URL",
		emoji: "эмодзі",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO дата і час",
		date: "ISO дата",
		time: "ISO час",
		duration: "ISO працягласць",
		ipv4: "IPv4 адрас",
		ipv6: "IPv6 адрас",
		cidrv4: "IPv4 дыяпазон",
		cidrv6: "IPv6 дыяпазон",
		base64: "радок у фармаце base64",
		base64url: "радок у фармаце base64url",
		json_string: "JSON радок",
		e164: "нумар E.164",
		jwt: "JWT",
		template_literal: "увод"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "лік",
		array: "масіў"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Няправільны ўвод: чакаўся instanceof ${issue.expected}, атрымана ${received}`;
				return `Няправільны ўвод: чакаўся ${expected}, атрымана ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Няправільны ўвод: чакалася ${stringifyPrimitive(issue.values[0])}`;
				return `Няправільны варыянт: чакаўся адзін з ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) {
					const unit = getBelarusianPlural(Number(issue.maximum), sizing.unit.one, sizing.unit.few, sizing.unit.many);
					return `Занадта вялікі: чакалася, што ${issue.origin ?? "значэнне"} павінна ${sizing.verb} ${adj}${issue.maximum.toString()} ${unit}`;
				}
				return `Занадта вялікі: чакалася, што ${issue.origin ?? "значэнне"} павінна быць ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) {
					const unit = getBelarusianPlural(Number(issue.minimum), sizing.unit.one, sizing.unit.few, sizing.unit.many);
					return `Занадта малы: чакалася, што ${issue.origin} павінна ${sizing.verb} ${adj}${issue.minimum.toString()} ${unit}`;
				}
				return `Занадта малы: чакалася, што ${issue.origin} павінна быць ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Няправільны радок: павінен пачынацца з "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Няправільны радок: павінен заканчвацца на "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Няправільны радок: павінен змяшчаць "${_issue.includes}"`;
				if (_issue.format === "regex") return `Няправільны радок: павінен адпавядаць шаблону ${_issue.pattern}`;
				return `Няправільны ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Няправільны лік: павінен быць кратным ${issue.divisor}`;
			case "unrecognized_keys": return `Нераспазнаны ${issue.keys.length > 1 ? "ключы" : "ключ"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Няправільны ключ у ${issue.origin}`;
			case "invalid_union": return "Няправільны ўвод";
			case "invalid_element": return `Няправільнае значэнне ў ${issue.origin}`;
			default: return `Няправільны ўвод`;
		}
	};
};
function be_default() {
	return { localeError: error$47() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/bg.js
const error$46 = () => {
	const Sizable = {
		string: {
			unit: "символа",
			verb: "да съдържа"
		},
		file: {
			unit: "байта",
			verb: "да съдържа"
		},
		array: {
			unit: "елемента",
			verb: "да съдържа"
		},
		set: {
			unit: "елемента",
			verb: "да съдържа"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "вход",
		email: "имейл адрес",
		url: "URL",
		emoji: "емоджи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO време",
		date: "ISO дата",
		time: "ISO време",
		duration: "ISO продължителност",
		ipv4: "IPv4 адрес",
		ipv6: "IPv6 адрес",
		cidrv4: "IPv4 диапазон",
		cidrv6: "IPv6 диапазон",
		base64: "base64-кодиран низ",
		base64url: "base64url-кодиран низ",
		json_string: "JSON низ",
		e164: "E.164 номер",
		jwt: "JWT",
		template_literal: "вход"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "число",
		array: "масив"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Невалиден вход: очакван instanceof ${issue.expected}, получен ${received}`;
				return `Невалиден вход: очакван ${expected}, получен ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Невалиден вход: очакван ${stringifyPrimitive(issue.values[0])}`;
				return `Невалидна опция: очаквано едно от ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Твърде голямо: очаква се ${issue.origin ?? "стойност"} да съдържа ${adj}${issue.maximum.toString()} ${sizing.unit ?? "елемента"}`;
				return `Твърде голямо: очаква се ${issue.origin ?? "стойност"} да бъде ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Твърде малко: очаква се ${issue.origin} да съдържа ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Твърде малко: очаква се ${issue.origin} да бъде ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Невалиден низ: трябва да започва с "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Невалиден низ: трябва да завършва с "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Невалиден низ: трябва да включва "${_issue.includes}"`;
				if (_issue.format === "regex") return `Невалиден низ: трябва да съвпада с ${_issue.pattern}`;
				let invalid_adj = "Невалиден";
				if (_issue.format === "emoji") invalid_adj = "Невалидно";
				if (_issue.format === "datetime") invalid_adj = "Невалидно";
				if (_issue.format === "date") invalid_adj = "Невалидна";
				if (_issue.format === "time") invalid_adj = "Невалидно";
				if (_issue.format === "duration") invalid_adj = "Невалидна";
				return `${invalid_adj} ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Невалидно число: трябва да бъде кратно на ${issue.divisor}`;
			case "unrecognized_keys": return `Неразпознат${issue.keys.length > 1 ? "и" : ""} ключ${issue.keys.length > 1 ? "ове" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Невалиден ключ в ${issue.origin}`;
			case "invalid_union": return "Невалиден вход";
			case "invalid_element": return `Невалидна стойност в ${issue.origin}`;
			default: return `Невалиден вход`;
		}
	};
};
function bg_default() {
	return { localeError: error$46() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ca.js
const error$45 = () => {
	const Sizable = {
		string: {
			unit: "caràcters",
			verb: "contenir"
		},
		file: {
			unit: "bytes",
			verb: "contenir"
		},
		array: {
			unit: "elements",
			verb: "contenir"
		},
		set: {
			unit: "elements",
			verb: "contenir"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "entrada",
		email: "adreça electrònica",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data i hora ISO",
		date: "data ISO",
		time: "hora ISO",
		duration: "durada ISO",
		ipv4: "adreça IPv4",
		ipv6: "adreça IPv6",
		cidrv4: "rang IPv4",
		cidrv6: "rang IPv6",
		base64: "cadena codificada en base64",
		base64url: "cadena codificada en base64url",
		json_string: "cadena JSON",
		e164: "número E.164",
		jwt: "JWT",
		template_literal: "entrada"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Tipus invàlid: s'esperava instanceof ${issue.expected}, s'ha rebut ${received}`;
				return `Tipus invàlid: s'esperava ${expected}, s'ha rebut ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Valor invàlid: s'esperava ${stringifyPrimitive(issue.values[0])}`;
				return `Opció invàlida: s'esperava una de ${joinValues(issue.values, " o ")}`;
			case "too_big": {
				const adj = issue.inclusive ? "com a màxim" : "menys de";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Massa gran: s'esperava que ${issue.origin ?? "el valor"} contingués ${adj} ${issue.maximum.toString()} ${sizing.unit ?? "elements"}`;
				return `Massa gran: s'esperava que ${issue.origin ?? "el valor"} fos ${adj} ${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? "com a mínim" : "més de";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Massa petit: s'esperava que ${issue.origin} contingués ${adj} ${issue.minimum.toString()} ${sizing.unit}`;
				return `Massa petit: s'esperava que ${issue.origin} fos ${adj} ${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Format invàlid: ha de començar amb "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Format invàlid: ha d'acabar amb "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Format invàlid: ha d'incloure "${_issue.includes}"`;
				if (_issue.format === "regex") return `Format invàlid: ha de coincidir amb el patró ${_issue.pattern}`;
				return `Format invàlid per a ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Número invàlid: ha de ser múltiple de ${issue.divisor}`;
			case "unrecognized_keys": return `Clau${issue.keys.length > 1 ? "s" : ""} no reconeguda${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Clau invàlida a ${issue.origin}`;
			case "invalid_union": return "Entrada invàlida";
			case "invalid_element": return `Element invàlid a ${issue.origin}`;
			default: return `Entrada invàlida`;
		}
	};
};
function ca_default() {
	return { localeError: error$45() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/cs.js
const error$44 = () => {
	const Sizable = {
		string: {
			unit: "znaků",
			verb: "mít"
		},
		file: {
			unit: "bajtů",
			verb: "mít"
		},
		array: {
			unit: "prvků",
			verb: "mít"
		},
		set: {
			unit: "prvků",
			verb: "mít"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "regulární výraz",
		email: "e-mailová adresa",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "datum a čas ve formátu ISO",
		date: "datum ve formátu ISO",
		time: "čas ve formátu ISO",
		duration: "doba trvání ISO",
		ipv4: "IPv4 adresa",
		ipv6: "IPv6 adresa",
		cidrv4: "rozsah IPv4",
		cidrv6: "rozsah IPv6",
		base64: "řetězec zakódovaný ve formátu base64",
		base64url: "řetězec zakódovaný ve formátu base64url",
		json_string: "řetězec ve formátu JSON",
		e164: "číslo E.164",
		jwt: "JWT",
		template_literal: "vstup"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "číslo",
		string: "řetězec",
		function: "funkce",
		array: "pole"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Neplatný vstup: očekáváno instanceof ${issue.expected}, obdrženo ${received}`;
				return `Neplatný vstup: očekáváno ${expected}, obdrženo ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Neplatný vstup: očekáváno ${stringifyPrimitive(issue.values[0])}`;
				return `Neplatná možnost: očekávána jedna z hodnot ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Hodnota je příliš velká: ${issue.origin ?? "hodnota"} musí mít ${adj}${issue.maximum.toString()} ${sizing.unit ?? "prvků"}`;
				return `Hodnota je příliš velká: ${issue.origin ?? "hodnota"} musí být ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Hodnota je příliš malá: ${issue.origin ?? "hodnota"} musí mít ${adj}${issue.minimum.toString()} ${sizing.unit ?? "prvků"}`;
				return `Hodnota je příliš malá: ${issue.origin ?? "hodnota"} musí být ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Neplatný řetězec: musí začínat na "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Neplatný řetězec: musí končit na "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Neplatný řetězec: musí obsahovat "${_issue.includes}"`;
				if (_issue.format === "regex") return `Neplatný řetězec: musí odpovídat vzoru ${_issue.pattern}`;
				return `Neplatný formát ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Neplatné číslo: musí být násobkem ${issue.divisor}`;
			case "unrecognized_keys": return `Neznámé klíče: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Neplatný klíč v ${issue.origin}`;
			case "invalid_union": return "Neplatný vstup";
			case "invalid_element": return `Neplatná hodnota v ${issue.origin}`;
			default: return `Neplatný vstup`;
		}
	};
};
function cs_default() {
	return { localeError: error$44() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/da.js
const error$43 = () => {
	const Sizable = {
		string: {
			unit: "tegn",
			verb: "havde"
		},
		file: {
			unit: "bytes",
			verb: "havde"
		},
		array: {
			unit: "elementer",
			verb: "indeholdt"
		},
		set: {
			unit: "elementer",
			verb: "indeholdt"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "e-mailadresse",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dato- og klokkeslæt",
		date: "ISO-dato",
		time: "ISO-klokkeslæt",
		duration: "ISO-varighed",
		ipv4: "IPv4-område",
		ipv6: "IPv6-område",
		cidrv4: "IPv4-spektrum",
		cidrv6: "IPv6-spektrum",
		base64: "base64-kodet streng",
		base64url: "base64url-kodet streng",
		json_string: "JSON-streng",
		e164: "E.164-nummer",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = {
		nan: "NaN",
		string: "streng",
		number: "tal",
		boolean: "boolean",
		array: "liste",
		object: "objekt",
		set: "sæt",
		file: "fil"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Ugyldigt input: forventede instanceof ${issue.expected}, fik ${received}`;
				return `Ugyldigt input: forventede ${expected}, fik ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Ugyldig værdi: forventede ${stringifyPrimitive(issue.values[0])}`;
				return `Ugyldigt valg: forventede en af følgende ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				if (sizing) return `For stor: forventede ${origin ?? "value"} ${sizing.verb} ${adj} ${issue.maximum.toString()} ${sizing.unit ?? "elementer"}`;
				return `For stor: forventede ${origin ?? "value"} havde ${adj} ${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				if (sizing) return `For lille: forventede ${origin} ${sizing.verb} ${adj} ${issue.minimum.toString()} ${sizing.unit}`;
				return `For lille: forventede ${origin} havde ${adj} ${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ugyldig streng: skal starte med "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Ugyldig streng: skal ende med "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Ugyldig streng: skal indeholde "${_issue.includes}"`;
				if (_issue.format === "regex") return `Ugyldig streng: skal matche mønsteret ${_issue.pattern}`;
				return `Ugyldig ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Ugyldigt tal: skal være deleligt med ${issue.divisor}`;
			case "unrecognized_keys": return `${issue.keys.length > 1 ? "Ukendte nøgler" : "Ukendt nøgle"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Ugyldig nøgle i ${issue.origin}`;
			case "invalid_union": return "Ugyldigt input: matcher ingen af de tilladte typer";
			case "invalid_element": return `Ugyldig værdi i ${issue.origin}`;
			default: return `Ugyldigt input`;
		}
	};
};
function da_default() {
	return { localeError: error$43() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/de.js
const error$42 = () => {
	const Sizable = {
		string: {
			unit: "Zeichen",
			verb: "zu haben"
		},
		file: {
			unit: "Bytes",
			verb: "zu haben"
		},
		array: {
			unit: "Elemente",
			verb: "zu haben"
		},
		set: {
			unit: "Elemente",
			verb: "zu haben"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "Eingabe",
		email: "E-Mail-Adresse",
		url: "URL",
		emoji: "Emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-Datum und -Uhrzeit",
		date: "ISO-Datum",
		time: "ISO-Uhrzeit",
		duration: "ISO-Dauer",
		ipv4: "IPv4-Adresse",
		ipv6: "IPv6-Adresse",
		cidrv4: "IPv4-Bereich",
		cidrv6: "IPv6-Bereich",
		base64: "Base64-codierter String",
		base64url: "Base64-URL-codierter String",
		json_string: "JSON-String",
		e164: "E.164-Nummer",
		jwt: "JWT",
		template_literal: "Eingabe"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "Zahl",
		array: "Array"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Ungültige Eingabe: erwartet instanceof ${issue.expected}, erhalten ${received}`;
				return `Ungültige Eingabe: erwartet ${expected}, erhalten ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Ungültige Eingabe: erwartet ${stringifyPrimitive(issue.values[0])}`;
				return `Ungültige Option: erwartet eine von ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Zu groß: erwartet, dass ${issue.origin ?? "Wert"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "Elemente"} hat`;
				return `Zu groß: erwartet, dass ${issue.origin ?? "Wert"} ${adj}${issue.maximum.toString()} ist`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Zu klein: erwartet, dass ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit} hat`;
				return `Zu klein: erwartet, dass ${issue.origin} ${adj}${issue.minimum.toString()} ist`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ungültiger String: muss mit "${_issue.prefix}" beginnen`;
				if (_issue.format === "ends_with") return `Ungültiger String: muss mit "${_issue.suffix}" enden`;
				if (_issue.format === "includes") return `Ungültiger String: muss "${_issue.includes}" enthalten`;
				if (_issue.format === "regex") return `Ungültiger String: muss dem Muster ${_issue.pattern} entsprechen`;
				return `Ungültig: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Ungültige Zahl: muss ein Vielfaches von ${issue.divisor} sein`;
			case "unrecognized_keys": return `${issue.keys.length > 1 ? "Unbekannte Schlüssel" : "Unbekannter Schlüssel"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Ungültiger Schlüssel in ${issue.origin}`;
			case "invalid_union": return "Ungültige Eingabe";
			case "invalid_element": return `Ungültiger Wert in ${issue.origin}`;
			default: return `Ungültige Eingabe`;
		}
	};
};
function de_default() {
	return { localeError: error$42() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/el.js
const error$41 = () => {
	const Sizable = {
		string: {
			unit: "χαρακτήρες",
			verb: "να έχει"
		},
		file: {
			unit: "bytes",
			verb: "να έχει"
		},
		array: {
			unit: "στοιχεία",
			verb: "να έχει"
		},
		set: {
			unit: "στοιχεία",
			verb: "να έχει"
		},
		map: {
			unit: "καταχωρήσεις",
			verb: "να έχει"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "είσοδος",
		email: "διεύθυνση email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO ημερομηνία και ώρα",
		date: "ISO ημερομηνία",
		time: "ISO ώρα",
		duration: "ISO διάρκεια",
		ipv4: "διεύθυνση IPv4",
		ipv6: "διεύθυνση IPv6",
		mac: "διεύθυνση MAC",
		cidrv4: "εύρος IPv4",
		cidrv6: "εύρος IPv6",
		base64: "συμβολοσειρά κωδικοποιημένη σε base64",
		base64url: "συμβολοσειρά κωδικοποιημένη σε base64url",
		json_string: "συμβολοσειρά JSON",
		e164: "αριθμός E.164",
		jwt: "JWT",
		template_literal: "είσοδος"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (typeof issue.expected === "string" && /^[A-Z]/.test(issue.expected)) return `Μη έγκυρη είσοδος: αναμενόταν instanceof ${issue.expected}, λήφθηκε ${received}`;
				return `Μη έγκυρη είσοδος: αναμενόταν ${expected}, λήφθηκε ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Μη έγκυρη είσοδος: αναμενόταν ${stringifyPrimitive(issue.values[0])}`;
				return `Μη έγκυρη επιλογή: αναμενόταν ένα από ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Πολύ μεγάλο: αναμενόταν ${issue.origin ?? "τιμή"} να έχει ${adj}${issue.maximum.toString()} ${sizing.unit ?? "στοιχεία"}`;
				return `Πολύ μεγάλο: αναμενόταν ${issue.origin ?? "τιμή"} να είναι ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Πολύ μικρό: αναμενόταν ${issue.origin} να έχει ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Πολύ μικρό: αναμενόταν ${issue.origin} να είναι ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Μη έγκυρη συμβολοσειρά: πρέπει να ξεκινά με "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Μη έγκυρη συμβολοσειρά: πρέπει να τελειώνει με "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Μη έγκυρη συμβολοσειρά: πρέπει να περιέχει "${_issue.includes}"`;
				if (_issue.format === "regex") return `Μη έγκυρη συμβολοσειρά: πρέπει να ταιριάζει με το μοτίβο ${_issue.pattern}`;
				return `Μη έγκυρο: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Μη έγκυρος αριθμός: πρέπει να είναι πολλαπλάσιο του ${issue.divisor}`;
			case "unrecognized_keys": return `Άγνωστ${issue.keys.length > 1 ? "α" : "ο"} κλειδ${issue.keys.length > 1 ? "ιά" : "ί"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Μη έγκυρο κλειδί στο ${issue.origin}`;
			case "invalid_union": return "Μη έγκυρη είσοδος";
			case "invalid_element": return `Μη έγκυρη τιμή στο ${issue.origin}`;
			default: return `Μη έγκυρη είσοδος`;
		}
	};
};
function el_default() {
	return { localeError: error$41() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/en.js
const error$40 = () => {
	const Sizable = {
		string: {
			unit: "characters",
			verb: "to have"
		},
		file: {
			unit: "bytes",
			verb: "to have"
		},
		array: {
			unit: "items",
			verb: "to have"
		},
		set: {
			unit: "items",
			verb: "to have"
		},
		map: {
			unit: "entries",
			verb: "to have"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "email address",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datetime",
		date: "ISO date",
		time: "ISO time",
		duration: "ISO duration",
		ipv4: "IPv4 address",
		ipv6: "IPv6 address",
		mac: "MAC address",
		cidrv4: "IPv4 range",
		cidrv6: "IPv6 range",
		base64: "base64-encoded string",
		base64url: "base64url-encoded string",
		json_string: "JSON string",
		e164: "E.164 number",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				return `Invalid input: expected ${expected}, received ${TypeDictionary[receivedType] ?? receivedType}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Invalid input: expected ${stringifyPrimitive(issue.values[0])}`;
				return `Invalid option: expected one of ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Too big: expected ${issue.origin ?? "value"} to have ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elements"}`;
				return `Too big: expected ${issue.origin ?? "value"} to be ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Too small: expected ${issue.origin} to have ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Too small: expected ${issue.origin} to be ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Invalid string: must start with "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Invalid string: must end with "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Invalid string: must include "${_issue.includes}"`;
				if (_issue.format === "regex") return `Invalid string: must match pattern ${_issue.pattern}`;
				return `Invalid ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Invalid number: must be a multiple of ${issue.divisor}`;
			case "unrecognized_keys": return `Unrecognized key${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Invalid key in ${issue.origin}`;
			case "invalid_union":
				if (issue.options && Array.isArray(issue.options) && issue.options.length > 0) return `Invalid discriminator value. Expected ${issue.options.map((o) => `'${o}'`).join(" | ")}`;
				return "Invalid input";
			case "invalid_element": return `Invalid value in ${issue.origin}`;
			default: return `Invalid input`;
		}
	};
};
function en_default() {
	return { localeError: error$40() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/eo.js
const error$39 = () => {
	const Sizable = {
		string: {
			unit: "karaktrojn",
			verb: "havi"
		},
		file: {
			unit: "bajtojn",
			verb: "havi"
		},
		array: {
			unit: "elementojn",
			verb: "havi"
		},
		set: {
			unit: "elementojn",
			verb: "havi"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "enigo",
		email: "retadreso",
		url: "URL",
		emoji: "emoĝio",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-datotempo",
		date: "ISO-dato",
		time: "ISO-tempo",
		duration: "ISO-daŭro",
		ipv4: "IPv4-adreso",
		ipv6: "IPv6-adreso",
		cidrv4: "IPv4-rango",
		cidrv6: "IPv6-rango",
		base64: "64-ume kodita karaktraro",
		base64url: "URL-64-ume kodita karaktraro",
		json_string: "JSON-karaktraro",
		e164: "E.164-nombro",
		jwt: "JWT",
		template_literal: "enigo"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "nombro",
		array: "tabelo",
		null: "senvalora"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Nevalida enigo: atendiĝis instanceof ${issue.expected}, riceviĝis ${received}`;
				return `Nevalida enigo: atendiĝis ${expected}, riceviĝis ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Nevalida enigo: atendiĝis ${stringifyPrimitive(issue.values[0])}`;
				return `Nevalida opcio: atendiĝis unu el ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Tro granda: atendiĝis ke ${issue.origin ?? "valoro"} havu ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementojn"}`;
				return `Tro granda: atendiĝis ke ${issue.origin ?? "valoro"} havu ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Tro malgranda: atendiĝis ke ${issue.origin} havu ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Tro malgranda: atendiĝis ke ${issue.origin} estu ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Nevalida karaktraro: devas komenciĝi per "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Nevalida karaktraro: devas finiĝi per "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Nevalida karaktraro: devas inkluzivi "${_issue.includes}"`;
				if (_issue.format === "regex") return `Nevalida karaktraro: devas kongrui kun la modelo ${_issue.pattern}`;
				return `Nevalida ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Nevalida nombro: devas esti oblo de ${issue.divisor}`;
			case "unrecognized_keys": return `Nekonata${issue.keys.length > 1 ? "j" : ""} ŝlosilo${issue.keys.length > 1 ? "j" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Nevalida ŝlosilo en ${issue.origin}`;
			case "invalid_union": return "Nevalida enigo";
			case "invalid_element": return `Nevalida valoro en ${issue.origin}`;
			default: return `Nevalida enigo`;
		}
	};
};
function eo_default() {
	return { localeError: error$39() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/es.js
const error$38 = () => {
	const Sizable = {
		string: {
			unit: "caracteres",
			verb: "tener"
		},
		file: {
			unit: "bytes",
			verb: "tener"
		},
		array: {
			unit: "elementos",
			verb: "tener"
		},
		set: {
			unit: "elementos",
			verb: "tener"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "entrada",
		email: "dirección de correo electrónico",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "fecha y hora ISO",
		date: "fecha ISO",
		time: "hora ISO",
		duration: "duración ISO",
		ipv4: "dirección IPv4",
		ipv6: "dirección IPv6",
		cidrv4: "rango IPv4",
		cidrv6: "rango IPv6",
		base64: "cadena codificada en base64",
		base64url: "URL codificada en base64",
		json_string: "cadena JSON",
		e164: "número E.164",
		jwt: "JWT",
		template_literal: "entrada"
	};
	const TypeDictionary = {
		nan: "NaN",
		string: "texto",
		number: "número",
		boolean: "booleano",
		array: "arreglo",
		object: "objeto",
		set: "conjunto",
		file: "archivo",
		date: "fecha",
		bigint: "número grande",
		symbol: "símbolo",
		undefined: "indefinido",
		null: "nulo",
		function: "función",
		map: "mapa",
		record: "registro",
		tuple: "tupla",
		enum: "enumeración",
		union: "unión",
		literal: "literal",
		promise: "promesa",
		void: "vacío",
		never: "nunca",
		unknown: "desconocido",
		any: "cualquiera"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Entrada inválida: se esperaba instanceof ${issue.expected}, recibido ${received}`;
				return `Entrada inválida: se esperaba ${expected}, recibido ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Entrada inválida: se esperaba ${stringifyPrimitive(issue.values[0])}`;
				return `Opción inválida: se esperaba una de ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				if (sizing) return `Demasiado grande: se esperaba que ${origin ?? "valor"} tuviera ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementos"}`;
				return `Demasiado grande: se esperaba que ${origin ?? "valor"} fuera ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				if (sizing) return `Demasiado pequeño: se esperaba que ${origin} tuviera ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Demasiado pequeño: se esperaba que ${origin} fuera ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Cadena inválida: debe comenzar con "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Cadena inválida: debe terminar en "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Cadena inválida: debe incluir "${_issue.includes}"`;
				if (_issue.format === "regex") return `Cadena inválida: debe coincidir con el patrón ${_issue.pattern}`;
				return `Inválido ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Número inválido: debe ser múltiplo de ${issue.divisor}`;
			case "unrecognized_keys": return `Llave${issue.keys.length > 1 ? "s" : ""} desconocida${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Llave inválida en ${TypeDictionary[issue.origin] ?? issue.origin}`;
			case "invalid_union": return "Entrada inválida";
			case "invalid_element": return `Valor inválido en ${TypeDictionary[issue.origin] ?? issue.origin}`;
			default: return `Entrada inválida`;
		}
	};
};
function es_default() {
	return { localeError: error$38() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/fa.js
const error$37 = () => {
	const Sizable = {
		string: {
			unit: "کاراکتر",
			verb: "داشته باشد"
		},
		file: {
			unit: "بایت",
			verb: "داشته باشد"
		},
		array: {
			unit: "آیتم",
			verb: "داشته باشد"
		},
		set: {
			unit: "آیتم",
			verb: "داشته باشد"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ورودی",
		email: "آدرس ایمیل",
		url: "URL",
		emoji: "ایموجی",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "تاریخ و زمان ایزو",
		date: "تاریخ ایزو",
		time: "زمان ایزو",
		duration: "مدت زمان ایزو",
		ipv4: "IPv4 آدرس",
		ipv6: "IPv6 آدرس",
		cidrv4: "IPv4 دامنه",
		cidrv6: "IPv6 دامنه",
		base64: "base64-encoded رشته",
		base64url: "base64url-encoded رشته",
		json_string: "JSON رشته",
		e164: "E.164 عدد",
		jwt: "JWT",
		template_literal: "ورودی"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "عدد",
		array: "آرایه"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `ورودی نامعتبر: می‌بایست instanceof ${issue.expected} می‌بود، ${received} دریافت شد`;
				return `ورودی نامعتبر: می‌بایست ${expected} می‌بود، ${received} دریافت شد`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `ورودی نامعتبر: می‌بایست ${stringifyPrimitive(issue.values[0])} می‌بود`;
				return `گزینه نامعتبر: می‌بایست یکی از ${joinValues(issue.values, "|")} می‌بود`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `خیلی بزرگ: ${issue.origin ?? "مقدار"} باید ${adj}${issue.maximum.toString()} ${sizing.unit ?? "عنصر"} باشد`;
				return `خیلی بزرگ: ${issue.origin ?? "مقدار"} باید ${adj}${issue.maximum.toString()} باشد`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `خیلی کوچک: ${issue.origin} باید ${adj}${issue.minimum.toString()} ${sizing.unit} باشد`;
				return `خیلی کوچک: ${issue.origin} باید ${adj}${issue.minimum.toString()} باشد`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `رشته نامعتبر: باید با "${_issue.prefix}" شروع شود`;
				if (_issue.format === "ends_with") return `رشته نامعتبر: باید با "${_issue.suffix}" تمام شود`;
				if (_issue.format === "includes") return `رشته نامعتبر: باید شامل "${_issue.includes}" باشد`;
				if (_issue.format === "regex") return `رشته نامعتبر: باید با الگوی ${_issue.pattern} مطابقت داشته باشد`;
				return `${FormatDictionary[_issue.format] ?? issue.format} نامعتبر`;
			}
			case "not_multiple_of": return `عدد نامعتبر: باید مضرب ${issue.divisor} باشد`;
			case "unrecognized_keys": return `کلید${issue.keys.length > 1 ? "های" : ""} ناشناس: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `کلید ناشناس در ${issue.origin}`;
			case "invalid_union": return `ورودی نامعتبر`;
			case "invalid_element": return `مقدار نامعتبر در ${issue.origin}`;
			default: return `ورودی نامعتبر`;
		}
	};
};
function fa_default() {
	return { localeError: error$37() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/fi.js
const error$36 = () => {
	const Sizable = {
		string: {
			unit: "merkkiä",
			subject: "merkkijonon"
		},
		file: {
			unit: "tavua",
			subject: "tiedoston"
		},
		array: {
			unit: "alkiota",
			subject: "listan"
		},
		set: {
			unit: "alkiota",
			subject: "joukon"
		},
		number: {
			unit: "",
			subject: "luvun"
		},
		bigint: {
			unit: "",
			subject: "suuren kokonaisluvun"
		},
		int: {
			unit: "",
			subject: "kokonaisluvun"
		},
		date: {
			unit: "",
			subject: "päivämäärän"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "säännöllinen lauseke",
		email: "sähköpostiosoite",
		url: "URL-osoite",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-aikaleima",
		date: "ISO-päivämäärä",
		time: "ISO-aika",
		duration: "ISO-kesto",
		ipv4: "IPv4-osoite",
		ipv6: "IPv6-osoite",
		cidrv4: "IPv4-alue",
		cidrv6: "IPv6-alue",
		base64: "base64-koodattu merkkijono",
		base64url: "base64url-koodattu merkkijono",
		json_string: "JSON-merkkijono",
		e164: "E.164-luku",
		jwt: "JWT",
		template_literal: "templaattimerkkijono"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Virheellinen tyyppi: odotettiin instanceof ${issue.expected}, oli ${received}`;
				return `Virheellinen tyyppi: odotettiin ${expected}, oli ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Virheellinen syöte: täytyy olla ${stringifyPrimitive(issue.values[0])}`;
				return `Virheellinen valinta: täytyy olla yksi seuraavista: ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Liian suuri: ${sizing.subject} täytyy olla ${adj}${issue.maximum.toString()} ${sizing.unit}`.trim();
				return `Liian suuri: arvon täytyy olla ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Liian pieni: ${sizing.subject} täytyy olla ${adj}${issue.minimum.toString()} ${sizing.unit}`.trim();
				return `Liian pieni: arvon täytyy olla ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Virheellinen syöte: täytyy alkaa "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Virheellinen syöte: täytyy loppua "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Virheellinen syöte: täytyy sisältää "${_issue.includes}"`;
				if (_issue.format === "regex") return `Virheellinen syöte: täytyy vastata säännöllistä lauseketta ${_issue.pattern}`;
				return `Virheellinen ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Virheellinen luku: täytyy olla luvun ${issue.divisor} monikerta`;
			case "unrecognized_keys": return `${issue.keys.length > 1 ? "Tuntemattomat avaimet" : "Tuntematon avain"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return "Virheellinen avain tietueessa";
			case "invalid_union": return "Virheellinen unioni";
			case "invalid_element": return "Virheellinen arvo joukossa";
			default: return `Virheellinen syöte`;
		}
	};
};
function fi_default() {
	return { localeError: error$36() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/fr.js
const error$35 = () => {
	const Sizable = {
		string: {
			unit: "caractères",
			verb: "avoir"
		},
		file: {
			unit: "octets",
			verb: "avoir"
		},
		array: {
			unit: "éléments",
			verb: "avoir"
		},
		set: {
			unit: "éléments",
			verb: "avoir"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "entrée",
		email: "adresse e-mail",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "date et heure ISO",
		date: "date ISO",
		time: "heure ISO",
		duration: "durée ISO",
		ipv4: "adresse IPv4",
		ipv6: "adresse IPv6",
		cidrv4: "plage IPv4",
		cidrv6: "plage IPv6",
		base64: "chaîne encodée en base64",
		base64url: "chaîne encodée en base64url",
		json_string: "chaîne JSON",
		e164: "numéro E.164",
		jwt: "JWT",
		template_literal: "entrée"
	};
	const TypeDictionary = {
		string: "chaîne",
		number: "nombre",
		int: "entier",
		boolean: "booléen",
		bigint: "grand entier",
		symbol: "symbole",
		undefined: "indéfini",
		null: "null",
		never: "jamais",
		void: "vide",
		date: "date",
		array: "tableau",
		object: "objet",
		tuple: "tuple",
		record: "enregistrement",
		map: "carte",
		set: "ensemble",
		file: "fichier",
		nonoptional: "non-optionnel",
		nan: "NaN",
		function: "fonction"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Entrée invalide : instanceof ${issue.expected} attendu, ${received} reçu`;
				return `Entrée invalide : ${expected} attendu, ${received} reçu`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Entrée invalide : ${stringifyPrimitive(issue.values[0])} attendu`;
				return `Option invalide : une valeur parmi ${joinValues(issue.values, "|")} attendue`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Trop grand : ${TypeDictionary[issue.origin] ?? "valeur"} doit ${sizing.verb} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "élément(s)"}`;
				return `Trop grand : ${TypeDictionary[issue.origin] ?? "valeur"} doit être ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Trop petit : ${TypeDictionary[issue.origin] ?? "valeur"} doit ${sizing.verb} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Trop petit : ${TypeDictionary[issue.origin] ?? "valeur"} doit être ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Chaîne invalide : doit commencer par "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Chaîne invalide : doit se terminer par "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Chaîne invalide : doit inclure "${_issue.includes}"`;
				if (_issue.format === "regex") return `Chaîne invalide : doit correspondre au modèle ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} invalide`;
			}
			case "not_multiple_of": return `Nombre invalide : doit être un multiple de ${issue.divisor}`;
			case "unrecognized_keys": return `Clé${issue.keys.length > 1 ? "s" : ""} non reconnue${issue.keys.length > 1 ? "s" : ""} : ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Clé invalide dans ${issue.origin}`;
			case "invalid_union": return "Entrée invalide";
			case "invalid_element": return `Valeur invalide dans ${issue.origin}`;
			default: return `Entrée invalide`;
		}
	};
};
function fr_default() {
	return { localeError: error$35() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/fr-CA.js
const error$34 = () => {
	const Sizable = {
		string: {
			unit: "caractères",
			verb: "avoir"
		},
		file: {
			unit: "octets",
			verb: "avoir"
		},
		array: {
			unit: "éléments",
			verb: "avoir"
		},
		set: {
			unit: "éléments",
			verb: "avoir"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "entrée",
		email: "adresse courriel",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "date-heure ISO",
		date: "date ISO",
		time: "heure ISO",
		duration: "durée ISO",
		ipv4: "adresse IPv4",
		ipv6: "adresse IPv6",
		cidrv4: "plage IPv4",
		cidrv6: "plage IPv6",
		base64: "chaîne encodée en base64",
		base64url: "chaîne encodée en base64url",
		json_string: "chaîne JSON",
		e164: "numéro E.164",
		jwt: "JWT",
		template_literal: "entrée"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Entrée invalide : attendu instanceof ${issue.expected}, reçu ${received}`;
				return `Entrée invalide : attendu ${expected}, reçu ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Entrée invalide : attendu ${stringifyPrimitive(issue.values[0])}`;
				return `Option invalide : attendu l'une des valeurs suivantes ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "≤" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Trop grand : attendu que ${issue.origin ?? "la valeur"} ait ${adj}${issue.maximum.toString()} ${sizing.unit}`;
				return `Trop grand : attendu que ${issue.origin ?? "la valeur"} soit ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? "≥" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Trop petit : attendu que ${issue.origin} ait ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Trop petit : attendu que ${issue.origin} soit ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Chaîne invalide : doit commencer par "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Chaîne invalide : doit se terminer par "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Chaîne invalide : doit inclure "${_issue.includes}"`;
				if (_issue.format === "regex") return `Chaîne invalide : doit correspondre au motif ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} invalide`;
			}
			case "not_multiple_of": return `Nombre invalide : doit être un multiple de ${issue.divisor}`;
			case "unrecognized_keys": return `Clé${issue.keys.length > 1 ? "s" : ""} non reconnue${issue.keys.length > 1 ? "s" : ""} : ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Clé invalide dans ${issue.origin}`;
			case "invalid_union": return "Entrée invalide";
			case "invalid_element": return `Valeur invalide dans ${issue.origin}`;
			default: return `Entrée invalide`;
		}
	};
};
function fr_CA_default() {
	return { localeError: error$34() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/he.js
const error$33 = () => {
	const TypeNames = {
		string: {
			label: "מחרוזת",
			gender: "f"
		},
		number: {
			label: "מספר",
			gender: "m"
		},
		boolean: {
			label: "ערך בוליאני",
			gender: "m"
		},
		bigint: {
			label: "BigInt",
			gender: "m"
		},
		date: {
			label: "תאריך",
			gender: "m"
		},
		array: {
			label: "מערך",
			gender: "m"
		},
		object: {
			label: "אובייקט",
			gender: "m"
		},
		null: {
			label: "ערך ריק (null)",
			gender: "m"
		},
		undefined: {
			label: "ערך לא מוגדר (undefined)",
			gender: "m"
		},
		symbol: {
			label: "סימבול (Symbol)",
			gender: "m"
		},
		function: {
			label: "פונקציה",
			gender: "f"
		},
		map: {
			label: "מפה (Map)",
			gender: "f"
		},
		set: {
			label: "קבוצה (Set)",
			gender: "f"
		},
		file: {
			label: "קובץ",
			gender: "m"
		},
		promise: {
			label: "Promise",
			gender: "m"
		},
		NaN: {
			label: "NaN",
			gender: "m"
		},
		unknown: {
			label: "ערך לא ידוע",
			gender: "m"
		},
		value: {
			label: "ערך",
			gender: "m"
		}
	};
	const Sizable = {
		string: {
			unit: "תווים",
			shortLabel: "קצר",
			longLabel: "ארוך"
		},
		file: {
			unit: "בייטים",
			shortLabel: "קטן",
			longLabel: "גדול"
		},
		array: {
			unit: "פריטים",
			shortLabel: "קטן",
			longLabel: "גדול"
		},
		set: {
			unit: "פריטים",
			shortLabel: "קטן",
			longLabel: "גדול"
		},
		number: {
			unit: "",
			shortLabel: "קטן",
			longLabel: "גדול"
		}
	};
	const typeEntry = (t) => t ? TypeNames[t] : void 0;
	const typeLabel = (t) => {
		const e = typeEntry(t);
		if (e) return e.label;
		return t ?? TypeNames.unknown.label;
	};
	const withDefinite = (t) => `ה${typeLabel(t)}`;
	const verbFor = (t) => {
		return (typeEntry(t)?.gender ?? "m") === "f" ? "צריכה להיות" : "צריך להיות";
	};
	const getSizing = (origin) => {
		if (!origin) return null;
		return Sizable[origin] ?? null;
	};
	const FormatDictionary = {
		regex: {
			label: "קלט",
			gender: "m"
		},
		email: {
			label: "כתובת אימייל",
			gender: "f"
		},
		url: {
			label: "כתובת רשת",
			gender: "f"
		},
		emoji: {
			label: "אימוג'י",
			gender: "m"
		},
		uuid: {
			label: "UUID",
			gender: "m"
		},
		nanoid: {
			label: "nanoid",
			gender: "m"
		},
		guid: {
			label: "GUID",
			gender: "m"
		},
		cuid: {
			label: "cuid",
			gender: "m"
		},
		cuid2: {
			label: "cuid2",
			gender: "m"
		},
		ulid: {
			label: "ULID",
			gender: "m"
		},
		xid: {
			label: "XID",
			gender: "m"
		},
		ksuid: {
			label: "KSUID",
			gender: "m"
		},
		datetime: {
			label: "תאריך וזמן ISO",
			gender: "m"
		},
		date: {
			label: "תאריך ISO",
			gender: "m"
		},
		time: {
			label: "זמן ISO",
			gender: "m"
		},
		duration: {
			label: "משך זמן ISO",
			gender: "m"
		},
		ipv4: {
			label: "כתובת IPv4",
			gender: "f"
		},
		ipv6: {
			label: "כתובת IPv6",
			gender: "f"
		},
		cidrv4: {
			label: "טווח IPv4",
			gender: "m"
		},
		cidrv6: {
			label: "טווח IPv6",
			gender: "m"
		},
		base64: {
			label: "מחרוזת בבסיס 64",
			gender: "f"
		},
		base64url: {
			label: "מחרוזת בבסיס 64 לכתובות רשת",
			gender: "f"
		},
		json_string: {
			label: "מחרוזת JSON",
			gender: "f"
		},
		e164: {
			label: "מספר E.164",
			gender: "m"
		},
		jwt: {
			label: "JWT",
			gender: "m"
		},
		ends_with: {
			label: "קלט",
			gender: "m"
		},
		includes: {
			label: "קלט",
			gender: "m"
		},
		lowercase: {
			label: "קלט",
			gender: "m"
		},
		starts_with: {
			label: "קלט",
			gender: "m"
		},
		uppercase: {
			label: "קלט",
			gender: "m"
		}
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expectedKey = issue.expected;
				const expected = TypeDictionary[expectedKey ?? ""] ?? typeLabel(expectedKey);
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? TypeNames[receivedType]?.label ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `קלט לא תקין: צריך להיות instanceof ${issue.expected}, התקבל ${received}`;
				return `קלט לא תקין: צריך להיות ${expected}, התקבל ${received}`;
			}
			case "invalid_value": {
				if (issue.values.length === 1) return `ערך לא תקין: הערך חייב להיות ${stringifyPrimitive(issue.values[0])}`;
				const stringified = issue.values.map((v) => stringifyPrimitive(v));
				if (issue.values.length === 2) return `ערך לא תקין: האפשרויות המתאימות הן ${stringified[0]} או ${stringified[1]}`;
				const lastValue = stringified[stringified.length - 1];
				return `ערך לא תקין: האפשרויות המתאימות הן ${stringified.slice(0, -1).join(", ")} או ${lastValue}`;
			}
			case "too_big": {
				const sizing = getSizing(issue.origin);
				const subject = withDefinite(issue.origin ?? "value");
				if (issue.origin === "string") return `${sizing?.longLabel ?? "ארוך"} מדי: ${subject} צריכה להכיל ${issue.maximum.toString()} ${sizing?.unit ?? ""} ${issue.inclusive ? "או פחות" : "לכל היותר"}`.trim();
				if (issue.origin === "number") return `גדול מדי: ${subject} צריך להיות ${issue.inclusive ? `קטן או שווה ל-${issue.maximum}` : `קטן מ-${issue.maximum}`}`;
				if (issue.origin === "array" || issue.origin === "set") return `גדול מדי: ${subject} ${issue.origin === "set" ? "צריכה" : "צריך"} להכיל ${issue.inclusive ? `${issue.maximum} ${sizing?.unit ?? ""} או פחות` : `פחות מ-${issue.maximum} ${sizing?.unit ?? ""}`}`.trim();
				const adj = issue.inclusive ? "<=" : "<";
				const be = verbFor(issue.origin ?? "value");
				if (sizing?.unit) return `${sizing.longLabel} מדי: ${subject} ${be} ${adj}${issue.maximum.toString()} ${sizing.unit}`;
				return `${sizing?.longLabel ?? "גדול"} מדי: ${subject} ${be} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const sizing = getSizing(issue.origin);
				const subject = withDefinite(issue.origin ?? "value");
				if (issue.origin === "string") return `${sizing?.shortLabel ?? "קצר"} מדי: ${subject} צריכה להכיל ${issue.minimum.toString()} ${sizing?.unit ?? ""} ${issue.inclusive ? "או יותר" : "לפחות"}`.trim();
				if (issue.origin === "number") return `קטן מדי: ${subject} צריך להיות ${issue.inclusive ? `גדול או שווה ל-${issue.minimum}` : `גדול מ-${issue.minimum}`}`;
				if (issue.origin === "array" || issue.origin === "set") {
					const verb = issue.origin === "set" ? "צריכה" : "צריך";
					if (issue.minimum === 1 && issue.inclusive) return `קטן מדי: ${subject} ${verb} להכיל ${issue.origin === "set" ? "לפחות פריט אחד" : "לפחות פריט אחד"}`;
					return `קטן מדי: ${subject} ${verb} להכיל ${issue.inclusive ? `${issue.minimum} ${sizing?.unit ?? ""} או יותר` : `יותר מ-${issue.minimum} ${sizing?.unit ?? ""}`}`.trim();
				}
				const adj = issue.inclusive ? ">=" : ">";
				const be = verbFor(issue.origin ?? "value");
				if (sizing?.unit) return `${sizing.shortLabel} מדי: ${subject} ${be} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `${sizing?.shortLabel ?? "קטן"} מדי: ${subject} ${be} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `המחרוזת חייבת להתחיל ב "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `המחרוזת חייבת להסתיים ב "${_issue.suffix}"`;
				if (_issue.format === "includes") return `המחרוזת חייבת לכלול "${_issue.includes}"`;
				if (_issue.format === "regex") return `המחרוזת חייבת להתאים לתבנית ${_issue.pattern}`;
				const nounEntry = FormatDictionary[_issue.format];
				return `${nounEntry?.label ?? _issue.format} לא ${(nounEntry?.gender ?? "m") === "f" ? "תקינה" : "תקין"}`;
			}
			case "not_multiple_of": return `מספר לא תקין: חייב להיות מכפלה של ${issue.divisor}`;
			case "unrecognized_keys": return `מפתח${issue.keys.length > 1 ? "ות" : ""} לא מזוה${issue.keys.length > 1 ? "ים" : "ה"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `שדה לא תקין באובייקט`;
			case "invalid_union": return "קלט לא תקין";
			case "invalid_element": return `ערך לא תקין ב${withDefinite(issue.origin ?? "array")}`;
			default: return `קלט לא תקין`;
		}
	};
};
function he_default() {
	return { localeError: error$33() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/hr.js
const error$32 = () => {
	const Sizable = {
		string: {
			unit: "znakova",
			verb: "imati"
		},
		file: {
			unit: "bajtova",
			verb: "imati"
		},
		array: {
			unit: "stavki",
			verb: "imati"
		},
		set: {
			unit: "stavki",
			verb: "imati"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "unos",
		email: "email adresa",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datum i vrijeme",
		date: "ISO datum",
		time: "ISO vrijeme",
		duration: "ISO trajanje",
		ipv4: "IPv4 adresa",
		ipv6: "IPv6 adresa",
		cidrv4: "IPv4 raspon",
		cidrv6: "IPv6 raspon",
		base64: "base64 kodirani tekst",
		base64url: "base64url kodirani tekst",
		json_string: "JSON tekst",
		e164: "E.164 broj",
		jwt: "JWT",
		template_literal: "unos"
	};
	const TypeDictionary = {
		nan: "NaN",
		string: "tekst",
		number: "broj",
		boolean: "boolean",
		array: "niz",
		object: "objekt",
		set: "skup",
		file: "datoteka",
		date: "datum",
		bigint: "bigint",
		symbol: "simbol",
		undefined: "undefined",
		null: "null",
		function: "funkcija",
		map: "mapa"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Neispravan unos: očekuje se instanceof ${issue.expected}, a primljeno je ${received}`;
				return `Neispravan unos: očekuje se ${expected}, a primljeno je ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Neispravna vrijednost: očekivano ${stringifyPrimitive(issue.values[0])}`;
				return `Neispravna opcija: očekivano jedno od ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				if (sizing) return `Preveliko: očekivano da ${origin ?? "vrijednost"} ima ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elemenata"}`;
				return `Preveliko: očekivano da ${origin ?? "vrijednost"} bude ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				if (sizing) return `Premalo: očekivano da ${origin} ima ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Premalo: očekivano da ${origin} bude ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Neispravan tekst: mora započinjati s "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Neispravan tekst: mora završavati s "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Neispravan tekst: mora sadržavati "${_issue.includes}"`;
				if (_issue.format === "regex") return `Neispravan tekst: mora odgovarati uzorku ${_issue.pattern}`;
				return `Neispravna ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Neispravan broj: mora biti višekratnik od ${issue.divisor}`;
			case "unrecognized_keys": return `Neprepoznat${issue.keys.length > 1 ? "i ključevi" : " ključ"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Neispravan ključ u ${TypeDictionary[issue.origin] ?? issue.origin}`;
			case "invalid_union": return "Neispravan unos";
			case "invalid_element": return `Neispravna vrijednost u ${TypeDictionary[issue.origin] ?? issue.origin}`;
			default: return `Neispravan unos`;
		}
	};
};
function hr_default() {
	return { localeError: error$32() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/hu.js
const error$31 = () => {
	const Sizable = {
		string: {
			unit: "karakter",
			verb: "legyen"
		},
		file: {
			unit: "byte",
			verb: "legyen"
		},
		array: {
			unit: "elem",
			verb: "legyen"
		},
		set: {
			unit: "elem",
			verb: "legyen"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "bemenet",
		email: "email cím",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO időbélyeg",
		date: "ISO dátum",
		time: "ISO idő",
		duration: "ISO időintervallum",
		ipv4: "IPv4 cím",
		ipv6: "IPv6 cím",
		cidrv4: "IPv4 tartomány",
		cidrv6: "IPv6 tartomány",
		base64: "base64-kódolt string",
		base64url: "base64url-kódolt string",
		json_string: "JSON string",
		e164: "E.164 szám",
		jwt: "JWT",
		template_literal: "bemenet"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "szám",
		array: "tömb"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Érvénytelen bemenet: a várt érték instanceof ${issue.expected}, a kapott érték ${received}`;
				return `Érvénytelen bemenet: a várt érték ${expected}, a kapott érték ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Érvénytelen bemenet: a várt érték ${stringifyPrimitive(issue.values[0])}`;
				return `Érvénytelen opció: valamelyik érték várt ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Túl nagy: ${issue.origin ?? "érték"} mérete túl nagy ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elem"}`;
				return `Túl nagy: a bemeneti érték ${issue.origin ?? "érték"} túl nagy: ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Túl kicsi: a bemeneti érték ${issue.origin} mérete túl kicsi ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Túl kicsi: a bemeneti érték ${issue.origin} túl kicsi ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Érvénytelen string: "${_issue.prefix}" értékkel kell kezdődnie`;
				if (_issue.format === "ends_with") return `Érvénytelen string: "${_issue.suffix}" értékkel kell végződnie`;
				if (_issue.format === "includes") return `Érvénytelen string: "${_issue.includes}" értéket kell tartalmaznia`;
				if (_issue.format === "regex") return `Érvénytelen string: ${_issue.pattern} mintának kell megfelelnie`;
				return `Érvénytelen ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Érvénytelen szám: ${issue.divisor} többszörösének kell lennie`;
			case "unrecognized_keys": return `Ismeretlen kulcs${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Érvénytelen kulcs ${issue.origin}`;
			case "invalid_union": return "Érvénytelen bemenet";
			case "invalid_element": return `Érvénytelen érték: ${issue.origin}`;
			default: return `Érvénytelen bemenet`;
		}
	};
};
function hu_default() {
	return { localeError: error$31() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/hy.js
function getArmenianPlural(count, one, many) {
	return Math.abs(count) === 1 ? one : many;
}
function withDefiniteArticle(word) {
	if (!word) return "";
	const vowels = [
		"ա",
		"ե",
		"ը",
		"ի",
		"ո",
		"ու",
		"օ"
	];
	const lastChar = word[word.length - 1];
	return word + (vowels.includes(lastChar) ? "ն" : "ը");
}
const error$30 = () => {
	const Sizable = {
		string: {
			unit: {
				one: "նշան",
				many: "նշաններ"
			},
			verb: "ունենալ"
		},
		file: {
			unit: {
				one: "բայթ",
				many: "բայթեր"
			},
			verb: "ունենալ"
		},
		array: {
			unit: {
				one: "տարր",
				many: "տարրեր"
			},
			verb: "ունենալ"
		},
		set: {
			unit: {
				one: "տարր",
				many: "տարրեր"
			},
			verb: "ունենալ"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "մուտք",
		email: "էլ. հասցե",
		url: "URL",
		emoji: "էմոջի",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO ամսաթիվ և ժամ",
		date: "ISO ամսաթիվ",
		time: "ISO ժամ",
		duration: "ISO տևողություն",
		ipv4: "IPv4 հասցե",
		ipv6: "IPv6 հասցե",
		cidrv4: "IPv4 միջակայք",
		cidrv6: "IPv6 միջակայք",
		base64: "base64 ձևաչափով տող",
		base64url: "base64url ձևաչափով տող",
		json_string: "JSON տող",
		e164: "E.164 համար",
		jwt: "JWT",
		template_literal: "մուտք"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "թիվ",
		array: "զանգված"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Սխալ մուտքագրում․ սպասվում էր instanceof ${issue.expected}, ստացվել է ${received}`;
				return `Սխալ մուտքագրում․ սպասվում էր ${expected}, ստացվել է ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Սխալ մուտքագրում․ սպասվում էր ${stringifyPrimitive(issue.values[1])}`;
				return `Սխալ տարբերակ․ սպասվում էր հետևյալներից մեկը՝ ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) {
					const unit = getArmenianPlural(Number(issue.maximum), sizing.unit.one, sizing.unit.many);
					return `Չափազանց մեծ արժեք․ սպասվում է, որ ${withDefiniteArticle(issue.origin ?? "արժեք")} կունենա ${adj}${issue.maximum.toString()} ${unit}`;
				}
				return `Չափազանց մեծ արժեք․ սպասվում է, որ ${withDefiniteArticle(issue.origin ?? "արժեք")} լինի ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) {
					const unit = getArmenianPlural(Number(issue.minimum), sizing.unit.one, sizing.unit.many);
					return `Չափազանց փոքր արժեք․ սպասվում է, որ ${withDefiniteArticle(issue.origin)} կունենա ${adj}${issue.minimum.toString()} ${unit}`;
				}
				return `Չափազանց փոքր արժեք․ սպասվում է, որ ${withDefiniteArticle(issue.origin)} լինի ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Սխալ տող․ պետք է սկսվի "${_issue.prefix}"-ով`;
				if (_issue.format === "ends_with") return `Սխալ տող․ պետք է ավարտվի "${_issue.suffix}"-ով`;
				if (_issue.format === "includes") return `Սխալ տող․ պետք է պարունակի "${_issue.includes}"`;
				if (_issue.format === "regex") return `Սխալ տող․ պետք է համապատասխանի ${_issue.pattern} ձևաչափին`;
				return `Սխալ ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Սխալ թիվ․ պետք է բազմապատիկ լինի ${issue.divisor}-ի`;
			case "unrecognized_keys": return `Չճանաչված բանալի${issue.keys.length > 1 ? "ներ" : ""}. ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Սխալ բանալի ${withDefiniteArticle(issue.origin)}-ում`;
			case "invalid_union": return "Սխալ մուտքագրում";
			case "invalid_element": return `Սխալ արժեք ${withDefiniteArticle(issue.origin)}-ում`;
			default: return `Սխալ մուտքագրում`;
		}
	};
};
function hy_default() {
	return { localeError: error$30() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/id.js
const error$29 = () => {
	const Sizable = {
		string: {
			unit: "karakter",
			verb: "memiliki"
		},
		file: {
			unit: "byte",
			verb: "memiliki"
		},
		array: {
			unit: "item",
			verb: "memiliki"
		},
		set: {
			unit: "item",
			verb: "memiliki"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "alamat email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "tanggal dan waktu format ISO",
		date: "tanggal format ISO",
		time: "jam format ISO",
		duration: "durasi format ISO",
		ipv4: "alamat IPv4",
		ipv6: "alamat IPv6",
		cidrv4: "rentang alamat IPv4",
		cidrv6: "rentang alamat IPv6",
		base64: "string dengan enkode base64",
		base64url: "string dengan enkode base64url",
		json_string: "string JSON",
		e164: "angka E.164",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Input tidak valid: diharapkan instanceof ${issue.expected}, diterima ${received}`;
				return `Input tidak valid: diharapkan ${expected}, diterima ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Input tidak valid: diharapkan ${stringifyPrimitive(issue.values[0])}`;
				return `Pilihan tidak valid: diharapkan salah satu dari ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Terlalu besar: diharapkan ${issue.origin ?? "value"} memiliki ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elemen"}`;
				return `Terlalu besar: diharapkan ${issue.origin ?? "value"} menjadi ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Terlalu kecil: diharapkan ${issue.origin} memiliki ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Terlalu kecil: diharapkan ${issue.origin} menjadi ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `String tidak valid: harus dimulai dengan "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `String tidak valid: harus berakhir dengan "${_issue.suffix}"`;
				if (_issue.format === "includes") return `String tidak valid: harus menyertakan "${_issue.includes}"`;
				if (_issue.format === "regex") return `String tidak valid: harus sesuai pola ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} tidak valid`;
			}
			case "not_multiple_of": return `Angka tidak valid: harus kelipatan dari ${issue.divisor}`;
			case "unrecognized_keys": return `Kunci tidak dikenali ${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Kunci tidak valid di ${issue.origin}`;
			case "invalid_union": return "Input tidak valid";
			case "invalid_element": return `Nilai tidak valid di ${issue.origin}`;
			default: return `Input tidak valid`;
		}
	};
};
function id_default() {
	return { localeError: error$29() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/is.js
const error$28 = () => {
	const Sizable = {
		string: {
			unit: "stafi",
			verb: "að hafa"
		},
		file: {
			unit: "bæti",
			verb: "að hafa"
		},
		array: {
			unit: "hluti",
			verb: "að hafa"
		},
		set: {
			unit: "hluti",
			verb: "að hafa"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "gildi",
		email: "netfang",
		url: "vefslóð",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dagsetning og tími",
		date: "ISO dagsetning",
		time: "ISO tími",
		duration: "ISO tímalengd",
		ipv4: "IPv4 address",
		ipv6: "IPv6 address",
		cidrv4: "IPv4 range",
		cidrv6: "IPv6 range",
		base64: "base64-encoded strengur",
		base64url: "base64url-encoded strengur",
		json_string: "JSON strengur",
		e164: "E.164 tölugildi",
		jwt: "JWT",
		template_literal: "gildi"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "númer",
		array: "fylki"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Rangt gildi: Þú slóst inn ${received} þar sem á að vera instanceof ${issue.expected}`;
				return `Rangt gildi: Þú slóst inn ${received} þar sem á að vera ${expected}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Rangt gildi: gert ráð fyrir ${stringifyPrimitive(issue.values[0])}`;
				return `Ógilt val: má vera eitt af eftirfarandi ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Of stórt: gert er ráð fyrir að ${issue.origin ?? "gildi"} hafi ${adj}${issue.maximum.toString()} ${sizing.unit ?? "hluti"}`;
				return `Of stórt: gert er ráð fyrir að ${issue.origin ?? "gildi"} sé ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Of lítið: gert er ráð fyrir að ${issue.origin} hafi ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Of lítið: gert er ráð fyrir að ${issue.origin} sé ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ógildur strengur: verður að byrja á "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Ógildur strengur: verður að enda á "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Ógildur strengur: verður að innihalda "${_issue.includes}"`;
				if (_issue.format === "regex") return `Ógildur strengur: verður að fylgja mynstri ${_issue.pattern}`;
				return `Rangt ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Röng tala: verður að vera margfeldi af ${issue.divisor}`;
			case "unrecognized_keys": return `Óþekkt ${issue.keys.length > 1 ? "ir lyklar" : "ur lykill"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Rangur lykill í ${issue.origin}`;
			case "invalid_union": return "Rangt gildi";
			case "invalid_element": return `Rangt gildi í ${issue.origin}`;
			default: return `Rangt gildi`;
		}
	};
};
function is_default() {
	return { localeError: error$28() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/it.js
const error$27 = () => {
	const Sizable = {
		string: {
			unit: "caratteri",
			verb: "avere"
		},
		file: {
			unit: "byte",
			verb: "avere"
		},
		array: {
			unit: "elementi",
			verb: "avere"
		},
		set: {
			unit: "elementi",
			verb: "avere"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "indirizzo email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data e ora ISO",
		date: "data ISO",
		time: "ora ISO",
		duration: "durata ISO",
		ipv4: "indirizzo IPv4",
		ipv6: "indirizzo IPv6",
		cidrv4: "intervallo IPv4",
		cidrv6: "intervallo IPv6",
		base64: "stringa codificata in base64",
		base64url: "URL codificata in base64",
		json_string: "stringa JSON",
		e164: "numero E.164",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "numero",
		array: "vettore"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Input non valido: atteso instanceof ${issue.expected}, ricevuto ${received}`;
				return `Input non valido: atteso ${expected}, ricevuto ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Input non valido: atteso ${stringifyPrimitive(issue.values[0])}`;
				return `Opzione non valida: atteso uno tra ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Troppo grande: ${issue.origin ?? "valore"} deve avere ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementi"}`;
				return `Troppo grande: ${issue.origin ?? "valore"} deve essere ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Troppo piccolo: ${issue.origin} deve avere ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Troppo piccolo: ${issue.origin} deve essere ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Stringa non valida: deve iniziare con "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Stringa non valida: deve terminare con "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Stringa non valida: deve includere "${_issue.includes}"`;
				if (_issue.format === "regex") return `Stringa non valida: deve corrispondere al pattern ${_issue.pattern}`;
				return `Input non valido: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Numero non valido: deve essere un multiplo di ${issue.divisor}`;
			case "unrecognized_keys": return `Chiav${issue.keys.length > 1 ? "i" : "e"} non riconosciut${issue.keys.length > 1 ? "e" : "a"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Chiave non valida in ${issue.origin}`;
			case "invalid_union": return "Input non valido";
			case "invalid_element": return `Valore non valido in ${issue.origin}`;
			default: return `Input non valido`;
		}
	};
};
function it_default() {
	return { localeError: error$27() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ja.js
const error$26 = () => {
	const Sizable = {
		string: {
			unit: "文字",
			verb: "である"
		},
		file: {
			unit: "バイト",
			verb: "である"
		},
		array: {
			unit: "要素",
			verb: "である"
		},
		set: {
			unit: "要素",
			verb: "である"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "入力値",
		email: "メールアドレス",
		url: "URL",
		emoji: "絵文字",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO日時",
		date: "ISO日付",
		time: "ISO時刻",
		duration: "ISO期間",
		ipv4: "IPv4アドレス",
		ipv6: "IPv6アドレス",
		cidrv4: "IPv4範囲",
		cidrv6: "IPv6範囲",
		base64: "base64エンコード文字列",
		base64url: "base64urlエンコード文字列",
		json_string: "JSON文字列",
		e164: "E.164番号",
		jwt: "JWT",
		template_literal: "入力値"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "数値",
		array: "配列"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `無効な入力: instanceof ${issue.expected}が期待されましたが、${received}が入力されました`;
				return `無効な入力: ${expected}が期待されましたが、${received}が入力されました`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `無効な入力: ${stringifyPrimitive(issue.values[0])}が期待されました`;
				return `無効な選択: ${joinValues(issue.values, "、")}のいずれかである必要があります`;
			case "too_big": {
				const adj = issue.inclusive ? "以下である" : "より小さい";
				const sizing = getSizing(issue.origin);
				if (sizing) return `大きすぎる値: ${issue.origin ?? "値"}は${issue.maximum.toString()}${sizing.unit ?? "要素"}${adj}必要があります`;
				return `大きすぎる値: ${issue.origin ?? "値"}は${issue.maximum.toString()}${adj}必要があります`;
			}
			case "too_small": {
				const adj = issue.inclusive ? "以上である" : "より大きい";
				const sizing = getSizing(issue.origin);
				if (sizing) return `小さすぎる値: ${issue.origin}は${issue.minimum.toString()}${sizing.unit}${adj}必要があります`;
				return `小さすぎる値: ${issue.origin}は${issue.minimum.toString()}${adj}必要があります`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `無効な文字列: "${_issue.prefix}"で始まる必要があります`;
				if (_issue.format === "ends_with") return `無効な文字列: "${_issue.suffix}"で終わる必要があります`;
				if (_issue.format === "includes") return `無効な文字列: "${_issue.includes}"を含む必要があります`;
				if (_issue.format === "regex") return `無効な文字列: パターン${_issue.pattern}に一致する必要があります`;
				return `無効な${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `無効な数値: ${issue.divisor}の倍数である必要があります`;
			case "unrecognized_keys": return `認識されていないキー${issue.keys.length > 1 ? "群" : ""}: ${joinValues(issue.keys, "、")}`;
			case "invalid_key": return `${issue.origin}内の無効なキー`;
			case "invalid_union": return "無効な入力";
			case "invalid_element": return `${issue.origin}内の無効な値`;
			default: return `無効な入力`;
		}
	};
};
function ja_default() {
	return { localeError: error$26() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ka.js
const error$25 = () => {
	const Sizable = {
		string: {
			unit: "სიმბოლო",
			verb: "უნდა შეიცავდეს"
		},
		file: {
			unit: "ბაიტი",
			verb: "უნდა შეიცავდეს"
		},
		array: {
			unit: "ელემენტი",
			verb: "უნდა შეიცავდეს"
		},
		set: {
			unit: "ელემენტი",
			verb: "უნდა შეიცავდეს"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "შეყვანა",
		email: "ელ-ფოსტის მისამართი",
		url: "URL",
		emoji: "ემოჯი",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "თარიღი-დრო",
		date: "თარიღი",
		time: "დრო",
		duration: "ხანგრძლივობა",
		ipv4: "IPv4 მისამართი",
		ipv6: "IPv6 მისამართი",
		cidrv4: "IPv4 დიაპაზონი",
		cidrv6: "IPv6 დიაპაზონი",
		base64: "base64-კოდირებული ველი",
		base64url: "base64url-კოდირებული ველი",
		json_string: "JSON ველი",
		e164: "E.164 ნომერი",
		jwt: "JWT",
		template_literal: "შეყვანა"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "რიცხვი",
		string: "ველი",
		boolean: "ბულეანი",
		function: "ფუნქცია",
		array: "მასივი"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `არასწორი შეყვანა: მოსალოდნელი instanceof ${issue.expected}, მიღებული ${received}`;
				return `არასწორი შეყვანა: მოსალოდნელი ${expected}, მიღებული ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `არასწორი შეყვანა: მოსალოდნელი ${stringifyPrimitive(issue.values[0])}`;
				return `არასწორი ვარიანტი: მოსალოდნელია ერთ-ერთი ${joinValues(issue.values, "|")}-დან`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `ზედმეტად დიდი: მოსალოდნელი ${issue.origin ?? "მნიშვნელობა"} ${sizing.verb} ${adj}${issue.maximum.toString()} ${sizing.unit}`;
				return `ზედმეტად დიდი: მოსალოდნელი ${issue.origin ?? "მნიშვნელობა"} იყოს ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `ზედმეტად პატარა: მოსალოდნელი ${issue.origin} ${sizing.verb} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `ზედმეტად პატარა: მოსალოდნელი ${issue.origin} იყოს ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `არასწორი ველი: უნდა იწყებოდეს "${_issue.prefix}"-ით`;
				if (_issue.format === "ends_with") return `არასწორი ველი: უნდა მთავრდებოდეს "${_issue.suffix}"-ით`;
				if (_issue.format === "includes") return `არასწორი ველი: უნდა შეიცავდეს "${_issue.includes}"-ს`;
				if (_issue.format === "regex") return `არასწორი ველი: უნდა შეესაბამებოდეს შაბლონს ${_issue.pattern}`;
				return `არასწორი ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `არასწორი რიცხვი: უნდა იყოს ${issue.divisor}-ის ჯერადი`;
			case "unrecognized_keys": return `უცნობი გასაღებ${issue.keys.length > 1 ? "ები" : "ი"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `არასწორი გასაღები ${issue.origin}-ში`;
			case "invalid_union": return "არასწორი შეყვანა";
			case "invalid_element": return `არასწორი მნიშვნელობა ${issue.origin}-ში`;
			default: return `არასწორი შეყვანა`;
		}
	};
};
function ka_default() {
	return { localeError: error$25() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/km.js
const error$24 = () => {
	const Sizable = {
		string: {
			unit: "តួអក្សរ",
			verb: "គួរមាន"
		},
		file: {
			unit: "បៃ",
			verb: "គួរមាន"
		},
		array: {
			unit: "ធាតុ",
			verb: "គួរមាន"
		},
		set: {
			unit: "ធាតុ",
			verb: "គួរមាន"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ទិន្នន័យបញ្ចូល",
		email: "អាសយដ្ឋានអ៊ីមែល",
		url: "URL",
		emoji: "សញ្ញាអារម្មណ៍",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "កាលបរិច្ឆេទ និងម៉ោង ISO",
		date: "កាលបរិច្ឆេទ ISO",
		time: "ម៉ោង ISO",
		duration: "រយៈពេល ISO",
		ipv4: "អាសយដ្ឋាន IPv4",
		ipv6: "អាសយដ្ឋាន IPv6",
		cidrv4: "ដែនអាសយដ្ឋាន IPv4",
		cidrv6: "ដែនអាសយដ្ឋាន IPv6",
		base64: "ខ្សែអក្សរអ៊ិកូដ base64",
		base64url: "ខ្សែអក្សរអ៊ិកូដ base64url",
		json_string: "ខ្សែអក្សរ JSON",
		e164: "លេខ E.164",
		jwt: "JWT",
		template_literal: "ទិន្នន័យបញ្ចូល"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "លេខ",
		array: "អារេ (Array)",
		null: "គ្មានតម្លៃ (null)"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `ទិន្នន័យបញ្ចូលមិនត្រឹមត្រូវ៖ ត្រូវការ instanceof ${issue.expected} ប៉ុន្តែទទួលបាន ${received}`;
				return `ទិន្នន័យបញ្ចូលមិនត្រឹមត្រូវ៖ ត្រូវការ ${expected} ប៉ុន្តែទទួលបាន ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `ទិន្នន័យបញ្ចូលមិនត្រឹមត្រូវ៖ ត្រូវការ ${stringifyPrimitive(issue.values[0])}`;
				return `ជម្រើសមិនត្រឹមត្រូវ៖ ត្រូវជាមួយក្នុងចំណោម ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `ធំពេក៖ ត្រូវការ ${issue.origin ?? "តម្លៃ"} ${adj} ${issue.maximum.toString()} ${sizing.unit ?? "ធាតុ"}`;
				return `ធំពេក៖ ត្រូវការ ${issue.origin ?? "តម្លៃ"} ${adj} ${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `តូចពេក៖ ត្រូវការ ${issue.origin} ${adj} ${issue.minimum.toString()} ${sizing.unit}`;
				return `តូចពេក៖ ត្រូវការ ${issue.origin} ${adj} ${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវចាប់ផ្តើមដោយ "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវបញ្ចប់ដោយ "${_issue.suffix}"`;
				if (_issue.format === "includes") return `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវមាន "${_issue.includes}"`;
				if (_issue.format === "regex") return `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវតែផ្គូផ្គងនឹងទម្រង់ដែលបានកំណត់ ${_issue.pattern}`;
				return `មិនត្រឹមត្រូវ៖ ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `លេខមិនត្រឹមត្រូវ៖ ត្រូវតែជាពហុគុណនៃ ${issue.divisor}`;
			case "unrecognized_keys": return `រកឃើញសោមិនស្គាល់៖ ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `សោមិនត្រឹមត្រូវនៅក្នុង ${issue.origin}`;
			case "invalid_union": return `ទិន្នន័យមិនត្រឹមត្រូវ`;
			case "invalid_element": return `ទិន្នន័យមិនត្រឹមត្រូវនៅក្នុង ${issue.origin}`;
			default: return `ទិន្នន័យមិនត្រឹមត្រូវ`;
		}
	};
};
function km_default() {
	return { localeError: error$24() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/kh.js
/** @deprecated Use `km` instead. */
function kh_default() {
	return km_default();
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ko.js
const error$23 = () => {
	const Sizable = {
		string: {
			unit: "문자",
			verb: "to have"
		},
		file: {
			unit: "바이트",
			verb: "to have"
		},
		array: {
			unit: "개",
			verb: "to have"
		},
		set: {
			unit: "개",
			verb: "to have"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "입력",
		email: "이메일 주소",
		url: "URL",
		emoji: "이모지",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO 날짜시간",
		date: "ISO 날짜",
		time: "ISO 시간",
		duration: "ISO 기간",
		ipv4: "IPv4 주소",
		ipv6: "IPv6 주소",
		cidrv4: "IPv4 범위",
		cidrv6: "IPv6 범위",
		base64: "base64 인코딩 문자열",
		base64url: "base64url 인코딩 문자열",
		json_string: "JSON 문자열",
		e164: "E.164 번호",
		jwt: "JWT",
		template_literal: "입력"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `잘못된 입력: 예상 타입은 instanceof ${issue.expected}, 받은 타입은 ${received}입니다`;
				return `잘못된 입력: 예상 타입은 ${expected}, 받은 타입은 ${received}입니다`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `잘못된 입력: 값은 ${stringifyPrimitive(issue.values[0])} 이어야 합니다`;
				return `잘못된 옵션: ${joinValues(issue.values, "또는 ")} 중 하나여야 합니다`;
			case "too_big": {
				const adj = issue.inclusive ? "이하" : "미만";
				const suffix = adj === "미만" ? "이어야 합니다" : "여야 합니다";
				const sizing = getSizing(issue.origin);
				const unit = sizing?.unit ?? "요소";
				if (sizing) return `${issue.origin ?? "값"}이 너무 큽니다: ${issue.maximum.toString()}${unit} ${adj}${suffix}`;
				return `${issue.origin ?? "값"}이 너무 큽니다: ${issue.maximum.toString()} ${adj}${suffix}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? "이상" : "초과";
				const suffix = adj === "이상" ? "이어야 합니다" : "여야 합니다";
				const sizing = getSizing(issue.origin);
				const unit = sizing?.unit ?? "요소";
				if (sizing) return `${issue.origin ?? "값"}이 너무 작습니다: ${issue.minimum.toString()}${unit} ${adj}${suffix}`;
				return `${issue.origin ?? "값"}이 너무 작습니다: ${issue.minimum.toString()} ${adj}${suffix}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `잘못된 문자열: "${_issue.prefix}"(으)로 시작해야 합니다`;
				if (_issue.format === "ends_with") return `잘못된 문자열: "${_issue.suffix}"(으)로 끝나야 합니다`;
				if (_issue.format === "includes") return `잘못된 문자열: "${_issue.includes}"을(를) 포함해야 합니다`;
				if (_issue.format === "regex") return `잘못된 문자열: 정규식 ${_issue.pattern} 패턴과 일치해야 합니다`;
				return `잘못된 ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `잘못된 숫자: ${issue.divisor}의 배수여야 합니다`;
			case "unrecognized_keys": return `인식할 수 없는 키: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `잘못된 키: ${issue.origin}`;
			case "invalid_union": return `잘못된 입력`;
			case "invalid_element": return `잘못된 값: ${issue.origin}`;
			default: return `잘못된 입력`;
		}
	};
};
function ko_default() {
	return { localeError: error$23() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/lt.js
const capitalizeFirstCharacter = (text) => {
	return text.charAt(0).toUpperCase() + text.slice(1);
};
function getUnitTypeFromNumber(number) {
	const abs = Math.abs(number);
	const last = abs % 10;
	const last2 = abs % 100;
	if (last2 >= 11 && last2 <= 19 || last === 0) return "many";
	if (last === 1) return "one";
	return "few";
}
const error$22 = () => {
	const Sizable = {
		string: {
			unit: {
				one: "simbolis",
				few: "simboliai",
				many: "simbolių"
			},
			verb: {
				smaller: {
					inclusive: "turi būti ne ilgesnė kaip",
					notInclusive: "turi būti trumpesnė kaip"
				},
				bigger: {
					inclusive: "turi būti ne trumpesnė kaip",
					notInclusive: "turi būti ilgesnė kaip"
				}
			}
		},
		file: {
			unit: {
				one: "baitas",
				few: "baitai",
				many: "baitų"
			},
			verb: {
				smaller: {
					inclusive: "turi būti ne didesnis kaip",
					notInclusive: "turi būti mažesnis kaip"
				},
				bigger: {
					inclusive: "turi būti ne mažesnis kaip",
					notInclusive: "turi būti didesnis kaip"
				}
			}
		},
		array: {
			unit: {
				one: "elementą",
				few: "elementus",
				many: "elementų"
			},
			verb: {
				smaller: {
					inclusive: "turi turėti ne daugiau kaip",
					notInclusive: "turi turėti mažiau kaip"
				},
				bigger: {
					inclusive: "turi turėti ne mažiau kaip",
					notInclusive: "turi turėti daugiau kaip"
				}
			}
		},
		set: {
			unit: {
				one: "elementą",
				few: "elementus",
				many: "elementų"
			},
			verb: {
				smaller: {
					inclusive: "turi turėti ne daugiau kaip",
					notInclusive: "turi turėti mažiau kaip"
				},
				bigger: {
					inclusive: "turi turėti ne mažiau kaip",
					notInclusive: "turi turėti daugiau kaip"
				}
			}
		}
	};
	function getSizing(origin, unitType, inclusive, targetShouldBe) {
		const result = Sizable[origin] ?? null;
		if (result === null) return result;
		return {
			unit: result.unit[unitType],
			verb: result.verb[targetShouldBe][inclusive ? "inclusive" : "notInclusive"]
		};
	}
	const FormatDictionary = {
		regex: "įvestis",
		email: "el. pašto adresas",
		url: "URL",
		emoji: "jaustukas",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO data ir laikas",
		date: "ISO data",
		time: "ISO laikas",
		duration: "ISO trukmė",
		ipv4: "IPv4 adresas",
		ipv6: "IPv6 adresas",
		cidrv4: "IPv4 tinklo prefiksas (CIDR)",
		cidrv6: "IPv6 tinklo prefiksas (CIDR)",
		base64: "base64 užkoduota eilutė",
		base64url: "base64url užkoduota eilutė",
		json_string: "JSON eilutė",
		e164: "E.164 numeris",
		jwt: "JWT",
		template_literal: "įvestis"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "skaičius",
		bigint: "sveikasis skaičius",
		string: "eilutė",
		boolean: "loginė reikšmė",
		undefined: "neapibrėžta reikšmė",
		function: "funkcija",
		symbol: "simbolis",
		array: "masyvas",
		object: "objektas",
		null: "nulinė reikšmė"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Gautas tipas ${received}, o tikėtasi - instanceof ${issue.expected}`;
				return `Gautas tipas ${received}, o tikėtasi - ${expected}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Privalo būti ${stringifyPrimitive(issue.values[0])}`;
				return `Privalo būti vienas iš ${joinValues(issue.values, "|")} pasirinkimų`;
			case "too_big": {
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				const sizing = getSizing(issue.origin, getUnitTypeFromNumber(Number(issue.maximum)), issue.inclusive ?? false, "smaller");
				if (sizing?.verb) return `${capitalizeFirstCharacter(origin ?? issue.origin ?? "reikšmė")} ${sizing.verb} ${issue.maximum.toString()} ${sizing.unit ?? "elementų"}`;
				const adj = issue.inclusive ? "ne didesnis kaip" : "mažesnis kaip";
				return `${capitalizeFirstCharacter(origin ?? issue.origin ?? "reikšmė")} turi būti ${adj} ${issue.maximum.toString()} ${sizing?.unit}`;
			}
			case "too_small": {
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				const sizing = getSizing(issue.origin, getUnitTypeFromNumber(Number(issue.minimum)), issue.inclusive ?? false, "bigger");
				if (sizing?.verb) return `${capitalizeFirstCharacter(origin ?? issue.origin ?? "reikšmė")} ${sizing.verb} ${issue.minimum.toString()} ${sizing.unit ?? "elementų"}`;
				const adj = issue.inclusive ? "ne mažesnis kaip" : "didesnis kaip";
				return `${capitalizeFirstCharacter(origin ?? issue.origin ?? "reikšmė")} turi būti ${adj} ${issue.minimum.toString()} ${sizing?.unit}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Eilutė privalo prasidėti "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Eilutė privalo pasibaigti "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Eilutė privalo įtraukti "${_issue.includes}"`;
				if (_issue.format === "regex") return `Eilutė privalo atitikti ${_issue.pattern}`;
				return `Neteisingas ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Skaičius privalo būti ${issue.divisor} kartotinis.`;
			case "unrecognized_keys": return `Neatpažint${issue.keys.length > 1 ? "i" : "as"} rakt${issue.keys.length > 1 ? "ai" : "as"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return "Rastas klaidingas raktas";
			case "invalid_union": return "Klaidinga įvestis";
			case "invalid_element": {
				const origin = TypeDictionary[issue.origin] ?? issue.origin;
				return `${capitalizeFirstCharacter(origin ?? issue.origin ?? "reikšmė")} turi klaidingą įvestį`;
			}
			default: return "Klaidinga įvestis";
		}
	};
};
function lt_default() {
	return { localeError: error$22() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/mk.js
const error$21 = () => {
	const Sizable = {
		string: {
			unit: "знаци",
			verb: "да имаат"
		},
		file: {
			unit: "бајти",
			verb: "да имаат"
		},
		array: {
			unit: "ставки",
			verb: "да имаат"
		},
		set: {
			unit: "ставки",
			verb: "да имаат"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "внес",
		email: "адреса на е-пошта",
		url: "URL",
		emoji: "емоџи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO датум и време",
		date: "ISO датум",
		time: "ISO време",
		duration: "ISO времетраење",
		ipv4: "IPv4 адреса",
		ipv6: "IPv6 адреса",
		cidrv4: "IPv4 опсег",
		cidrv6: "IPv6 опсег",
		base64: "base64-енкодирана низа",
		base64url: "base64url-енкодирана низа",
		json_string: "JSON низа",
		e164: "E.164 број",
		jwt: "JWT",
		template_literal: "внес"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "број",
		array: "низа"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Грешен внес: се очекува instanceof ${issue.expected}, примено ${received}`;
				return `Грешен внес: се очекува ${expected}, примено ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Invalid input: expected ${stringifyPrimitive(issue.values[0])}`;
				return `Грешана опција: се очекува една ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Премногу голем: се очекува ${issue.origin ?? "вредноста"} да има ${adj}${issue.maximum.toString()} ${sizing.unit ?? "елементи"}`;
				return `Премногу голем: се очекува ${issue.origin ?? "вредноста"} да биде ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Премногу мал: се очекува ${issue.origin} да има ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Премногу мал: се очекува ${issue.origin} да биде ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Неважечка низа: мора да започнува со "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Неважечка низа: мора да завршува со "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Неважечка низа: мора да вклучува "${_issue.includes}"`;
				if (_issue.format === "regex") return `Неважечка низа: мора да одгоара на патернот ${_issue.pattern}`;
				return `Invalid ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Грешен број: мора да биде делив со ${issue.divisor}`;
			case "unrecognized_keys": return `${issue.keys.length > 1 ? "Непрепознаени клучеви" : "Непрепознаен клуч"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Грешен клуч во ${issue.origin}`;
			case "invalid_union": return "Грешен внес";
			case "invalid_element": return `Грешна вредност во ${issue.origin}`;
			default: return `Грешен внес`;
		}
	};
};
function mk_default() {
	return { localeError: error$21() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ms.js
const error$20 = () => {
	const Sizable = {
		string: {
			unit: "aksara",
			verb: "mempunyai"
		},
		file: {
			unit: "bait",
			verb: "mempunyai"
		},
		array: {
			unit: "elemen",
			verb: "mempunyai"
		},
		set: {
			unit: "elemen",
			verb: "mempunyai"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "alamat e-mel",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "tarikh masa ISO",
		date: "tarikh ISO",
		time: "masa ISO",
		duration: "tempoh ISO",
		ipv4: "alamat IPv4",
		ipv6: "alamat IPv6",
		cidrv4: "julat IPv4",
		cidrv6: "julat IPv6",
		base64: "string dikodkan base64",
		base64url: "string dikodkan base64url",
		json_string: "string JSON",
		e164: "nombor E.164",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "nombor"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Input tidak sah: dijangka instanceof ${issue.expected}, diterima ${received}`;
				return `Input tidak sah: dijangka ${expected}, diterima ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Input tidak sah: dijangka ${stringifyPrimitive(issue.values[0])}`;
				return `Pilihan tidak sah: dijangka salah satu daripada ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Terlalu besar: dijangka ${issue.origin ?? "nilai"} ${sizing.verb} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elemen"}`;
				return `Terlalu besar: dijangka ${issue.origin ?? "nilai"} adalah ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Terlalu kecil: dijangka ${issue.origin} ${sizing.verb} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Terlalu kecil: dijangka ${issue.origin} adalah ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `String tidak sah: mesti bermula dengan "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `String tidak sah: mesti berakhir dengan "${_issue.suffix}"`;
				if (_issue.format === "includes") return `String tidak sah: mesti mengandungi "${_issue.includes}"`;
				if (_issue.format === "regex") return `String tidak sah: mesti sepadan dengan corak ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} tidak sah`;
			}
			case "not_multiple_of": return `Nombor tidak sah: perlu gandaan ${issue.divisor}`;
			case "unrecognized_keys": return `Kunci tidak dikenali: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Kunci tidak sah dalam ${issue.origin}`;
			case "invalid_union": return "Input tidak sah";
			case "invalid_element": return `Nilai tidak sah dalam ${issue.origin}`;
			default: return `Input tidak sah`;
		}
	};
};
function ms_default() {
	return { localeError: error$20() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/nl.js
const error$19 = () => {
	const Sizable = {
		string: {
			unit: "tekens",
			verb: "heeft"
		},
		file: {
			unit: "bytes",
			verb: "heeft"
		},
		array: {
			unit: "elementen",
			verb: "heeft"
		},
		set: {
			unit: "elementen",
			verb: "heeft"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "invoer",
		email: "emailadres",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datum en tijd",
		date: "ISO datum",
		time: "ISO tijd",
		duration: "ISO duur",
		ipv4: "IPv4-adres",
		ipv6: "IPv6-adres",
		cidrv4: "IPv4-bereik",
		cidrv6: "IPv6-bereik",
		base64: "base64-gecodeerde tekst",
		base64url: "base64 URL-gecodeerde tekst",
		json_string: "JSON string",
		e164: "E.164-nummer",
		jwt: "JWT",
		template_literal: "invoer"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "getal"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Ongeldige invoer: verwacht instanceof ${issue.expected}, ontving ${received}`;
				return `Ongeldige invoer: verwacht ${expected}, ontving ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Ongeldige invoer: verwacht ${stringifyPrimitive(issue.values[0])}`;
				return `Ongeldige optie: verwacht één van ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				const longName = issue.origin === "date" ? "laat" : issue.origin === "string" ? "lang" : "groot";
				if (sizing) return `Te ${longName}: verwacht dat ${issue.origin ?? "waarde"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementen"} ${sizing.verb}`;
				return `Te ${longName}: verwacht dat ${issue.origin ?? "waarde"} ${adj}${issue.maximum.toString()} is`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				const shortName = issue.origin === "date" ? "vroeg" : issue.origin === "string" ? "kort" : "klein";
				if (sizing) return `Te ${shortName}: verwacht dat ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit} ${sizing.verb}`;
				return `Te ${shortName}: verwacht dat ${issue.origin} ${adj}${issue.minimum.toString()} is`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ongeldige tekst: moet met "${_issue.prefix}" beginnen`;
				if (_issue.format === "ends_with") return `Ongeldige tekst: moet op "${_issue.suffix}" eindigen`;
				if (_issue.format === "includes") return `Ongeldige tekst: moet "${_issue.includes}" bevatten`;
				if (_issue.format === "regex") return `Ongeldige tekst: moet overeenkomen met patroon ${_issue.pattern}`;
				return `Ongeldig: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Ongeldig getal: moet een veelvoud van ${issue.divisor} zijn`;
			case "unrecognized_keys": return `Onbekende key${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Ongeldige key in ${issue.origin}`;
			case "invalid_union": return "Ongeldige invoer";
			case "invalid_element": return `Ongeldige waarde in ${issue.origin}`;
			default: return `Ongeldige invoer`;
		}
	};
};
function nl_default() {
	return { localeError: error$19() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/no.js
const error$18 = () => {
	const Sizable = {
		string: {
			unit: "tegn",
			verb: "å ha"
		},
		file: {
			unit: "bytes",
			verb: "å ha"
		},
		array: {
			unit: "elementer",
			verb: "å inneholde"
		},
		set: {
			unit: "elementer",
			verb: "å inneholde"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "input",
		email: "e-postadresse",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dato- og klokkeslett",
		date: "ISO-dato",
		time: "ISO-klokkeslett",
		duration: "ISO-varighet",
		ipv4: "IPv4-område",
		ipv6: "IPv6-område",
		cidrv4: "IPv4-spekter",
		cidrv6: "IPv6-spekter",
		base64: "base64-enkodet streng",
		base64url: "base64url-enkodet streng",
		json_string: "JSON-streng",
		e164: "E.164-nummer",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "tall",
		array: "liste"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Ugyldig input: forventet instanceof ${issue.expected}, fikk ${received}`;
				return `Ugyldig input: forventet ${expected}, fikk ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Ugyldig verdi: forventet ${stringifyPrimitive(issue.values[0])}`;
				return `Ugyldig valg: forventet en av ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `For stor(t): forventet ${issue.origin ?? "value"} til å ha ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementer"}`;
				return `For stor(t): forventet ${issue.origin ?? "value"} til å ha ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `For lite(n): forventet ${issue.origin} til å ha ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `For lite(n): forventet ${issue.origin} til å ha ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ugyldig streng: må starte med "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Ugyldig streng: må ende med "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Ugyldig streng: må inneholde "${_issue.includes}"`;
				if (_issue.format === "regex") return `Ugyldig streng: må matche mønsteret ${_issue.pattern}`;
				return `Ugyldig ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Ugyldig tall: må være et multiplum av ${issue.divisor}`;
			case "unrecognized_keys": return `${issue.keys.length > 1 ? "Ukjente nøkler" : "Ukjent nøkkel"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Ugyldig nøkkel i ${issue.origin}`;
			case "invalid_union": return "Ugyldig input";
			case "invalid_element": return `Ugyldig verdi i ${issue.origin}`;
			default: return `Ugyldig input`;
		}
	};
};
function no_default() {
	return { localeError: error$18() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ota.js
const error$17 = () => {
	const Sizable = {
		string: {
			unit: "harf",
			verb: "olmalıdır"
		},
		file: {
			unit: "bayt",
			verb: "olmalıdır"
		},
		array: {
			unit: "unsur",
			verb: "olmalıdır"
		},
		set: {
			unit: "unsur",
			verb: "olmalıdır"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "giren",
		email: "epostagâh",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO hengâmı",
		date: "ISO tarihi",
		time: "ISO zamanı",
		duration: "ISO müddeti",
		ipv4: "IPv4 nişânı",
		ipv6: "IPv6 nişânı",
		cidrv4: "IPv4 menzili",
		cidrv6: "IPv6 menzili",
		base64: "base64-şifreli metin",
		base64url: "base64url-şifreli metin",
		json_string: "JSON metin",
		e164: "E.164 sayısı",
		jwt: "JWT",
		template_literal: "giren"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "numara",
		array: "saf",
		null: "gayb"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Fâsit giren: umulan instanceof ${issue.expected}, alınan ${received}`;
				return `Fâsit giren: umulan ${expected}, alınan ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Fâsit giren: umulan ${stringifyPrimitive(issue.values[0])}`;
				return `Fâsit tercih: mûteberler ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Fazla büyük: ${issue.origin ?? "value"}, ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elements"} sahip olmalıydı.`;
				return `Fazla büyük: ${issue.origin ?? "value"}, ${adj}${issue.maximum.toString()} olmalıydı.`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Fazla küçük: ${issue.origin}, ${adj}${issue.minimum.toString()} ${sizing.unit} sahip olmalıydı.`;
				return `Fazla küçük: ${issue.origin}, ${adj}${issue.minimum.toString()} olmalıydı.`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Fâsit metin: "${_issue.prefix}" ile başlamalı.`;
				if (_issue.format === "ends_with") return `Fâsit metin: "${_issue.suffix}" ile bitmeli.`;
				if (_issue.format === "includes") return `Fâsit metin: "${_issue.includes}" ihtivâ etmeli.`;
				if (_issue.format === "regex") return `Fâsit metin: ${_issue.pattern} nakşına uymalı.`;
				return `Fâsit ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Fâsit sayı: ${issue.divisor} katı olmalıydı.`;
			case "unrecognized_keys": return `Tanınmayan anahtar ${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `${issue.origin} için tanınmayan anahtar var.`;
			case "invalid_union": return "Giren tanınamadı.";
			case "invalid_element": return `${issue.origin} için tanınmayan kıymet var.`;
			default: return `Kıymet tanınamadı.`;
		}
	};
};
function ota_default() {
	return { localeError: error$17() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ps.js
const error$16 = () => {
	const Sizable = {
		string: {
			unit: "توکي",
			verb: "ولري"
		},
		file: {
			unit: "بایټس",
			verb: "ولري"
		},
		array: {
			unit: "توکي",
			verb: "ولري"
		},
		set: {
			unit: "توکي",
			verb: "ولري"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ورودي",
		email: "بریښنالیک",
		url: "یو آر ال",
		emoji: "ایموجي",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "نیټه او وخت",
		date: "نېټه",
		time: "وخت",
		duration: "موده",
		ipv4: "د IPv4 پته",
		ipv6: "د IPv6 پته",
		cidrv4: "د IPv4 ساحه",
		cidrv6: "د IPv6 ساحه",
		base64: "base64-encoded متن",
		base64url: "base64url-encoded متن",
		json_string: "JSON متن",
		e164: "د E.164 شمېره",
		jwt: "JWT",
		template_literal: "ورودي"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "عدد",
		array: "ارې"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `ناسم ورودي: باید instanceof ${issue.expected} وای, مګر ${received} ترلاسه شو`;
				return `ناسم ورودي: باید ${expected} وای, مګر ${received} ترلاسه شو`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `ناسم ورودي: باید ${stringifyPrimitive(issue.values[0])} وای`;
				return `ناسم انتخاب: باید یو له ${joinValues(issue.values, "|")} څخه وای`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `ډیر لوی: ${issue.origin ?? "ارزښت"} باید ${adj}${issue.maximum.toString()} ${sizing.unit ?? "عنصرونه"} ولري`;
				return `ډیر لوی: ${issue.origin ?? "ارزښت"} باید ${adj}${issue.maximum.toString()} وي`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `ډیر کوچنی: ${issue.origin} باید ${adj}${issue.minimum.toString()} ${sizing.unit} ولري`;
				return `ډیر کوچنی: ${issue.origin} باید ${adj}${issue.minimum.toString()} وي`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `ناسم متن: باید د "${_issue.prefix}" سره پیل شي`;
				if (_issue.format === "ends_with") return `ناسم متن: باید د "${_issue.suffix}" سره پای ته ورسيږي`;
				if (_issue.format === "includes") return `ناسم متن: باید "${_issue.includes}" ولري`;
				if (_issue.format === "regex") return `ناسم متن: باید د ${_issue.pattern} سره مطابقت ولري`;
				return `${FormatDictionary[_issue.format] ?? issue.format} ناسم دی`;
			}
			case "not_multiple_of": return `ناسم عدد: باید د ${issue.divisor} مضرب وي`;
			case "unrecognized_keys": return `ناسم ${issue.keys.length > 1 ? "کلیډونه" : "کلیډ"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `ناسم کلیډ په ${issue.origin} کې`;
			case "invalid_union": return `ناسمه ورودي`;
			case "invalid_element": return `ناسم عنصر په ${issue.origin} کې`;
			default: return `ناسمه ورودي`;
		}
	};
};
function ps_default() {
	return { localeError: error$16() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/pl.js
const error$15 = () => {
	const Sizable = {
		string: {
			unit: "znaków",
			verb: "mieć"
		},
		file: {
			unit: "bajtów",
			verb: "mieć"
		},
		array: {
			unit: "elementów",
			verb: "mieć"
		},
		set: {
			unit: "elementów",
			verb: "mieć"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "wyrażenie",
		email: "adres email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data i godzina w formacie ISO",
		date: "data w formacie ISO",
		time: "godzina w formacie ISO",
		duration: "czas trwania ISO",
		ipv4: "adres IPv4",
		ipv6: "adres IPv6",
		cidrv4: "zakres IPv4",
		cidrv6: "zakres IPv6",
		base64: "ciąg znaków zakodowany w formacie base64",
		base64url: "ciąg znaków zakodowany w formacie base64url",
		json_string: "ciąg znaków w formacie JSON",
		e164: "liczba E.164",
		jwt: "JWT",
		template_literal: "wejście"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "liczba",
		array: "tablica"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Nieprawidłowe dane wejściowe: oczekiwano instanceof ${issue.expected}, otrzymano ${received}`;
				return `Nieprawidłowe dane wejściowe: oczekiwano ${expected}, otrzymano ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Nieprawidłowe dane wejściowe: oczekiwano ${stringifyPrimitive(issue.values[0])}`;
				return `Nieprawidłowa opcja: oczekiwano jednej z wartości ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Za duża wartość: oczekiwano, że ${issue.origin ?? "wartość"} będzie mieć ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementów"}`;
				return `Zbyt duż(y/a/e): oczekiwano, że ${issue.origin ?? "wartość"} będzie wynosić ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Za mała wartość: oczekiwano, że ${issue.origin ?? "wartość"} będzie mieć ${adj}${issue.minimum.toString()} ${sizing.unit ?? "elementów"}`;
				return `Zbyt mał(y/a/e): oczekiwano, że ${issue.origin ?? "wartość"} będzie wynosić ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Nieprawidłowy ciąg znaków: musi zaczynać się od "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Nieprawidłowy ciąg znaków: musi kończyć się na "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Nieprawidłowy ciąg znaków: musi zawierać "${_issue.includes}"`;
				if (_issue.format === "regex") return `Nieprawidłowy ciąg znaków: musi odpowiadać wzorcowi ${_issue.pattern}`;
				return `Nieprawidłow(y/a/e) ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Nieprawidłowa liczba: musi być wielokrotnością ${issue.divisor}`;
			case "unrecognized_keys": return `Nierozpoznane klucze${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Nieprawidłowy klucz w ${issue.origin}`;
			case "invalid_union": return "Nieprawidłowe dane wejściowe";
			case "invalid_element": return `Nieprawidłowa wartość w ${issue.origin}`;
			default: return `Nieprawidłowe dane wejściowe`;
		}
	};
};
function pl_default() {
	return { localeError: error$15() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/pt.js
const error$14 = () => {
	const Sizable = {
		string: {
			unit: "caracteres",
			verb: "ter"
		},
		file: {
			unit: "bytes",
			verb: "ter"
		},
		array: {
			unit: "itens",
			verb: "ter"
		},
		set: {
			unit: "itens",
			verb: "ter"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "padrão",
		email: "endereço de e-mail",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data e hora ISO",
		date: "data ISO",
		time: "hora ISO",
		duration: "duração ISO",
		ipv4: "endereço IPv4",
		ipv6: "endereço IPv6",
		cidrv4: "faixa de IPv4",
		cidrv6: "faixa de IPv6",
		base64: "texto codificado em base64",
		base64url: "URL codificada em base64",
		json_string: "texto JSON",
		e164: "número E.164",
		jwt: "JWT",
		template_literal: "entrada"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "número",
		null: "nulo"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Tipo inválido: esperado instanceof ${issue.expected}, recebido ${received}`;
				return `Tipo inválido: esperado ${expected}, recebido ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Entrada inválida: esperado ${stringifyPrimitive(issue.values[0])}`;
				return `Opção inválida: esperada uma das ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Muito grande: esperado que ${issue.origin ?? "valor"} tivesse ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementos"}`;
				return `Muito grande: esperado que ${issue.origin ?? "valor"} fosse ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Muito pequeno: esperado que ${issue.origin} tivesse ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Muito pequeno: esperado que ${issue.origin} fosse ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Texto inválido: deve começar com "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Texto inválido: deve terminar com "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Texto inválido: deve incluir "${_issue.includes}"`;
				if (_issue.format === "regex") return `Texto inválido: deve corresponder ao padrão ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} inválido`;
			}
			case "not_multiple_of": return `Número inválido: deve ser múltiplo de ${issue.divisor}`;
			case "unrecognized_keys": return `Chave${issue.keys.length > 1 ? "s" : ""} desconhecida${issue.keys.length > 1 ? "s" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Chave inválida em ${issue.origin}`;
			case "invalid_union": return "Entrada inválida";
			case "invalid_element": return `Valor inválido em ${issue.origin}`;
			default: return `Campo inválido`;
		}
	};
};
function pt_default() {
	return { localeError: error$14() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ro.js
const error$13 = () => {
	const Sizable = {
		string: {
			unit: "caractere",
			verb: "să aibă"
		},
		file: {
			unit: "octeți",
			verb: "să aibă"
		},
		array: {
			unit: "elemente",
			verb: "să aibă"
		},
		set: {
			unit: "elemente",
			verb: "să aibă"
		},
		map: {
			unit: "intrări",
			verb: "să aibă"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "intrare",
		email: "adresă de email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "dată și oră ISO",
		date: "dată ISO",
		time: "oră ISO",
		duration: "durată ISO",
		ipv4: "adresă IPv4",
		ipv6: "adresă IPv6",
		mac: "adresă MAC",
		cidrv4: "interval IPv4",
		cidrv6: "interval IPv6",
		base64: "șir codat base64",
		base64url: "șir codat base64url",
		json_string: "șir JSON",
		e164: "număr E.164",
		jwt: "JWT",
		template_literal: "intrare"
	};
	const TypeDictionary = {
		nan: "NaN",
		string: "șir",
		number: "număr",
		boolean: "boolean",
		function: "funcție",
		array: "matrice",
		object: "obiect",
		undefined: "nedefinit",
		symbol: "simbol",
		bigint: "număr mare",
		void: "void",
		never: "never",
		map: "hartă",
		set: "set"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				return `Intrare invalidă: așteptat ${expected}, primit ${TypeDictionary[receivedType] ?? receivedType}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Intrare invalidă: așteptat ${stringifyPrimitive(issue.values[0])}`;
				return `Opțiune invalidă: așteptat una dintre ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Prea mare: așteptat ca ${issue.origin ?? "valoarea"} ${sizing.verb} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elemente"}`;
				return `Prea mare: așteptat ca ${issue.origin ?? "valoarea"} să fie ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Prea mic: așteptat ca ${issue.origin} ${sizing.verb} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Prea mic: așteptat ca ${issue.origin} să fie ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Șir invalid: trebuie să înceapă cu "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Șir invalid: trebuie să se termine cu "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Șir invalid: trebuie să includă "${_issue.includes}"`;
				if (_issue.format === "regex") return `Șir invalid: trebuie să se potrivească cu modelul ${_issue.pattern}`;
				return `Format invalid: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Număr invalid: trebuie să fie multiplu de ${issue.divisor}`;
			case "unrecognized_keys": return `Chei nerecunoscute: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Cheie invalidă în ${issue.origin}`;
			case "invalid_union": return "Intrare invalidă";
			case "invalid_element": return `Valoare invalidă în ${issue.origin}`;
			default: return `Intrare invalidă`;
		}
	};
};
function ro_default() {
	return { localeError: error$13() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ru.js
function getRussianPlural(count, one, few, many) {
	const absCount = Math.abs(count);
	const lastDigit = absCount % 10;
	const lastTwoDigits = absCount % 100;
	if (lastTwoDigits >= 11 && lastTwoDigits <= 19) return many;
	if (lastDigit === 1) return one;
	if (lastDigit >= 2 && lastDigit <= 4) return few;
	return many;
}
const error$12 = () => {
	const Sizable = {
		string: {
			unit: {
				one: "символ",
				few: "символа",
				many: "символов"
			},
			verb: "иметь"
		},
		file: {
			unit: {
				one: "байт",
				few: "байта",
				many: "байт"
			},
			verb: "иметь"
		},
		array: {
			unit: {
				one: "элемент",
				few: "элемента",
				many: "элементов"
			},
			verb: "иметь"
		},
		set: {
			unit: {
				one: "элемент",
				few: "элемента",
				many: "элементов"
			},
			verb: "иметь"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ввод",
		email: "email адрес",
		url: "URL",
		emoji: "эмодзи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO дата и время",
		date: "ISO дата",
		time: "ISO время",
		duration: "ISO длительность",
		ipv4: "IPv4 адрес",
		ipv6: "IPv6 адрес",
		cidrv4: "IPv4 диапазон",
		cidrv6: "IPv6 диапазон",
		base64: "строка в формате base64",
		base64url: "строка в формате base64url",
		json_string: "JSON строка",
		e164: "номер E.164",
		jwt: "JWT",
		template_literal: "ввод"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "число",
		array: "массив"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Неверный ввод: ожидалось instanceof ${issue.expected}, получено ${received}`;
				return `Неверный ввод: ожидалось ${expected}, получено ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Неверный ввод: ожидалось ${stringifyPrimitive(issue.values[0])}`;
				return `Неверный вариант: ожидалось одно из ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) {
					const unit = getRussianPlural(Number(issue.maximum), sizing.unit.one, sizing.unit.few, sizing.unit.many);
					return `Слишком большое значение: ожидалось, что ${issue.origin ?? "значение"} будет иметь ${adj}${issue.maximum.toString()} ${unit}`;
				}
				return `Слишком большое значение: ожидалось, что ${issue.origin ?? "значение"} будет ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) {
					const unit = getRussianPlural(Number(issue.minimum), sizing.unit.one, sizing.unit.few, sizing.unit.many);
					return `Слишком маленькое значение: ожидалось, что ${issue.origin} будет иметь ${adj}${issue.minimum.toString()} ${unit}`;
				}
				return `Слишком маленькое значение: ожидалось, что ${issue.origin} будет ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Неверная строка: должна начинаться с "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Неверная строка: должна заканчиваться на "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Неверная строка: должна содержать "${_issue.includes}"`;
				if (_issue.format === "regex") return `Неверная строка: должна соответствовать шаблону ${_issue.pattern}`;
				return `Неверный ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Неверное число: должно быть кратным ${issue.divisor}`;
			case "unrecognized_keys": return `Нераспознанн${issue.keys.length > 1 ? "ые" : "ый"} ключ${issue.keys.length > 1 ? "и" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Неверный ключ в ${issue.origin}`;
			case "invalid_union": return "Неверные входные данные";
			case "invalid_element": return `Неверное значение в ${issue.origin}`;
			default: return `Неверные входные данные`;
		}
	};
};
function ru_default() {
	return { localeError: error$12() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/sl.js
const error$11 = () => {
	const Sizable = {
		string: {
			unit: "znakov",
			verb: "imeti"
		},
		file: {
			unit: "bajtov",
			verb: "imeti"
		},
		array: {
			unit: "elementov",
			verb: "imeti"
		},
		set: {
			unit: "elementov",
			verb: "imeti"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "vnos",
		email: "e-poštni naslov",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datum in čas",
		date: "ISO datum",
		time: "ISO čas",
		duration: "ISO trajanje",
		ipv4: "IPv4 naslov",
		ipv6: "IPv6 naslov",
		cidrv4: "obseg IPv4",
		cidrv6: "obseg IPv6",
		base64: "base64 kodiran niz",
		base64url: "base64url kodiran niz",
		json_string: "JSON niz",
		e164: "E.164 številka",
		jwt: "JWT",
		template_literal: "vnos"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "število",
		array: "tabela"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Neveljaven vnos: pričakovano instanceof ${issue.expected}, prejeto ${received}`;
				return `Neveljaven vnos: pričakovano ${expected}, prejeto ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Neveljaven vnos: pričakovano ${stringifyPrimitive(issue.values[0])}`;
				return `Neveljavna možnost: pričakovano eno izmed ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Preveliko: pričakovano, da bo ${issue.origin ?? "vrednost"} imelo ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elementov"}`;
				return `Preveliko: pričakovano, da bo ${issue.origin ?? "vrednost"} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Premajhno: pričakovano, da bo ${issue.origin} imelo ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Premajhno: pričakovano, da bo ${issue.origin} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Neveljaven niz: mora se začeti z "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Neveljaven niz: mora se končati z "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Neveljaven niz: mora vsebovati "${_issue.includes}"`;
				if (_issue.format === "regex") return `Neveljaven niz: mora ustrezati vzorcu ${_issue.pattern}`;
				return `Neveljaven ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Neveljavno število: mora biti večkratnik ${issue.divisor}`;
			case "unrecognized_keys": return `Neprepoznan${issue.keys.length > 1 ? "i ključi" : " ključ"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Neveljaven ključ v ${issue.origin}`;
			case "invalid_union": return "Neveljaven vnos";
			case "invalid_element": return `Neveljavna vrednost v ${issue.origin}`;
			default: return "Neveljaven vnos";
		}
	};
};
function sl_default() {
	return { localeError: error$11() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/sv.js
const error$10 = () => {
	const Sizable = {
		string: {
			unit: "tecken",
			verb: "att ha"
		},
		file: {
			unit: "bytes",
			verb: "att ha"
		},
		array: {
			unit: "objekt",
			verb: "att innehålla"
		},
		set: {
			unit: "objekt",
			verb: "att innehålla"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "reguljärt uttryck",
		email: "e-postadress",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-datum och tid",
		date: "ISO-datum",
		time: "ISO-tid",
		duration: "ISO-varaktighet",
		ipv4: "IPv4-intervall",
		ipv6: "IPv6-intervall",
		cidrv4: "IPv4-spektrum",
		cidrv6: "IPv6-spektrum",
		base64: "base64-kodad sträng",
		base64url: "base64url-kodad sträng",
		json_string: "JSON-sträng",
		e164: "E.164-nummer",
		jwt: "JWT",
		template_literal: "mall-literal"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "antal",
		array: "lista"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Ogiltig inmatning: förväntat instanceof ${issue.expected}, fick ${received}`;
				return `Ogiltig inmatning: förväntat ${expected}, fick ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Ogiltig inmatning: förväntat ${stringifyPrimitive(issue.values[0])}`;
				return `Ogiltigt val: förväntade en av ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `För stor(t): förväntade ${issue.origin ?? "värdet"} att ha ${adj}${issue.maximum.toString()} ${sizing.unit ?? "element"}`;
				return `För stor(t): förväntat ${issue.origin ?? "värdet"} att ha ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `För lite(t): förväntade ${issue.origin ?? "värdet"} att ha ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `För lite(t): förväntade ${issue.origin ?? "värdet"} att ha ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ogiltig sträng: måste börja med "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Ogiltig sträng: måste sluta med "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Ogiltig sträng: måste innehålla "${_issue.includes}"`;
				if (_issue.format === "regex") return `Ogiltig sträng: måste matcha mönstret "${_issue.pattern}"`;
				return `Ogiltig(t) ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Ogiltigt tal: måste vara en multipel av ${issue.divisor}`;
			case "unrecognized_keys": return `${issue.keys.length > 1 ? "Okända nycklar" : "Okänd nyckel"}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Ogiltig nyckel i ${issue.origin ?? "värdet"}`;
			case "invalid_union": return "Ogiltig input";
			case "invalid_element": return `Ogiltigt värde i ${issue.origin ?? "värdet"}`;
			default: return `Ogiltig input`;
		}
	};
};
function sv_default() {
	return { localeError: error$10() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ta.js
const error$9 = () => {
	const Sizable = {
		string: {
			unit: "எழுத்துக்கள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		file: {
			unit: "பைட்டுகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		array: {
			unit: "உறுப்புகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		set: {
			unit: "உறுப்புகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "உள்ளீடு",
		email: "மின்னஞ்சல் முகவரி",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO தேதி நேரம்",
		date: "ISO தேதி",
		time: "ISO நேரம்",
		duration: "ISO கால அளவு",
		ipv4: "IPv4 முகவரி",
		ipv6: "IPv6 முகவரி",
		cidrv4: "IPv4 வரம்பு",
		cidrv6: "IPv6 வரம்பு",
		base64: "base64-encoded சரம்",
		base64url: "base64url-encoded சரம்",
		json_string: "JSON சரம்",
		e164: "E.164 எண்",
		jwt: "JWT",
		template_literal: "input"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "எண்",
		array: "அணி",
		null: "வெறுமை"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `தவறான உள்ளீடு: எதிர்பார்க்கப்பட்டது instanceof ${issue.expected}, பெறப்பட்டது ${received}`;
				return `தவறான உள்ளீடு: எதிர்பார்க்கப்பட்டது ${expected}, பெறப்பட்டது ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `தவறான உள்ளீடு: எதிர்பார்க்கப்பட்டது ${stringifyPrimitive(issue.values[0])}`;
				return `தவறான விருப்பம்: எதிர்பார்க்கப்பட்டது ${joinValues(issue.values, "|")} இல் ஒன்று`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `மிக பெரியது: எதிர்பார்க்கப்பட்டது ${issue.origin ?? "மதிப்பு"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "உறுப்புகள்"} ஆக இருக்க வேண்டும்`;
				return `மிக பெரியது: எதிர்பார்க்கப்பட்டது ${issue.origin ?? "மதிப்பு"} ${adj}${issue.maximum.toString()} ஆக இருக்க வேண்டும்`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `மிகச் சிறியது: எதிர்பார்க்கப்பட்டது ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit} ஆக இருக்க வேண்டும்`;
				return `மிகச் சிறியது: எதிர்பார்க்கப்பட்டது ${issue.origin} ${adj}${issue.minimum.toString()} ஆக இருக்க வேண்டும்`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `தவறான சரம்: "${_issue.prefix}" இல் தொடங்க வேண்டும்`;
				if (_issue.format === "ends_with") return `தவறான சரம்: "${_issue.suffix}" இல் முடிவடைய வேண்டும்`;
				if (_issue.format === "includes") return `தவறான சரம்: "${_issue.includes}" ஐ உள்ளடக்க வேண்டும்`;
				if (_issue.format === "regex") return `தவறான சரம்: ${_issue.pattern} முறைபாட்டுடன் பொருந்த வேண்டும்`;
				return `தவறான ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `தவறான எண்: ${issue.divisor} இன் பலமாக இருக்க வேண்டும்`;
			case "unrecognized_keys": return `அடையாளம் தெரியாத விசை${issue.keys.length > 1 ? "கள்" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `${issue.origin} இல் தவறான விசை`;
			case "invalid_union": return "தவறான உள்ளீடு";
			case "invalid_element": return `${issue.origin} இல் தவறான மதிப்பு`;
			default: return `தவறான உள்ளீடு`;
		}
	};
};
function ta_default() {
	return { localeError: error$9() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/th.js
const error$8 = () => {
	const Sizable = {
		string: {
			unit: "ตัวอักษร",
			verb: "ควรมี"
		},
		file: {
			unit: "ไบต์",
			verb: "ควรมี"
		},
		array: {
			unit: "รายการ",
			verb: "ควรมี"
		},
		set: {
			unit: "รายการ",
			verb: "ควรมี"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ข้อมูลที่ป้อน",
		email: "ที่อยู่อีเมล",
		url: "URL",
		emoji: "อิโมจิ",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "วันที่เวลาแบบ ISO",
		date: "วันที่แบบ ISO",
		time: "เวลาแบบ ISO",
		duration: "ช่วงเวลาแบบ ISO",
		ipv4: "ที่อยู่ IPv4",
		ipv6: "ที่อยู่ IPv6",
		cidrv4: "ช่วง IP แบบ IPv4",
		cidrv6: "ช่วง IP แบบ IPv6",
		base64: "ข้อความแบบ Base64",
		base64url: "ข้อความแบบ Base64 สำหรับ URL",
		json_string: "ข้อความแบบ JSON",
		e164: "เบอร์โทรศัพท์ระหว่างประเทศ (E.164)",
		jwt: "โทเคน JWT",
		template_literal: "ข้อมูลที่ป้อน"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "ตัวเลข",
		array: "อาร์เรย์ (Array)",
		null: "ไม่มีค่า (null)"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `ประเภทข้อมูลไม่ถูกต้อง: ควรเป็น instanceof ${issue.expected} แต่ได้รับ ${received}`;
				return `ประเภทข้อมูลไม่ถูกต้อง: ควรเป็น ${expected} แต่ได้รับ ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `ค่าไม่ถูกต้อง: ควรเป็น ${stringifyPrimitive(issue.values[0])}`;
				return `ตัวเลือกไม่ถูกต้อง: ควรเป็นหนึ่งใน ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "ไม่เกิน" : "น้อยกว่า";
				const sizing = getSizing(issue.origin);
				if (sizing) return `เกินกำหนด: ${issue.origin ?? "ค่า"} ควรมี${adj} ${issue.maximum.toString()} ${sizing.unit ?? "รายการ"}`;
				return `เกินกำหนด: ${issue.origin ?? "ค่า"} ควรมี${adj} ${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? "อย่างน้อย" : "มากกว่า";
				const sizing = getSizing(issue.origin);
				if (sizing) return `น้อยกว่ากำหนด: ${issue.origin} ควรมี${adj} ${issue.minimum.toString()} ${sizing.unit}`;
				return `น้อยกว่ากำหนด: ${issue.origin} ควรมี${adj} ${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `รูปแบบไม่ถูกต้อง: ข้อความต้องขึ้นต้นด้วย "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `รูปแบบไม่ถูกต้อง: ข้อความต้องลงท้ายด้วย "${_issue.suffix}"`;
				if (_issue.format === "includes") return `รูปแบบไม่ถูกต้อง: ข้อความต้องมี "${_issue.includes}" อยู่ในข้อความ`;
				if (_issue.format === "regex") return `รูปแบบไม่ถูกต้อง: ต้องตรงกับรูปแบบที่กำหนด ${_issue.pattern}`;
				return `รูปแบบไม่ถูกต้อง: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `ตัวเลขไม่ถูกต้อง: ต้องเป็นจำนวนที่หารด้วย ${issue.divisor} ได้ลงตัว`;
			case "unrecognized_keys": return `พบคีย์ที่ไม่รู้จัก: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `คีย์ไม่ถูกต้องใน ${issue.origin}`;
			case "invalid_union": return "ข้อมูลไม่ถูกต้อง: ไม่ตรงกับรูปแบบยูเนียนที่กำหนดไว้";
			case "invalid_element": return `ข้อมูลไม่ถูกต้องใน ${issue.origin}`;
			default: return `ข้อมูลไม่ถูกต้อง`;
		}
	};
};
function th_default() {
	return { localeError: error$8() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/tr.js
const error$7 = () => {
	const Sizable = {
		string: {
			unit: "karakter",
			verb: "olmalı"
		},
		file: {
			unit: "bayt",
			verb: "olmalı"
		},
		array: {
			unit: "öğe",
			verb: "olmalı"
		},
		set: {
			unit: "öğe",
			verb: "olmalı"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "girdi",
		email: "e-posta adresi",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO tarih ve saat",
		date: "ISO tarih",
		time: "ISO saat",
		duration: "ISO süre",
		ipv4: "IPv4 adresi",
		ipv6: "IPv6 adresi",
		cidrv4: "IPv4 aralığı",
		cidrv6: "IPv6 aralığı",
		base64: "base64 ile şifrelenmiş metin",
		base64url: "base64url ile şifrelenmiş metin",
		json_string: "JSON dizesi",
		e164: "E.164 sayısı",
		jwt: "JWT",
		template_literal: "Şablon dizesi"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Geçersiz değer: beklenen instanceof ${issue.expected}, alınan ${received}`;
				return `Geçersiz değer: beklenen ${expected}, alınan ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Geçersiz değer: beklenen ${stringifyPrimitive(issue.values[0])}`;
				return `Geçersiz seçenek: aşağıdakilerden biri olmalı: ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Çok büyük: beklenen ${issue.origin ?? "değer"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "öğe"}`;
				return `Çok büyük: beklenen ${issue.origin ?? "değer"} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Çok küçük: beklenen ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Çok küçük: beklenen ${issue.origin} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Geçersiz metin: "${_issue.prefix}" ile başlamalı`;
				if (_issue.format === "ends_with") return `Geçersiz metin: "${_issue.suffix}" ile bitmeli`;
				if (_issue.format === "includes") return `Geçersiz metin: "${_issue.includes}" içermeli`;
				if (_issue.format === "regex") return `Geçersiz metin: ${_issue.pattern} desenine uymalı`;
				return `Geçersiz ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Geçersiz sayı: ${issue.divisor} ile tam bölünebilmeli`;
			case "unrecognized_keys": return `Tanınmayan anahtar${issue.keys.length > 1 ? "lar" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `${issue.origin} içinde geçersiz anahtar`;
			case "invalid_union": return "Geçersiz değer";
			case "invalid_element": return `${issue.origin} içinde geçersiz değer`;
			default: return `Geçersiz değer`;
		}
	};
};
function tr_default() {
	return { localeError: error$7() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/uk.js
const error$6 = () => {
	const Sizable = {
		string: {
			unit: "символів",
			verb: "матиме"
		},
		file: {
			unit: "байтів",
			verb: "матиме"
		},
		array: {
			unit: "елементів",
			verb: "матиме"
		},
		set: {
			unit: "елементів",
			verb: "матиме"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "вхідні дані",
		email: "адреса електронної пошти",
		url: "URL",
		emoji: "емодзі",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "дата та час ISO",
		date: "дата ISO",
		time: "час ISO",
		duration: "тривалість ISO",
		ipv4: "адреса IPv4",
		ipv6: "адреса IPv6",
		cidrv4: "діапазон IPv4",
		cidrv6: "діапазон IPv6",
		base64: "рядок у кодуванні base64",
		base64url: "рядок у кодуванні base64url",
		json_string: "рядок JSON",
		e164: "номер E.164",
		jwt: "JWT",
		template_literal: "вхідні дані"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "число",
		array: "масив"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Неправильні вхідні дані: очікується instanceof ${issue.expected}, отримано ${received}`;
				return `Неправильні вхідні дані: очікується ${expected}, отримано ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Неправильні вхідні дані: очікується ${stringifyPrimitive(issue.values[0])}`;
				return `Неправильна опція: очікується одне з ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Занадто велике: очікується, що ${issue.origin ?? "значення"} ${sizing.verb} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "елементів"}`;
				return `Занадто велике: очікується, що ${issue.origin ?? "значення"} буде ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Занадто мале: очікується, що ${issue.origin} ${sizing.verb} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Занадто мале: очікується, що ${issue.origin} буде ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Неправильний рядок: повинен починатися з "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Неправильний рядок: повинен закінчуватися на "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Неправильний рядок: повинен містити "${_issue.includes}"`;
				if (_issue.format === "regex") return `Неправильний рядок: повинен відповідати шаблону ${_issue.pattern}`;
				return `Неправильний ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Неправильне число: повинно бути кратним ${issue.divisor}`;
			case "unrecognized_keys": return `Нерозпізнаний ключ${issue.keys.length > 1 ? "і" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Неправильний ключ у ${issue.origin}`;
			case "invalid_union": return "Неправильні вхідні дані";
			case "invalid_element": return `Неправильне значення у ${issue.origin}`;
			default: return `Неправильні вхідні дані`;
		}
	};
};
function uk_default() {
	return { localeError: error$6() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ua.js
/** @deprecated Use `uk` instead. */
function ua_default() {
	return uk_default();
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/ur.js
const error$5 = () => {
	const Sizable = {
		string: {
			unit: "حروف",
			verb: "ہونا"
		},
		file: {
			unit: "بائٹس",
			verb: "ہونا"
		},
		array: {
			unit: "آئٹمز",
			verb: "ہونا"
		},
		set: {
			unit: "آئٹمز",
			verb: "ہونا"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ان پٹ",
		email: "ای میل ایڈریس",
		url: "یو آر ایل",
		emoji: "ایموجی",
		uuid: "یو یو آئی ڈی",
		uuidv4: "یو یو آئی ڈی وی 4",
		uuidv6: "یو یو آئی ڈی وی 6",
		nanoid: "نینو آئی ڈی",
		guid: "جی یو آئی ڈی",
		cuid: "سی یو آئی ڈی",
		cuid2: "سی یو آئی ڈی 2",
		ulid: "یو ایل آئی ڈی",
		xid: "ایکس آئی ڈی",
		ksuid: "کے ایس یو آئی ڈی",
		datetime: "آئی ایس او ڈیٹ ٹائم",
		date: "آئی ایس او تاریخ",
		time: "آئی ایس او وقت",
		duration: "آئی ایس او مدت",
		ipv4: "آئی پی وی 4 ایڈریس",
		ipv6: "آئی پی وی 6 ایڈریس",
		cidrv4: "آئی پی وی 4 رینج",
		cidrv6: "آئی پی وی 6 رینج",
		base64: "بیس 64 ان کوڈڈ سٹرنگ",
		base64url: "بیس 64 یو آر ایل ان کوڈڈ سٹرنگ",
		json_string: "جے ایس او این سٹرنگ",
		e164: "ای 164 نمبر",
		jwt: "جے ڈبلیو ٹی",
		template_literal: "ان پٹ"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "نمبر",
		array: "آرے",
		null: "نل"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `غلط ان پٹ: instanceof ${issue.expected} متوقع تھا، ${received} موصول ہوا`;
				return `غلط ان پٹ: ${expected} متوقع تھا، ${received} موصول ہوا`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `غلط ان پٹ: ${stringifyPrimitive(issue.values[0])} متوقع تھا`;
				return `غلط آپشن: ${joinValues(issue.values, "|")} میں سے ایک متوقع تھا`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `بہت بڑا: ${issue.origin ?? "ویلیو"} کے ${adj}${issue.maximum.toString()} ${sizing.unit ?? "عناصر"} ہونے متوقع تھے`;
				return `بہت بڑا: ${issue.origin ?? "ویلیو"} کا ${adj}${issue.maximum.toString()} ہونا متوقع تھا`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `بہت چھوٹا: ${issue.origin} کے ${adj}${issue.minimum.toString()} ${sizing.unit} ہونے متوقع تھے`;
				return `بہت چھوٹا: ${issue.origin} کا ${adj}${issue.minimum.toString()} ہونا متوقع تھا`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `غلط سٹرنگ: "${_issue.prefix}" سے شروع ہونا چاہیے`;
				if (_issue.format === "ends_with") return `غلط سٹرنگ: "${_issue.suffix}" پر ختم ہونا چاہیے`;
				if (_issue.format === "includes") return `غلط سٹرنگ: "${_issue.includes}" شامل ہونا چاہیے`;
				if (_issue.format === "regex") return `غلط سٹرنگ: پیٹرن ${_issue.pattern} سے میچ ہونا چاہیے`;
				return `غلط ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `غلط نمبر: ${issue.divisor} کا مضاعف ہونا چاہیے`;
			case "unrecognized_keys": return `غیر تسلیم شدہ کی${issue.keys.length > 1 ? "ز" : ""}: ${joinValues(issue.keys, "، ")}`;
			case "invalid_key": return `${issue.origin} میں غلط کی`;
			case "invalid_union": return "غلط ان پٹ";
			case "invalid_element": return `${issue.origin} میں غلط ویلیو`;
			default: return `غلط ان پٹ`;
		}
	};
};
function ur_default() {
	return { localeError: error$5() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/uz.js
const error$4 = () => {
	const Sizable = {
		string: {
			unit: "belgi",
			verb: "bo‘lishi kerak"
		},
		file: {
			unit: "bayt",
			verb: "bo‘lishi kerak"
		},
		array: {
			unit: "element",
			verb: "bo‘lishi kerak"
		},
		set: {
			unit: "element",
			verb: "bo‘lishi kerak"
		},
		map: {
			unit: "yozuv",
			verb: "bo‘lishi kerak"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "kirish",
		email: "elektron pochta manzili",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO sana va vaqti",
		date: "ISO sana",
		time: "ISO vaqt",
		duration: "ISO davomiylik",
		ipv4: "IPv4 manzil",
		ipv6: "IPv6 manzil",
		mac: "MAC manzil",
		cidrv4: "IPv4 diapazon",
		cidrv6: "IPv6 diapazon",
		base64: "base64 kodlangan satr",
		base64url: "base64url kodlangan satr",
		json_string: "JSON satr",
		e164: "E.164 raqam",
		jwt: "JWT",
		template_literal: "kirish"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "raqam",
		array: "massiv"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Noto‘g‘ri kirish: kutilgan instanceof ${issue.expected}, qabul qilingan ${received}`;
				return `Noto‘g‘ri kirish: kutilgan ${expected}, qabul qilingan ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Noto‘g‘ri kirish: kutilgan ${stringifyPrimitive(issue.values[0])}`;
				return `Noto‘g‘ri variant: quyidagilardan biri kutilgan ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Juda katta: kutilgan ${issue.origin ?? "qiymat"} ${adj}${issue.maximum.toString()} ${sizing.unit} ${sizing.verb}`;
				return `Juda katta: kutilgan ${issue.origin ?? "qiymat"} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Juda kichik: kutilgan ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit} ${sizing.verb}`;
				return `Juda kichik: kutilgan ${issue.origin} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Noto‘g‘ri satr: "${_issue.prefix}" bilan boshlanishi kerak`;
				if (_issue.format === "ends_with") return `Noto‘g‘ri satr: "${_issue.suffix}" bilan tugashi kerak`;
				if (_issue.format === "includes") return `Noto‘g‘ri satr: "${_issue.includes}" ni o‘z ichiga olishi kerak`;
				if (_issue.format === "regex") return `Noto‘g‘ri satr: ${_issue.pattern} shabloniga mos kelishi kerak`;
				return `Noto‘g‘ri ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Noto‘g‘ri raqam: ${issue.divisor} ning karralisi bo‘lishi kerak`;
			case "unrecognized_keys": return `Noma’lum kalit${issue.keys.length > 1 ? "lar" : ""}: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `${issue.origin} dagi kalit noto‘g‘ri`;
			case "invalid_union": return "Noto‘g‘ri kirish";
			case "invalid_element": return `${issue.origin} da noto‘g‘ri qiymat`;
			default: return `Noto‘g‘ri kirish`;
		}
	};
};
function uz_default() {
	return { localeError: error$4() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/vi.js
const error$3 = () => {
	const Sizable = {
		string: {
			unit: "ký tự",
			verb: "có"
		},
		file: {
			unit: "byte",
			verb: "có"
		},
		array: {
			unit: "phần tử",
			verb: "có"
		},
		set: {
			unit: "phần tử",
			verb: "có"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "đầu vào",
		email: "địa chỉ email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ngày giờ ISO",
		date: "ngày ISO",
		time: "giờ ISO",
		duration: "khoảng thời gian ISO",
		ipv4: "địa chỉ IPv4",
		ipv6: "địa chỉ IPv6",
		cidrv4: "dải IPv4",
		cidrv6: "dải IPv6",
		base64: "chuỗi mã hóa base64",
		base64url: "chuỗi mã hóa base64url",
		json_string: "chuỗi JSON",
		e164: "số E.164",
		jwt: "JWT",
		template_literal: "đầu vào"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "số",
		array: "mảng"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Đầu vào không hợp lệ: mong đợi instanceof ${issue.expected}, nhận được ${received}`;
				return `Đầu vào không hợp lệ: mong đợi ${expected}, nhận được ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Đầu vào không hợp lệ: mong đợi ${stringifyPrimitive(issue.values[0])}`;
				return `Tùy chọn không hợp lệ: mong đợi một trong các giá trị ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Quá lớn: mong đợi ${issue.origin ?? "giá trị"} ${sizing.verb} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "phần tử"}`;
				return `Quá lớn: mong đợi ${issue.origin ?? "giá trị"} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Quá nhỏ: mong đợi ${issue.origin} ${sizing.verb} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `Quá nhỏ: mong đợi ${issue.origin} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Chuỗi không hợp lệ: phải bắt đầu bằng "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Chuỗi không hợp lệ: phải kết thúc bằng "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Chuỗi không hợp lệ: phải bao gồm "${_issue.includes}"`;
				if (_issue.format === "regex") return `Chuỗi không hợp lệ: phải khớp với mẫu ${_issue.pattern}`;
				return `${FormatDictionary[_issue.format] ?? issue.format} không hợp lệ`;
			}
			case "not_multiple_of": return `Số không hợp lệ: phải là bội số của ${issue.divisor}`;
			case "unrecognized_keys": return `Khóa không được nhận dạng: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Khóa không hợp lệ trong ${issue.origin}`;
			case "invalid_union": return "Đầu vào không hợp lệ";
			case "invalid_element": return `Giá trị không hợp lệ trong ${issue.origin}`;
			default: return `Đầu vào không hợp lệ`;
		}
	};
};
function vi_default() {
	return { localeError: error$3() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/zh-CN.js
const error$2 = () => {
	const Sizable = {
		string: {
			unit: "字符",
			verb: "包含"
		},
		file: {
			unit: "字节",
			verb: "包含"
		},
		array: {
			unit: "项",
			verb: "包含"
		},
		set: {
			unit: "项",
			verb: "包含"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "输入",
		email: "电子邮件",
		url: "URL",
		emoji: "表情符号",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO日期时间",
		date: "ISO日期",
		time: "ISO时间",
		duration: "ISO时长",
		ipv4: "IPv4地址",
		ipv6: "IPv6地址",
		cidrv4: "IPv4网段",
		cidrv6: "IPv6网段",
		base64: "base64编码字符串",
		base64url: "base64url编码字符串",
		json_string: "JSON字符串",
		e164: "E.164号码",
		jwt: "JWT",
		template_literal: "输入"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "数字",
		array: "数组",
		null: "空值(null)"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `无效输入：期望 instanceof ${issue.expected}，实际接收 ${received}`;
				return `无效输入：期望 ${expected}，实际接收 ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `无效输入：期望 ${stringifyPrimitive(issue.values[0])}`;
				return `无效选项：期望以下之一 ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `数值过大：期望 ${issue.origin ?? "值"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "个元素"}`;
				return `数值过大：期望 ${issue.origin ?? "值"} ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `数值过小：期望 ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `数值过小：期望 ${issue.origin} ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `无效字符串：必须以 "${_issue.prefix}" 开头`;
				if (_issue.format === "ends_with") return `无效字符串：必须以 "${_issue.suffix}" 结尾`;
				if (_issue.format === "includes") return `无效字符串：必须包含 "${_issue.includes}"`;
				if (_issue.format === "regex") return `无效字符串：必须满足正则表达式 ${_issue.pattern}`;
				return `无效${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `无效数字：必须是 ${issue.divisor} 的倍数`;
			case "unrecognized_keys": return `出现未知的键(key): ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `${issue.origin} 中的键(key)无效`;
			case "invalid_union": return "无效输入";
			case "invalid_element": return `${issue.origin} 中包含无效值(value)`;
			default: return `无效输入`;
		}
	};
};
function zh_CN_default() {
	return { localeError: error$2() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/zh-TW.js
const error$1 = () => {
	const Sizable = {
		string: {
			unit: "字元",
			verb: "擁有"
		},
		file: {
			unit: "位元組",
			verb: "擁有"
		},
		array: {
			unit: "項目",
			verb: "擁有"
		},
		set: {
			unit: "項目",
			verb: "擁有"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "輸入",
		email: "郵件地址",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO 日期時間",
		date: "ISO 日期",
		time: "ISO 時間",
		duration: "ISO 期間",
		ipv4: "IPv4 位址",
		ipv6: "IPv6 位址",
		cidrv4: "IPv4 範圍",
		cidrv6: "IPv6 範圍",
		base64: "base64 編碼字串",
		base64url: "base64url 編碼字串",
		json_string: "JSON 字串",
		e164: "E.164 數值",
		jwt: "JWT",
		template_literal: "輸入"
	};
	const TypeDictionary = { nan: "NaN" };
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `無效的輸入值：預期為 instanceof ${issue.expected}，但收到 ${received}`;
				return `無效的輸入值：預期為 ${expected}，但收到 ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `無效的輸入值：預期為 ${stringifyPrimitive(issue.values[0])}`;
				return `無效的選項：預期為以下其中之一 ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `數值過大：預期 ${issue.origin ?? "值"} 應為 ${adj}${issue.maximum.toString()} ${sizing.unit ?? "個元素"}`;
				return `數值過大：預期 ${issue.origin ?? "值"} 應為 ${adj}${issue.maximum.toString()}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `數值過小：預期 ${issue.origin} 應為 ${adj}${issue.minimum.toString()} ${sizing.unit}`;
				return `數值過小：預期 ${issue.origin} 應為 ${adj}${issue.minimum.toString()}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `無效的字串：必須以 "${_issue.prefix}" 開頭`;
				if (_issue.format === "ends_with") return `無效的字串：必須以 "${_issue.suffix}" 結尾`;
				if (_issue.format === "includes") return `無效的字串：必須包含 "${_issue.includes}"`;
				if (_issue.format === "regex") return `無效的字串：必須符合格式 ${_issue.pattern}`;
				return `無效的 ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `無效的數字：必須為 ${issue.divisor} 的倍數`;
			case "unrecognized_keys": return `無法識別的鍵值${issue.keys.length > 1 ? "們" : ""}：${joinValues(issue.keys, "、")}`;
			case "invalid_key": return `${issue.origin} 中有無效的鍵值`;
			case "invalid_union": return "無效的輸入值";
			case "invalid_element": return `${issue.origin} 中有無效的值`;
			default: return `無效的輸入值`;
		}
	};
};
function zh_TW_default() {
	return { localeError: error$1() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/yo.js
const error = () => {
	const Sizable = {
		string: {
			unit: "àmi",
			verb: "ní"
		},
		file: {
			unit: "bytes",
			verb: "ní"
		},
		array: {
			unit: "nkan",
			verb: "ní"
		},
		set: {
			unit: "nkan",
			verb: "ní"
		}
	};
	function getSizing(origin) {
		return Sizable[origin] ?? null;
	}
	const FormatDictionary = {
		regex: "ẹ̀rọ ìbáwọlé",
		email: "àdírẹ́sì ìmẹ́lì",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "àkókò ISO",
		date: "ọjọ́ ISO",
		time: "àkókò ISO",
		duration: "àkókò tó pé ISO",
		ipv4: "àdírẹ́sì IPv4",
		ipv6: "àdírẹ́sì IPv6",
		cidrv4: "àgbègbè IPv4",
		cidrv6: "àgbègbè IPv6",
		base64: "ọ̀rọ̀ tí a kọ́ ní base64",
		base64url: "ọ̀rọ̀ base64url",
		json_string: "ọ̀rọ̀ JSON",
		e164: "nọ́mbà E.164",
		jwt: "JWT",
		template_literal: "ẹ̀rọ ìbáwọlé"
	};
	const TypeDictionary = {
		nan: "NaN",
		number: "nọ́mbà",
		array: "akopọ"
	};
	return (issue) => {
		switch (issue.code) {
			case "invalid_type": {
				const expected = TypeDictionary[issue.expected] ?? issue.expected;
				const receivedType = parsedType(issue.input);
				const received = TypeDictionary[receivedType] ?? receivedType;
				if (/^[A-Z]/.test(issue.expected)) return `Ìbáwọlé aṣìṣe: a ní láti fi instanceof ${issue.expected}, àmọ̀ a rí ${received}`;
				return `Ìbáwọlé aṣìṣe: a ní láti fi ${expected}, àmọ̀ a rí ${received}`;
			}
			case "invalid_value":
				if (issue.values.length === 1) return `Ìbáwọlé aṣìṣe: a ní láti fi ${stringifyPrimitive(issue.values[0])}`;
				return `Àṣàyàn aṣìṣe: yan ọ̀kan lára ${joinValues(issue.values, "|")}`;
			case "too_big": {
				const adj = issue.inclusive ? "<=" : "<";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Tó pọ̀ jù: a ní láti jẹ́ pé ${issue.origin ?? "iye"} ${sizing.verb} ${adj}${issue.maximum} ${sizing.unit}`;
				return `Tó pọ̀ jù: a ní láti jẹ́ ${adj}${issue.maximum}`;
			}
			case "too_small": {
				const adj = issue.inclusive ? ">=" : ">";
				const sizing = getSizing(issue.origin);
				if (sizing) return `Kéré ju: a ní láti jẹ́ pé ${issue.origin} ${sizing.verb} ${adj}${issue.minimum} ${sizing.unit}`;
				return `Kéré ju: a ní láti jẹ́ ${adj}${issue.minimum}`;
			}
			case "invalid_format": {
				const _issue = issue;
				if (_issue.format === "starts_with") return `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ bẹ̀rẹ̀ pẹ̀lú "${_issue.prefix}"`;
				if (_issue.format === "ends_with") return `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ parí pẹ̀lú "${_issue.suffix}"`;
				if (_issue.format === "includes") return `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ ní "${_issue.includes}"`;
				if (_issue.format === "regex") return `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ bá àpẹẹrẹ mu ${_issue.pattern}`;
				return `Aṣìṣe: ${FormatDictionary[_issue.format] ?? issue.format}`;
			}
			case "not_multiple_of": return `Nọ́mbà aṣìṣe: gbọ́dọ̀ jẹ́ èyà pípín ti ${issue.divisor}`;
			case "unrecognized_keys": return `Bọtìnì àìmọ̀: ${joinValues(issue.keys, ", ")}`;
			case "invalid_key": return `Bọtìnì aṣìṣe nínú ${issue.origin}`;
			case "invalid_union": return "Ìbáwọlé aṣìṣe";
			case "invalid_element": return `Iye aṣìṣe nínú ${issue.origin}`;
			default: return "Ìbáwọlé aṣìṣe";
		}
	};
};
function yo_default() {
	return { localeError: error() };
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/locales/index.js
var locales_exports = /* @__PURE__ */ __exportAll({
	ar: () => ar_default,
	az: () => az_default,
	be: () => be_default,
	bg: () => bg_default,
	ca: () => ca_default,
	cs: () => cs_default,
	da: () => da_default,
	de: () => de_default,
	el: () => el_default,
	en: () => en_default,
	eo: () => eo_default,
	es: () => es_default,
	fa: () => fa_default,
	fi: () => fi_default,
	fr: () => fr_default,
	frCA: () => fr_CA_default,
	he: () => he_default,
	hr: () => hr_default,
	hu: () => hu_default,
	hy: () => hy_default,
	id: () => id_default,
	is: () => is_default,
	it: () => it_default,
	ja: () => ja_default,
	ka: () => ka_default,
	kh: () => kh_default,
	km: () => km_default,
	ko: () => ko_default,
	lt: () => lt_default,
	mk: () => mk_default,
	ms: () => ms_default,
	nl: () => nl_default,
	no: () => no_default,
	ota: () => ota_default,
	pl: () => pl_default,
	ps: () => ps_default,
	pt: () => pt_default,
	ro: () => ro_default,
	ru: () => ru_default,
	sl: () => sl_default,
	sv: () => sv_default,
	ta: () => ta_default,
	th: () => th_default,
	tr: () => tr_default,
	ua: () => ua_default,
	uk: () => uk_default,
	ur: () => ur_default,
	uz: () => uz_default,
	vi: () => vi_default,
	yo: () => yo_default,
	zhCN: () => zh_CN_default,
	zhTW: () => zh_TW_default
});
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/core/json-schema-generator.js
/**
* Legacy class-based interface for JSON Schema generation.
* This class wraps the new functional implementation to provide backward compatibility.
*
* @deprecated Use the `toJSONSchema` function instead for new code.
*
* @example
* ```typescript
* // Legacy usage (still supported)
* const gen = new JSONSchemaGenerator({ target: "draft-07" });
* gen.process(schema);
* const result = gen.emit(schema);
*
* // Preferred modern usage
* const result = toJSONSchema(schema, { target: "draft-07" });
* ```
*/
var JSONSchemaGenerator = class {
	/** @deprecated Access via ctx instead */
	get metadataRegistry() {
		return this.ctx.metadataRegistry;
	}
	/** @deprecated Access via ctx instead */
	get target() {
		return this.ctx.target;
	}
	/** @deprecated Access via ctx instead */
	get unrepresentable() {
		return this.ctx.unrepresentable;
	}
	/** @deprecated Access via ctx instead */
	get override() {
		return this.ctx.override;
	}
	/** @deprecated Access via ctx instead */
	get io() {
		return this.ctx.io;
	}
	/** @deprecated Access via ctx instead */
	get counter() {
		return this.ctx.counter;
	}
	set counter(value) {
		this.ctx.counter = value;
	}
	/** @deprecated Access via ctx instead */
	get seen() {
		return this.ctx.seen;
	}
	constructor(params) {
		let normalizedTarget = params?.target ?? "draft-2020-12";
		if (normalizedTarget === "draft-4") normalizedTarget = "draft-04";
		if (normalizedTarget === "draft-7") normalizedTarget = "draft-07";
		this.ctx = initializeContext({
			processors: allProcessors,
			target: normalizedTarget,
			...params?.metadata && { metadata: params.metadata },
			...params?.unrepresentable && { unrepresentable: params.unrepresentable },
			...params?.override && { override: params.override },
			...params?.io && { io: params.io }
		});
	}
	/**
	* Process a schema to prepare it for JSON Schema generation.
	* This must be called before emit().
	*/
	process(schema, _params = {
		path: [],
		schemaPath: []
	}) {
		return process(schema, this.ctx, _params);
	}
	/**
	* Emit the final JSON Schema after processing.
	* Must call process() first.
	*/
	emit(schema, _params) {
		if (_params) {
			if (_params.cycles) this.ctx.cycles = _params.cycles;
			if (_params.reused) this.ctx.reused = _params.reused;
			if (_params.external) this.ctx.external = _params.external;
		}
		extractDefs(this.ctx, schema);
		const { "~standard": _, ...plainResult } = finalize(this.ctx, schema);
		return plainResult;
	}
};
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/core/json-schema.js
var json_schema_exports = /* @__PURE__ */ __exportAll({});
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/core/index.js
var core_exports = /* @__PURE__ */ __exportAll({
	$ZodAny: () => $ZodAny,
	$ZodArray: () => $ZodArray,
	$ZodAsyncError: () => $ZodAsyncError,
	$ZodBase64: () => $ZodBase64,
	$ZodBase64URL: () => $ZodBase64URL,
	$ZodBigInt: () => $ZodBigInt,
	$ZodBigIntFormat: () => $ZodBigIntFormat,
	$ZodBoolean: () => $ZodBoolean,
	$ZodCIDRv4: () => $ZodCIDRv4,
	$ZodCIDRv6: () => $ZodCIDRv6,
	$ZodCUID: () => $ZodCUID,
	$ZodCUID2: () => $ZodCUID2,
	$ZodCatch: () => $ZodCatch,
	$ZodCheck: () => $ZodCheck,
	$ZodCheckBigIntFormat: () => $ZodCheckBigIntFormat,
	$ZodCheckEndsWith: () => $ZodCheckEndsWith,
	$ZodCheckGreaterThan: () => $ZodCheckGreaterThan,
	$ZodCheckIncludes: () => $ZodCheckIncludes,
	$ZodCheckLengthEquals: () => $ZodCheckLengthEquals,
	$ZodCheckLessThan: () => $ZodCheckLessThan,
	$ZodCheckLowerCase: () => $ZodCheckLowerCase,
	$ZodCheckMaxLength: () => $ZodCheckMaxLength,
	$ZodCheckMaxSize: () => $ZodCheckMaxSize,
	$ZodCheckMimeType: () => $ZodCheckMimeType,
	$ZodCheckMinLength: () => $ZodCheckMinLength,
	$ZodCheckMinSize: () => $ZodCheckMinSize,
	$ZodCheckMultipleOf: () => $ZodCheckMultipleOf,
	$ZodCheckNumberFormat: () => $ZodCheckNumberFormat,
	$ZodCheckOverwrite: () => $ZodCheckOverwrite,
	$ZodCheckProperty: () => $ZodCheckProperty,
	$ZodCheckRegex: () => $ZodCheckRegex,
	$ZodCheckSizeEquals: () => $ZodCheckSizeEquals,
	$ZodCheckStartsWith: () => $ZodCheckStartsWith,
	$ZodCheckStringFormat: () => $ZodCheckStringFormat,
	$ZodCheckUpperCase: () => $ZodCheckUpperCase,
	$ZodCodec: () => $ZodCodec,
	$ZodCustom: () => $ZodCustom,
	$ZodCustomStringFormat: () => $ZodCustomStringFormat,
	$ZodDate: () => $ZodDate,
	$ZodDefault: () => $ZodDefault,
	$ZodDiscriminatedUnion: () => $ZodDiscriminatedUnion,
	$ZodE164: () => $ZodE164,
	$ZodEmail: () => $ZodEmail,
	$ZodEmoji: () => $ZodEmoji,
	$ZodEncodeError: () => $ZodEncodeError,
	$ZodEnum: () => $ZodEnum,
	$ZodError: () => $ZodError,
	$ZodExactOptional: () => $ZodExactOptional,
	$ZodFile: () => $ZodFile,
	$ZodFunction: () => $ZodFunction,
	$ZodGUID: () => $ZodGUID,
	$ZodIPv4: () => $ZodIPv4,
	$ZodIPv6: () => $ZodIPv6,
	$ZodISODate: () => $ZodISODate,
	$ZodISODateTime: () => $ZodISODateTime,
	$ZodISODuration: () => $ZodISODuration,
	$ZodISOTime: () => $ZodISOTime,
	$ZodIntersection: () => $ZodIntersection,
	$ZodJWT: () => $ZodJWT,
	$ZodKSUID: () => $ZodKSUID,
	$ZodLazy: () => $ZodLazy,
	$ZodLiteral: () => $ZodLiteral,
	$ZodMAC: () => $ZodMAC,
	$ZodMap: () => $ZodMap,
	$ZodNaN: () => $ZodNaN,
	$ZodNanoID: () => $ZodNanoID,
	$ZodNever: () => $ZodNever,
	$ZodNonOptional: () => $ZodNonOptional,
	$ZodNull: () => $ZodNull,
	$ZodNullable: () => $ZodNullable,
	$ZodNumber: () => $ZodNumber,
	$ZodNumberFormat: () => $ZodNumberFormat,
	$ZodObject: () => $ZodObject,
	$ZodObjectJIT: () => $ZodObjectJIT,
	$ZodOptional: () => $ZodOptional,
	$ZodPipe: () => $ZodPipe,
	$ZodPrefault: () => $ZodPrefault,
	$ZodPreprocess: () => $ZodPreprocess,
	$ZodPromise: () => $ZodPromise,
	$ZodReadonly: () => $ZodReadonly,
	$ZodRealError: () => $ZodRealError,
	$ZodRecord: () => $ZodRecord,
	$ZodRegistry: () => $ZodRegistry,
	$ZodSet: () => $ZodSet,
	$ZodString: () => $ZodString,
	$ZodStringFormat: () => $ZodStringFormat,
	$ZodSuccess: () => $ZodSuccess,
	$ZodSymbol: () => $ZodSymbol,
	$ZodTemplateLiteral: () => $ZodTemplateLiteral,
	$ZodTransform: () => $ZodTransform,
	$ZodTuple: () => $ZodTuple,
	$ZodType: () => $ZodType,
	$ZodULID: () => $ZodULID,
	$ZodURL: () => $ZodURL,
	$ZodUUID: () => $ZodUUID,
	$ZodUndefined: () => $ZodUndefined,
	$ZodUnion: () => $ZodUnion,
	$ZodUnknown: () => $ZodUnknown,
	$ZodVoid: () => $ZodVoid,
	$ZodXID: () => $ZodXID,
	$ZodXor: () => $ZodXor,
	$brand: () => $brand,
	$constructor: () => $constructor,
	$input: () => $input,
	$output: () => $output,
	Doc: () => Doc,
	JSONSchema: () => json_schema_exports,
	JSONSchemaGenerator: () => JSONSchemaGenerator,
	NEVER: () => NEVER,
	TimePrecision: () => TimePrecision,
	_any: () => _any,
	_array: () => _array,
	_base64: () => _base64,
	_base64url: () => _base64url,
	_bigint: () => _bigint,
	_boolean: () => _boolean,
	_catch: () => _catch,
	_check: () => _check,
	_cidrv4: () => _cidrv4,
	_cidrv6: () => _cidrv6,
	_coercedBigint: () => _coercedBigint,
	_coercedBoolean: () => _coercedBoolean,
	_coercedDate: () => _coercedDate,
	_coercedNumber: () => _coercedNumber,
	_coercedString: () => _coercedString,
	_cuid: () => _cuid,
	_cuid2: () => _cuid2,
	_custom: () => _custom,
	_date: () => _date,
	_decode: () => _decode,
	_decodeAsync: () => _decodeAsync,
	_default: () => _default,
	_discriminatedUnion: () => _discriminatedUnion,
	_e164: () => _e164,
	_email: () => _email,
	_emoji: () => _emoji,
	_encode: () => _encode,
	_encodeAsync: () => _encodeAsync,
	_endsWith: () => _endsWith,
	_enum: () => _enum,
	_file: () => _file,
	_float32: () => _float32,
	_float64: () => _float64,
	_gt: () => _gt,
	_gte: () => _gte,
	_guid: () => _guid,
	_includes: () => _includes,
	_int: () => _int,
	_int32: () => _int32,
	_int64: () => _int64,
	_intersection: () => _intersection,
	_ipv4: () => _ipv4,
	_ipv6: () => _ipv6,
	_isoDate: () => _isoDate,
	_isoDateTime: () => _isoDateTime,
	_isoDuration: () => _isoDuration,
	_isoTime: () => _isoTime,
	_jwt: () => _jwt,
	_ksuid: () => _ksuid,
	_lazy: () => _lazy,
	_length: () => _length,
	_literal: () => _literal,
	_lowercase: () => _lowercase,
	_lt: () => _lt,
	_lte: () => _lte,
	_mac: () => _mac,
	_map: () => _map,
	_max: () => _lte,
	_maxLength: () => _maxLength,
	_maxSize: () => _maxSize,
	_mime: () => _mime,
	_min: () => _gte,
	_minLength: () => _minLength,
	_minSize: () => _minSize,
	_multipleOf: () => _multipleOf,
	_nan: () => _nan,
	_nanoid: () => _nanoid,
	_nativeEnum: () => _nativeEnum,
	_negative: () => _negative,
	_never: () => _never,
	_nonnegative: () => _nonnegative,
	_nonoptional: () => _nonoptional,
	_nonpositive: () => _nonpositive,
	_normalize: () => _normalize,
	_null: () => _null,
	_nullable: () => _nullable,
	_number: () => _number,
	_optional: () => _optional,
	_overwrite: () => _overwrite,
	_parse: () => _parse,
	_parseAsync: () => _parseAsync,
	_pipe: () => _pipe,
	_positive: () => _positive,
	_promise: () => _promise,
	_property: () => _property,
	_readonly: () => _readonly,
	_record: () => _record,
	_refine: () => _refine,
	_regex: () => _regex,
	_safeDecode: () => _safeDecode,
	_safeDecodeAsync: () => _safeDecodeAsync,
	_safeEncode: () => _safeEncode,
	_safeEncodeAsync: () => _safeEncodeAsync,
	_safeParse: () => _safeParse,
	_safeParseAsync: () => _safeParseAsync,
	_set: () => _set,
	_size: () => _size,
	_slugify: () => _slugify,
	_startsWith: () => _startsWith,
	_string: () => _string,
	_stringFormat: () => _stringFormat,
	_stringbool: () => _stringbool,
	_success: () => _success,
	_superRefine: () => _superRefine,
	_symbol: () => _symbol,
	_templateLiteral: () => _templateLiteral,
	_toLowerCase: () => _toLowerCase,
	_toUpperCase: () => _toUpperCase,
	_transform: () => _transform,
	_trim: () => _trim,
	_tuple: () => _tuple,
	_uint32: () => _uint32,
	_uint64: () => _uint64,
	_ulid: () => _ulid,
	_undefined: () => _undefined,
	_union: () => _union,
	_unknown: () => _unknown,
	_uppercase: () => _uppercase,
	_url: () => _url,
	_uuid: () => _uuid,
	_uuidv4: () => _uuidv4,
	_uuidv6: () => _uuidv6,
	_uuidv7: () => _uuidv7,
	_void: () => _void,
	_xid: () => _xid,
	_xor: () => _xor,
	clone: () => clone,
	config: () => config,
	createStandardJSONSchemaMethod: () => createStandardJSONSchemaMethod,
	createToJSONSchemaMethod: () => createToJSONSchemaMethod,
	decode: () => decode,
	decodeAsync: () => decodeAsync,
	describe: () => describe,
	encode: () => encode,
	encodeAsync: () => encodeAsync,
	extractDefs: () => extractDefs,
	finalize: () => finalize,
	flattenError: () => flattenError,
	formatError: () => formatError,
	globalConfig: () => globalConfig,
	globalRegistry: () => globalRegistry,
	initializeContext: () => initializeContext,
	isValidBase64: () => isValidBase64,
	isValidBase64URL: () => isValidBase64URL,
	isValidJWT: () => isValidJWT,
	locales: () => locales_exports,
	meta: () => meta,
	parse: () => parse,
	parseAsync: () => parseAsync,
	prettifyError: () => prettifyError,
	process: () => process,
	regexes: () => regexes_exports,
	registry: () => registry,
	safeDecode: () => safeDecode,
	safeDecodeAsync: () => safeDecodeAsync,
	safeEncode: () => safeEncode,
	safeEncodeAsync: () => safeEncodeAsync,
	safeParse: () => safeParse,
	safeParseAsync: () => safeParseAsync,
	toDotPath: () => toDotPath,
	toJSONSchema: () => toJSONSchema,
	treeifyError: () => treeifyError,
	util: () => util_exports,
	version: () => version
});
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/classic/checks.js
var checks_exports = /* @__PURE__ */ __exportAll({
	endsWith: () => _endsWith,
	gt: () => _gt,
	gte: () => _gte,
	includes: () => _includes,
	length: () => _length,
	lowercase: () => _lowercase,
	lt: () => _lt,
	lte: () => _lte,
	maxLength: () => _maxLength,
	maxSize: () => _maxSize,
	mime: () => _mime,
	minLength: () => _minLength,
	minSize: () => _minSize,
	multipleOf: () => _multipleOf,
	negative: () => _negative,
	nonnegative: () => _nonnegative,
	nonpositive: () => _nonpositive,
	normalize: () => _normalize,
	overwrite: () => _overwrite,
	positive: () => _positive,
	property: () => _property,
	regex: () => _regex,
	size: () => _size,
	slugify: () => _slugify,
	startsWith: () => _startsWith,
	toLowerCase: () => _toLowerCase,
	toUpperCase: () => _toUpperCase,
	trim: () => _trim,
	uppercase: () => _uppercase
});
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/classic/compat.js
/** @deprecated Use the raw string literal codes instead, e.g. "invalid_type". */
const ZodIssueCode = {
	invalid_type: "invalid_type",
	too_big: "too_big",
	too_small: "too_small",
	invalid_format: "invalid_format",
	not_multiple_of: "not_multiple_of",
	unrecognized_keys: "unrecognized_keys",
	invalid_union: "invalid_union",
	invalid_key: "invalid_key",
	invalid_element: "invalid_element",
	invalid_value: "invalid_value",
	custom: "custom"
};
/** @deprecated Use `z.config(params)` instead. */
function setErrorMap(map) {
	config({ customError: map });
}
/** @deprecated Use `z.config()` instead. */
function getErrorMap() {
	return config().customError;
}
/** @deprecated Do not use. Stub definition, only included for zod-to-json-schema compatibility. */
var ZodFirstPartyTypeKind;
ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {});
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/classic/from-json-schema.js
const z = {
	...schemas_exports,
	...checks_exports,
	iso: iso_exports
};
const RECOGNIZED_KEYS = /*@__PURE__*/ new Set([
	"$schema",
	"$ref",
	"$defs",
	"definitions",
	"$id",
	"id",
	"$comment",
	"$anchor",
	"$vocabulary",
	"$dynamicRef",
	"$dynamicAnchor",
	"type",
	"enum",
	"const",
	"anyOf",
	"oneOf",
	"allOf",
	"not",
	"properties",
	"required",
	"additionalProperties",
	"patternProperties",
	"propertyNames",
	"minProperties",
	"maxProperties",
	"items",
	"prefixItems",
	"additionalItems",
	"minItems",
	"maxItems",
	"uniqueItems",
	"contains",
	"minContains",
	"maxContains",
	"minLength",
	"maxLength",
	"pattern",
	"format",
	"minimum",
	"maximum",
	"exclusiveMinimum",
	"exclusiveMaximum",
	"multipleOf",
	"description",
	"default",
	"contentEncoding",
	"contentMediaType",
	"contentSchema",
	"unevaluatedItems",
	"unevaluatedProperties",
	"if",
	"then",
	"else",
	"dependentSchemas",
	"dependentRequired",
	"nullable",
	"readOnly"
]);
function detectVersion(schema, defaultTarget) {
	const $schema = schema.$schema;
	if ($schema === "https://json-schema.org/draft/2020-12/schema") return "draft-2020-12";
	if ($schema === "http://json-schema.org/draft-07/schema#") return "draft-7";
	if ($schema === "http://json-schema.org/draft-04/schema#") return "draft-4";
	return defaultTarget ?? "draft-2020-12";
}
function resolveRef(ref, ctx) {
	if (!ref.startsWith("#")) throw new Error("External $ref is not supported, only local refs (#/...) are allowed");
	const path = ref.slice(1).split("/").filter(Boolean);
	if (path.length === 0) return ctx.rootSchema;
	const defsKey = ctx.version === "draft-2020-12" ? "$defs" : "definitions";
	if (path[0] === defsKey) {
		const key = path[1];
		if (!key || !ctx.defs[key]) throw new Error(`Reference not found: ${ref}`);
		return ctx.defs[key];
	}
	throw new Error(`Reference not found: ${ref}`);
}
function convertBaseSchema(schema, ctx) {
	if (schema.not !== void 0) {
		if (typeof schema.not === "object" && Object.keys(schema.not).length === 0) return z.never();
		throw new Error("not is not supported in Zod (except { not: {} } for never)");
	}
	if (schema.unevaluatedItems !== void 0) throw new Error("unevaluatedItems is not supported");
	if (schema.unevaluatedProperties !== void 0) throw new Error("unevaluatedProperties is not supported");
	if (schema.if !== void 0 || schema.then !== void 0 || schema.else !== void 0) throw new Error("Conditional schemas (if/then/else) are not supported");
	if (schema.dependentSchemas !== void 0 || schema.dependentRequired !== void 0) throw new Error("dependentSchemas and dependentRequired are not supported");
	if (schema.$ref) {
		const refPath = schema.$ref;
		if (ctx.refs.has(refPath)) return ctx.refs.get(refPath);
		if (ctx.processing.has(refPath)) return z.lazy(() => {
			if (!ctx.refs.has(refPath)) throw new Error(`Circular reference not resolved: ${refPath}`);
			return ctx.refs.get(refPath);
		});
		ctx.processing.add(refPath);
		const zodSchema = convertSchema(resolveRef(refPath, ctx), ctx);
		ctx.refs.set(refPath, zodSchema);
		ctx.processing.delete(refPath);
		return zodSchema;
	}
	if (schema.enum !== void 0) {
		const enumValues = schema.enum;
		if (ctx.version === "openapi-3.0" && schema.nullable === true && enumValues.length === 1 && enumValues[0] === null) return z.null();
		if (enumValues.length === 0) return z.never();
		if (enumValues.length === 1) return z.literal(enumValues[0]);
		if (enumValues.every((v) => typeof v === "string")) return z.enum(enumValues);
		const literalSchemas = enumValues.map((v) => z.literal(v));
		if (literalSchemas.length < 2) return literalSchemas[0];
		return z.union([
			literalSchemas[0],
			literalSchemas[1],
			...literalSchemas.slice(2)
		]);
	}
	if (schema.const !== void 0) return z.literal(schema.const);
	const type = schema.type;
	if (Array.isArray(type)) {
		const typeSchemas = type.map((t) => {
			return convertBaseSchema({
				...schema,
				type: t
			}, ctx);
		});
		if (typeSchemas.length === 0) return z.never();
		if (typeSchemas.length === 1) return typeSchemas[0];
		return z.union(typeSchemas);
	}
	if (!type) return z.any();
	let zodSchema;
	switch (type) {
		case "string": {
			let stringSchema = z.string();
			if (schema.format) {
				const format = schema.format;
				if (format === "email") stringSchema = stringSchema.check(z.email());
				else if (format === "uri" || format === "uri-reference") stringSchema = stringSchema.check(z.url());
				else if (format === "uuid" || format === "guid") stringSchema = stringSchema.check(z.uuid());
				else if (format === "date-time") stringSchema = stringSchema.check(z.iso.datetime());
				else if (format === "date") stringSchema = stringSchema.check(z.iso.date());
				else if (format === "time") stringSchema = stringSchema.check(z.iso.time());
				else if (format === "duration") stringSchema = stringSchema.check(z.iso.duration());
				else if (format === "ipv4") stringSchema = stringSchema.check(z.ipv4());
				else if (format === "ipv6") stringSchema = stringSchema.check(z.ipv6());
				else if (format === "mac") stringSchema = stringSchema.check(z.mac());
				else if (format === "cidr") stringSchema = stringSchema.check(z.cidrv4());
				else if (format === "cidr-v6") stringSchema = stringSchema.check(z.cidrv6());
				else if (format === "base64") stringSchema = stringSchema.check(z.base64());
				else if (format === "base64url") stringSchema = stringSchema.check(z.base64url());
				else if (format === "e164") stringSchema = stringSchema.check(z.e164());
				else if (format === "jwt") stringSchema = stringSchema.check(z.jwt());
				else if (format === "emoji") stringSchema = stringSchema.check(z.emoji());
				else if (format === "nanoid") stringSchema = stringSchema.check(z.nanoid());
				else if (format === "cuid") stringSchema = stringSchema.check(z.cuid());
				else if (format === "cuid2") stringSchema = stringSchema.check(z.cuid2());
				else if (format === "ulid") stringSchema = stringSchema.check(z.ulid());
				else if (format === "xid") stringSchema = stringSchema.check(z.xid());
				else if (format === "ksuid") stringSchema = stringSchema.check(z.ksuid());
			}
			if (typeof schema.minLength === "number") stringSchema = stringSchema.min(schema.minLength);
			if (typeof schema.maxLength === "number") stringSchema = stringSchema.max(schema.maxLength);
			if (schema.pattern) stringSchema = stringSchema.regex(new RegExp(schema.pattern));
			zodSchema = stringSchema;
			break;
		}
		case "number":
		case "integer": {
			let numberSchema = type === "integer" ? z.number().int() : z.number();
			if (typeof schema.minimum === "number") numberSchema = numberSchema.min(schema.minimum);
			if (typeof schema.maximum === "number") numberSchema = numberSchema.max(schema.maximum);
			if (typeof schema.exclusiveMinimum === "number") numberSchema = numberSchema.gt(schema.exclusiveMinimum);
			else if (schema.exclusiveMinimum === true && typeof schema.minimum === "number") numberSchema = numberSchema.gt(schema.minimum);
			if (typeof schema.exclusiveMaximum === "number") numberSchema = numberSchema.lt(schema.exclusiveMaximum);
			else if (schema.exclusiveMaximum === true && typeof schema.maximum === "number") numberSchema = numberSchema.lt(schema.maximum);
			if (typeof schema.multipleOf === "number") numberSchema = numberSchema.multipleOf(schema.multipleOf);
			zodSchema = numberSchema;
			break;
		}
		case "boolean":
			zodSchema = z.boolean();
			break;
		case "null":
			zodSchema = z.null();
			break;
		case "object": {
			const shape = {};
			const properties = schema.properties || {};
			const requiredSet = new Set(schema.required || []);
			for (const [key, propSchema] of Object.entries(properties)) {
				const propZodSchema = convertSchema(propSchema, ctx);
				shape[key] = requiredSet.has(key) ? propZodSchema : propZodSchema.optional();
			}
			if (schema.propertyNames) {
				const keySchema = convertSchema(schema.propertyNames, ctx);
				const valueSchema = schema.additionalProperties && typeof schema.additionalProperties === "object" ? convertSchema(schema.additionalProperties, ctx) : z.any();
				if (Object.keys(shape).length === 0) {
					zodSchema = z.record(keySchema, valueSchema);
					break;
				}
				const objectSchema = z.object(shape).passthrough();
				const recordSchema = z.looseRecord(keySchema, valueSchema);
				zodSchema = z.intersection(objectSchema, recordSchema);
				break;
			}
			if (schema.patternProperties) {
				const patternProps = schema.patternProperties;
				const patternKeys = Object.keys(patternProps);
				const looseRecords = [];
				for (const pattern of patternKeys) {
					const patternValue = convertSchema(patternProps[pattern], ctx);
					const keySchema = z.string().regex(new RegExp(pattern));
					looseRecords.push(z.looseRecord(keySchema, patternValue));
				}
				const schemasToIntersect = [];
				if (Object.keys(shape).length > 0) schemasToIntersect.push(z.object(shape).passthrough());
				schemasToIntersect.push(...looseRecords);
				if (schemasToIntersect.length === 0) zodSchema = z.object({}).passthrough();
				else if (schemasToIntersect.length === 1) zodSchema = schemasToIntersect[0];
				else {
					let result = z.intersection(schemasToIntersect[0], schemasToIntersect[1]);
					for (let i = 2; i < schemasToIntersect.length; i++) result = z.intersection(result, schemasToIntersect[i]);
					zodSchema = result;
				}
				break;
			}
			const objectSchema = z.object(shape);
			if (schema.additionalProperties === false) zodSchema = objectSchema.strict();
			else if (typeof schema.additionalProperties === "object") zodSchema = objectSchema.catchall(convertSchema(schema.additionalProperties, ctx));
			else zodSchema = objectSchema.passthrough();
			break;
		}
		case "array": {
			const prefixItems = schema.prefixItems;
			const items = schema.items;
			if (prefixItems && Array.isArray(prefixItems)) {
				const tupleItems = prefixItems.map((item) => convertSchema(item, ctx));
				const rest = items && typeof items === "object" && !Array.isArray(items) ? convertSchema(items, ctx) : void 0;
				if (rest) zodSchema = z.tuple(tupleItems).rest(rest);
				else zodSchema = z.tuple(tupleItems);
				if (typeof schema.minItems === "number") zodSchema = zodSchema.check(z.minLength(schema.minItems));
				if (typeof schema.maxItems === "number") zodSchema = zodSchema.check(z.maxLength(schema.maxItems));
			} else if (Array.isArray(items)) {
				const tupleItems = items.map((item) => convertSchema(item, ctx));
				const rest = schema.additionalItems && typeof schema.additionalItems === "object" ? convertSchema(schema.additionalItems, ctx) : void 0;
				if (rest) zodSchema = z.tuple(tupleItems).rest(rest);
				else zodSchema = z.tuple(tupleItems);
				if (typeof schema.minItems === "number") zodSchema = zodSchema.check(z.minLength(schema.minItems));
				if (typeof schema.maxItems === "number") zodSchema = zodSchema.check(z.maxLength(schema.maxItems));
			} else if (items !== void 0) {
				const element = convertSchema(items, ctx);
				let arraySchema = z.array(element);
				if (typeof schema.minItems === "number") arraySchema = arraySchema.min(schema.minItems);
				if (typeof schema.maxItems === "number") arraySchema = arraySchema.max(schema.maxItems);
				zodSchema = arraySchema;
			} else zodSchema = z.array(z.any());
			break;
		}
		default: throw new Error(`Unsupported type: ${type}`);
	}
	return zodSchema;
}
function convertSchema(schema, ctx) {
	if (typeof schema === "boolean") return schema ? z.any() : z.never();
	let baseSchema = convertBaseSchema(schema, ctx);
	const hasExplicitType = schema.type || schema.enum !== void 0 || schema.const !== void 0;
	if (schema.anyOf && Array.isArray(schema.anyOf)) {
		const options = schema.anyOf.map((s) => convertSchema(s, ctx));
		const anyOfUnion = z.union(options);
		baseSchema = hasExplicitType ? z.intersection(baseSchema, anyOfUnion) : anyOfUnion;
	}
	if (schema.oneOf && Array.isArray(schema.oneOf)) {
		const options = schema.oneOf.map((s) => convertSchema(s, ctx));
		const oneOfUnion = z.xor(options);
		baseSchema = hasExplicitType ? z.intersection(baseSchema, oneOfUnion) : oneOfUnion;
	}
	if (schema.allOf && Array.isArray(schema.allOf)) {
		if (schema.allOf.length === 0) baseSchema = hasExplicitType ? baseSchema : z.any();
		else {
			let result = hasExplicitType ? baseSchema : convertSchema(schema.allOf[0], ctx);
			const startIdx = hasExplicitType ? 0 : 1;
			for (let i = startIdx; i < schema.allOf.length; i++) result = z.intersection(result, convertSchema(schema.allOf[i], ctx));
			baseSchema = result;
		}
	}
	if (schema.nullable === true && ctx.version === "openapi-3.0") baseSchema = z.nullable(baseSchema);
	if (schema.readOnly === true) baseSchema = z.readonly(baseSchema);
	if (schema.default !== void 0) baseSchema = baseSchema.default(schema.default);
	const extraMeta = {};
	for (const key of [
		"$id",
		"id",
		"$comment",
		"$anchor",
		"$vocabulary",
		"$dynamicRef",
		"$dynamicAnchor"
	]) if (key in schema) extraMeta[key] = schema[key];
	for (const key of [
		"contentEncoding",
		"contentMediaType",
		"contentSchema"
	]) if (key in schema) extraMeta[key] = schema[key];
	for (const key of Object.keys(schema)) if (!RECOGNIZED_KEYS.has(key)) extraMeta[key] = schema[key];
	if (Object.keys(extraMeta).length > 0) ctx.registry.add(baseSchema, extraMeta);
	if (schema.description) baseSchema = baseSchema.describe(schema.description);
	return baseSchema;
}
/**
* Converts a JSON Schema to a Zod schema. This function should be considered semi-experimental. It's behavior is liable to change. */
function fromJSONSchema(schema, params) {
	if (typeof schema === "boolean") return schema ? z.any() : z.never();
	let normalized;
	try {
		normalized = JSON.parse(JSON.stringify(schema));
	} catch {
		throw new Error("fromJSONSchema input is not valid JSON (possibly cyclic); use $defs/$ref for recursive schemas");
	}
	const ctx = {
		version: detectVersion(normalized, params?.defaultTarget),
		defs: normalized.$defs || normalized.definitions || {},
		refs: /* @__PURE__ */ new Map(),
		processing: /* @__PURE__ */ new Set(),
		rootSchema: normalized,
		registry: params?.registry ?? globalRegistry
	};
	return convertSchema(normalized, ctx);
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/classic/coerce.js
var coerce_exports = /* @__PURE__ */ __exportAll({
	bigint: () => bigint,
	boolean: () => boolean,
	date: () => date,
	number: () => number,
	string: () => string
});
function string(params) {
	return _coercedString(ZodString, params);
}
function number(params) {
	return _coercedNumber(ZodNumber, params);
}
function boolean(params) {
	return _coercedBoolean(ZodBoolean, params);
}
function bigint(params) {
	return _coercedBigint(ZodBigInt, params);
}
function date(params) {
	return _coercedDate(ZodDate, params);
}
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/v4/classic/external.js
var external_exports = /* @__PURE__ */ __exportAll({
	$brand: () => $brand,
	$input: () => $input,
	$output: () => $output,
	NEVER: () => NEVER,
	TimePrecision: () => TimePrecision,
	ZodAny: () => ZodAny,
	ZodArray: () => ZodArray,
	ZodBase64: () => ZodBase64,
	ZodBase64URL: () => ZodBase64URL,
	ZodBigInt: () => ZodBigInt,
	ZodBigIntFormat: () => ZodBigIntFormat,
	ZodBoolean: () => ZodBoolean,
	ZodCIDRv4: () => ZodCIDRv4,
	ZodCIDRv6: () => ZodCIDRv6,
	ZodCUID: () => ZodCUID,
	ZodCUID2: () => ZodCUID2,
	ZodCatch: () => ZodCatch,
	ZodCodec: () => ZodCodec,
	ZodCustom: () => ZodCustom,
	ZodCustomStringFormat: () => ZodCustomStringFormat,
	ZodDate: () => ZodDate,
	ZodDefault: () => ZodDefault,
	ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
	ZodE164: () => ZodE164,
	ZodEmail: () => ZodEmail,
	ZodEmoji: () => ZodEmoji,
	ZodEnum: () => ZodEnum,
	ZodError: () => ZodError,
	ZodExactOptional: () => ZodExactOptional,
	ZodFile: () => ZodFile,
	ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
	ZodFunction: () => ZodFunction,
	ZodGUID: () => ZodGUID,
	ZodIPv4: () => ZodIPv4,
	ZodIPv6: () => ZodIPv6,
	ZodISODate: () => ZodISODate,
	ZodISODateTime: () => ZodISODateTime,
	ZodISODuration: () => ZodISODuration,
	ZodISOTime: () => ZodISOTime,
	ZodIntersection: () => ZodIntersection,
	ZodIssueCode: () => ZodIssueCode,
	ZodJWT: () => ZodJWT,
	ZodKSUID: () => ZodKSUID,
	ZodLazy: () => ZodLazy,
	ZodLiteral: () => ZodLiteral,
	ZodMAC: () => ZodMAC,
	ZodMap: () => ZodMap,
	ZodNaN: () => ZodNaN,
	ZodNanoID: () => ZodNanoID,
	ZodNever: () => ZodNever,
	ZodNonOptional: () => ZodNonOptional,
	ZodNull: () => ZodNull,
	ZodNullable: () => ZodNullable,
	ZodNumber: () => ZodNumber,
	ZodNumberFormat: () => ZodNumberFormat,
	ZodObject: () => ZodObject,
	ZodOptional: () => ZodOptional,
	ZodPipe: () => ZodPipe,
	ZodPrefault: () => ZodPrefault,
	ZodPreprocess: () => ZodPreprocess,
	ZodPromise: () => ZodPromise,
	ZodReadonly: () => ZodReadonly,
	ZodRealError: () => ZodRealError,
	ZodRecord: () => ZodRecord,
	ZodSet: () => ZodSet,
	ZodString: () => ZodString,
	ZodStringFormat: () => ZodStringFormat,
	ZodSuccess: () => ZodSuccess,
	ZodSymbol: () => ZodSymbol,
	ZodTemplateLiteral: () => ZodTemplateLiteral,
	ZodTransform: () => ZodTransform,
	ZodTuple: () => ZodTuple,
	ZodType: () => ZodType,
	ZodULID: () => ZodULID,
	ZodURL: () => ZodURL,
	ZodUUID: () => ZodUUID,
	ZodUndefined: () => ZodUndefined,
	ZodUnion: () => ZodUnion,
	ZodUnknown: () => ZodUnknown,
	ZodVoid: () => ZodVoid,
	ZodXID: () => ZodXID,
	ZodXor: () => ZodXor,
	_ZodString: () => _ZodString,
	_default: () => _default$1,
	_function: () => _function,
	any: () => any,
	array: () => array,
	base64: () => base64,
	base64url: () => base64url,
	bigint: () => bigint$1,
	boolean: () => boolean$1,
	catch: () => _catch$1,
	check: () => check,
	cidrv4: () => cidrv4,
	cidrv6: () => cidrv6,
	clone: () => clone,
	codec: () => codec,
	coerce: () => coerce_exports,
	config: () => config,
	core: () => core_exports,
	cuid: () => cuid,
	cuid2: () => cuid2,
	custom: () => custom,
	date: () => date$1,
	decode: () => decode$1,
	decodeAsync: () => decodeAsync$1,
	describe: () => describe$1,
	discriminatedUnion: () => discriminatedUnion,
	e164: () => e164,
	email: () => email,
	emoji: () => emoji,
	encode: () => encode$1,
	encodeAsync: () => encodeAsync$1,
	endsWith: () => _endsWith,
	enum: () => _enum$1,
	exactOptional: () => exactOptional,
	file: () => file,
	flattenError: () => flattenError,
	float32: () => float32,
	float64: () => float64,
	formatError: () => formatError,
	fromJSONSchema: () => fromJSONSchema,
	function: () => _function,
	getErrorMap: () => getErrorMap,
	globalRegistry: () => globalRegistry,
	gt: () => _gt,
	gte: () => _gte,
	guid: () => guid,
	hash: () => hash,
	hex: () => hex,
	hostname: () => hostname,
	httpUrl: () => httpUrl,
	includes: () => _includes,
	instanceof: () => _instanceof,
	int: () => int,
	int32: () => int32,
	int64: () => int64,
	intersection: () => intersection,
	invertCodec: () => invertCodec,
	ipv4: () => ipv4,
	ipv6: () => ipv6,
	iso: () => iso_exports,
	json: () => json,
	jwt: () => jwt,
	keyof: () => keyof,
	ksuid: () => ksuid,
	lazy: () => lazy,
	length: () => _length,
	literal: () => literal,
	locales: () => locales_exports,
	looseObject: () => looseObject,
	looseRecord: () => looseRecord,
	lowercase: () => _lowercase,
	lt: () => _lt,
	lte: () => _lte,
	mac: () => mac,
	map: () => map,
	maxLength: () => _maxLength,
	maxSize: () => _maxSize,
	meta: () => meta$1,
	mime: () => _mime,
	minLength: () => _minLength,
	minSize: () => _minSize,
	multipleOf: () => _multipleOf,
	nan: () => nan,
	nanoid: () => nanoid,
	nativeEnum: () => nativeEnum,
	negative: () => _negative,
	never: () => never,
	nonnegative: () => _nonnegative,
	nonoptional: () => nonoptional,
	nonpositive: () => _nonpositive,
	normalize: () => _normalize,
	null: () => _null$1,
	nullable: () => nullable,
	nullish: () => nullish,
	number: () => number$1,
	object: () => object,
	optional: () => optional,
	overwrite: () => _overwrite,
	parse: () => parse$1,
	parseAsync: () => parseAsync$1,
	partialRecord: () => partialRecord,
	pipe: () => pipe,
	positive: () => _positive,
	prefault: () => prefault,
	preprocess: () => preprocess,
	prettifyError: () => prettifyError,
	promise: () => promise,
	property: () => _property,
	readonly: () => readonly,
	record: () => record,
	refine: () => refine,
	regex: () => _regex,
	regexes: () => regexes_exports,
	registry: () => registry,
	safeDecode: () => safeDecode$1,
	safeDecodeAsync: () => safeDecodeAsync$1,
	safeEncode: () => safeEncode$1,
	safeEncodeAsync: () => safeEncodeAsync$1,
	safeParse: () => safeParse$1,
	safeParseAsync: () => safeParseAsync$1,
	set: () => set,
	setErrorMap: () => setErrorMap,
	size: () => _size,
	slugify: () => _slugify,
	startsWith: () => _startsWith,
	strictObject: () => strictObject,
	string: () => string$1,
	stringFormat: () => stringFormat,
	stringbool: () => stringbool,
	success: () => success,
	superRefine: () => superRefine,
	symbol: () => symbol,
	templateLiteral: () => templateLiteral,
	toJSONSchema: () => toJSONSchema,
	toLowerCase: () => _toLowerCase,
	toUpperCase: () => _toUpperCase,
	transform: () => transform,
	treeifyError: () => treeifyError,
	trim: () => _trim,
	tuple: () => tuple,
	uint32: () => uint32,
	uint64: () => uint64,
	ulid: () => ulid,
	undefined: () => _undefined$1,
	union: () => union,
	unknown: () => unknown,
	uppercase: () => _uppercase,
	url: () => url,
	util: () => util_exports,
	uuid: () => uuid,
	uuidv4: () => uuidv4,
	uuidv6: () => uuidv6,
	uuidv7: () => uuidv7,
	void: () => _void$1,
	xid: () => xid,
	xor: () => xor
});
config(en_default());
//#endregion
//#region node_modules/.pnpm/zod@4.4.3/node_modules/zod/index.js
var zod_default = external_exports;
//#endregion
//#region node_modules/.pnpm/@deepseek-ai+dsh-compaction_433516940b548e7f8507762c30a53880/node_modules/@deepseek-ai/dsh-compaction/lib/index.js
/**
* Brand an implementation-minted compaction identity.
* @param id - opaque transaction identity.
* @returns the same string, branded; no validation is performed.
*/
function CompactionId(id) {
	return id;
}
/**
* Compaction checkpoint provenance: the correlated source constructor and type
* every backend uses for its replacement user message, plus the predicate that
* recognizes persisted checkpoints.
*
* The seam itself lives in `@deepseek-ai/dsh-compaction`, which re-exports these
* contracts; this module is a pure type/value/predicate outlet (no cordis
* imports, no module augmentation) so client and wire programs can name the
* checkpoint source without loading the host plugin's Context merges — the
* `dsh-commands/brand` shape.
*
* @module @deepseek-ai/dsh-compaction/checkpoint
*/
const COMPACT_CHECKPOINT_MARKER = Object.freeze({
	kind: "plugin",
	plugin: "compact"
});
/**
* Create checkpoint provenance correlated with one compaction transaction.
* @param compactionId - owning compaction identity.
* @param sourceCommandId - initiating manual command, when present.
* @returns immutable checkpoint source.
*/
function compactCheckpointSource(compactionId, sourceCommandId) {
	return Object.freeze({
		...COMPACT_CHECKPOINT_MARKER,
		compactionId,
		...sourceCommandId === void 0 ? {} : { sourceCommandId }
	});
}
/**
* Compaction Service Definition (`ctx.compaction`): providers decide when to
* compact and replace a history range with one summary node by subclassing
* {@link CompactionEngine}. This interface necessarily depends on session and LLM
* vocabulary; the rationale is in the
* [compaction Agent Note](../../../../.agents/notes/implemented/feature/2026-06-18-compaction-capability-seam.md).
* @module @deepseek-ai/dsh-compaction
*/
/**
* Expected manual-compaction failure suitable for a direct human-command result.
* Shared durable-lock entry assertions may also throw the `busy` subtype from
* automatic compaction paths.
*/
var ManualCompactionError = class extends Error {
	code;
	name = "ManualCompactionError";
	/**
	* Create one classified compaction failure.
	* @param code - stable failure class; `busy` may originate from any compaction entry path.
	* @param message - backend diagnostic retained as the Error message.
	* @param options - optional original failure.
	*/
	constructor(code, message, options) {
		super(message, options);
		this.code = code;
	}
};
/**
* Abstract compaction service. Implementations own trigger policy, retention,
* and summarization, and may consume a separate measurement service. A
* successful run replaces the selected surface span with one summary node and
* prevents concurrent compaction of the same session. The replacement user
* message uses {@link compactCheckpointSource} with the transaction identity
* so consumers recognize and correlate it independently of the backend. Load
* one implementation per context as `ctx.compaction`.
*/
var CompactionEngine = class extends Service {
	constructor(ctx) {
		super(ctx, "compaction");
	}
};
function buildToolGroupSummaryInput(session, group) {
	return {
		version: 1,
		group,
		items: group.sourceSeqs.map((seq) => {
			const event = session.eventAt(seq);
			if (event === void 0) throw new Error(`tool-group-summary: source seq ${seq} is missing`);
			return inputItem(event);
		})
	};
}
function parseToolGroupSummary(value, input) {
	if (!isRecord(value)) throw new Error("tool-group-summary: output must be an object");
	if (value.version !== 1) throw new Error("tool-group-summary: unsupported schema version");
	const summary = requiredString(value.groupSummary, "groupSummary");
	const items = requiredArray(value.items, "items").map((item, index) => parseItem(item, index));
	const groupErrors = stringArray(value.groupErrors, "groupErrors");
	const unresolved = stringArray(value.unresolved, "unresolved");
	validateSources(items, input);
	return {
		version: 1,
		groupSummary: summary,
		items,
		groupErrors,
		unresolved
	};
}
function parseItem(value, index) {
	if (!isRecord(value)) throw new Error(`tool-group-summary: items[${index}] must be an object`);
	const sourceSeq = value.sourceSeq;
	if (typeof sourceSeq !== "number" || !Number.isSafeInteger(sourceSeq) || sourceSeq < 0) throw new Error(`tool-group-summary: items[${index}].sourceSeq is invalid`);
	return {
		sourceSeq,
		...value.callId === void 0 ? {} : { callId: requiredString(value.callId, `items[${index}].callId`) },
		summary: requiredString(value.summary, `items[${index}].summary`),
		facts: stringArray(value.facts, `items[${index}].facts`),
		files: stringArray(value.files, `items[${index}].files`),
		identifiers: stringArray(value.identifiers, `items[${index}].identifiers`),
		errors: stringArray(value.errors, `items[${index}].errors`),
		unresolved: stringArray(value.unresolved, `items[${index}].unresolved`)
	};
}
function validateSources(items, input) {
	const allowed = /* @__PURE__ */ new Map();
	for (const item of input.items) allowed.set(item.sourceSeq, item);
	const seen = /* @__PURE__ */ new Set();
	for (const item of items) {
		const source = allowed.get(item.sourceSeq);
		if (source === void 0) throw new Error(`tool-group-summary: source seq ${item.sourceSeq} is not in the input group`);
		if (seen.has(item.sourceSeq)) throw new Error(`tool-group-summary: source seq ${item.sourceSeq} is duplicated`);
		seen.add(item.sourceSeq);
		if (item.callId !== void 0 && source.callId !== item.callId) throw new Error(`tool-group-summary: source seq ${item.sourceSeq} call id does not match input`);
		assertFactsBelongToInput(item, source);
	}
	for (const source of input.items) if (!seen.has(source.sourceSeq)) throw new Error(`tool-group-summary: missing summary item for source seq ${source.sourceSeq}`);
}
function assertFactsBelongToInput(item, source) {
	const input = source.content;
	const claims = [
		...item.facts,
		...item.files,
		...item.identifiers,
		...item.errors
	];
	for (const claim of claims) if (claim.trim().length === 0 || !input.includes(claim)) throw new Error(`tool-group-summary: claim is not present in source seq ${source.sourceSeq}`);
}
function inputItem(event) {
	if (event.type === "tool/result") {
		const block = event.data.message.content[0];
		return {
			sourceSeq: event.seq,
			role: "tool-result",
			callId: String(event.data.message.source.callId),
			content: block?.type === "tool-result" ? typeof block.content === "string" ? block.content : JSON.stringify(block.content) : ""
		};
	}
	if (event.type === "assistant/message") {
		const calls = event.data.message.content.filter((block) => block.type === "tool-call");
		return {
			sourceSeq: event.seq,
			role: "tool-call",
			content: calls.map((call) => `${call.name} ${call.arguments}`).join("\n")
		};
	}
	throw new Error(`tool-group-summary: source seq ${event.seq} is not a tool node`);
}
function isRecord(value) {
	return typeof value === "object" && value !== null && !Array.isArray(value);
}
function requiredString(value, name) {
	if (typeof value !== "string" || value.trim().length === 0) throw new Error(`tool-group-summary: ${name} must be a non-empty string`);
	return value;
}
function requiredArray(value, name) {
	if (!Array.isArray(value)) throw new Error(`tool-group-summary: ${name} must be an array`);
	return value;
}
function stringArray(value, name) {
	return requiredArray(value, name).map((entry, index) => requiredString(entry, `${name}[${index}]`));
}
//#endregion
//#region lib/types/internal/compaction/tool-group-summarizer.js
var ToolGroupSummaryFallbackError = class extends Error {
	reason;
	constructor(reason, message, options) {
		super(message, options);
		this.reason = reason;
	}
};
const INSTRUCTION = `You are summarizing one complete group of tool calls and results for a coding assistant.
Return ONLY valid JSON matching this exact shape:
{"version":1,"groupSummary":"...","items":[{"sourceSeq":0,"callId":"...","summary":"...","facts":[],"files":[],"identifiers":[],"errors":[],"unresolved":[]}],"groupErrors":[],"unresolved":[]}
Rules:
- Include exactly one item for every sourceSeq in the input, including tool-call and tool-result nodes.
- Copy only facts, paths, identifiers, errors, and statuses that appear verbatim in that source item.
- Never infer success, completion, paths, IDs, or test results.
- Keep unknown information in unresolved.
- Preserve exact sourceSeq and callId values.
- Do not include Markdown fences or any text outside the JSON object.`;
function buildToolGroupSummaryMessage(session, group) {
	const input = buildToolGroupSummaryInput(session, group);
	return createUserMessage({
		content: [{
			type: "text",
			text: `${INSTRUCTION}\n\nINPUT:\n${JSON.stringify(input)}`
		}],
		source: {
			kind: "plugin",
			plugin: "dsh-context-enhancement/tool-group-summarizer"
		}
	});
}
function estimateToolGroupAuxiliaryRequestTokens(session, group, reserveTokens, estimateMessage) {
	return estimateMessage(buildToolGroupSummaryMessage(session, group)) + Math.max(0, reserveTokens);
}
async function summarizeToolGroup(ctx, session, group, agent, route, signal, inputCapTokens) {
	if (route.provider.length === 0 || route.model.length === 0) throw new ToolGroupSummaryFallbackError("route", "tool-group-summary: provider/model route is empty");
	if (inputCapTokens !== void 0) {
		const fullTokens = estimateToolGroupAuxiliaryRequestTokens(session, group, route.maxTokens, (msg) => ctx.tokenMeter.estimateMessage(msg));
		if (fullTokens > inputCapTokens) throw new ToolGroupSummaryFallbackError("stream", `tool-group-summary: complete auxiliary request (${fullTokens} tokens) exceeds input cap (${inputCapTokens} tokens)`);
	}
	const input = buildToolGroupSummaryInput(session, group);
	const assembler = new BlockAssembler();
	try {
		const options = {
			provider: route.provider,
			model: route.model,
			messages: [buildToolGroupSummaryMessage(session, group)],
			maxTokens: route.maxTokens,
			sessionId: agent.session.id,
			purpose: "compaction",
			...signal === void 0 ? {} : { signal }
		};
		for await (const chunk of ctx.llm.stream(options)) assembler.push(chunk);
		const rawOutput = assembler.blocks();
		const text = rawOutput.filter((block) => block.type === "text").map((block) => block.text).join("");
		if (text.trim().length === 0) throw new ToolGroupSummaryFallbackError("empty", "tool-group-summary: model returned no text");
		let parsed;
		try {
			parsed = JSON.parse(stripCodeFence(text));
		} catch (error) {
			throw new ToolGroupSummaryFallbackError("json", "tool-group-summary: model output is not valid JSON", { cause: error });
		}
		let summary;
		try {
			summary = parseToolGroupSummary(parsed, input);
		} catch (error) {
			throw new ToolGroupSummaryFallbackError(error instanceof Error && /source|claim/.test(error.message) ? "source" : "schema", error instanceof Error ? error.message : String(error), { cause: error });
		}
		return {
			summary,
			rawOutput,
			...assembler.usage === void 0 ? {} : { usage: assembler.usage },
			provider: route.provider,
			model: route.model
		};
	} catch (error) {
		if (error instanceof ToolGroupSummaryFallbackError) throw error;
		throw new ToolGroupSummaryFallbackError("stream", error instanceof Error ? error.message : String(error), { cause: error });
	}
}
function stripCodeFence(text) {
	const trimmed = text.trim();
	if (!trimmed.startsWith("```")) return trimmed;
	return trimmed.replace(/^```(?:json)?\s*/i, "").replace(/\s*```$/, "").trim();
}
//#endregion
//#region lib/types/internal/compaction/tool-group-audit.js
/**
* Machine-readable reasons this state machine stores on a row and logs.
*
* They are stable strings rather than prose so a diagnostic can be asserted by
* a test and grepped in a run log, and so the durable row states WHY a
* transition happened instead of only that one did.
*/
const AUDIT_DIAGNOSTIC = {
	/** An `open` row whose owning instance is gone (restart) or holds it elsewhere. */
	interruptedAttempt: "interrupted-attempt",
	/** An interrupted row was adopted for a new, budgeted attempt. */
	adoptedInterruptedAttempt: "adopted-interrupted-attempt",
	/** Session provenance proves this group's reduction landed. */
	sessionEvidenceServed: "session-evidence-served",
	/** The row's claims are not backed by this group's Session provenance. */
	landingClaimConflict: "landing-claim-conflict",
	/** The attempt budget of one group is spent (its failure/abort history). */
	attemptsExhausted: "attempts-exhausted",
	/** A durable audit write was rejected; Session provenance still decides types. */
	auditWriteFailed: "audit-write-failed",
	/** The durable row a transition targeted is gone (whole-document overwrite). */
	auditRecordMissing: "audit-record-missing",
	/** Session provenance proves a landing no audit row records. */
	auditLandingUnrecorded: "audit-landing-unrecorded"
};
function toolGroupFingerprint(input) {
	const canonical = JSON.stringify({
		lifecycle: input.lifecycle,
		sourceSeqs: [...input.sourceSeqs],
		callIds: [...input.callIds],
		eventTypes: [...input.eventTypes],
		contentDigest: input.contentDigest,
		schemaVersion: input.schemaVersion
	});
	return createHash("sha256").update(canonical).digest("hex");
}
function contentDigest(parts) {
	return createHash("sha256").update(JSON.stringify(parts)).digest("hex");
}
function openToolGroupAudit(requestId, sessionId, group, surfaceGeneration, provider, model, fingerprint, lifecycle = { createdAt: 0 }, attempt = {}) {
	return {
		requestId,
		sessionId,
		lifecycle,
		fingerprint,
		sourceSeqs: [...group.sourceSeqs],
		surfaceGeneration,
		provider,
		model,
		schemaVersion: 1,
		status: "open",
		attempt: attempt.attempt ?? 1,
		...attempt.ownerId === void 0 ? {} : { ownerId: attempt.ownerId },
		...attempt.recoveredFrom === void 0 ? {} : { recoveredFrom: attempt.recoveredFrom }
	};
}
function finishToolGroupAudit(record, status, patch = {}) {
	if (record.status !== "open") throw new Error(`tool-group-audit: cannot finish ${record.status} record`);
	return {
		...record,
		...patch,
		status
	};
}
/** Record landed replacements on an audit record while preserving its open status. */
function recordToolGroupLanded(record, replacementSeqs, patch = {}) {
	const existing = record.replacementSeqs ?? [];
	const merged = Array.from(/* @__PURE__ */ new Set([...existing, ...replacementSeqs]));
	return {
		...record,
		...patch,
		replacementSeqs: merged
	};
}
/**
* Session-proven landed reductions of one tool group, read from the VALIDATED
* provenance of the current surface.
*
* This is the authority recovery uses: a node counts only when its own durable
* record says `tool-summary` AND carries this exact group identity, so neither
* the audit document nor a shadow-price event's mere adjacency can manufacture
* evidence. A third-party replacement with no usable provenance, a prune, and a
* node whose record was damaged all contribute nothing, which is why a repair
* can never promote a type the Session log does not already state.
* @param index - classifier built from the Session surface.
* @param fingerprint - durable tool-group identity the audit schedules under.
* @returns the matching surface seqs, ascending.
*/
function sessionLandedReductions(index, fingerprint) {
	const landed = [];
	for (const [seq, entry] of index.entries) {
		const reduction = entry.reduction;
		if (reduction === null || reduction.kind !== "tool-summary") continue;
		if (reduction.groupId !== fingerprint) continue;
		landed.push(seq);
	}
	return landed.sort((left, right) => left - right);
}
/**
* One record copy without the named optional fields.
*
* `exactOptionalPropertyTypes` forbids clearing an optional field by assigning
* `undefined`, and a recovery transition MUST clear one rather than keep it: a
* stale `error` on a repaired row, or a rejected landing claim surviving into a
* new attempt, would misreport the row to every later reader.
* @param record - the row being transitioned.
* @param keys - optional fields to remove.
* @returns the row without those fields.
*/
function withoutAuditFields(record, keys) {
	const copy = { ...record };
	for (const key of keys) delete copy[key];
	return copy;
}
/**
* Turn one `open`/`aborted` row into the durable `aborted` verdict.
*
* `aborted` states that no landed reduction is proven for this row: it is
* repairable (adoptable for a new attempt) and is never read as committed. The
* rejected claims of a conflicting row stay on the record as forensic evidence
* of what the writable document asserted; they are inert because every reader
* classifies a row by its status first.
* @param record - the row being judged.
* @param diagnostic - reason code, one of {@link AUDIT_DIAGNOSTIC}.
* @returns the row in its `aborted` state.
*/
function abortToolGroupAudit(record, diagnostic) {
	if (record.status !== "open" && record.status !== "aborted") throw new Error(`tool-group-audit: cannot abort ${record.status} record`);
	return {
		...record,
		status: "aborted",
		diagnostic
	};
}
/**
* Turn one `open`/`aborted` row into the durable `repaired` verdict.
*
* This is the ONLY recovery transition that asserts a landed reduction, and it
* does so on Session evidence rather than on the audit's own word: the caller
* passes the seqs {@link sessionLandedReductions} proved. Without evidence the
* call throws — a repair that could invent a landing would be exactly the
* fabrication the state machine exists to prevent.
* @param record - the row being judged.
* @param evidence - Session-proven landed replacements; must be non-empty.
* @param diagnostic - reason code, one of {@link AUDIT_DIAGNOSTIC}.
* @returns the row in its `repaired` state.
*/
function repairToolGroupAudit(record, evidence, diagnostic = AUDIT_DIAGNOSTIC.sessionEvidenceServed) {
	if (record.status !== "open" && record.status !== "aborted") throw new Error(`tool-group-audit: cannot repair ${record.status} record`);
	if (evidence.length === 0) throw new Error("tool-group-audit: repair requires Session-proven landing evidence");
	return {
		...withoutAuditFields(record, ["error"]),
		status: "repaired",
		diagnostic,
		recoveredFrom: record.status,
		replacementSeqs: [...evidence],
		repairEvidence: [...evidence]
	};
}
/**
* Adopt one interrupted (`open` by another instance, or `aborted`) row for a
* new attempt, IN PLACE.
*
* In place matters: one durable row per group keeps
* {@link toolGroupAttemptsSpent} honest, because a second row would
* double-count the budget and refuse the group a single restart earlier than
* the policy states. Nothing unproven survives the adoption — the claims of a
* rejected row and any stale error are dropped, and the only status this can
* produce is `open`.
* @param record - the row being adopted.
* @param adoption - new attempt identity and the re-selected group.
* @returns the row, now owned by the adopting attempt.
*/
function adoptToolGroupAudit(record, adoption) {
	if (record.status !== "open" && record.status !== "aborted") throw new Error(`tool-group-audit: cannot adopt ${record.status} record`);
	return {
		...withoutAuditFields(record, [
			"replacementSeqs",
			"repairEvidence",
			"error",
			"diagnostic",
			"ownerId"
		]),
		status: "open",
		attempt: (record.attempt ?? 1) + 1,
		recoveredFrom: record.status,
		sourceSeqs: [...adoption.sourceSeqs],
		surfaceGeneration: adoption.surfaceGeneration,
		...adoption.ownerId === void 0 ? {} : { ownerId: adoption.ownerId },
		...adoption.diagnostic === void 0 ? {} : { diagnostic: adoption.diagnostic }
	};
}
/**
* Restate one of THIS instance's own `open` rows for a fresh attempt.
*
* The group is re-selected and the surface generation re-observed, so the
* stability fence in {@link assertToolGroupCommitStable} guards the new model
* call rather than a surface state that no longer exists. The attempt ordinal
* deliberately stays: this is the same attempt continuing, not a new one.
* @param record - this instance's open row.
* @param restated - re-selected source seqs and the observed generation.
* @returns the row, refreshed for the resumed attempt.
*/
function resumeToolGroupAudit(record, restated) {
	if (record.status !== "open") throw new Error(`tool-group-audit: cannot resume ${record.status} record`);
	return {
		...record,
		sourceSeqs: [...restated.sourceSeqs],
		surfaceGeneration: restated.surfaceGeneration
	};
}
/**
* Find the durable row an attempt for one fingerprint should use.
*
* An `open` row is resumable when this instance owns it and adopted otherwise;
* an `aborted` row is adopted in place. Rows whose reduction is already proven
* (`success`, `repaired`, or an `open` row carrying verified claims) are never
* returned: {@link shouldAttemptToolGroupSummary} refuses them before an
* attempt starts.
* @param records - rows of the session's lifecycle.
* @param fingerprint - durable tool-group identity.
* @param ownerId - this engine instance's identity, when it has one.
* @returns the slot, or `undefined` when a brand-new row should be opened.
*/
function attemptSlotFor(records, fingerprint, ownerId) {
	const open = records.find((record) => record.fingerprint === fingerprint && record.status === "open");
	if (open !== void 0) return {
		record: open,
		adopted: open.ownerId !== ownerId
	};
	const aborted = records.find((record) => record.fingerprint === fingerprint && record.status === "aborted");
	if (aborted !== void 0) return {
		record: aborted,
		adopted: true
	};
}
/**
* Judge every `open`/`aborted` row of one session against the Session log and
* plan the durable transitions it needs (B6.2).
*
* The decisions, in order:
*
* 1. the row carries claims the Session log does not back → `aborted` with
*    {@link AUDIT_DIAGNOSTIC.landingClaimConflict}: the writable document and
*    the log disagree, so no landing is believed and no row is promoted;
* 2. Session provenance proves this group's reduction landed → `repaired` with
*    the proven seqs, whatever the row said (including nothing at all, which is
*    the audit-commit-failed case);
* 3. an `open` row this instance owns is its live attempt and is left alone;
* 4. any other `open` row is `aborted` with
*    {@link AUDIT_DIAGNOSTIC.interruptedAttempt} — a restart, or a concurrent
*    writer this package cannot distinguish from one (B1's unresolved
*    single-writer gap). It stays repairable, and it is never read as
*    committed;
* 5. an `aborted` row with nothing proven stays as it is (no repeated writes).
* @param records - rows of the session's lifecycle.
* @param index - classifier built from the SAME session's current surface.
* @param ownerId - this engine instance's identity, when it has one. A row that
*   carries no owner is never treated as this instance's live attempt.
* @returns the durable transitions to apply, in row order.
*/
function planAuditRecovery(records, index, ownerId) {
	const steps = [];
	for (const record of records) {
		if (record.status !== "open" && record.status !== "aborted") continue;
		const landed = sessionLandedReductions(index, record.fingerprint);
		const claims = record.replacementSeqs ?? [];
		const unverified = claims.filter((seq) => !landed.includes(seq));
		const planned = claims.length > 0 && unverified.length > 0 ? {
			status: "aborted",
			diagnostic: AUDIT_DIAGNOSTIC.landingClaimConflict
		} : landed.length > 0 ? {
			status: "repaired",
			diagnostic: AUDIT_DIAGNOSTIC.sessionEvidenceServed
		} : record.status === "open" && record.ownerId !== ownerId ? {
			status: "aborted",
			diagnostic: AUDIT_DIAGNOSTIC.interruptedAttempt
		} : void 0;
		if (planned === void 0) continue;
		if (record.status === planned.status && record.diagnostic === planned.diagnostic) continue;
		steps.push({
			requestId: record.requestId,
			status: planned.status,
			diagnostic: planned.diagnostic,
			apply: planned.status === "repaired" ? (current) => repairToolGroupAudit(current, landed, planned.diagnostic) : (current) => abortToolGroupAudit(current, planned.diagnostic)
		});
	}
	return steps;
}
function successfulAuditFor(records, fingerprint) {
	return records.find((record) => record.fingerprint === fingerprint && record.status === "success");
}
function recoverableOpenAuditFor(records, fingerprint) {
	return records.find((record) => record.fingerprint === fingerprint && record.status === "open");
}
/**
* Whether one audit record CLAIMS its reduction landed on the surface.
*
* A `success` or `repaired` record does, and so does an `open` record carrying
* `replacementSeqs`: the success commit can fail AFTER the surface rewrite is
* durable (see the engine's `commitToolGroupSuccess`), and that record must
* still be treated as served evidence — its replacements keep tool-summary
* provenance, and the group is not outstanding work owed another call.
*
* The claim is a CLAIM: `aborted` rows (including a row whose claims recovery
* rejected) never make it, and a caller that can read the Session log must
* confirm it with {@link sessionLandedReductions} before spending model work.
* The claim can only ever make a classification stricter, never create a kind.
*/
function isServedAudit(record) {
	if (record.status === "success" || record.status === "repaired") return true;
	if (record.status !== "open") return false;
	return (record.replacementSeqs?.length ?? 0) > 0;
}
/** Every replacement seq CLAIMED as landed by {@link isServedAudit}. */
function servedReplacementSeqs(records) {
	return records.filter(isServedAudit).flatMap((record) => record.replacementSeqs ?? []);
}
/** Attempts started for one fingerprint, counting the initial one of each row. */
function toolGroupAttemptsSpent(records, fingerprint) {
	return records.filter((record) => record.fingerprint === fingerprint).reduce((total, record) => total + (record.attempt ?? 1), 0);
}
/** Whether one fingerprint may still start an attempt under the durable budget. */
function hasToolGroupAttemptBudget(records, fingerprint) {
	return toolGroupAttemptsSpent(records, fingerprint) < 2;
}
/**
* Whether one durable tool group still permits semantic summarization work.
*
* The Session evidence wins over every audit state: a group whose reduction the
* durable provenance proves landed is not outstanding work, whether the audit
* row is missing, stale, still `open` after a failed commit, or a
* whole-document last-write-wins overwrite replaced it (R-P2-3). Everything
* else is read from the rows, and the attempt budget is durable:
*
* - `success`/`repaired`, or an `open` row claiming landed replacements → no;
* - a `fallback` refusal → no;
* - an `open`/`aborted` row with nothing proven → yes while the budget lasts;
* - otherwise the remaining budget decides, so two failed attempts end the work
*   exactly as the pre-B6.2 rule did.
* @param records - rows of the session's lifecycle.
* @param fingerprint - durable tool-group identity.
* @param evidence - Session-proven landing evidence, when the caller can read
*   the log. Omitting it is the audit-only (degraded) view and is never what
*   the engine uses.
* @returns whether an attempt is still owed.
*/
function shouldAttemptToolGroupSummary(records, fingerprint, evidence = {}) {
	if ((evidence.landed?.length ?? 0) > 0) return false;
	if (successfulAuditFor(records, fingerprint) !== void 0) return false;
	if (records.some((record) => record.fingerprint === fingerprint && record.status === "repaired")) return false;
	const open = recoverableOpenAuditFor(records, fingerprint);
	if (open !== void 0) {
		if (isServedAudit(open)) return false;
		return hasToolGroupAttemptBudget(records, fingerprint);
	}
	if (records.some((record) => record.fingerprint === fingerprint && record.status === "fallback")) return false;
	return hasToolGroupAttemptBudget(records, fingerprint);
}
function assertToolGroupCommitStable(sessionId, currentGeneration, currentSourceSeqs, record) {
	if (record.sessionId !== sessionId) throw new Error("tool-group-audit: session lifecycle changed");
	if (record.surfaceGeneration !== currentGeneration) throw new Error("tool-group-audit: surface generation changed");
	if (record.sourceSeqs.length !== currentSourceSeqs.length || record.sourceSeqs.some((seq, index) => seq !== currentSourceSeqs[index])) throw new Error("tool-group-audit: source surface changed");
}
//#endregion
//#region lib/types/internal/compaction/tool-group-replacement.js
/**
* Replace every tool result of one summarized group on the current surface.
*
* Each landed replacement follows the shared shadow-price protocol exactly like
* the deterministic pruner: a `compaction/prune` event stating the replaced
* node's price is appended synchronously immediately before the surface
* `replace`. That single durable event vocabulary therefore records EVERY
* successful tool-result reduction path — semantic group summarization here and
* the model-free head/middle prune — and keeps replay folds exact instead of
* folding an unpriced replacement neutrally.
*
* That same event also carries this reduction's durable provenance record
* (`kind: 'tool-summary'`, the whole group's source seqs, the group identity,
* the observed replace generation, and a digest over the replacement text).
* It is what a restart or replay classifies from, so losing the tool-group
* audit can no longer downgrade a committed summary.
* @param session - session whose current surface is rewritten.
* @param group - selected group whose tool results are replaced.
* @param summary - validated per-source summary items.
* @param options - pricing seam for the logged shadow price.
* @returns landed replacements and the cited group sources. A source node whose
*   formatted summary would price more than the original is skipped by the
*   per-node shrink guard and contributes no replacement; a source node with
*   no text to distill (an empty content array, or only rich blocks) stays raw
*   without an exception too.
* @throws when a target is no longer a tool result or has no summary item;
* nothing is appended in that case.
*/
function replaceToolGroup(session, group, summary, options) {
	const replacementSeqs = [];
	const groupId = toolGroupIdentity(session, group);
	const snapshot = group.toolResultSeqs.map((sourceSeq) => {
		const event = session.eventAt(sourceSeq);
		if (event?.type !== "tool/result") throw new Error(`tool-group-replacement: source seq ${sourceSeq} is no longer a tool result`);
		const item = summary.items.find((candidate) => candidate.sourceSeq === sourceSeq);
		if (item === void 0) throw new Error(`tool-group-replacement: missing summary item for source seq ${sourceSeq}`);
		return {
			sourceSeq,
			event,
			item
		};
	});
	for (const { sourceSeq, event, item } of snapshot) {
		const original = event.data.message;
		const blockIndex = original.content.findIndex((block) => block.type === "tool-result");
		if (blockIndex === -1) continue;
		const block = original.content[blockIndex];
		if (toolResultTextLength(original.content) === 0) continue;
		const text = {
			type: "text",
			text: formatSummary(item.summary, item.facts, item.files, item.errors, item.unresolved, sourceSeq)
		};
		const preserved = block.content.filter((inner) => inner.type !== "text");
		const content = original.content.map((messageBlock, index) => {
			if (messageBlock.type !== "tool-result") return messageBlock;
			if (index === blockIndex) return {
				...messageBlock,
				content: [text, ...preserved]
			};
			return {
				...messageBlock,
				content: messageBlock.content.filter((inner) => inner.type !== "text")
			};
		});
		const message = freezeMessage({
			...original,
			content
		});
		const shadowedTokens = options.estimateTokens(original);
		if (options.estimateTokens(message) >= shadowedTokens) continue;
		const provenance = reductionProvenance({
			kind: "tool-summary",
			coveredSeqs: [sourceSeq],
			sourceEventSeqs: [...group.sourceSeqs],
			groupId,
			generation: session.surface.replaceGeneration,
			content: message.content
		});
		session.append("compaction/prune", shadowPriceWithProvenance(sourceSeq, shadowedTokens, provenance));
		const replacement = session.append("tool/result", {
			...event.data,
			message
		}, {
			surfaceOp: {
				op: "replace",
				start: sourceSeq,
				end: sourceSeq
			},
			sourceEventSeqs: [sourceSeq]
		});
		replacementSeqs.push(replacement.seq);
		options.onLanded?.(replacement.seq);
	}
	return {
		replacementSeqs,
		sourceSeqs: [...group.sourceSeqs]
	};
}
/**
* Durable identity of one selected tool group.
*
* It is the SAME fingerprint `BasicCompactionEngine.toolGroupFingerprint`
* computes for the audit — same lifecycle identity, same source seqs, same call
* ids, same event types, same content digest, same schema version — rebuilt here
* from the group and the Session so the provenance written into the log can
* always be correlated with the audit row that schedules this group's work. The
* equality is asserted by `tests/compaction-tool-provenance-replay.spec.ts`.
*
* It is derived rather than passed in on purpose: the replacement writer must
* not be able to claim an identity its caller invented, and the two producers of
* this value agree because they read the same durable inputs.
* @param session - session owning the group's events.
* @param group - the selected group.
* @returns the group's durable fingerprint.
*/
function toolGroupIdentity(session, group) {
	const events = group.sourceSeqs.map((seq) => session.eventAt(seq));
	return toolGroupFingerprint({
		lifecycle: {
			sessionId: session.id,
			createdAt: session.header.createdAt
		},
		sourceSeqs: group.sourceSeqs,
		callIds: group.callIds,
		eventTypes: events.map((event) => event?.type ?? "missing"),
		contentDigest: contentDigest(events.map((event) => JSON.stringify(event))),
		schemaVersion: 1
	});
}
function formatSummary(summary, facts, files, errors, unresolved, sourceSeq) {
	return [
		"[tool group summary]",
		`Result: ${summary}`,
		`Facts: ${facts.length === 0 ? "(none)" : facts.join("; ")}`,
		`Files: ${files.length === 0 ? "(none)" : files.join(", ")}`,
		`Errors: ${errors.length === 0 ? "(none)" : errors.join("; ")}`,
		`Unresolved: ${unresolved.length === 0 ? "(none)" : unresolved.join("; ")}`,
		`Source event: seq=${sourceSeq}`,
		"[/tool group summary]"
	].join("\n");
}
//#endregion
//#region lib/types/internal/compaction/tool-group-domain.js
const nonNegative = zod_default.number().int().nonnegative().max(Number.MAX_SAFE_INTEGER);
const nonEmpty = zod_default.string().min(1).refine((value) => value.trim() === value);
const status = zod_default.enum([
	"open",
	"success",
	"fallback",
	"failure",
	"aborted",
	"repaired"
]);
const toolGroupAuditSchema = zod_default.object({
	requestId: nonEmpty,
	sessionId: nonEmpty,
	lifecycle: zod_default.object({ createdAt: nonNegative }).partial().optional(),
	fingerprint: nonEmpty,
	sourceSeqs: zod_default.array(nonNegative),
	surfaceGeneration: nonNegative,
	provider: nonEmpty,
	model: nonEmpty,
	schemaVersion: nonNegative,
	status,
	rawOutput: zod_default.unknown().optional(),
	summary: zod_default.unknown().optional(),
	replacementSeqs: zod_default.array(nonNegative).optional(),
	error: zod_default.string().optional(),
	attempt: nonNegative.optional(),
	ownerId: nonEmpty.optional(),
	recoveredFrom: status.optional(),
	diagnostic: nonEmpty.optional(),
	repairEvidence: zod_default.array(nonNegative).optional()
}).strict();
const toolGroupAuditDomainSpec = defineDomain({
	name: "context_enhancement_tool_group_summary",
	version: 1,
	layout: "single",
	tables: { audit: domainTable(toolGroupAuditSchema) }
});
/**
* Whether one rejected audit write is worth retrying.
*
* A backend write failure (an I/O error, a busy medium) is transient and gets
* the bounded retry. A domain-layer refusal is deterministic and is NOT: a
* `closed` domain rejects every retry, and a `missing-key` update means the row
* this transition targeted is gone — which is precisely the durable symptom of
* a whole-document last-write-wins overwrite by another writer, and the caller
* must report it as that instead of hammering the store. Retrying it could not
* succeed, and pretending it had would be worse than failing.
* @param error - the rejection.
* @returns whether one more attempt may be made.
*/
function isRetryableAuditWrite(error) {
	if (error instanceof DomainError) return error.code !== "closed" && error.code !== "missing-key";
	return true;
}
/**
* Run one durable audit write with a bounded retry.
*
* The write itself stays unconditional — the storage contract exposes no
* compare-and-swap (B1's `blocked-specific-api` gap), so this is availability,
* not conflict detection: a failure that survives the retry propagates to the
* caller, which reports it and keeps classifying from the Session log.
* @param write - the write to attempt.
* @returns the write's value.
* @throws the last rejection when every attempt failed.
*/
async function withAuditWriteRetry(write) {
	for (let attempt = 0;; attempt += 1) try {
		return await write();
	} catch (error) {
		if (attempt + 1 >= 2 || !isRetryableAuditWrite(error)) throw error;
	}
}
async function openToolGroupAuditStore(ctx) {
	const domain = await ctx.storageDomain.open(toolGroupAuditDomainSpec);
	const table = domain.table("audit");
	return {
		open: (record) => withAuditWriteRetry(() => table.put(record.requestId, record)),
		finish: (requestId, update) => withAuditWriteRetry(async () => {
			await table.update(requestId, update);
		}),
		recordsForSession: (sessionId, createdAt) => [...table.entries()].map(([, record]) => record).filter((record) => record.sessionId === sessionId && createdAt !== void 0 && record.lifecycle?.createdAt === createdAt),
		close: () => domain.close()
	};
}
//#endregion
//#region lib/types/internal/compaction/config.js
/** Three-zone compaction configuration resolution and model-policy inheritance. */
const DEFAULT_PRESSURE_RATIO = .8;
const DEFAULT_RECENT_RATIO = .2;
const DEFAULT_FORGET_BOUNDARY_RATIO = .5;
const DEFAULT_TOOL_MAINTENANCE_RATIO = .4;
const DEFAULT_FORGET_MAINTENANCE_RATIO = .7;
const DEFAULT_TARGET_BATCH_TOKENS = 16e3;
const DEFAULT_MAX_BATCH_TOKENS = 24e3;
const DEFAULT_MAX_MAINTENANCE_BATCHES = 1;
const DEFAULT_MAX_PRESSURE_BATCHES = 2;
const DEFAULT_MIN_REENTRY_TURNS = 1;
const DEFAULT_RESPONSE_RESERVE_TOKENS = 8192;
const DEFAULT_SAFETY_MARGIN_TOKENS = 2048;
/**
* Net-release floors of one semantic pressure batch. The absolute floor catches
* a batch that releases almost nothing; the relative floor catches a batch whose
* release is small compared with the span it paid for. Either one passing is
* enough, so the pair is a gate on "was this batch worth its call", not a demand
* for both.
*/
const DEFAULT_MIN_NET_RELEASE_TOKENS = 1024;
const DEFAULT_MIN_NET_RELEASE_RATIO = .15;
const POLICY_CONFIG_KEYS = [
	"thresholdRatio",
	"retainRatio",
	"retainTokens",
	"recentRatio",
	"forgetBoundaryRatio",
	"toolMaintenanceRatio",
	"forgetMaintenanceRatio",
	"pressureRatio",
	"pressureExitRatio",
	"minNetReleaseTokens",
	"minNetReleaseRatio",
	"targetBatchTokens",
	"maxBatchTokens",
	"maxMaintenanceBatches",
	"maxPressureBatches",
	"minReentryTurns",
	"summarizationProvider",
	"summarizationModel",
	"maxTokens",
	"compactionRetries",
	"maxOverflowRetries",
	"responseReserveTokens",
	"safetyMarginTokens"
];
const TOOL_GROUP_KEYS = /* @__PURE__ */ new Set([
	"enabled",
	"minGroupResults",
	"minGroupChars",
	"minGroupTokens",
	"maxGroupTokens",
	"maxGroupsPerPass",
	"maxSummaryTokens"
]);
const BASIC_COMPACT_CONFIG_KEYS = /* @__PURE__ */ new Set([
	...POLICY_CONFIG_KEYS,
	"modelPolicies",
	"auto",
	"toolGroupSummarizer"
]);
const MODEL_POLICY_KEYS = /* @__PURE__ */ new Set([
	"provider",
	"model",
	...POLICY_CONFIG_KEYS
]);
var TargetPressureConfigError = class extends Error {
	targetKey;
	constructor(targetKey, message) {
		super(message);
		this.targetKey = targetKey;
	}
};
function resolveConfig(config = {}) {
	validateKeys(config, BASIC_COMPACT_CONFIG_KEYS, "BasicCompactionConfig");
	validateToolGroupConfig(config.toolGroupSummarizer);
	validatePolicy(config, "BasicCompactionConfig");
	if (config.auto !== void 0 && typeof config.auto !== "boolean") throw new Error("BasicCompactionConfig: auto must be a boolean");
	const resolved = resolvePolicy(config, void 0, "BasicCompactionConfig");
	const modelPolicies = resolveModelPolicies(config.modelPolicies);
	for (const [index, policy] of modelPolicies.entries()) resolvePolicy(policy, resolved, `BasicCompactionConfig: modelPolicies[${index}]`);
	return deepFreeze({
		...resolved,
		modelPolicies,
		auto: config.auto ?? true,
		toolGroupSummarizer: {
			enabled: config.toolGroupSummarizer?.enabled ?? true,
			minGroupResults: config.toolGroupSummarizer?.minGroupResults ?? 2,
			minGroupChars: config.toolGroupSummarizer?.minGroupChars ?? 12e3,
			minGroupTokens: config.toolGroupSummarizer?.minGroupTokens ?? 2e3,
			maxGroupTokens: config.toolGroupSummarizer?.maxGroupTokens ?? 12e3,
			maxGroupsPerPass: config.toolGroupSummarizer?.maxGroupsPerPass ?? 2,
			maxSummaryTokens: config.toolGroupSummarizer?.maxSummaryTokens ?? 1200
		}
	});
}
function resolveTargetPolicy(config, target) {
	const resolved = resolvePolicy(config.modelPolicies.find((policy) => policy.provider === target.provider && policy.model === target.model) ?? {}, config, `${target.provider}/${target.model}`);
	return deepFreeze({
		...resolved,
		target: { ...target }
	});
}
function resolveCompactSpec(policy, contextWindow) {
	const targetKey = `${policy.target.provider}/${policy.target.model}`;
	if (!Number.isInteger(contextWindow) || contextWindow <= 0) throw new TargetPressureConfigError(targetKey, `BasicCompactionConfig: contextWindow (${contextWindow}) must be a positive integer`);
	const thresholdTokens = Math.floor(contextWindow * policy.pressureRatio);
	const pressureExitTokens = Math.floor(contextWindow * policy.pressureExitRatio);
	const retainTokens = policy.retainTokens === void 0 ? Math.floor(contextWindow * policy.recentRatio) : policy.retainTokens;
	const forgetBoundaryTokens = Math.floor(contextWindow * policy.forgetBoundaryRatio);
	if (retainTokens >= thresholdTokens) throw new TargetPressureConfigError(targetKey, `BasicCompactionConfig: ${targetKey} retainTokens (${retainTokens}) must be less than threshold tokens ${thresholdTokens}`);
	if (retainTokens >= forgetBoundaryTokens) throw new TargetPressureConfigError(targetKey, `BasicCompactionConfig: ${targetKey} retainTokens (${retainTokens}) must be less than forget boundary tokens ${forgetBoundaryTokens}`);
	if (pressureExitTokens >= thresholdTokens) throw new TargetPressureConfigError(targetKey, `BasicCompactionConfig: ${targetKey} pressureExitTokens (${pressureExitTokens}) must be less than threshold tokens ${thresholdTokens}`);
	const heldFreeTokens = policy.responseReserveTokens + policy.safetyMarginTokens;
	if (heldFreeTokens >= contextWindow) throw new TargetPressureConfigError(targetKey, `BasicCompactionConfig: ${targetKey} responseReserveTokens + safetyMarginTokens (${heldFreeTokens}) must be less than the context window ${contextWindow}`);
	return deepFreeze({
		...policy,
		target: { ...policy.target },
		contextWindow,
		thresholdRatio: policy.pressureRatio,
		thresholdTokens,
		retainTokens,
		pressureExitTokens
	});
}
function resolvePolicy(config, fallback, name) {
	if (config.compactionRetries !== void 0 && config.maxPressureBatches !== void 0) throw new Error(`${name}: compactionRetries conflicts with maxPressureBatches; configure only one pressure budget`);
	const pressureRatio = config.pressureRatio ?? config.thresholdRatio ?? fallback?.pressureRatio ?? DEFAULT_PRESSURE_RATIO;
	const recentRatio = config.recentRatio ?? config.retainRatio ?? fallback?.recentRatio ?? DEFAULT_RECENT_RATIO;
	const forgetBoundaryRatio = config.forgetBoundaryRatio ?? fallback?.forgetBoundaryRatio ?? DEFAULT_FORGET_BOUNDARY_RATIO;
	const toolMaintenanceRatio = config.toolMaintenanceRatio ?? fallback?.toolMaintenanceRatio ?? DEFAULT_TOOL_MAINTENANCE_RATIO;
	const forgetMaintenanceRatio = config.forgetMaintenanceRatio ?? fallback?.forgetMaintenanceRatio ?? DEFAULT_FORGET_MAINTENANCE_RATIO;
	const pressureExitRatio = config.pressureExitRatio ?? fallback?.pressureExitRatio ?? forgetMaintenanceRatio;
	validateZoneRatios(recentRatio, forgetBoundaryRatio, toolMaintenanceRatio, forgetMaintenanceRatio, pressureRatio, pressureExitRatio, name);
	validateRetention(config, pressureRatio, recentRatio, name);
	const retention = config.retainTokens !== void 0 ? { retainTokens: config.retainTokens } : config.retainRatio !== void 0 || config.recentRatio !== void 0 ? { retainRatio: recentRatio } : fallback?.retainTokens !== void 0 ? { retainTokens: fallback.retainTokens } : { retainRatio: recentRatio };
	const fields = {
		thresholdRatio: pressureRatio,
		recentRatio,
		forgetBoundaryRatio,
		toolMaintenanceRatio,
		forgetMaintenanceRatio,
		pressureRatio,
		pressureExitRatio,
		minNetReleaseTokens: config.minNetReleaseTokens ?? fallback?.minNetReleaseTokens ?? DEFAULT_MIN_NET_RELEASE_TOKENS,
		minNetReleaseRatio: config.minNetReleaseRatio ?? fallback?.minNetReleaseRatio ?? DEFAULT_MIN_NET_RELEASE_RATIO,
		targetBatchTokens: config.targetBatchTokens ?? fallback?.targetBatchTokens ?? DEFAULT_TARGET_BATCH_TOKENS,
		maxBatchTokens: config.maxBatchTokens ?? fallback?.maxBatchTokens ?? DEFAULT_MAX_BATCH_TOKENS,
		maxMaintenanceBatches: config.maxMaintenanceBatches ?? fallback?.maxMaintenanceBatches ?? DEFAULT_MAX_MAINTENANCE_BATCHES,
		maxPressureBatches: config.maxPressureBatches ?? (config.compactionRetries !== void 0 ? config.compactionRetries + 1 : fallback?.maxPressureBatches ?? DEFAULT_MAX_PRESSURE_BATCHES),
		minReentryTurns: config.minReentryTurns ?? fallback?.minReentryTurns ?? DEFAULT_MIN_REENTRY_TURNS,
		summarizationProvider: config.summarizationProvider ?? fallback?.summarizationProvider ?? "",
		summarizationModel: config.summarizationModel ?? fallback?.summarizationModel ?? "",
		maxTokens: config.maxTokens ?? fallback?.maxTokens ?? 8192,
		compactionRetries: config.compactionRetries ?? fallback?.compactionRetries ?? 1,
		maxOverflowRetries: config.maxOverflowRetries ?? fallback?.maxOverflowRetries ?? 1,
		responseReserveTokens: config.responseReserveTokens ?? fallback?.responseReserveTokens ?? DEFAULT_RESPONSE_RESERVE_TOKENS,
		safetyMarginTokens: config.safetyMarginTokens ?? fallback?.safetyMarginTokens ?? DEFAULT_SAFETY_MARGIN_TOKENS,
		...retention
	};
	validateResolvedBudgets(fields, name);
	return fields;
}
function validateResolvedBudgets(fields, name) {
	for (const [key, value] of Object.entries(fields)) if (key === "maxMaintenanceBatches" || key === "maxPressureBatches" || key === "minNetReleaseTokens") assertNonNegativeInteger(`${name}.${key}`, value);
	else if (key === "targetBatchTokens" || key === "maxBatchTokens" || key === "minReentryTurns") assertPositiveInteger(`${name}.${key}`, value);
	else if (key === "minNetReleaseRatio") assertBoundedRatio(`${name}.${key}`, value);
	else if (key === "pressureExitRatio") assertRatio(`${name}.${key}`, value);
	if (fields.targetBatchTokens > fields.maxBatchTokens) throw new Error(`${name}: targetBatchTokens must not exceed maxBatchTokens`);
}
function validateZoneRatios(recent, forget, tool, forgetMaint, pressure, pressureExit, name) {
	for (const [key, value] of Object.entries({
		recentRatio: recent,
		forgetBoundaryRatio: forget,
		toolMaintenanceRatio: tool,
		forgetMaintenanceRatio: forgetMaint,
		pressureRatio: pressure,
		pressureExitRatio: pressureExit
	})) assertRatio(`${name}.${key}`, value);
	if (!(recent < forget && forget < forgetMaint && forgetMaint < pressure)) throw new Error(`${name}: require recentRatio < forgetBoundaryRatio < forgetMaintenanceRatio < pressureRatio`);
	if (!(forgetMaint <= pressureExit && pressureExit < pressure)) throw new Error(`${name}: require forgetMaintenanceRatio <= pressureExitRatio < pressureRatio`);
	if (!(tool >= recent && tool < forgetMaint)) throw new Error(`${name}: require toolMaintenanceRatio >= recentRatio and < forgetMaintenanceRatio`);
}
function validateRetention(config, pressure, recent, name) {
	if (config.thresholdRatio !== void 0 && config.pressureRatio !== void 0 && config.thresholdRatio !== config.pressureRatio) throw new Error(`${name}: thresholdRatio conflicts with pressureRatio`);
	if (config.retainRatio !== void 0 && config.recentRatio !== void 0 && config.retainRatio !== config.recentRatio) throw new Error(`${name}: retainRatio conflicts with recentRatio`);
	if (config.retainTokens !== void 0 && (config.retainRatio !== void 0 || config.recentRatio !== void 0)) throw new Error(`${name}: retainTokens is mutually exclusive with retainRatio and recentRatio`);
	if (config.retainRatio !== void 0) assertRatio(`${name}.retainRatio`, config.retainRatio);
	if (config.retainTokens !== void 0) assertNonNegativeInteger(`${name}.retainTokens`, config.retainTokens);
	if (recent >= pressure) throw new Error(`${name}: recentRatio must be less than pressureRatio`);
}
function resolveModelPolicies(configured) {
	if (configured === void 0) return [];
	if (!Array.isArray(configured)) throw new Error("BasicCompactionConfig: modelPolicies must be an array");
	const seen = /* @__PURE__ */ new Set();
	return configured.map((source, index) => {
		const name = `BasicCompactionConfig: modelPolicies[${index}]`;
		if (!isUnknownRecord(source)) throw new Error(`${name} must be an object`);
		validateKeys(source, MODEL_POLICY_KEYS, name);
		assertNonEmptyString(`${name}.provider`, source.provider);
		assertNonEmptyString(`${name}.model`, source.model);
		validatePolicy(source, name);
		const key = `${source.provider}\u0000${source.model}`;
		if (seen.has(key)) throw new Error(`BasicCompactionConfig: duplicate model policy for ${source.provider}/${source.model}`);
		seen.add(key);
		return { ...source };
	});
}
function validatePolicy(config, name) {
	const values = config;
	for (const key of [
		"thresholdRatio",
		"retainRatio",
		"recentRatio",
		"forgetBoundaryRatio",
		"toolMaintenanceRatio",
		"forgetMaintenanceRatio",
		"pressureRatio",
		"pressureExitRatio"
	]) if (values[key] !== void 0) assertRatio(`${name}.${key}`, values[key]);
	if (values.retainTokens !== void 0) assertNonNegativeInteger(`${name}.retainTokens`, values.retainTokens);
	if (values.minNetReleaseRatio !== void 0) assertBoundedRatio(`${name}.minNetReleaseRatio`, values.minNetReleaseRatio);
	if (values.minNetReleaseTokens !== void 0) assertNonNegativeInteger(`${name}.minNetReleaseTokens`, values.minNetReleaseTokens);
	for (const key of [
		"targetBatchTokens",
		"maxBatchTokens",
		"maxMaintenanceBatches",
		"maxPressureBatches",
		"minReentryTurns",
		"maxTokens",
		"compactionRetries",
		"maxOverflowRetries"
	]) if (values[key] !== void 0) key === "compactionRetries" || key === "maxOverflowRetries" || key === "maxMaintenanceBatches" || key === "maxPressureBatches" ? assertNonNegativeInteger(`${name}.${key}`, values[key]) : assertPositiveInteger(`${name}.${key}`, values[key]);
	if (typeof values.targetBatchTokens === "number" && typeof values.maxBatchTokens === "number" && values.targetBatchTokens > values.maxBatchTokens) throw new Error(`${name}: targetBatchTokens must not exceed maxBatchTokens`);
	for (const key of ["responseReserveTokens", "safetyMarginTokens"]) if (values[key] !== void 0) assertNonNegativeInteger(`${name}.${key}`, values[key]);
	validateSummarizationPair(values, name);
}
function validateSummarizationPair(config, name) {
	const provider = config.summarizationProvider;
	const model = config.summarizationModel;
	if (provider !== void 0 && typeof provider !== "string") throw new Error(`${name}.summarizationProvider must be a string`);
	if (model !== void 0 && typeof model !== "string") throw new Error(`${name}.summarizationModel must be a string`);
	if (provider === void 0 && model === void 0) return;
	if (provider === void 0 || model === void 0 || provider.length === 0 !== (model.length === 0)) throw new Error(`${name}: summarizationProvider and summarizationModel must be set together as an empty or non-empty pair`);
}
function validateToolGroupConfig(config) {
	if (config === void 0) return;
	if (!isUnknownRecord(config)) throw new Error("BasicCompactionConfig: toolGroupSummarizer must be an object");
	validateKeys(config, TOOL_GROUP_KEYS, "BasicCompactionConfig.toolGroupSummarizer");
	if (config.enabled !== void 0 && typeof config.enabled !== "boolean") throw new Error("BasicCompactionConfig.toolGroupSummarizer.enabled must be a boolean");
	for (const key of [
		"minGroupResults",
		"minGroupChars",
		"minGroupTokens",
		"maxGroupTokens",
		"maxGroupsPerPass",
		"maxSummaryTokens"
	]) if (config[key] !== void 0) assertPositiveInteger(`BasicCompactionConfig.toolGroupSummarizer.${key}`, config[key]);
	if (typeof config.minGroupTokens === "number" && typeof config.maxGroupTokens === "number" && config.minGroupTokens > config.maxGroupTokens) throw new Error("BasicCompactionConfig.toolGroupSummarizer.minGroupTokens must not exceed maxGroupTokens");
}
function validateKeys(config, keys, name) {
	for (const key of Object.keys(config)) if (!keys.has(key)) throw new Error(`${name}: unknown key "${key}"`);
}
function isUnknownRecord(value) {
	return typeof value === "object" && value !== null && !Array.isArray(value);
}
function assertNonEmptyString(name, value) {
	if (typeof value !== "string" || value.length === 0) throw new Error(`${name} must be a non-empty string`);
}
function assertPositiveInteger(name, value) {
	if (typeof value !== "number" || !Number.isInteger(value) || value <= 0) throw new Error(`${name} (${String(value)}) must be a positive integer`);
}
function assertNonNegativeInteger(name, value) {
	if (typeof value !== "number" || !Number.isInteger(value) || value < 0) throw new Error(`${name} (${String(value)}) must be a non-negative integer`);
}
function assertRatio(name, value) {
	if (typeof value !== "number" || !Number.isFinite(value) || value <= 0 || value > 1) throw new Error(`${name} (${String(value)}) must be a number in (0, 1]`);
}
function assertBoundedRatio(name, value) {
	if (typeof value !== "number" || !Number.isFinite(value) || value < 0 || value > 1) throw new Error(`${name} (${String(value)}) must be a number in [0, 1]`);
}
//#endregion
//#region node_modules/.pnpm/@deepseek-ai+dsh-session@0._ee5063a80d448ae764858c08e7528ed1/node_modules/@deepseek-ai/dsh-session/lib/index.js
/**
* Admit a numeric value as an existing Session event position.
* @param value - non-negative safe integer admitted by the owning log operation.
* @returns the same number with the Session-sequence brand.
*/
function SessionSeq(value) {
	if (!Number.isSafeInteger(value) || value < 0 || Object.is(value, -0)) throw new TypeError(`SessionSeq must be a non-negative safe integer, got ${String(value)}`);
	return brandNumber(value);
}
//#endregion
//#region lib/types/internal/compaction/selection-guard.js
/**
* Empty-benefit guard for recursive global compaction, re-implemented for
* `dsh-context-enhancement` from the MIT-licensed Card5/6 migration delta of
* the official rc.1 tree.
*
* SOURCE: this file is a local copy of the working-tree Card5/6 addition
* `packages/compaction/compaction/src/selection-guard.ts` (MIT, on top of tag
* 0.1.2-rc.1 of the `deepseek-harness` repository). The upstream file lands in
* the published package only after the migration ships; this standalone package
* must not depend on that unshipped subpath, so the helper is copied here.
* Unlike the upstream leaf it consumes the checkpoint predicate through the
* published rc1 `@deepseek-ai/dsh-compaction/checkpoint` subpath instead of a
* local `./checkpoint.ts`, so no checkpoint module is re-implemented.
* Provenance is recorded in THIRD_PARTY_NOTICES.md.
*
* The guard rejects a candidate older surface span that contains only old
* compaction summaries with no current non-checkpoint node, so a later
* compaction pass never spends a model call re-summarizing an isolated old
* summary. Cordis-free: it reads only the session surface (through the
* structural {@link SessionRead} type), the shared checkpoint predicate, and
* the session surface predicate, and never loads the host plugin's Context
* merges.
*
* An old compaction replacement is otherwise a normal current surface node and
* may enter a later global compaction span together with newer old content —
* no summary is permanently protected. This guard only rejects the
* all-summary case; the existing non-shrink transaction assertion remains the
* final guard.
*
* @module dsh-context-enhancement/internal/compaction/selection-guard
*/
/**
* Whether a current surface node is an old compaction summary: a user-role
* replacement message carrying the compaction checkpoint marker. The
* replacement requirement matches the compaction invariant's checkpoint
* recognition, so an appended user message that merely carries the marker
* without shadowing a range is not treated as a summary.
* @param event - a current surface-node event.
* @returns true when the node is a compaction replacement checkpoint.
*/
function isOldCompactionSummary(event) {
	if (event.type !== "user/message") return false;
	if (!isReplacementSurfaceEvent(event)) return false;
	const { source } = event.data;
	return source.kind === "plugin" && isCompactCheckpointSource(source);
}
/**
* Whether a candidate surface span holds only old compaction summaries with no
* current non-checkpoint node, making a recursive compaction of that span an
* empty-benefit pass.
*
* Caller contract: `seqs` must name a complete, contiguous, duplicate-free
* span of the session's current surface, listed in current surface order —
* exactly `session.surface.nodes.slice(start, end)` for one `start`/`end`.
* Any other input (an out-of-order list, a sparse list that skips a surface
* node, a repeated seq, a seq that is not a current surface node, or a seq
* with no matching log event) is a caller bug and is rejected loudly rather
* than answered silently. Pass the empty array for "no candidate".
*
* @param session - session whose current surface contains the candidate seqs.
* @param seqs - the candidate span's current surface node seqs, in surface order.
* @returns true when the span is non-empty and every node is an old compaction
* summary; false when the span is empty or contains at least one
* non-checkpoint node.
* @throws when `seqs` is not a complete contiguous duplicate-free span of the
* current surface in surface order, when a seq names no current surface node,
* or when a current surface seq names no matching log event.
*/
function isIsolatedOldSummaryRange(session, seqs) {
	if (seqs.length === 0) return false;
	const surfaceNodes = session.surface.nodes;
	const seen = /* @__PURE__ */ new Set();
	let startIndex = -1;
	for (const [offset, seq] of seqs.entries()) {
		const position = surfaceNodes.indexOf(seq);
		if (position === -1) throw new Error(`selection-guard: surface seq ${seq} is not a current surface node`);
		if (seen.has(seq)) throw new Error(`selection-guard: candidate seqs must not repeat surface seq ${seq}`);
		seen.add(seq);
		if (offset === 0) {
			startIndex = position;
			continue;
		}
		if (position !== startIndex + offset) throw new Error(`selection-guard: candidate seqs must name a contiguous span of the current surface in surface order; seq ${seq} does not follow surface position ${startIndex + offset}`);
	}
	return seqs.every((seq) => {
		const event = session.eventAt(seq);
		if (event === void 0 || event.seq !== seq) throw new Error(`selection-guard: surface seq ${seq} has no matching session event (corrupt surface)`);
		return isOldCompactionSummary(event);
	});
}
//#endregion
//#region lib/types/internal/compaction/summarizer.js
/**
* Default one-shot summarization and durable checkpoint framing for the
* `dsh-context-enhancement` basic compaction backend, re-implemented from the
* MIT-licensed official rc1 source.
*
* SOURCE: local copy of the official rc.1 `dsh-compaction-basic`
* `src/summarizer.ts` (MIT, tag 0.1.2-rc.1 of `deepseek-harness`). It lives
* under `internal/` because official `@deepseek-ai/dsh-compaction-basic`
* publishes only its root (the backend class); the summarizer helper and the
* checkpoint framing are internal to that package and not a stable API.
* Provenance is recorded in THIRD_PARTY_NOTICES.md.
*
* @module dsh-context-enhancement/internal/compaction/summarizer
*/
/** Tags wrapping the structured summary inside the landed checkpoint node. */
const SUMMARY_OPEN_TAG = "<compacted-summary>";
const SUMMARY_CLOSE_TAG = "</compacted-summary>";
/**
* The summarization directive, delivered as the FINAL user message after the
* replayed conversation rather than as a distinct summarizer system prompt.
* Keeping the conversation's own system prompt, tools, and message prefix in
* front of it makes the auxiliary call a genuine prefix of the last routed
* request, so the provider's KV cache is reused instead of invalidated.
*/
const COMPACTION_INSTRUCTION = [
	"You are now acting as a compaction engine for this AI coding assistant. Condense the conversation ABOVE into a structured checkpoint that lets another model resume the work with no loss of essential context.",
	"",
	"Output EXACTLY the Markdown structure below: keep every section, in order. Use terse bullets, not prose paragraphs. Write \"(none)\" for an empty section — never drop a section.",
	"",
	"## Primary Request and Intent",
	"- [the user's original and evolving goals; quote verbatim where the exact wording matters]",
	"",
	"## Key Technical Concepts",
	"- [technologies, frameworks, patterns, and conventions in play]",
	"",
	"## Files and Code",
	"- [exact path: why it matters, key changes or snippets]",
	"",
	"## Errors and Fixes",
	"- [error: how it was resolved, plus any related user feedback]",
	"",
	"## Pending Jobs",
	"- [explicitly requested work not yet completed]",
	"",
	"## Current Work",
	"- [precisely what was in progress at this checkpoint]",
	"",
	"## Next Step",
	"- [the single next action, directly in line with the most recent request, or \"(none)\"]",
	"",
	"## Critical Context",
	"- [decisions and their rationale, constraints, user preferences, open questions, data needed to continue]",
	"",
	"Rules:",
	"- Write concise English engineering prose. Preserve exact file paths, commands, error strings, identifiers, numeric values, function signatures, and syntax fragments.",
	"- Capture user feedback and explicit instructions faithfully, especially corrections.",
	"- Do NOT mention this summarization request or that the context was compacted.",
	"- Output only the checkpoint text: do not call any tool or take any other action.",
	`- If the conversation already contains a ${SUMMARY_OPEN_TAG} block, it is a PRIOR checkpoint. Do not copy it forward verbatim: preserve still-true facts, drop stale ones, and merge newer information into a single consolidated summary under the same structure.`
].join("\n");
/** Framing that makes the replacement user message established context. */
const CHECKPOINT_PREAMBLE = "This is an automatically generated checkpoint condensing an earlier span of the conversation to free up context. Treat the captured context as established background and build on it without restating it. Continue the task directly from the messages that follow, without acknowledging this checkpoint.";
/**
* Run the default cache-reusing `ctx.llm.stream()` summarization call: replay
* the conversation prefix, then append the compaction instruction as the final
* user message so the provider's warm prefix cache is reused.
* @param ctx - context providing the LLM service.
* @param config - resolved backend configuration.
* @param input - replayed conversation prefix (system, tools, and leading messages) to condense.
* @param agent - supplies routed-model history, fallback model, and session id.
* @param signal - optional cancellation forwarded to the adapter.
* @returns safe text-only summary blocks and the exact call envelope and output.
*/
async function summarizeWithLlm(ctx, config, input, agent, signal) {
	const latest = agent.session.requestHeader()?.config;
	const configured = config.summarizationProvider.length === 0 ? void 0 : {
		provider: config.summarizationProvider,
		model: config.summarizationModel
	};
	const agentTarget = agent.options.provider !== void 0 && agent.options.provider.length > 0 && agent.options.model !== void 0 && agent.options.model.length > 0 ? {
		provider: agent.options.provider,
		model: agent.options.model
	} : void 0;
	const target = configured ?? latest ?? agentTarget;
	if (target === void 0) throw new Error("no provider/model available for summarization: set both BasicCompactionConfig summarization fields, route one request, or set both AgentOptions fields");
	const assembler = new BlockAssembler();
	const messages = [...input.messages, createUserMessage({
		content: [{
			type: "text",
			text: COMPACTION_INSTRUCTION
		}],
		source: {
			kind: "plugin",
			plugin: "dsh-context-enhancement"
		}
	})];
	const options = {
		provider: target.provider,
		model: target.model,
		messages,
		...input.system === void 0 ? {} : { system: input.system },
		...input.tools === void 0 ? {} : { tools: [...input.tools] },
		maxTokens: config.maxTokens,
		sessionId: agent.session.id,
		purpose: "compaction",
		...signal === void 0 ? {} : { signal }
	};
	for await (const chunk of ctx.llm.stream(options)) assembler.push(chunk);
	const error = finishError(assembler.finish);
	if (error !== void 0) throw error;
	const rawOutput = assembler.blocks();
	const summary = summaryText(rawOutput);
	if (!summary.some((block) => block.text.trim().length > 0)) throw new Error("summarization produced no text summary content");
	return {
		summary,
		rawOutput,
		llmStreamCall: true,
		provider: options.provider,
		model: options.model,
		maxTokens: config.maxTokens,
		...assembler.usage === void 0 ? {} : { usage: assembler.usage }
	};
}
/**
* Wrap raw summary blocks in the durable checkpoint framing.
* @param summary - safe text-only model output.
* @returns content for the synthesized replacement user message.
*/
function frameSummary(summary) {
	return [
		{
			type: "text",
			text: `${CHECKPOINT_PREAMBLE}\n\n${SUMMARY_OPEN_TAG}`
		},
		...summary,
		{
			type: "text",
			text: SUMMARY_CLOSE_TAG
		}
	];
}
/**
* Price the fixed compaction instruction with the session estimator, so the
* envelope-budget input cap accounts for the only novel part of the auxiliary
* request.
* @param meter - effective session token meter.
* @returns heuristic price of the trailing instruction user message.
*/
function compactionInstructionTokens(meter) {
	return meter.estimateMessage(createUserMessage({
		content: [{
			type: "text",
			text: COMPACTION_INSTRUCTION
		}],
		source: {
			kind: "plugin",
			plugin: "dsh-context-enhancement"
		}
	}));
}
/**
* Price the smallest possible framed checkpoint. A span at or below this price
* can never satisfy the shrink requirement, so the budget pass must decline it
* before paying for a call.
* @param meter - effective session token meter.
* @returns heuristic price of a checkpoint with an empty summary body.
*/
function minimumCheckpointTokens(meter) {
	return meter.estimateMessage(createUserMessage({
		content: frameSummary([]),
		source: {
			kind: "plugin",
			plugin: "dsh-context-enhancement"
		}
	}));
}
/** Map a terminal summarization finish to its fail-closed error. */
function finishError(finish) {
	switch (finish.kind) {
		case "error":
		case "aborted": {
			const error = new Error(finish.failure.message);
			error.code = finish.failure.code;
			return error;
		}
		case "max-tokens": {
			const error = /* @__PURE__ */ new Error("summarization truncated at the token cap (incomplete checkpoint)");
			error.code = "MAX_TOKENS";
			return error;
		}
		default: return;
	}
}
/** Reject visual output and keep only text before synthesizing a user message. */
function summaryText(blocks) {
	if (contentHasImage(blocks)) throw new LlmError("compaction summary cannot contain image output", "UNSUPPORTED_CONTENT");
	return blocks.filter((block) => block.type === "text");
}
//#endregion
//#region lib/types/internal/compaction/region.js
/**
* Surface retention selection and the shared log-recorded compaction
* transaction for the `dsh-context-enhancement` basic compaction backend,
* re-implemented from the MIT-licensed official rc1 source with the Card5/6
* region-aware migration delta folded in.
*
* SOURCE: local copy of the official rc.1 `dsh-compaction-basic`
* `src/region.ts` (MIT, tag 0.1.2-rc.1 of `deepseek-harness`) plus the
* Card5/6 working-tree delta of the same file (`splitRetainedTail`, the
* `olderRange` split consumed by the region-aware pruner pass, and the
* recursive empty-benefit guard wired into range selection). The
* `isIsolatedOldSummaryRange` guard and the pairing predicates are imported
* from the local pure leaves under `./` instead of the official package root.
* Only the published rc1 exports of the official packages are consumed.
* Provenance is recorded in THIRD_PARTY_NOTICES.md.
*
* @module dsh-context-enhancement/internal/compaction/region
*/
/**
* Rejects a summary whose replacement boundaries are no longer the ones it was
* built from, distinguished from summarizer and shrink failures so a manual
* caller can report the two causes differently.
*/
var SurfaceChangedError = class extends Error {};
/**
* Resolve the retained-tail boundary and the older head span before it from one
* priced snapshot, rounding the boundary head-ward to a balanced tool-pair cut
* so a call/result pair is never split. The span's `start`/`end` seqs are the
* current-surface first/last nodes of the older head in surface order; when
* surface replacements made the visible seqs non-monotonic, `start` may be
* numerically larger than `end` and the range is still the closed position
* interval.
* @param session - session supplying authoritative current surface positions.
* @param measurement - unified pressure and surface measurement from the conversation meter.
* @param retainTokens - minimum recent tail budget retained verbatim.
* @returns the retained-tail split with the older head seqs in surface order.
*/
function splitRetainedTail(session, measurement, retainTokens) {
	const pricedNodes = measurement.nodes;
	if (pricedNodes.length === 0) return {
		keepFromIdx: 0,
		cutoffSeq: void 0,
		olderRange: null,
		olderSeqs: []
	};
	const surfaceNodes = session.surface.nodes;
	if (surfaceNodes.length !== pricedNodes.length || surfaceNodes.some((seq, index) => seq !== pricedNodes[index]?.seq)) throw new Error("compaction: token-meter surface does not match the current session surface");
	let accumulated = 0;
	let keepFromIdx = pricedNodes.length;
	for (let index = pricedNodes.length - 1; index >= 0; index -= 1) {
		accumulated += pricedNodes[index].tokens;
		keepFromIdx = index;
		if (accumulated >= retainTokens) break;
	}
	while (keepFromIdx > 0) {
		if (toolPairingBalancedBefore(session, surfaceNodes[keepFromIdx])) break;
		keepFromIdx -= 1;
	}
	const olderSeqs = surfaceNodes.slice(0, keepFromIdx);
	const olderStart = olderSeqs.length === 0 ? void 0 : olderSeqs[0];
	const olderEnd = olderSeqs.length === 0 ? void 0 : olderSeqs.at(-1);
	return {
		keepFromIdx,
		cutoffSeq: olderEnd,
		olderRange: olderStart === void 0 || olderEnd === void 0 ? null : {
			start: olderStart,
			end: olderEnd
		},
		olderSeqs
	};
}
/**
* Resolve the next head-anchored range while retaining a priced recent tail,
* never splitting an assistant tool-call/result pair. Unless the caller opts
* out, it also rejects a head holding only old compaction summaries with no
* non-checkpoint node, so pressure and manual compaction never spend a model
* call on an empty-benefit pass. Overflow recovery opts out (the fourth
* positional parameter is `false`): its retry depends on advancing the surface,
* and re-compacting an isolated checkpoint is that path's last deterministic
* reduction (the non-shrink assertion still guards growth).
* @param session - session supplying authoritative current surface positions.
* @param measurement - unified pressure and surface measurement from the conversation meter.
* @param retainTokens - minimum recent tail budget retained verbatim.
* @param guardIsolatedOldSummary - reject an all-checkpoint older head. Defaults to `true`.
* @returns the inclusive positional seq range to compact, or `null`.
*/
function selectCompactableRange(session, measurement, retainTokens, guardIsolatedOldSummary = true) {
	const split = splitRetainedTail(session, measurement, retainTokens);
	if (split.cutoffSeq === void 0) return null;
	if (guardIsolatedOldSummary && isIsolatedOldSummaryRange(session, split.olderSeqs)) return null;
	return {
		start: session.surface.nodes[0],
		end: split.cutoffSeq
	};
}
/**
* Run the single compaction transaction over one selected positional span.
* Selection and validation are read-only. Idle/log validation and
* `compaction/start` are synchronously adjacent, so the durable opening marker is
* the compaction lock before summarization yields. Every later failure makes
* exactly one `compaction/end` attempt; a failed close deliberately leaves the
* unmatched start detectable.
* @param dependencies - conversation meter and dynamically dispatched summarizer hook.
* @param session - session whose surface is mutated.
* @param start - inclusive first surface-node seq.
* @param end - inclusive last surface-node seq.
* @param agent - agent used by the summarizer.
* @param options - bracket owner, stability rule, and optional durability checkpoint.
* @param signal - optional summarization cancellation signal.
* @returns the successful durable compaction result.
*/
async function compactSurfaceRegion(dependencies, session, start, end, agent, options, signal) {
	if (options.owner === null) signal?.throwIfAborted();
	const selection = validateSurfaceRegion(session, start, end);
	const entryState = inspectCompactionEntryState(session);
	assertCompactionInactive(entryState.unmatchedCompactionStart, entryState.latestEndSeedSeq, "compaction");
	let owner;
	if (options.owner === null) {
		if (entryState.openTurn !== null) throw new ManualCompactionError("busy", "manual compaction: the session already has an open turn");
		owner = null;
	} else {
		if (entryState.openTurn === null) throw new Error("compactRegion: no open turn — automatic compaction events must be enclosed in a turn");
		owner = entryState.openTurn;
	}
	const compactionId = CompactionId(randomUUID());
	const lifecycle = {
		compactionId,
		...options.sourceCommandId === void 0 ? {} : { sourceCommandId: options.sourceCommandId },
		turn: owner
	};
	const startEvent = session.append("compaction/start", lifecycle);
	const assertStable = options.stability === "whole-surface" ? assertWholeSurfaceUnchanged : assertSelectedSpanStable;
	let failure;
	let flushFailure;
	let result;
	let closed = false;
	let closing = false;
	let stage = "summary";
	try {
		const summarized = await summarizeCompaction(dependencies, prepareCompaction(dependencies, session, selection), agent, compactionId, options.sourceCommandId, signal);
		if (options.owner === null) signal?.throwIfAborted();
		assertStable(dependencies, session, summarized);
		stage = "commit";
		const pending = commitCompactionBody(session, startEvent, summarized);
		closing = true;
		const endEvent = session.append("compaction/end", lifecycle);
		closed = true;
		result = completeCompaction(pending, endEvent);
	} catch (error) {
		failure = {
			error,
			stage: closing ? "commit" : stage
		};
		if (!closing) {
			closing = true;
			try {
				session.append("compaction/end", {
					...lifecycle,
					error: errorChain(error)
				});
				closed = true;
			} catch (closeError) {
				failure = {
					error: closeError,
					stage: "commit"
				};
			}
		}
	}
	if (closed && options.flush !== void 0) try {
		await options.flush();
	} catch (error) {
		flushFailure = error;
	}
	if (options.owner === null) signal?.throwIfAborted();
	if (failure !== void 0) {
		if (options.owner === null) throwManualFailure(failure);
		throw failure.error;
	}
	if (flushFailure !== void 0) throw new ManualCompactionError("persistence", "manual compaction durability checkpoint failed", { cause: flushFailure });
	if (result === void 0) throw new Error("compaction committed without a result");
	return result;
}
/** Classify one closed manual attempt without weakening cancellation precedence. */
function throwManualFailure(failure) {
	if (failure.stage === "commit") throw new ManualCompactionError("commit", "manual compaction did not commit cleanly", { cause: failure.error });
	if (failure.error instanceof SurfaceChangedError) throw new ManualCompactionError("changed", "the compacted history changed during manual compaction", { cause: failure.error });
	throw new ManualCompactionError("summary", "manual compaction could not produce a smaller summary", { cause: failure.error });
}
/**
* Reject a durable unmatched compaction marker unless a later constructor-seed
* boundary proves that its owner belongs to an earlier session lifecycle.
* @param unmatchedCompactionStart - latest unmatched opening marker, if any.
* @param latestEndSeedSeq - newest constructor-seed boundary, if any.
* @param stage - operation label included in the busy diagnostic.
*/
function assertCompactionInactive(unmatchedCompactionStart, latestEndSeedSeq, stage) {
	if (unmatchedCompactionStart === void 0 || latestEndSeedSeq !== void 0 && latestEndSeedSeq > unmatchedCompactionStart.seq) return;
	throw new ManualCompactionError("busy", `${stage}: compaction already in progress; the session compaction lock is already active`);
}
/**
* Recheck the durable compaction lock after an asynchronous policy decision.
* @param session - session whose latest marker state is inspected.
* @param stage - operation label included in the busy diagnostic.
*/
function assertNoActiveCompaction(session, stage) {
	const entryState = inspectCompactionEntryState(session);
	assertCompactionInactive(entryState.unmatchedCompactionStart, entryState.latestEndSeedSeq, stage);
}
/** Validate one requested surface-position span before asynchronous work begins. */
function validateSurfaceRegion(session, start, end) {
	const nodes = session.surface.nodes;
	const startIdx = nodes.indexOf(start);
	const endIdx = nodes.indexOf(end);
	if (startIdx === -1) throw new Error(`compactRegion: start seq ${start} not found in surface`);
	if (endIdx === -1) throw new Error(`compactRegion: end seq ${end} not found in surface`);
	if (startIdx > endIdx) throw new Error(`compactRegion: start seq ${start} (position ${startIdx}) is after end seq ${end} (position ${endIdx}) on the surface`);
	if (!toolPairingBalancedBefore(session, nodes[startIdx])) throw new Error(`compactRegion: start seq ${start} is not a balanced boundary (would split a step's tool-call/result pair)`);
	if (!toolPairingBalancedAfter(session, nodes[endIdx])) throw new Error(`compactRegion: end seq ${end} is not a balanced boundary (would split a step, or the step is still open)`);
	return {
		start,
		end,
		startIdx,
		endIdx,
		shadowedSeqs: nodes.slice(startIdx, endIdx + 1)
	};
}
/** Snapshot pricing and replay input for a validated surface range. */
function prepareCompaction(dependencies, session, selection) {
	const measurement = dependencies.meter.measure(session);
	const selectedNodes = measurement.nodes.slice(selection.startIdx, selection.endIdx + 1);
	if (selectedNodes.length !== selection.shadowedSeqs.length || selectedNodes.some((node, index) => node.seq !== selection.shadowedSeqs[index])) throw new SurfaceChangedError("compaction: selected surface changed before summarization began");
	return {
		...selection,
		measurement,
		selectedNodes,
		shadowedTokenCount: selectedNodes.reduce((total, node) => total + node.heuristicTokens, 0),
		shadowedRouteTokenCount: selectedNodes.reduce((total, node) => total + node.tokens, 0),
		input: buildSummarizationInput(session, selection.shadowedSeqs)
	};
}
/** Run the summarizer and frame its replacement checkpoint. */
async function summarizeCompaction(dependencies, prepared, agent, compactionId, sourceCommandId, signal) {
	const summaryResult = await dependencies.summarize(prepared.input, agent, signal);
	const checkpointMessage = createUserMessage({
		content: frameSummary(summaryResult.summary),
		source: compactCheckpointSource(compactionId, sourceCommandId)
	});
	const framedSummaryTokenCount = dependencies.meter.estimateMessage(checkpointMessage);
	if (framedSummaryTokenCount >= prepared.shadowedRouteTokenCount) throw new Error(`summary is not smaller than the shadowed content (${framedSummaryTokenCount} estimated framed tokens >= ${prepared.shadowedRouteTokenCount})`);
	return {
		...prepared,
		...summaryResult,
		checkpointMessage
	};
}
/** Reject a summary prepared against any earlier surface generation. */
function assertWholeSurfaceUnchanged(dependencies, session, prepared) {
	const current = dependencies.meter.measure(session);
	if (!isDeepStrictEqual(current.nodes, prepared.measurement.nodes)) throw new SurfaceChangedError("compaction: session surface changed during summarization");
}
/**
* Require only that the selected span remain the same present, contiguous,
* equally priced, balanced replacement target. Nodes added outside it remain
* visible and do not invalidate the summary.
*/
function assertSelectedSpanStable(dependencies, session, prepared) {
	let current;
	try {
		current = validateSurfaceRegion(session, prepared.start, prepared.end);
	} catch (error) {
		throw new SurfaceChangedError("compaction: the selected span is no longer a valid replacement target", { cause: error });
	}
	if (!isDeepStrictEqual([...current.shadowedSeqs], [...prepared.shadowedSeqs])) throw new SurfaceChangedError("compaction: the selected span changed during summarization");
	const measured = dependencies.meter.measure(session).nodes.slice(current.startIdx, current.endIdx + 1);
	if (!isDeepStrictEqual(measured, prepared.selectedNodes)) throw new SurfaceChangedError("compaction: the selected span was rewritten during summarization");
}
/** Append one completed summary record and replacement body without yielding. */
function commitCompactionBody(session, startEvent, summarized) {
	const { start, end, shadowedSeqs, shadowedTokenCount, summary, provider, model, maxTokens, usage, checkpointMessage } = summarized;
	const callProvenance = summarized.llmStreamCall === true ? {
		rawOutput: summarized.rawOutput,
		llmStreamCall: true
	} : summarized.rawOutput === void 0 ? {} : { rawOutput: summarized.rawOutput };
	const summaryEvent = session.append("compaction/summary", {
		compactionId: startEvent.data.compactionId,
		...startEvent.data.sourceCommandId === void 0 ? {} : { sourceCommandId: startEvent.data.sourceCommandId },
		summary,
		...callProvenance,
		shadowedRange: {
			start,
			end
		},
		shadowedSeqs: [...shadowedSeqs],
		shadowedTokenCount,
		provider,
		model,
		...maxTokens === void 0 ? {} : { maxTokens },
		...usage === void 0 ? {} : { usage }
	});
	session.append("user/message", checkpointMessage, {
		surfaceOp: {
			op: "replace",
			start,
			end
		},
		sourceEventSeqs: [
			startEvent.seq,
			summaryEvent.seq,
			...shadowedSeqs
		]
	});
	return {
		compactionId: startEvent.data.compactionId,
		...startEvent.data.sourceCommandId === void 0 ? {} : { sourceCommandId: startEvent.data.sourceCommandId },
		startSeq: startEvent.seq,
		summarySeq: summaryEvent.seq,
		summary,
		shadowedRange: {
			start,
			end
		},
		shadowedSeqs: [...shadowedSeqs],
		shadowedTokenCount
	};
}
/** Attach the successfully appended close event to a pending result. */
function completeCompaction(pending, endEvent) {
	return {
		...pending,
		endSeq: endEvent.seq
	};
}
/**
* Reconstruct the last routed request's cacheable prefix for the shadowed
* region: its system prompt and tool schemas, then the region's own derived
* messages in surface order. The summarizer appends only the compaction
* instruction after this, so the call is a genuine prefix of the conversation
* and reuses the provider's KV cache.
* @param session - session supplying the request header and per-node projection.
* @param shadowedSeqs - the surface-node seqs, in order, being compacted.
* @returns the replayed conversation prefix to condense.
*/
function buildSummarizationInput(session, shadowedSeqs) {
	const header = session.requestHeader();
	const regionMessages = shadowedSeqs.map((seq) => session.deriveEventMessage(session.eventAt(seq))).filter((message) => message !== null);
	return {
		...header?.system === void 0 ? {} : { system: header.system },
		...header?.tools === void 0 ? {} : { tools: header.tools },
		messages: regionMessages
	};
}
/** Find a prior start marker with the same durable compaction id. */
function findMatchingCompactionStart(session, endSeq, compactionId) {
	for (let seq = Number(endSeq) - 1; seq >= 0; seq -= 1) {
		const event = session.eventAt(SessionSeq(seq));
		if (event?.type === "compaction/start" && event.data.compactionId === compactionId) return event;
	}
}
/** Inspect open-turn, unmatched-compaction, and latest seed-boundary state independently. */
function inspectCompactionEntryState(session) {
	let openTurn = null;
	let openTurnStateKnown = false;
	let unmatchedCompactionStart;
	let compactionEntryStateKnown = false;
	let latestEndSeedSeq;
	for (let seq = session.seq - 1; seq >= 0; seq -= 1) {
		const event = session.eventAt(SessionSeq(seq));
		if (latestEndSeedSeq === void 0 && event.type === "session/end-seed") latestEndSeedSeq = event.seq;
		if (!compactionEntryStateKnown) {
			if (event.type === "compaction/start") {
				unmatchedCompactionStart = event;
				compactionEntryStateKnown = true;
			} else if (event.type === "compaction/end") {
				if (event.data.compactionId === void 0) compactionEntryStateKnown = true;
				else {
					const startId = event.data.compactionId;
					if (findMatchingCompactionStart(session, event.seq, startId) !== void 0) compactionEntryStateKnown = true;
				}
			}
		}
		if (!openTurnStateKnown) {
			if (event.type === "turn/start") {
				openTurn = event.data.turn;
				openTurnStateKnown = true;
			} else if (event.type === "turn/end") openTurnStateKnown = true;
		}
		if (openTurnStateKnown && compactionEntryStateKnown && latestEndSeedSeq !== void 0) break;
	}
	return {
		openTurn,
		unmatchedCompactionStart,
		latestEndSeedSeq
	};
}
//#endregion
//#region lib/types/internal/compaction/zones.js
/**
* Positional three-zone planning for progressive context compaction.
*
* All ranges are expressed as current-surface indexes. This is deliberate:
* replacement events append fresh sequence numbers and therefore visible seqs
* are not necessarily numerically ordered. Pairing and step-boundary checks
* keep every emitted range whole.
*/
/**
* Partition one token snapshot into non-overlapping forget/tool/recent zones.
* Boundaries are measured from the newest surface tail and moved toward the
* historical side until the cut is outside any open tool pair.
*/
function partitionSurfaceZones(session, measurement, ratios) {
	const nodes = [...session.surface.nodes];
	assertMeasurementSurface(nodes, measurement);
	if (nodes.length === 0) return {
		totalTokens: 0,
		recentBoundaryTokens: 0,
		forgetBoundaryTokens: 0,
		forget: null,
		tool: null,
		recent: null
	};
	const capacity = ratios.contextWindow;
	if (!Number.isFinite(capacity) || capacity <= 0) throw new Error("compaction zones: contextWindow must be positive");
	const recentBoundaryTokens = absoluteBoundary(ratios.recentBoundaryTokens, capacity, ratios.recentRatio, "recentBoundaryTokens");
	const forgetBoundaryTokens = absoluteBoundary(ratios.forgetBoundaryTokens, capacity, ratios.forgetBoundaryRatio, "forgetBoundaryTokens");
	const recentStart = boundaryFromTail(session, measurement, recentBoundaryTokens);
	const forgetStart = Math.min(recentStart, boundaryFromTail(session, measurement, forgetBoundaryTokens));
	return {
		totalTokens: measurement.surfaceTokens,
		recentBoundaryTokens,
		forgetBoundaryTokens,
		forget: makeRange(nodes, measurement, 0, forgetStart - 1),
		tool: makeRange(nodes, measurement, forgetStart, recentStart - 1),
		recent: makeRange(nodes, measurement, recentStart, nodes.length - 1)
	};
}
/**
* Select the oldest complete forget-zone batch. The first complete unit is
* allowed to be below the target, but never above max; if it is above max no
* semantic call is made. The returned range never crosses the forget boundary.
*/
function planForgetBatch(session, measurement, zones, options) {
	const zone = zones.forget;
	if (zone === null) return {
		kind: "blocked",
		reason: "no-forget-range"
	};
	if (!toolPairingBalancedBefore(session, zone.startSeq)) return {
		kind: "blocked",
		reason: "unsafe-forget-start"
	};
	const target = Math.max(1, options.targetBatchTokens);
	const max = Math.max(target, options.maxBatchTokens);
	let candidate = null;
	let sawSafeEnd = false;
	for (let end = zone.startIndex; end <= zone.endIndex; end += 1) {
		if (!toolPairingBalancedAfter(session, session.surface.nodes[end])) continue;
		if (!stepBoundaryAfter(session, end, zone.endIndex)) continue;
		sawSafeEnd = true;
		const range = makeRange([...session.surface.nodes], measurement, zone.startIndex, end);
		if (range === null) continue;
		if (range.tokens > max) return candidate === null ? {
			kind: "blocked",
			reason: "oldest-unit-too-large"
		} : {
			kind: "selected",
			range: candidate
		};
		candidate = range;
		if (range.tokens >= target) break;
	}
	if (candidate !== null) return {
		kind: "selected",
		range: candidate
	};
	return {
		kind: "blocked",
		reason: sawSafeEnd ? "oldest-unit-too-large" : "no-safe-batch-end"
	};
}
function selectForgetBatch(session, measurement, zones, options) {
	const plan = planForgetBatch(session, measurement, zones, options);
	return plan.kind === "selected" ? plan.range : null;
}
/**
* Select the single span an above-threshold pressure pass may shadow.
*
* There is exactly one pressure planner. It is envelope-aware end to end: the
* span is sized to the request's actual pressure deficit rather than to a
* window fraction, and it is bounded by `Bcap` — the largest span whose
* auxiliary input `E + span + instruction` still leaves the configured response
* reserve, safety margin, and summary generation cap free — so a dominating
* request envelope (system prompt, tool catalog, provider anchor above the
* heuristic price) can never turn a pressure pass into an over-budget call.
*
* The planner walks the older head — every surface position before
* `zones.recent.startIndex`, regardless of how the forget/tool boundaries split
* it — and tracks two safe answers in one pass:
*
* - the FIRST pairing/step-safe prefix priced at or above the reclaim target
*   `U + minSpan + 1` (the deficit-sized answer: the smallest span that CAN end
*   pressure, and only when its replacement lands at the checkpoint floor —
*   the shrink assertion alone promises at least one token of progress), and
* - the LAST safe prefix priced at or below `Bcap` (the widest answer).
*
* When no safe prefix reaches the strict target under the cap, the widest safe
* prefix still wins whenever it prices above the checkpoint floor: head and
* deficit grow together (`head - U = threshold - E - tail`), so a head that
* cannot reach the target now never will, and progress beats a permanent stop.
* Otherwise the pass is refused with a typed reason:
*
* - `envelope-dominated`: `Bcap <= 0`, so no auxiliary call can be sent at all;
* - `no-safe-prefix`: the surface head itself is pairing-pinned (no prefix can
*   start there) or no safe end exists under the cap;
* - `span-below-minimum`: every safe prefix would be shadowed by a summary as
*   expensive as itself.
*
* The selection is a progress guarantee, not a deficit guarantee: every
* returned span prices above the checkpoint floor, so a strict-shrink
* replacement exists, but the shrink assertion lets that replacement reclaim
* anywhere between one token and `P - minSpan`, so how much pressure one pass
* releases depends on the replacement price the summarizer produces. The
* caller must re-measure after the replacement and re-plan within its own
* bounded budget while the request is still above threshold.
*
* The retained tail is never touched: every candidate ends before
* `zones.recent.startIndex`, which the partition already rounded head-ward to a
* pairing-balanced step boundary. Only call this for a pressure pass: it
* deliberately reaches across the forget boundary, which every bounded
* maintenance batch must still respect.
*
* @param session - session whose current surface supplies the older head.
* @param measurement - priced snapshot of the current surface.
* @param zones - partition of that same surface and measurement.
* @param options - pressure deficit, auxiliary input cap, and span floor.
* @returns the selected span, or a blocked plan naming the reason.
*/
function planPressureSpan(session, measurement, zones, options) {
	const cap = Math.max(0, Math.floor(options.inputCapTokens));
	const minSpan = Math.max(0, Math.floor(options.minSpanTokens));
	const reclaim = Math.max(0, Math.floor(options.reclaimTokens));
	if (cap <= 0) return {
		kind: "blocked",
		reason: "envelope-dominated"
	};
	const nodes = [...session.surface.nodes];
	assertMeasurementSurface(nodes, measurement);
	const recentEnd = (zones.recent?.startIndex ?? nodes.length) - 1;
	const headEndIndex = options.maxEndIndex !== void 0 ? Math.min(recentEnd, Math.floor(options.maxEndIndex)) : recentEnd;
	const requestedStart = Math.max(0, Math.floor(options.minStartIndex ?? 0));
	let startIndex = requestedStart;
	if (requestedStart === 0) {
		if (headEndIndex < 0 || !toolPairingBalancedBefore(session, nodes[0])) return {
			kind: "blocked",
			reason: "no-safe-prefix"
		};
	} else {
		while (startIndex <= headEndIndex && (!toolPairingBalancedBefore(session, nodes[startIndex]) || !stepBoundaryAfter(session, startIndex - 1, headEndIndex))) startIndex += 1;
		if (startIndex > headEndIndex) return {
			kind: "blocked",
			reason: "no-safe-prefix"
		};
	}
	const endTarget = reclaim + minSpan + 1;
	let deficitSized = null;
	let widest = null;
	let tokens = 0;
	for (let endIndex = startIndex; endIndex <= headEndIndex; endIndex += 1) {
		tokens += measurement.nodes[endIndex].tokens;
		if (tokens > cap) break;
		if (!toolPairingBalancedAfter(session, nodes[endIndex])) continue;
		if (!stepBoundaryAfter(session, endIndex, headEndIndex)) continue;
		const range = {
			startIndex,
			endIndex,
			startSeq: nodes[startIndex],
			endSeq: nodes[endIndex],
			tokens
		};
		widest = range;
		if (deficitSized === null && tokens >= endTarget) deficitSized = range;
	}
	const range = deficitSized ?? widest;
	if (range === null) return {
		kind: "blocked",
		reason: "no-safe-prefix"
	};
	if (range.tokens <= minSpan) return {
		kind: "blocked",
		reason: "span-below-minimum"
	};
	return {
		kind: "selected",
		range
	};
}
/** A batch boundary must not split a surface step with the same turn/step. */
function stepBoundaryAfter(session, index, _zoneEnd) {
	if (index >= session.surface.nodes.length - 1) return true;
	const current = session.eventAt(session.surface.nodes[index]);
	const next = session.eventAt(session.surface.nodes[index + 1]);
	if (current === void 0 || next === void 0) return false;
	return !sameStep(current, next);
}
function sameStep(left, right) {
	const leftData = left.data;
	const rightData = right.data;
	return leftData.turn !== void 0 && leftData.step !== void 0 && leftData.turn === rightData.turn && leftData.step === rightData.step;
}
/**
* One zone boundary in surface tokens: the caller's absolute value when it
* supplied one, otherwise the legacy window fraction. Absolute values are
* normalized to non-negative integers so a budget derived from integer meter
* prices reaches the partition unchanged.
*/
function absoluteBoundary(absolute, capacity, ratio, name) {
	if (absolute === void 0) return Math.floor(capacity * ratio);
	if (!Number.isFinite(absolute)) throw new Error(`compaction zones: ${name} must be finite`);
	return Math.max(0, Math.floor(absolute));
}
function boundaryFromTail(session, measurement, boundaryTokens) {
	const nodes = [...session.surface.nodes];
	if (boundaryTokens <= 0) return nodes.length;
	let total = 0;
	let start = nodes.length;
	for (let index = nodes.length - 1; index >= 0; index -= 1) {
		total += measurement.nodes[index].tokens;
		start = index;
		if (total >= boundaryTokens) break;
	}
	while (start > 0 && (!toolPairingBalancedBefore(session, nodes[start]) || !stepBoundaryAfter(session, start - 1, nodes.length - 1))) start -= 1;
	return start;
}
function makeRange(nodes, measurement, startIndex, endIndex) {
	if (startIndex > endIndex || startIndex < 0 || endIndex >= nodes.length) return null;
	let tokens = 0;
	for (let index = startIndex; index <= endIndex; index += 1) tokens += measurement.nodes[index].tokens;
	return {
		startIndex,
		endIndex,
		startSeq: nodes[startIndex],
		endSeq: nodes[endIndex],
		tokens
	};
}
function assertMeasurementSurface(nodes, measurement) {
	if (nodes.length !== measurement.nodes.length || nodes.some((seq, index) => measurement.nodes[index]?.seq !== seq)) throw new Error("compaction zones: token-meter surface does not match current session surface");
}
//#endregion
//#region lib/types/internal/compaction/envelope-budget.js
/**
* Envelope-aware surface budget for compaction.
*
* Triggers are priced on the whole request envelope (`measurement.totalTokens`)
* while every retention and span boundary is a price in SURFACE tokens
* (`measurement.surfaceTokens`). The two axes differ by the request envelope
*
*   E = max(0, totalTokens - surfaceTokens)
*
* — system prompt, tool schemas, and provider-anchor mispricing — so a session
* whose envelope dominates can sit above the pressure threshold while its
* surface has nothing safe left to release. This module derives the surface side
* of the same budget so a pass never spends an auxiliary call whose input
* already exceeds the window:
*
*   E     = max(0, T - S)                        request envelope
*   G     = max(0, W - E - R - M)                safe surface grant (S units)
*   Rkeep = max(Rmin, min(G, R0))                affordable retained tail
*   Bcap  = max(0, G - I - C)                    auxiliary input cap (S units)
*
* `R` is the configured response reserve, `M` the configured mispricing margin,
* `I` the priced compaction instruction, and `C` the summarization generation
* cap. A span priced at or below `Bcap` keeps the auxiliary call at
* `E + span + I <= W - R - M - C`, so the reserve and the margin always stay
* free. `Rkeep` never drops below the priced tail covering the open turn and the
* last completed turn, so the newest dialogue stays verbatim in every case.
*
* Nothing here reads or mutates the session: the budget is a pure re-pricing of
* one meter snapshot.
*/
/**
* Surface turns the retained budget must always cover verbatim: the open turn
* and the last completed one. The floor only binds when the envelope is large
* enough to shrink the affordable tail below two turns.
*/
const GUARANTEED_TAIL_TURNS = 2;
/**
* The one retained-tail formula: `max(Rmin, min(grant, R0))`. Both resolvers
* clamp the same configured budget `R0` to the same tail floor `Rmin`, differing
* only in which surface grant they are given, so every consumer sees one shape.
*/
function retainedTailWithin(grant, retain, minRetain) {
	return Math.max(Math.max(0, minRetain), Math.min(grant, Math.max(0, retain)));
}
/** Derive the surface budget of one meter snapshot. */
function resolveEnvelopeBudget(measurement, input) {
	const surfaceTokens = Math.max(0, measurement.surfaceTokens);
	const totalTokens = Math.max(0, measurement.totalTokens);
	const envelopeTokens = Math.max(0, totalTokens - surfaceTokens);
	const injectionTokens = Math.max(0, input.injectionTokens ?? 0);
	const surfaceGrantTokens = Math.max(0, input.contextWindow - envelopeTokens - input.responseReserveTokens - input.safetyMarginTokens - injectionTokens);
	const retainedTailTokens = retainedTailWithin(surfaceGrantTokens, input.retainTokens, input.minRetainTokens);
	const summarizerInputCapTokens = Math.max(0, surfaceGrantTokens - Math.max(0, input.instructionTokens) - Math.max(0, input.summaryMaxTokens));
	return {
		totalTokens,
		surfaceTokens,
		envelopeTokens,
		injectionTokens,
		surfaceGrantTokens,
		retainedTailTokens,
		summarizerInputCapTokens,
		envelopeDominated: summarizerInputCapTokens <= 0
	};
}
/**
* Price the verbatim tail that must survive any budget: the working set covering
* the open turn (if one is currently open) and the last successfully/normally
* completed turn (`turn/end` with `reason.kind === 'completed'`).
*
* Interrupted, aborted, or failed newer turns do not count as completed turns,
* so the retained tail reaches back past them to include the true last completed
* turn, preserving "open turn + last completed turn" semantics.
* If turn metadata is absent or incomplete, it conservatively retains at least
* {@link GUARANTEED_TAIL_TURNS} distinct surface turns so the protection floor
* never drops into an unsafe state.
* @param session - session supplying the current surface and event data.
* @param measurement - priced snapshot of that same surface.
* @returns the summed route price of the guaranteed tail.
*/
function retainedTailFloorTokens(session, measurement) {
	const nodes = session.surface.nodes;
	if (nodes.length === 0) return 0;
	const events = session.snapshotEvents();
	let hasTurnMetadata = false;
	let openTurn;
	let lastCompletedTurn;
	for (let i = events.length - 1; i >= 0; i -= 1) {
		const ev = events[i];
		if (ev.type === "turn/start" || ev.type === "turn/end") {
			hasTurnMetadata = true;
			break;
		}
	}
	if (hasTurnMetadata) {
		for (let i = events.length - 1; i >= 0; i -= 1) {
			const ev = events[i];
			if (ev.type === "turn/start") {
				const turn = ev.data?.turn;
				if (typeof turn === "number" && Number.isInteger(turn)) openTurn = turn;
				break;
			}
			if (ev.type === "turn/end") break;
		}
		for (let i = events.length - 1; i >= 0; i -= 1) {
			const ev = events[i];
			if (ev.type === "turn/end") {
				const data = ev.data;
				if (data?.reason?.kind === "completed" && typeof data.turn === "number" && Number.isInteger(data.turn)) {
					lastCompletedTurn = data.turn;
					break;
				}
			}
		}
	}
	const seenTurns = /* @__PURE__ */ new Set();
	let tokens = 0;
	for (let index = nodes.length - 1; index >= 0; index -= 1) {
		const turn = surfaceTurn(session, nodes[index]);
		if (turn !== void 0 && !seenTurns.has(turn)) {
			const satisfiedLastCompleted = lastCompletedTurn === void 0 || seenTurns.has(lastCompletedTurn);
			const satisfiedOpenTurn = openTurn === void 0 || seenTurns.has(openTurn);
			const satisfiedMinTurns = seenTurns.size >= GUARANTEED_TAIL_TURNS;
			if (satisfiedLastCompleted && satisfiedOpenTurn && satisfiedMinTurns) break;
			seenTurns.add(turn);
		}
		tokens += measurement.nodes[index]?.tokens ?? 0;
	}
	return tokens;
}
/**
* Derive the envelope-aware zone boundaries of one meter snapshot.
*
* `E + injectionTokens + retainedTailTokens >= pressureTokens` is exact, not heuristic: the
* partition never shadows the retained tail or the active runtime slot, so after the largest possible
* reduction the request still prices at `E + injectionTokens` plus at least the retained tail
* price. When that reaches the trigger no span can end the above-threshold
* state, which is the typed `envelope-dominated` stop — the caller must stop
* without paying for a semantic call.
* @param measurement - one priced meter snapshot.
* @param input - resolved policy budgets in tokens.
* @returns the effective boundaries, deficit, and domination verdict.
*/
function resolveEnvelopeZoneBudget(measurement, input) {
	const surfaceTokens = Math.max(0, measurement.surfaceTokens);
	const envelopeTokens = Math.max(0, Math.max(0, measurement.totalTokens) - surfaceTokens);
	const injectionTokens = Math.max(0, input.injectionTokens ?? 0);
	const waterlineGrantTokens = Math.max(0, input.forgetWatermarkTokens - envelopeTokens - injectionTokens);
	const retainedTailTokens = retainedTailWithin(waterlineGrantTokens, input.retainTokens, input.minRetainTokens);
	return {
		envelopeTokens,
		injectionTokens,
		waterlineGrantTokens,
		forgetBoundaryTokens: Math.min(Math.max(0, input.forgetBoundaryTokens), waterlineGrantTokens),
		retainedTailTokens,
		deficitTokens: Math.max(0, surfaceTokens - waterlineGrantTokens),
		envelopeDominated: envelopeTokens + injectionTokens + retainedTailTokens >= Math.max(0, input.pressureTokens)
	};
}
function surfaceTurn(session, seq) {
	const turn = (session.eventAt(seq)?.data)?.turn;
	return typeof turn === "number" && Number.isInteger(turn) ? turn : void 0;
}
//#endregion
//#region lib/types/compaction-basic.js
/**
* `dsh-context-enhancement` — `./compaction-basic` subpath entry.
*
* A replacement for the official `@deepseek-ai/dsh-compaction-basic` backend
* provider, delivered from this standalone root package. It does NOT
* re-declare `ctx.compaction`: the default-exported class extends the official
* rc1 {@link CompactionEngine} Service Definition consumed from the published
* `@deepseek-ai/dsh-compaction` package, so the Loader recognizes it as the
* `compaction` service provider. The official rc1 behavior and Config
* vocabulary are preserved exactly; the Card5/6 region-aware migration delta
* is folded in (see the internal module provenance headers). The optional
* `ctx.toolResultPruner` pruning companion is this package's own
* `./tool-result-pruner` service — compaction-basic and the pruner are mounted
* in the same preset isolate realm and must not have two providers.
*
* Only the published rc1 exports of the official packages are imported; the
* rc1 `src/*` subpaths are not a stable third-party API. The internal
* implementation files under `src/internal/compaction/` are local MIT copies
* (with provenance headers) of the official rc1 backend sources.
*
* @module dsh-context-enhancement/compaction-basic
*/
/**
* Envelope-budget stop reasons that must not repeat a paid semantic call while
* nothing about the request changes: each stop verdict is only as fresh as the
* surface and pressure state it was derived from. `low-yield` is deliberately
* NOT terminal: it is a verdict about one batch's realised release, and a later
* step with different content must be allowed to try again. `batch-limit` is not
* terminal either: the batch budget of ONE invocation is not a property of the
* surface, so the next above-threshold step must be free to re-plan.
*/
const TERMINAL_PRESSURE_REASONS = /* @__PURE__ */ new Set(["envelope-dominated", "no-progress"]);
/**
* Fallback ledger store for seam-based harnesses that borrow individual
* prototype methods on a bare object literal: the per-instance field below is
* absent there, and a diagnostic write must never turn a pressure pass into a
* runtime error. A real engine always has its own store, so two engines can
* never share one ledger.
*/
const fallbackPressureLedgers = /* @__PURE__ */ new WeakMap();
/** The engine's own ledger store, or the module fallback when it has none. */
function pressureLedgerStore(store) {
	return store ?? fallbackPressureLedgers;
}
const auditEngineStates = /* @__PURE__ */ new WeakMap();
/** The audit bookkeeping of one engine, created on first use. */
function auditEngineState(engine) {
	const existing = auditEngineStates.get(engine);
	if (existing !== void 0) return existing;
	const created = {
		owner: void 0,
		recovered: /* @__PURE__ */ new WeakSet(),
		unwritten: /* @__PURE__ */ new Map(),
		logged: /* @__PURE__ */ new Set()
	};
	auditEngineStates.set(engine, created);
	return created;
}
/** Report one audit diagnostic through the engine's logging seam. */
function logAuditDiagnostic(engine, message) {
	engine.ctx?.logger?.warn(message);
}
/** Report one audit diagnostic once per key, so repeated passes stay quiet. */
function logAuditDiagnosticOnce(engine, key, message) {
	const state = auditEngineState(engine);
	if (state.logged.has(key)) return;
	state.logged.add(key);
	logAuditDiagnostic(engine, message);
}
/** This engine instance's audit attempt identity, created on first use. */
function auditOwnerId(engine) {
	const state = auditEngineState(engine);
	return state.owner ??= randomUUID();
}
/**
* Judge every `open`/`aborted` audit row of one session against the Session
* log's own landing evidence and persist the verdict (B6.2).
*
* The audit is a diagnostic and a work schedule; the Session log is the type
* authority. Recovery therefore REPAIRS the schedule on evidence and never
* promotes a type: `repaired` is written only when the validated provenance of
* the current surface already proves a `tool-summary` of this exact group
* identity landed, and an `open` row nobody owns becomes `aborted` —
* repairable, and never read as committed. A rejected write (for instance
* `missing-key`, the durable symptom of a whole-document last-write-wins
* overwrite removing the row) is reported with its diagnostic and changes
* nothing about classification.
* @param engine - engine whose audit store and identity are used.
* @param session - session whose audit rows are judged.
*/
async function recoverToolGroupAudits(engine, session) {
	const store = engine.toolGroupAuditStore;
	if (store === void 0) return;
	const done = auditEngineState(engine).recovered;
	if (done.has(session)) return;
	const steps = planAuditRecovery(store.recordsForSession(session.id, session.header.createdAt), buildSurfaceSourceIndex(session), auditOwnerId(engine));
	for (const step of steps) try {
		await store.finish(step.requestId, step.apply);
		logAuditDiagnostic(engine, `tool-group audit ${step.status}: ${step.diagnostic} (${step.requestId})`);
	} catch (error) {
		const missing = error.code === "missing-key";
		logAuditDiagnostic(engine, `tool-group audit recovery write failed (${step.diagnostic})${missing ? `; the row is gone (${AUDIT_DIAGNOSTIC.auditRecordMissing})` : ""}: ${error instanceof Error ? error.message : String(error)}`);
	}
	done.add(session);
}
/**
* Whether one durable tool group still owes semantic summarization work, as the
* scheduler must judge it.
*
* The Session log decides first: a group whose reduction its own validated
* provenance proves landed owes nothing, whether the audit row is present,
* stale, still `open` after a failed commit, or gone with the whole document
* (R-P2-3). Only then do the rows decide, under the durable attempt budget —
* and an attempt started while the audit could not record it is counted in
* memory, so a store that keeps failing cannot pay for a model call on every
* pass.
* @param engine - engine carrying the degraded-attempt ledger.
* @param index - classifier of the CURRENT surface, the evidence source.
* @param records - audit rows of the session's lifecycle.
* @param fingerprint - durable tool-group identity.
* @returns whether an attempt is still owed.
*/
function mayAttemptToolGroupSummary(engine, index, records, fingerprint) {
	const landed = sessionLandedReductions(index, fingerprint);
	if (landed.length > 0) return false;
	if ((auditEngineState(engine).unwritten.get(fingerprint) ?? 0) >= 2) {
		logAuditDiagnosticOnce(engine, `unwritten:${fingerprint}`, `tool-group summary attempts exhausted while the audit could not record them (${AUDIT_DIAGNOSTIC.attemptsExhausted})`);
		return false;
	}
	if (!hasToolGroupAttemptBudget(records, fingerprint)) {
		logAuditDiagnosticOnce(engine, `budget:${fingerprint}`, `tool-group summary attempts exhausted (${AUDIT_DIAGNOSTIC.attemptsExhausted})`);
		return false;
	}
	return shouldAttemptToolGroupSummary(records, fingerprint, { landed });
}
/**
* Report every Session-proven `tool-summary` whose group the audit has no row
* for.
*
* This is the observable symptom of an audit document that was deleted,
* truncated, or replaced by another writer's whole-document write
* (`layout: 'single'`, last write wins — the E10/B1 gap this package cannot fix
* without an upstream revision/compare-and-swap API). It is reported, never
* acted on: classification comes from the Session log, so an audit row that is
* missing changes no kind and no schedule — but an operator can see that the
* diagnostic document no longer agrees with the log, instead of the loss being
* silent.
* @param engine - engine whose logging seam receives the report.
* @param index - classifier of the current surface.
* @param records - audit rows of this session's lifecycle.
*/
function reportUnrecordedAuditLandings(engine, index, records) {
	const missing = /* @__PURE__ */ new Set();
	for (const [, entry] of index.entries) {
		const reduction = entry.reduction;
		if (reduction === null || reduction.kind !== "tool-summary") continue;
		const groupId = reduction.groupId;
		if (groupId === null || groupId === void 0) continue;
		if (records.some((record) => record.fingerprint === groupId)) continue;
		missing.add(groupId);
	}
	for (const groupId of missing) logAuditDiagnosticOnce(engine, `unrecorded:${groupId}`, `tool-group audit has no row for a reduction the Session log proves landed (${AUDIT_DIAGNOSTIC.auditLandingUnrecorded})`);
}
/**
* Attribute token price of the active model-visible task-state slot node.
* Resolves from the session surface and token measurement nodes.
*/
function visibleSlotTokens(session, measurement) {
	for (const seq of session.surface.nodes) {
		const event = session.eventAt(seq);
		if (event?.type === "user/message" && isTaskStateSlotSource(event.data?.source)) return measurement.nodes.find((n) => n.seq === seq)?.tokens ?? event.data.source.injectionTokens ?? 0;
	}
	return 0;
}
/**
* Report one above-threshold pressure stop to the engine's own logging seam when
* it has one, and to the warning line otherwise.
*
* The seam is dispatched THROUGH the receiver rather than through the class
* prototype, so a harness that overlays `logPressureStop` on a bare object
* literal observes every reason the pressure pass reports — the seam harnesses in
* this package's specs borrow individual prototype methods and would otherwise
* bypass their own recorder. A harness that borrows the stop funnel without
* supplying the seam still degrades to a warning line instead of a runtime error,
* and the engine's private method stays private: the receiver is typed
* structurally, so no `this as {…}` cast and no `any` is involved.
*/
function reportStoppedReason(engine, logger, reason, totalTokens, thresholdTokens) {
	if (totalTokens < thresholdTokens) return;
	const sink = engine;
	if (typeof sink.logPressureStop === "function") {
		sink.logPressureStop(reason, totalTokens, thresholdTokens);
		return;
	}
	logger.warn(`three-zone pressure stopped: ${reason} (${totalTokens} >= ${thresholdTokens})`);
}
/**
* Why a candidate span containing an already-condensed checkpoint may or may
* not be folded: a verdict about ONE plan over the CURRENT surface, never a
* session-wide suppression.
*
* `'new-coverage'` means the span reaches surface content the last pressure
* replacement does not stand for — its shadowed seqs are still addressable as
* positions on the current surface at or beyond the checkpoint's own position.
* `'no-new-coverage'` means the checkpoint already covers everything the span
* would shadow, so another summary over it would release nothing and only pay
* again; a later surface that grows new content past the checkpoint flips the
* same verdict without any stored ban.
*
* The verdict reads no instance state, so it is a module function: the
* compaction specs run the real planner through seam objects that borrow
* individual prototype methods, and a method call there would leave
* `this.pressureReentryVerdict` undefined and turn a plan verdict into a
* runtime error.
* @param session - session supplying the current surface order.
* @param sources - source classification of that same surface.
* @param blockerSeq - the checkpoint inside the candidate span.
* @returns the plan's reentry verdict.
*/
function pressureReentryVerdict(session, sources, blockerSeq) {
	const blockerEntry = sources.entry(blockerSeq);
	if (blockerEntry.kind === "original") return "new-original-content";
	if (blockerEntry.kind === "unknown-replacement") return "unknown-provenance";
	if (blockerEntry.completedTurnsAfter > 0) return "completed-turns-deferred";
	if (blockerEntry.kind !== "history-summary") return "new-original-content";
	return session.surface.nodes.some((seq) => seq > blockerSeq) ? "new-coverage" : "no-new-coverage";
}
/** Resolve the exact provider/model durably routed for the latest request. */
function routedTarget(session) {
	const config = session.requestHeader()?.config;
	if (config === void 0 || config.provider.length === 0 || config.model.length === 0) return;
	return {
		provider: config.provider,
		model: config.model
	};
}
/** Resolve the conversation target used to select an optional policy override. */
function conversationTarget(agent) {
	const routed = routedTarget(agent.session);
	if (routed !== void 0) return routed;
	if (agent.options.provider === void 0 || agent.options.provider.length === 0 || agent.options.model === void 0 || agent.options.model.length === 0) return void 0;
	return {
		provider: agent.options.provider,
		model: agent.options.model
	};
}
const thresholdRatioSchema = Schema.number();
const retainRatioSchema = Schema.number();
const retainTokensSchema = Schema.number().step(1).min(0);
const summarizationProviderSchema = Schema.string();
const summarizationModelSchema = Schema.string();
const maxTokensSchema = Schema.number().step(1).min(1);
const compactionRetriesSchema = Schema.number().step(1).min(0);
const maxOverflowRetriesSchema = Schema.number().step(1).min(0);
const ratioSchema = Schema.number();
const positiveIntegerSchema = Schema.number().step(1).min(1);
const policyFields = {
	thresholdRatio: thresholdRatioSchema,
	retainRatio: retainRatioSchema,
	retainTokens: retainTokensSchema,
	recentRatio: ratioSchema,
	forgetBoundaryRatio: ratioSchema,
	toolMaintenanceRatio: ratioSchema,
	forgetMaintenanceRatio: ratioSchema,
	pressureRatio: ratioSchema,
	pressureExitRatio: ratioSchema,
	minNetReleaseTokens: Schema.number().step(1).min(0),
	minNetReleaseRatio: Schema.number(),
	targetBatchTokens: positiveIntegerSchema,
	maxBatchTokens: positiveIntegerSchema,
	maxMaintenanceBatches: Schema.number().step(1).min(0),
	maxPressureBatches: Schema.number().step(1).min(0),
	minReentryTurns: positiveIntegerSchema,
	summarizationProvider: summarizationProviderSchema,
	summarizationModel: summarizationModelSchema,
	maxTokens: maxTokensSchema,
	compactionRetries: compactionRetriesSchema,
	maxOverflowRetries: maxOverflowRetriesSchema,
	responseReserveTokens: Schema.number().step(1).min(0),
	safetyMarginTokens: Schema.number().step(1).min(0)
};
const modelPolicy = Schema.object({
	provider: Schema.string().required(),
	model: Schema.string().required(),
	...policyFields
});
/**
* Dependency-light compaction backend using `ctx.tokenMeter` for pressure,
* retention, cited source events, and summary-convergence pricing. Extends the
* official rc1 {@link CompactionEngine}, preserving official behavior and
* Config while adding the Card5/6 region-aware passes: retention-aware older
* pruning, the recursive empty-benefit guard on the old-summary head, and the
* overflow opt-out that re-compacts an isolated checkpoint as the last
* deterministic reduction.
*
* `summarize()` is the sole subclass customization hook; the replay and durable
* mutation strategy stays fixed so every pricing decision uses the singleton
* token meter.
*/
var BasicCompactionEngine = class BasicCompactionEngine extends CompactionEngine {
	static inject = [
		"llm",
		"tokenMeter",
		"sessions",
		"storageDomain"
	];
	static Config = Schema.object({
		...policyFields,
		modelPolicies: Schema.array(modelPolicy),
		auto: Schema.boolean(),
		toolGroupSummarizer: Schema.object({
			enabled: Schema.boolean(),
			minGroupResults: Schema.number().step(1).min(1),
			minGroupChars: Schema.number().step(1).min(1),
			minGroupTokens: Schema.number().step(1).min(1),
			maxGroupTokens: Schema.number().step(1).min(1),
			maxGroupsPerPass: Schema.number().step(1).min(1),
			maxSummaryTokens: Schema.number().step(1).min(1)
		})
	});
	/** Resolved and validated compaction configuration. */
	config;
	warnedPressureConfigTargets = /* @__PURE__ */ new Set();
	overflowRetries = /* @__PURE__ */ new WeakMap();
	overflowAgents = /* @__PURE__ */ new WeakMap();
	/** Envelope-budget stop reasons already terminal for one surface generation. */
	pressureStops = /* @__PURE__ */ new WeakMap();
	/** The structured ledger of the latest pressure invocation per session. */
	pressureLedgers = /* @__PURE__ */ new WeakMap();
	/**
	* The verdict list of the pressure pass THIS engine is currently running, so
	* the one stop funnel records each verdict where it is reported instead of the
	* loop recording it again by hand. `undefined` between passes: a stop reached
	* outside a pass must not write into a finished ledger.
	*/
	activePressureStopReasons;
	toolGroupAuditStore;
	toolGroupAuditStorePromise;
	constructor(ctx, config = {}) {
		super(ctx);
		this.config = resolveConfig(config);
		this.toolGroupAuditStorePromise = openToolGroupAuditStore(ctx).then((store) => {
			this.toolGroupAuditStore = store;
		}).catch((error) => {
			this.toolGroupAuditStorePromise = void 0;
			ctx.logger.warn(`tool-group summarization audit disabled: ${error instanceof Error ? error.message : String(error)}`);
		});
		ctx.effect(() => async () => {
			await this.toolGroupAuditStorePromise;
			await this.toolGroupAuditStore?.close();
			this.toolGroupAuditStore = void 0;
		}, "tool-group-summary.audit-store");
		if (this.config.auto) this._registerAutomaticCompaction();
	}
	/**
	* Register automatic between-step pressure and model-request overflow
	* recovery. `compactIfNeeded` stays dynamically dispatched so subclass
	* overrides are honored at event time.
	*/
	_registerAutomaticCompaction() {
		const { ctx } = this;
		const logResult = (result, trigger) => {
			ctx.logger.info(`compaction (${trigger}): shadowed ${result.shadowedSeqs.length} surface nodes (seqs ${result.shadowedRange.start}-${result.shadowedRange.end}, ~${result.shadowedTokenCount} tokens)`);
		};
		ctx.on("agent/pre-step", async ({ agent, signal }, next) => {
			if (!signal.aborted) try {
				const result = await this.compactIfNeeded(agent, "pressure", signal);
				if (result !== null) logResult(result, "step pressure");
			} catch (error) {
				if (error instanceof TargetPressureConfigError) {
					if (this.warnedPressureConfigTargets.has(error.targetKey)) return next();
					this.warnedPressureConfigTargets.add(error.targetKey);
				}
				const message = error instanceof Error ? error.message : String(error);
				ctx.logger.warn(`step compaction failed: ${message}; continuing the turn`);
			}
			return next();
		});
		ctx.on("agent/status", ({ agent, status }) => {
			if (status === "idle") this.overflowRetries.delete(agent);
		});
		ctx.on("session/event", (session, event) => {
			if (event.type !== "assistant/message") return;
			const agent = this.overflowAgents.get(session);
			if (agent !== void 0) this.overflowRetries.delete(agent);
		});
		ctx.on("agent/request-error", async ({ agent, failure, signal }, next) => {
			if (failure.code !== "CONTEXT_WINDOW_EXCEEDED" || signal.aborted) return next();
			this.overflowAgents.set(agent.session, agent);
			const target = routedTarget(agent.session);
			if (target === void 0) return next();
			const policy = resolveTargetPolicy(this.config, target);
			const retries = this.overflowRetries.get(agent) ?? 0;
			if (retries >= policy.maxOverflowRetries) return next();
			const generation = agent.session.surface.replaceGeneration;
			let result;
			try {
				result = await this.compactIfNeeded(agent, "context-overflow", signal);
			} catch (recoveryError) {
				const message = recoveryError instanceof Error ? recoveryError.message : String(recoveryError);
				if (!signal.aborted && agent.session.surface.replaceGeneration > generation) {
					ctx.logger.warn(`context-overflow compaction failed after durable surface progress: ${message}; retrying from the replacement surface`);
					this.overflowRetries.set(agent, retries + 1);
					return { kind: "retry" };
				}
				ctx.logger.warn(`context-overflow compaction failed: ${message}; ${signal.aborted ? "cancellation prevents retry" : "preserving the original request error"}`);
				return next();
			}
			if (signal.aborted || agent.session.surface.replaceGeneration <= generation) return next();
			if (result !== null) logResult(result, "context overflow recovery");
			this.overflowRetries.set(agent, retries + 1);
			return { kind: "retry" };
		});
	}
	/**
	* Summarize the replayed conversation region through a direct one-shot
	* `ctx.llm.stream()` call whose prefix reuses the conversation's own system
	* prompt, tools, and messages so the provider's KV cache is not invalidated.
	* Override this sole hook for a template or remote summarizer.
	* @param input - replayed conversation prefix (system, tools, and leading messages) to condense.
	* @param agent - supplies routed-model history, fallback model, and session id.
	* @param signal - optional cancellation forwarded to the adapter.
	* @returns safe text summary blocks and the exact auxiliary call envelope and output.
	*/
	async summarize(input, agent, signal) {
		const target = conversationTarget(agent);
		const config = target === void 0 ? this.config : resolveTargetPolicy(this.config, target);
		return summarizeWithLlm(this.ctx, config, input, agent, signal);
	}
	/**
	* Compact for replayed step-boundary pressure or one provider-confirmed context
	* overflow. Both triggers price the latest durable routed request envelope;
	* overflow bypasses the normal threshold and retained-tail policy so it can
	* force one useful balanced reduction. Pressure first prunes only the older
	* head outside the retained tail (region-aware), remeasures, and stops early
	* when the deterministic reduction cleared pressure.
	*
	* Once tool-stage debt that has aged into the forget zone is cleared, normal
	* (non-overflow) pressure runs a BOUNDED LOOP of semantic batches. The exit
	* target is a fixed release line — `floor(contextWindow * pressureExitRatio)`,
	* both trigger-independent and strictly below the trigger — not "just below the
	* threshold". Each iteration re-measures, re-partitions, re-derives both
	* envelope budgets, and re-plans from the fresh surface; the loop ends when the
	* request is AT or BELOW the exit target (`pressure-exit`), at
	* `maxPressureBatches` while still above it (`batch-limit`), at a typed veto, or
	* after a batch whose realised release missed BOTH net-release floors
	* (`low-yield`, which ends the paid pass immediately).
	*
	* The loop's termination test is the exit line and NOT the trigger. The exit
	* line sits strictly below the trigger by configuration, so a batch that only
	* dunks under 80% leaves the request above `pressureExitTokens`; ending the pass
	* there is precisely the E03 finding (6/6 passes exiting at 78%-79% with less
	* than one typical tool result of headroom, which made maintenance re-fire on
	* every call) and it silently reduced this loop to a single batch.
	*
	* The pass is NOT a whole-zone selection. `planPressureSpan` always starts at
	* the surface head, always ends strictly before `zones.recent.startIndex`, and
	* is always truncated at `Bcap`; a larger exit target narrows the answer to the
	* deficit-sized prefix and at worst falls back to the widest safe prefix under
	* the cap. E02 (`E02-压力回落`) dynamically falsified the whole-zone reading,
	* and E03 (`E03-压力抖动` O5) recorded `selectedStartIndex = 0` with a
	* deficit-sized span — both are the behaviour this comment describes.
	*
	* The retained ~20% recent tail is untouched and the tool zone keeps its
	* governance result. The loop never folds a replacement its OWN invocation
	* produced (round-local set), and it re-enters an earlier invocation's
	* checkpoint only when that checkpoint has genuine newer coverage; the verdict
	* is about one plan, never a session-wide ban on historical summaries. Tool
	* pairing, step boundaries, surface stability, and the shrink requirement live
	* inside `compactRegion`.
	*
	* The envelope-derived budget replaces the window-fraction limits: the
	* retained tail becomes the affordable surface grant (never below the
	* open/last-completed-turn floor), and the span becomes the largest safe prefix
	* whose auxiliary input still leaves the configured response reserve and safety
	* margin free (`planPressureSpan`). A selected span carries a safe boundary and
	* progress potential, not a deficit guarantee: the shrink assertion only bounds
	* a legal replacement between the checkpoint floor and one token under the
	* span, so one batch can reclaim as little as a single token. That is why the
	* loop re-measures after every replacement and why a pass that cannot lower
	* pressure at all is stopped with the typed, memoized `no-progress` verdict
	* instead of paying again for the same state.
	* @param agent - agent whose latest durable routed request is measured.
	* @param trigger - normal step-boundary pressure or context-overflow recovery.
	* @param signal - live turn cancellation signal forwarded to summarization.
	* @returns the latest summary compaction result, or `null` when no summary ran.
	*/
	async compactIfNeeded(agent, trigger, signal) {
		const target = routedTarget(agent.session);
		if (target === void 0) return null;
		const policy = resolveTargetPolicy(this.config, target);
		const meter = this.ctx.tokenMeter;
		let measurement = meter.measure(agent.session);
		if (trigger !== "pressure" && trigger !== "context-overflow") assertNever(trigger, "compaction trigger");
		const prune = this.ctx.get("toolResultPruner");
		await recoverToolGroupAudits(this, agent.session);
		if (trigger === "context-overflow") return this.recoverOverflow(agent, prune, signal);
		const context = (await this.ctx.llm.resolveModelInfo(target.provider, target.model, signal)).context;
		assertNoActiveCompaction(agent.session, "automatic three-zone compaction");
		const targetKey = `${target.provider}/${target.model}`;
		if (context === void 0) throw new TargetPressureConfigError(targetKey, `compaction-basic: no context capacity for ${targetKey}; configure contextWindow on that adapter model`);
		const spec = resolveCompactSpec(policy, context.contextWindow);
		const toolWatermark = Math.floor(spec.contextWindow * policy.toolMaintenanceRatio);
		const forgetWatermark = Math.floor(spec.contextWindow * policy.forgetMaintenanceRatio);
		if (measurement.totalTokens < toolWatermark) return null;
		const roundReplacements = /* @__PURE__ */ new Set();
		let zones = this.zones(agent.session, measurement, spec);
		await this.summarizeToolGroups(agent, target, policy, zones.tool, signal, roundReplacements, this.envelopeBudget(agent.session, measurement, spec).summarizerInputCapTokens);
		measurement = meter.measure(agent.session);
		zones = this.zones(agent.session, measurement, spec);
		if (prune !== void 0 && zones.tool !== null) {
			const index = this.sourceIndex(agent.session);
			const candidates = agent.session.surface.nodes.slice(zones.tool.startIndex).filter((seq) => index.isOriginalToolResult(seq));
			prune.pruneSession(agent.session, {
				olderRange: {
					start: zones.tool.startSeq,
					end: zones.tool.endSeq
				},
				candidateSeqs: candidates,
				onReplacement: (entry) => roundReplacements.add(entry.replacementSeq)
			}).pruned.forEach((entry) => roundReplacements.add(entry.replacementSeq));
			measurement = meter.measure(agent.session);
		}
		if (measurement.totalTokens < forgetWatermark) return null;
		zones = this.zones(agent.session, measurement, spec);
		if (zones.forget !== null) {
			await this.summarizeToolGroups(agent, target, policy, zones.forget, signal, roundReplacements, this.envelopeBudget(agent.session, measurement, spec).summarizerInputCapTokens);
			measurement = meter.measure(agent.session);
			zones = this.zones(agent.session, measurement, spec);
			if (prune !== void 0 && zones.forget !== null) {
				const index = this.sourceIndex(agent.session);
				const candidates = agent.session.surface.nodes.slice(zones.forget.startIndex, zones.forget.endIndex + 1).filter((seq) => index.isOriginalToolResult(seq));
				prune.pruneSession(agent.session, {
					olderRange: {
						start: zones.forget.startSeq,
						end: zones.forget.endSeq
					},
					candidateSeqs: candidates,
					onReplacement: (entry) => roundReplacements.add(entry.replacementSeq)
				}).pruned.forEach((entry) => roundReplacements.add(entry.replacementSeq));
				measurement = meter.measure(agent.session);
			}
		}
		if (measurement.totalTokens < forgetWatermark) return null;
		if (!(measurement.totalTokens >= spec.thresholdTokens)) {
			let result = null;
			let batch = 0;
			while (batch < policy.maxMaintenanceBatches) {
				measurement = meter.measure(agent.session);
				if (measurement.totalTokens < forgetWatermark) break;
				const currentZones = this.zones(agent.session, measurement, spec);
				const budget = this.envelopeBudget(agent.session, measurement, spec);
				const plan = planForgetBatch(agent.session, measurement, currentZones, {
					targetBatchTokens: Math.min(policy.targetBatchTokens, budget.summarizerInputCapTokens),
					maxBatchTokens: Math.min(policy.maxBatchTokens, budget.summarizerInputCapTokens)
				});
				if (plan.kind === "blocked") break;
				const selected = plan.range;
				const selectedSeqs = agent.session.surface.nodes.slice(selected.startIndex, selected.endIndex + 1);
				const sources = this.sourceIndex(agent.session);
				if (selectedSeqs.some((seq) => roundReplacements.has(seq))) break;
				if (selectedSeqs.some((seq) => !sources.canCompactHistory(seq, policy.minReentryTurns))) break;
				const debt = this.hasPendingToolIntermediateWork(agent.session, selected.startSeq, selected.endSeq, prune, budget.summarizerInputCapTokens);
				if (debt === "actionable") break;
				if (debt === "inert") this.ctx.logger.warn(`three-zone maintenance proceeding past inert tool-stage debt before batch ${batch + 1}`);
				const beforeTokens = measurement.totalTokens;
				result = await this.compactRegion(selected.startSeq, selected.endSeq, agent, signal);
				batch += 1;
				measurement = meter.measure(agent.session);
				if (measurement.totalTokens >= beforeTokens) break;
				if (measurement.totalTokens < spec.thresholdTokens) break;
			}
			return result;
		}
		if (this.pressurePassTerminated(agent.session, measurement.totalTokens)) return null;
		const exitTokens = Math.min(spec.thresholdTokens, spec.pressureExitTokens);
		const minSpanTokens = minimumCheckpointTokens(this.ctx.tokenMeter);
		const injectionTokens = visibleSlotTokens(agent.session, measurement);
		const usageBaseline = measurement.baseline.kind === "usage";
		const stopReasons = [];
		const ledger = {
			trigger: "pressure",
			generation: agent.session.surface.replaceGeneration,
			beforeTokens: measurement.totalTokens,
			afterTokens: measurement.totalTokens,
			thresholdTokens: spec.thresholdTokens,
			exitTokens,
			batches: [],
			batchesRun: 0,
			netReleaseTokens: 0,
			exitReached: false,
			terminalReason: null,
			stopReasons,
			injectionTokens,
			usageBaseline
		};
		pressureLedgerStore(this.pressureLedgers).set(agent.session, ledger);
		this.activePressureStopReasons = stopReasons;
		const recordVerdict = (reason) => {
			if (!stopReasons.includes(reason)) stopReasons.push(reason);
		};
		const finishLedger = (reason, exitReached) => {
			ledger.afterTokens = measurement.totalTokens;
			ledger.exitReached = exitReached;
			ledger.terminalReason = reason;
			if (reason !== null) recordVerdict(reason);
			this.activePressureStopReasons = void 0;
		};
		let result = null;
		let batch = 0;
		let maxEndIndex = void 0;
		let activeVeto = null;
		while (batch < policy.maxPressureBatches) {
			measurement = meter.measure(agent.session);
			if (measurement.totalTokens <= exitTokens) {
				this.stopEnvelopeBudgetPass(agent.session, "pressure-exit", measurement.totalTokens, spec.thresholdTokens);
				finishLedger("pressure-exit", true);
				return result;
			}
			if (this.pressurePassTerminated(agent.session, measurement.totalTokens)) {
				finishLedger(ledger.terminalReason ?? "no-progress", false);
				return result;
			}
			zones = this.zones(agent.session, measurement, spec);
			const budget = this.envelopeBudget(agent.session, measurement, spec);
			if (this.envelopeZoneBudget(agent.session, measurement, spec).envelopeDominated) {
				this.stopEnvelopeBudgetPass(agent.session, "envelope-dominated", measurement.totalTokens, spec.thresholdTokens);
				finishLedger("envelope-dominated", false);
				return result;
			}
			if (budget.summarizerInputCapTokens <= 0) {
				this.stopEnvelopeBudgetPass(agent.session, "envelope-dominated", measurement.totalTokens, spec.thresholdTokens);
				finishLedger("envelope-dominated", false);
				return result;
			}
			let minStartIndex = 0;
			if (batch > 0) while (minStartIndex < agent.session.surface.nodes.length && roundReplacements.has(agent.session.surface.nodes[minStartIndex])) minStartIndex += 1;
			const plan = planPressureSpan(agent.session, measurement, zones, {
				reclaimTokens: Math.max(0, measurement.totalTokens - exitTokens),
				inputCapTokens: budget.summarizerInputCapTokens,
				minSpanTokens,
				maxEndIndex,
				minStartIndex
			});
			if (plan.kind === "blocked") {
				const reason = activeVeto ?? plan.reason;
				this.stopEnvelopeBudgetPass(agent.session, reason, measurement.totalTokens, spec.thresholdTokens);
				finishLedger(reason, false);
				return result;
			}
			const sources = this.sourceIndex(agent.session);
			const candidateSeqs = agent.session.surface.nodes.slice(plan.range.startIndex, plan.range.endIndex + 1);
			const samePassIdx = candidateSeqs.findIndex((seq) => roundReplacements.has(seq));
			if (samePassIdx >= 0) {
				activeVeto = "same-pass-tool-replacement";
				const blockerSurfaceIndex = plan.range.startIndex + samePassIdx;
				if (blockerSurfaceIndex <= plan.range.startIndex) {
					this.stopEnvelopeBudgetPass(agent.session, activeVeto, measurement.totalTokens, spec.thresholdTokens);
					finishLedger(activeVeto, false);
					return result;
				}
				maxEndIndex = blockerSurfaceIndex - 1;
				continue;
			}
			const reentryIdx = candidateSeqs.findIndex((seq) => !sources.canCompactHistory(seq, policy.minReentryTurns, true));
			if (reentryIdx >= 0) {
				const blockingIndex = plan.range.startIndex + reentryIdx;
				activeVeto = "reentry-deferred";
				if (blockingIndex <= plan.range.startIndex) {
					this.stopEnvelopeBudgetPass(agent.session, activeVeto, measurement.totalTokens, spec.thresholdTokens);
					finishLedger(activeVeto, false);
					return result;
				}
				maxEndIndex = blockingIndex - 1;
				continue;
			}
			const historySummaryIdx = candidateSeqs.findIndex((seq) => {
				try {
					return sources.entry(seq).kind === "history-summary";
				} catch {
					return false;
				}
			});
			if (historySummaryIdx >= 0) {
				const blockerSeq = candidateSeqs[historySummaryIdx];
				const blockingIndex = plan.range.startIndex + historySummaryIdx;
				const reentry = pressureReentryVerdict(agent.session, sources, blockerSeq);
				if (!(reentry === "new-original-content" || reentry === "new-coverage")) {
					activeVeto = "reentry-deferred";
					this.ctx.logger.warn(`three-zone pressure reentry deferred: ${reentry}`);
					if (blockingIndex <= plan.range.startIndex) {
						this.stopEnvelopeBudgetPass(agent.session, activeVeto, measurement.totalTokens, spec.thresholdTokens);
						finishLedger(activeVeto, false);
						return result;
					}
					maxEndIndex = blockingIndex - 1;
					continue;
				}
			}
			const debt = this.hasPendingToolIntermediateWork(agent.session, plan.range.startSeq, plan.range.endSeq, prune, budget.summarizerInputCapTokens);
			if (debt === "actionable") {
				activeVeto = "tool-stage-deferred";
				const blockerSurfaceIndex = BasicCompactionEngine.prototype.internalFindFirstToolStageDebtIndex.call(this, agent.session, plan.range.startSeq, plan.range.endSeq, prune, budget.summarizerInputCapTokens, sources) ?? plan.range.endIndex;
				if (blockerSurfaceIndex <= plan.range.startIndex) {
					this.stopEnvelopeBudgetPass(agent.session, activeVeto, measurement.totalTokens, spec.thresholdTokens);
					finishLedger(activeVeto, false);
					return result;
				}
				maxEndIndex = blockerSurfaceIndex - 1;
				continue;
			}
			if (debt === "inert") this.ctx.logger.warn(`three-zone pressure proceeding past inert tool-stage debt (${measurement.totalTokens} >= ${spec.thresholdTokens})`);
			activeVeto = null;
			const span = plan.range;
			const beforeTokens = measurement.totalTokens;
			result = await this.compactRegion(span.startSeq, span.endSeq, agent, signal);
			if (result !== null) {
				roundReplacements.add(result.summarySeq);
				roundReplacements.add(result.startSeq);
			}
			batch += 1;
			ledger.batchesRun = batch;
			maxEndIndex = void 0;
			measurement = meter.measure(agent.session);
			const netReleaseTokens = beforeTokens - measurement.totalTokens;
			const netReleaseRatio = span.tokens === 0 ? 0 : netReleaseTokens / span.tokens;
			const batchReasons = [];
			if (measurement.totalTokens >= beforeTokens) {
				this.stopEnvelopeBudgetPass(agent.session, "no-progress", measurement.totalTokens, spec.thresholdTokens);
				batchReasons.push("no-progress");
			} else if (netReleaseTokens < policy.minNetReleaseTokens && netReleaseRatio < policy.minNetReleaseRatio) batchReasons.push("low-yield");
			ledger.batches.push({
				batch,
				spanStartIndex: span.startIndex,
				spanEndIndex: span.endIndex,
				spanTokens: span.tokens,
				beforeTokens,
				afterTokens: measurement.totalTokens,
				netReleaseTokens,
				netReleaseRatio,
				reasons: batchReasons
			});
			ledger.netReleaseTokens += netReleaseTokens;
			for (const reason of batchReasons) recordVerdict(reason);
			if (batchReasons.includes("no-progress")) {
				finishLedger("no-progress", false);
				return result;
			}
			if (batchReasons.includes("low-yield")) {
				this.stopEnvelopeBudgetPass(agent.session, "low-yield", measurement.totalTokens, spec.thresholdTokens);
				finishLedger("low-yield", false);
				return result;
			}
		}
		if (measurement.totalTokens <= exitTokens) {
			finishLedger("pressure-exit", true);
			return result;
		}
		this.ctx.logger.warn(`three-zone pressure ended after ${batch} batch(es) at ${measurement.totalTokens} > exit ${exitTokens}`);
		finishLedger("batch-limit", false);
		return result;
	}
	/**
	* The structured ledger of the latest pressure invocation on one session, or
	* `undefined` when this engine has not run a pressure pass for it. The record
	* is in-memory only: it carries the shape the batch needs without introducing
	* a persistent schema an older build could misread.
	*/
	pressureLedger(session) {
		return pressureLedgerStore(this.pressureLedgers).get(session);
	}
	/**
	* Attribute token price of the active model-visible task-state slot node.
	* Resolves from the session surface and token measurement nodes.
	*/
	visibleSlotTokens(session, measurement) {
		return visibleSlotTokens(session, measurement);
	}
	/**
	* Inspect or reconcile the token ledger for one session under a given measurement.
	* Derives slot injection token attribution and baseline kind without requiring a full pressure loop run.
	*/
	inspectLedger(session, measurement, contextWindow = 128e3) {
		const spec = resolveCompactSpec(resolveTargetPolicy(this.config, {
			provider: "default",
			model: "default"
		}), contextWindow);
		const injectionTokens = visibleSlotTokens(session, measurement);
		const usageBaseline = measurement.baseline.kind === "usage";
		const exitTokens = Math.min(spec.thresholdTokens, spec.pressureExitTokens);
		return {
			trigger: "pressure",
			generation: session.surface.replaceGeneration,
			beforeTokens: measurement.totalTokens,
			afterTokens: measurement.totalTokens,
			thresholdTokens: spec.thresholdTokens,
			exitTokens,
			batches: [],
			batchesRun: 0,
			netReleaseTokens: 0,
			exitReached: measurement.totalTokens <= exitTokens,
			terminalReason: null,
			stopReasons: [],
			injectionTokens,
			usageBaseline
		};
	}
	/** Rebuild source types from Session provenance and durable served audits. */
	sourceIndex(session) {
		const store = this.toolGroupAuditStore;
		const records = store === void 0 ? [] : store.recordsForSession(session.id, session.header.createdAt);
		const index = buildSurfaceSourceIndex(session, servedReplacementSeqs(records));
		reportUnrecordedAuditLandings(this, index, records);
		return index;
	}
	/** Rebuild the durable fingerprint used to classify one selected tool group. */
	toolGroupFingerprint(session, group) {
		const events = group.sourceSeqs.map((seq) => session.eventAt(seq));
		return toolGroupFingerprint({
			lifecycle: {
				sessionId: session.id,
				createdAt: session.header.createdAt
			},
			sourceSeqs: group.sourceSeqs,
			callIds: group.callIds,
			eventTypes: events.map((event) => event?.type ?? "missing"),
			contentDigest: contentDigest(events.map((event) => JSON.stringify(event))),
			schemaVersion: 1
		});
	}
	/**
	* Judge the tool-stage debt of one surface span. `'actionable'` names work an
	* actor of THIS engine will actually perform on it: the deterministic pruner
	* reducing an over-threshold original, or `summarizeToolGroups` selecting and
	* re-attempting a group. `'inert'` names pending-SHAPED content no actor can
	* act on — an unresolvable span, or groups the audit already refuses — which
	* must allow the history pass instead of deferring it forever.
	*/
	hasPendingToolIntermediateWork(session, start, end, prune, inputCapTokens) {
		const nodes = session.surface.nodes;
		const startIndex = nodes.indexOf(start);
		const endIndex = nodes.indexOf(end);
		if (startIndex < 0 || endIndex < startIndex) return "inert";
		const index = this.sourceIndex(session);
		const selected = nodes.slice(startIndex, endIndex + 1);
		if (prune !== void 0 && selected.some((seq) => {
			const event = session.eventAt(seq);
			if (event?.type !== "tool/result" || !index.isOriginalToolResult(seq)) return false;
			const messageContent = event.data.message.content;
			if (messageContent.length === 0) return false;
			return prune.measureMessageText(messageContent) > prune.config.thresholdChars;
		})) return "actionable";
		if (!this.config.toolGroupSummarizer.enabled || this.toolGroupAuditStore === void 0) return "none";
		const records = this.toolGroupAuditStore.recordsForSession(session.id, session.header.createdAt);
		const groups = selectToolGroups(session, this.toolGroupSelectionOptions({
			start,
			end
		}, inputCapTokens, index, session));
		const actionable = groups.some((group) => group.toolResultSeqs.every((seq) => index.isOriginalToolResult(seq)) && mayAttemptToolGroupSummary(this, index, records, this.toolGroupFingerprint(session, group)));
		if (groups.length > 0 && !actionable) return "inert";
		return actionable ? "actionable" : "none";
	}
	/**
	* Find the surface index of the earliest actionable tool-stage work within a
	* surface span, so pressure planning can back off before that exact debt.
	*/
	internalFindFirstToolStageDebtIndex(session, start, end, prune, inputCapTokens, index) {
		const nodes = session.surface.nodes;
		const startIndex = nodes.indexOf(start);
		const endIndex = nodes.indexOf(end);
		if (startIndex < 0 || endIndex < startIndex) return void 0;
		const selected = nodes.slice(startIndex, endIndex + 1);
		if (prune !== void 0) {
			const pruneDebtOffset = selected.findIndex((seq) => {
				const event = session.eventAt(seq);
				if (event?.type !== "tool/result" || !index.isOriginalToolResult(seq)) return false;
				const messageContent = event.data.message.content;
				if (messageContent.length === 0) return false;
				return prune.measureMessageText(messageContent) > prune.config.thresholdChars;
			});
			if (pruneDebtOffset >= 0) return startIndex + pruneDebtOffset;
		}
		if (!this.config?.toolGroupSummarizer?.enabled || this.toolGroupAuditStore === void 0) return void 0;
		const records = this.toolGroupAuditStore.recordsForSession(session.id, session.header.createdAt);
		const groups = selectToolGroups(session, this.toolGroupSelectionOptions({
			start,
			end
		}, inputCapTokens, index, session));
		for (const group of groups) if (group.toolResultSeqs.every((seq) => index.isOriginalToolResult(seq)) && mayAttemptToolGroupSummary(this, index, records, this.toolGroupFingerprint(session, group))) {
			const firstSeq = group.sourceSeqs[0] ?? group.startSeq;
			const idx = nodes.indexOf(firstSeq);
			if (idx >= 0) return idx;
		}
	}
	/**
	* The ONE selection option object the summarize actor and the pending-work
	* probe share: same surface span, eligibility classification, code-point
	* text metric, estimator, max-group cap, and envelope input cap. Divergence
	* between the two is what let the probe report pending work the actor could
	* never act on, vetoing every pressure pass with `tool-stage-deferred`.
	*/
	toolGroupSelectionOptions(olderRange, inputCapTokens, index, session) {
		const config = this.config.toolGroupSummarizer;
		return {
			olderRange: {
				start: olderRange.start,
				end: olderRange.end
			},
			minGroupResults: config.minGroupResults,
			minGroupChars: config.minGroupChars,
			minGroupTokens: config.minGroupTokens,
			maxGroupTokens: this.cappedMaxGroupTokens(config.maxGroupTokens, inputCapTokens),
			maxGroups: config.maxGroupsPerPass,
			estimateTokens: (event) => event.type === "tool/result" || event.type === "assistant/message" ? this.ctx.tokenMeter.estimateMessage(event.data.message) : 0,
			isEligibleResult: (seq) => index.isOriginalToolResult(seq),
			isGroupFittable: inputCapTokens === void 0 || session === void 0 ? void 0 : (group) => estimateToolGroupAuxiliaryRequestTokens(session, group, config.maxSummaryTokens, (msg) => this.ctx.tokenMeter.estimateMessage(msg)) <= inputCapTokens
		};
	}
	/** Derive current zones from one fresh meter snapshot and the routed capacity. */
	zones(session, measurement, spec) {
		const zoneBudget = this.envelopeZoneBudget(session, measurement, spec);
		return partitionSurfaceZones(session, measurement, {
			recentRatio: spec.retainTokens / spec.contextWindow,
			forgetBoundaryRatio: spec.forgetBoundaryRatio,
			contextWindow: spec.contextWindow,
			recentBoundaryTokens: zoneBudget.retainedTailTokens,
			forgetBoundaryTokens: zoneBudget.forgetBoundaryTokens
		});
	}
	/**
	* Derive the envelope-aware zone boundaries of one snapshot. `retainedTailTokens`
	* matches {@link envelopeBudget}'s affordable tail exactly (same floor, same
	* clamp), and the domination verdict is the pass's typed `envelope-dominated`
	* stop condition.
	*/
	envelopeZoneBudget(session, measurement, spec) {
		const injectionTokens = visibleSlotTokens(session, measurement);
		return resolveEnvelopeZoneBudget(measurement, {
			pressureTokens: spec.thresholdTokens,
			forgetWatermarkTokens: Math.floor(spec.contextWindow * spec.forgetMaintenanceRatio),
			forgetBoundaryTokens: Math.floor(spec.contextWindow * spec.forgetBoundaryRatio),
			retainTokens: spec.retainTokens,
			minRetainTokens: retainedTailFloorTokens(session, measurement),
			injectionTokens
		});
	}
	/**
	* Resolve the envelope budget of one snapshot. The instruction and the
	* minimum framed checkpoint are priced per call so the cap follows the
	* session's own estimator rather than a constant.
	*/
	envelopeBudget(session, measurement, spec) {
		const meter = this.ctx.tokenMeter;
		const injectionTokens = visibleSlotTokens(session, measurement);
		return resolveEnvelopeBudget(measurement, {
			contextWindow: spec.contextWindow,
			responseReserveTokens: spec.responseReserveTokens,
			safetyMarginTokens: spec.safetyMarginTokens,
			instructionTokens: compactionInstructionTokens(meter),
			summaryMaxTokens: spec.maxTokens,
			retainTokens: spec.retainTokens,
			minRetainTokens: retainedTailFloorTokens(session, measurement),
			injectionTokens
		});
	}
	/**
	* The effective per-group input cap: the configured group budget narrowed by
	* the envelope auxiliary input cap, so a group whose summarization call
	* could not fit the window is never selected, probed, or paid for.
	*/
	cappedMaxGroupTokens(maxGroupTokens, inputCapTokens) {
		return inputCapTokens === void 0 ? maxGroupTokens : Math.min(maxGroupTokens, Math.max(0, inputCapTokens));
	}
	/**
	* Whether a terminal envelope-budget stop still holds for the current
	* pressure/surface state. The memo reopens — and is dropped — when the
	* surface generation, the surface node count, or the measured pressure no
	* longer matches the state the verdict was derived from: newly appended
	* content or a moved pressure anchor can change the plan and the domination
	* verdicts, so the suppression must not outlive its own evidence.
	*/
	pressurePassTerminated(session, totalTokens) {
		const memo = this.pressureStops.get(session);
		if (memo === void 0) return false;
		if (memo.generation !== session.surface.replaceGeneration || memo.surfaceNodes !== session.surface.nodes.length || memo.totalTokens !== totalTokens) {
			this.pressureStops.delete(session);
			return false;
		}
		for (const reason of memo.reasons) if (TERMINAL_PRESSURE_REASONS.has(reason)) return true;
		return false;
	}
	/**
	* Record one envelope-budget stop together with the pressure/surface state it
	* was derived from, log it the first time only for that state, and — when a
	* pressure pass of THIS engine is running — record it in that pass's ledger.
	* This is the ONE stop funnel: every in-loop typed verdict goes through it, so
	* a reason is never both logged by the funnel and pushed into the ledger by
	* the loop for the same state. The generation advances exactly when a
	* positional replacement lands, so within one unchanged state a repeated
	* reason cannot behave differently; when the node count or the measured
	* pressure moves instead, the stale verdict is replaced so the next pass
	* re-evaluates against the new state.
	*/
	stopEnvelopeBudgetPass(session, reason, totalTokens, thresholdTokens) {
		const generation = session.surface.replaceGeneration;
		const surfaceNodes = session.surface.nodes.length;
		const memo = this.pressureStops.get(session);
		const current = memo !== void 0 && memo.generation === generation && memo.surfaceNodes === surfaceNodes && memo.totalTokens === totalTokens ? memo : void 0;
		if (current !== void 0 && current.reasons.has(reason)) return;
		if (current !== void 0) current.reasons.add(reason);
		else this.pressureStops.set(session, {
			generation,
			surfaceNodes,
			totalTokens,
			reasons: /* @__PURE__ */ new Set([reason])
		});
		const stopReasons = this.activePressureStopReasons;
		if (stopReasons !== void 0 && !stopReasons.includes(reason)) stopReasons.push(reason);
		reportStoppedReason(this, this.ctx.logger, reason, totalTokens, thresholdTokens);
	}
	/**
	* Overflow is the only path allowed to relax normal zones. It first prunes
	* provenance-indexed original results, then progresses oldest-first through
	* forget, tool, and recent ranges. No custom SessionEventMap entry is needed:
	* each reduction uses the official compaction/prune or compaction transaction.
	*/
	async recoverOverflow(agent, prune, signal) {
		const session = agent.session;
		if (prune !== void 0) {
			const sources = this.sourceIndex(session);
			prune.pruneSession(session, { candidateSeqs: session.surface.nodes.filter((seq) => sources.isOriginalToolResult(seq)) });
		}
		const target = routedTarget(session);
		if (target === void 0) return null;
		const context = (await this.ctx.llm.resolveModelInfo(target.provider, target.model, signal)).context;
		if (context === void 0) return null;
		const policy = resolveTargetPolicy(this.config, target);
		const spec = resolveCompactSpec(policy, context.contextWindow);
		let latest = null;
		for (const level of [
			"overflow-forget",
			"overflow-tool-zone",
			"overflow-recent"
		]) for (let batch = 0; batch < policy.maxPressureBatches; batch += 1) {
			const current = this.ctx.tokenMeter.measure(session);
			if (current.totalTokens < spec.thresholdTokens) return latest;
			const zones = this.zones(session, current, spec);
			const range = level === "overflow-forget" ? zones.forget : level === "overflow-tool-zone" ? zones.tool : zones.recent;
			if (range === null) break;
			const batchBudget = this.envelopeBudget(session, current, spec);
			const selected = selectForgetBatch(session, current, {
				...zones,
				forget: range
			}, {
				targetBatchTokens: Math.min(spec.targetBatchTokens, batchBudget.summarizerInputCapTokens),
				maxBatchTokens: Math.min(spec.maxBatchTokens, batchBudget.summarizerInputCapTokens)
			});
			if (selected === null) break;
			const sourceIndex = this.sourceIndex(session);
			if (session.surface.nodes.slice(selected.startIndex, selected.endIndex + 1).some((seq) => !sourceIndex.canCompactHistory(seq, policy.minReentryTurns))) break;
			this.ctx.logger.warn(`context-overflow recovery level ${level}, batch ${batch + 1}`);
			latest = await this.compactRegion(selected.startSeq, selected.endSeq, agent, signal);
			if (batch + 1 === policy.maxPressureBatches) {
				const after = this.ctx.tokenMeter.measure(session);
				const afterZones = this.zones(session, after, spec);
				const remaining = level === "overflow-forget" ? afterZones.forget : level === "overflow-tool-zone" ? afterZones.tool : afterZones.recent;
				const afterBudget = this.envelopeBudget(session, after, spec);
				if (remaining !== null && selectForgetBatch(session, after, {
					...afterZones,
					forget: remaining
				}, {
					targetBatchTokens: Math.min(spec.targetBatchTokens, afterBudget.summarizerInputCapTokens),
					maxBatchTokens: Math.min(spec.maxBatchTokens, afterBudget.summarizerInputCapTokens)
				}) !== null) return latest;
			}
		}
		return latest;
	}
	async summarizeToolGroups(agent, target, policy, olderRange, signal, roundReplacements, inputCapTokens) {
		const config = this.config.toolGroupSummarizer;
		if (!config.enabled || olderRange === null) return;
		await this.toolGroupAuditStorePromise;
		const store = this.toolGroupAuditStore;
		if (store === void 0) return;
		const session = agent.session;
		await recoverToolGroupAudits(this, session);
		const selectionSources = this.sourceIndex(session);
		const groups = selectToolGroups(session, this.toolGroupSelectionOptions({
			start: olderRange.startSeq,
			end: olderRange.endSeq
		}, inputCapTokens, selectionSources, session));
		const unwritten = auditEngineState(this).unwritten;
		for (const group of groups) {
			const sources = this.sourceIndex(session);
			if (group.toolResultSeqs.some((seq) => !sources.isOriginalToolResult(seq))) continue;
			const fingerprint = this.toolGroupFingerprint(session, group);
			const records = store.recordsForSession(session.id, session.header.createdAt);
			if (!mayAttemptToolGroupSummary(this, sources, records, fingerprint)) continue;
			const slot = attemptSlotFor(records, fingerprint, auditOwnerId(this));
			const open = slot === void 0 ? openToolGroupAudit(`tg-${randomUUID()}`, session.id, group, session.surface.replaceGeneration, policy.summarizationProvider || target.provider, policy.summarizationModel || target.model, fingerprint, { createdAt: session.header.createdAt }, { ownerId: auditOwnerId(this) }) : slot.adopted ? adoptToolGroupAudit(slot.record, {
				ownerId: auditOwnerId(this),
				sourceSeqs: group.sourceSeqs,
				surfaceGeneration: session.surface.replaceGeneration,
				diagnostic: AUDIT_DIAGNOSTIC.adoptedInterruptedAttempt
			}) : resumeToolGroupAudit(slot.record, {
				sourceSeqs: group.sourceSeqs,
				surfaceGeneration: session.surface.replaceGeneration
			});
			const requestId = open.requestId;
			let auditWrites = true;
			try {
				await store.open(open);
				unwritten.delete(fingerprint);
			} catch (error) {
				auditWrites = false;
				unwritten.set(fingerprint, (unwritten.get(fingerprint) ?? 0) + 1);
				logAuditDiagnostic(this, `tool-group summary audit write failed (${AUDIT_DIAGNOSTIC.auditWriteFailed}); the durable Session provenance still owns classification: ${error instanceof Error ? error.message : String(error)}`);
			}
			let summarized;
			let replacement;
			const landedSeqs = [];
			try {
				summarized = await summarizeToolGroup(this.ctx, session, group, agent, {
					provider: open.provider,
					model: open.model,
					maxTokens: config.maxSummaryTokens
				}, signal, inputCapTokens);
				assertToolGroupCommitStable(session.id, session.surface.replaceGeneration, group.sourceSeqs, open);
				replacement = replaceToolGroup(session, group, summarized.summary, {
					estimateTokens: (message) => this.ctx.tokenMeter.estimateMessage(message),
					onLanded: (seq) => {
						landedSeqs.push(seq);
						roundReplacements.add(seq);
					}
				});
			} catch (error) {
				if (landedSeqs.length > 0) {
					if (auditWrites) try {
						await store.finish(requestId, (current) => recordToolGroupLanded(current, landedSeqs, {
							...summarized?.summary === void 0 ? {} : { summary: summarized.summary },
							...summarized?.rawOutput === void 0 ? {} : { rawOutput: summarized.rawOutput },
							error: error instanceof Error ? error.message : String(error)
						}));
					} catch (auditError) {
						this.ctx.logger.warn(`tool-group summary audit landing record failed: ${auditError instanceof Error ? auditError.message : String(auditError)}`);
					}
					continue;
				}
				if (!auditWrites) {
					logAuditDiagnostic(this, `tool-group summary attempt failed without an audit row (${AUDIT_DIAGNOSTIC.auditWriteFailed}): ${error instanceof Error ? error.message : String(error)}`);
					continue;
				}
				const reason = error instanceof ToolGroupSummaryFallbackError ? error.reason : "failure";
				const message = error instanceof Error ? error.message : String(error);
				const transient = reason === "stream" || reason === "failure";
				try {
					await store.finish(requestId, (current) => finishToolGroupAudit(current, transient ? "failure" : "fallback", { error: message }));
				} catch (auditError) {
					this.ctx.logger.warn(`tool-group summary audit finish failed: ${auditError instanceof Error ? auditError.message : String(auditError)}`);
				}
				continue;
			}
			replacement.replacementSeqs.forEach((seq) => roundReplacements.add(seq));
			if (!auditWrites) {
				logAuditDiagnostic(this, `tool-group summary audit commit skipped (${AUDIT_DIAGNOSTIC.auditWriteFailed}); the landed reduction keeps its durable Session provenance`);
				continue;
			}
			if (replacement.replacementSeqs.length > 0) try {
				await store.finish(requestId, (current) => recordToolGroupLanded(current, replacement.replacementSeqs, {
					rawOutput: summarized.rawOutput,
					summary: summarized.summary
				}));
			} catch (auditError) {
				this.ctx.logger.warn(`tool-group summary audit landing record failed: ${auditError instanceof Error ? auditError.message : String(auditError)}`);
			}
			await this.commitToolGroupSuccess(store, requestId, {
				rawOutput: summarized.rawOutput,
				summary: summarized.summary,
				replacementSeqs: replacement.replacementSeqs
			});
		}
	}
	/**
	* Commit the durable `success` record for one already-landed tool-group
	* reduction.
	*
	* The replacement and its shadow price are in the Session log before this
	* runs, so this phase must never produce a terminal `failure`/`fallback`
	* record: such a record would misreport a reduction that really happened,
	* drop the durable `tool-summary` classification of its replacement seqs
	* (leaving only the weaker shadow-price identity), and suppress the group
	* fingerprint as if the work had been refused. One retry absorbs a transient
	* store error; if the commit still fails, the record stays `open`, so the
	* group remains recoverable and the landed reduction keeps its provenance.
	* @param store - durable audit store bound to this engine's session domain.
	* @param requestId - request id of the open record to finish.
	* @param patch - success evidence: raw model output, parsed summary, landed replacement seqs.
	*/
	async commitToolGroupSuccess(store, requestId, patch) {
		for (let attempt = 0; attempt < 2; attempt += 1) try {
			await store.finish(requestId, (current) => finishToolGroupAudit(current, "success", patch));
			return;
		} catch (error) {
			if (attempt === 0) continue;
			this.ctx.logger.warn(`tool-group summary audit success commit failed: ${error instanceof Error ? error.message : String(error)}; the durable replacement stays recoverable`);
		}
		try {
			await store.finish(requestId, (current) => recordToolGroupLanded(current, patch.replacementSeqs ?? [], {
				rawOutput: patch.rawOutput,
				...patch.summary === void 0 ? {} : { summary: patch.summary }
			}));
		} catch (fallbackError) {
			this.ctx.logger.warn(`tool-group summary audit open-record fallback persistence failed: ${fallbackError instanceof Error ? fallbackError.message : String(fallbackError)}`);
		}
	}
	/**
	* Compact one inclusive positional range from the agent-owned surface using
	* the effective token meter for all retention and shrink pricing.
	* @param start - inclusive first surface-node seq.
	* @param end - inclusive last surface-node seq.
	* @param agent - owner of the target session, used by the summarizer.
	* @param signal - optional summarization cancellation signal.
	* @returns the successful durable compaction result.
	*/
	async compactRegion(start, end, agent, signal) {
		return compactSurfaceRegion(this.regionDependencies(), agent.session, start, end, agent, {
			owner: "current-turn",
			stability: "whole-surface"
		}, signal);
	}
	/**
	* Force one useful idle-session compaction below the pressure threshold, and
	* resolve only after its standalone marker pair is durably checkpointed. The
	* durable compaction lock is rechecked before range selection so a live
	* unmatched marker reports busy even when the only remaining head is an
	* isolated checkpoint the empty-benefit guard would otherwise decline.
	* @param agent - idle agent whose next-turn admission this call reserves.
	* @param signal - cancellation scoped to this compaction request.
	* @param sourceCommandId - initiating command identity for presentation correlation.
	* @returns the committed result, or `null` when no safe useful range exists.
	*/
	compactNow(agent, signal, sourceCommandId) {
		signal.throwIfAborted();
		try {
			return agent.runMaintenance(async (agentSignal) => {
				const operationSignal = AbortSignal.any([agentSignal, signal]);
				try {
					operationSignal.throwIfAborted();
					assertNoActiveCompaction(agent.session, "manual compaction");
					const range = selectCompactableRange(agent.session, this.ctx.tokenMeter.measure(agent.session), 0);
					if (range === null) return null;
					return await compactSurfaceRegion(this.regionDependencies(), agent.session, range.start, range.end, agent, {
						owner: null,
						stability: "selected-span",
						...sourceCommandId === void 0 ? {} : { sourceCommandId },
						flush: async () => {
							await this.ctx.sessions.flush(agent.session);
						}
					}, operationSignal);
				} catch (error) {
					if (agentSignal.aborted && operationSignal.reason === agentSignal.reason) throw new ManualCompactionError("cancelled", "manual compaction was cancelled", { cause: error });
					operationSignal.throwIfAborted();
					throw error;
				}
			});
		} catch (error) {
			throw new ManualCompactionError("busy", "manual compaction requires an idle agent with no waking queued work", { cause: error });
		}
	}
	/** Bind the effective token meter and dynamically dispatched summarizer hook. */
	regionDependencies() {
		return {
			meter: this.ctx.tokenMeter,
			summarize: (input, owner, abort) => this.summarize(input, owner, abort)
		};
	}
};
//#endregion
export { BasicCompactionEngine, BasicCompactionEngine as default };
