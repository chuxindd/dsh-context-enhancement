# B7 主代理验收

## 结论

B7 验收通过，选定语义为 A：fork child 从 durable `inheritedEventCount` own-events boundary 独立重建；未实现父 stable 一次性 bootstrap，且不将未选方案记为失败。

## 主代理独立验证

- B7 focused regression：7 files，60/60；
- `pnpm run typecheck`：exit 0；
- B5 fixed-slot、B6 provenance/audit、B3 pressure 全部保持通过。

## 选定契约

- 继承前缀为 `[0, inheritedEventCount)`；
- cursor 起点为 `inheritedEventCount - 1`，不使用 `firstLiveSeq`；
- child stable/base/id/digest/audit/slot 均独立；
- inherited replacement 有显式标记；
- inherited source 不获得 immediate-reentry waiver；
- source-index 只统计 child own completed turns；
- durable marker 与覆盖声明冲突时 fail closed，不折叠继承前缀。

## E09 对照

历史 E09 目录保持逐字节不变。由于 B5 fixed-slot 和 B4 v2 clean-break 改变了旧脚本的观测通道，新增 `B7-E09修复后回归` counterpart 以当前真实栈验证：child 首批窗口只含 own events，继承 eligible prefix 不进入 child live window，`base=null`，inherited marker 进入 v2 durable record。

## 保留风险

- B1 多实例 CAS 仍 `blocked-upstream`；
- collision-lifecycle identity 仍未处理；
- B8 artifact parity 待完成；
- 未实现未选的 parent stable bootstrap 语义。

```text
B7 = completed-single-instance
B1 = blocked-upstream
B8 = pending
```
