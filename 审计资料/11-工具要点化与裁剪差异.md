# 11 · 工具要点化（操作①）与工具裁剪（操作②）差异审计

> 对象：工作区 `C:\Users\chuxi\Documents\trae_projects\code\dsh-context-enhancement` 的**静态设计与实现语义**（`src/**` 与生效配置）。不审运行实例、watcher、用户 settings。
> 对照基线：`理想化落地方案.md` §3.7、§4.4、§4.5、§6（另参 §3.5/§3.8/§7/§8 仅作条款定位）。
> 事实来源：`审计资料/工作区基线.json`、`审计资料/00/01`、`调查资料/10`、`调查资料/12`、`调查资料/02`。
> 本轮未修改、未格式化、未构建、未测试、未安装、未还原任何既有文件；未派生子代理；未重新调查 DSH。只新增本文件。
> 分类口径：【确认差异】结构/语义与理想方案不同；【参数差异】结构相同、取值或阈值不同（可校准）；【等价实现】要求达成但机制不同；【需实验确认】静态不可判，须真实轨迹/实验；【构建产物差异】仅指 `lib` 与其应承载的 `src` 设计不一致（本轮未发现）。
> 已检查实现/配置：`src/compaction-basic.ts`、`src/tool-result-pruner.ts`、`src/internal/compaction/{tool-groups,tool-segments,tool-pairing,source-index,tool-group-audit,tool-group-audit-store,tool-group-domain,tool-group-replacement,tool-group-summary,tool-group-summarizer,pruner-types,pruner-config,config}.ts`（14）；测试：`compaction-tool-group-audit-failure.spec.ts`、`compaction-served-provenance.spec.ts`、`compaction-tool-groups.spec.ts`、`compaction-three-zone.spec.ts`、`compaction-tool-group-audit.spec.ts`、`compaction-audit-store-injection.spec.ts`、`compaction-recent-hard-limit.spec.ts`（7，另以 grep 覆盖全部 23 个 `compaction-*.spec.ts` 用例名）；旁证：`tests/effect-projection.spec.ts`、`presets/contextual/agent.cordis.yml`、`lib/compaction-basic.js`。合计 21 个，核心文件 18 个以内。

---

## 0. 结论速览

**确认差异 6 项、参数差异 4 项、等价实现 10 项、需实验确认 3 项、构建产物差异 0 项、未找到 1 项。**

- 理想 §4.4 要求的**轮次五值与逐结果 outcome 完全未实现**（`summarizeToolGroups(): Promise<void>`），但其调度价值由 provenance + audit 状态等价承担，§4.4「失败只影响该组；返回轮次分类供操作②使用」的后半句不成立（分类未返回）。
- §6.1「provenance 可仅凭 Session 恢复」**不成立**：`tool-summary` 与 `tool-pruned` 的区分必须依赖独立 audit 表；audit 域文档丢失会让语义摘要静默降级（不得误判动作已由「unknown-replacement 永不放开」兜住，故安全但非等价）。
- §4.5 的禁止项（全 surface 扫描、裁剪近区）在普通维护成立；**overflow 第 1 级例外**按 `thresholdChars` 处理近区，与 §3.8 第 5 条不符。
- §7 要求 `hardLimitChars` 必须配置模型安全值，当前**未配置**（近区保护默认全开）。
- §6.1 要求持久化的操作编号/维护轮 id/recovery level **未进入 Session**，只在插件 audit 与其自建影子价格事件里。

---

## 1. 等价实现清单（先记录达成项，避免只列问题）

| ID | 理想要求 | 当前等价机制 | 证据 |
|---|---|---|---|
| E1 | §4.4 完整配对 + 同一安全 step/turn burst | `toolPairingBalancedBefore/After` 按当前位置切口；`toolSegments()` 只发射「同 turn、burst 连续、首尾平衡」的最长跨度，未闭合尾调用不发射 | `src/internal/compaction/tool-segments.ts:117-163`、`src/internal/compaction/tool-pairing.ts:156-170` |
| E2 | §4.4 来源为 original | `isEligibleResult → index.isOriginalToolResult`；组内任一结果非 original 即跳过 | `src/compaction-basic.ts:873,1113`、`src/internal/compaction/source-index.ts:81-84` |
| E3 | §4.4 最小结果数/字符/token + 每轮组数有限 | 6 阈值 + `maxGroups` 上限 + `splitOversizedRun` 安全切分 | `src/internal/compaction/tool-groups.ts:80,88,110-114,151-156,205-246` |
| E4 | §4.4 完整摘要请求不超过输入预算 | `isGroupFittable = estimateToolGroupAuxiliaryRequestTokens(...) <= inputCapTokens`（指令 + JSON + 生成预留整链）；且 actor 与 probe 共享同一 `toolGroupSelectionOptions()` | `src/compaction-basic.ts:853-883`、`src/internal/compaction/tool-group-summarizer.ts:60-68,82-95` |
| E5 | §4.4 模型输出按 result 结构化 + 调用 id 一一覆盖 + 事实只能来自输入 | `parseToolGroupSummary` 逐项校验：sourceSeq 必须在输入组内、不得重复、callId 必须与输入一致、每个输入项都必须有 item；`assertFactsBelongToInput` 要求 facts/files/identifiers/errors 逐字出现在来源项文本中 | `src/internal/compaction/tool-group-summary.ts:75-102`、`tool-group-summarizer.ts:42-47,119-125` |
| E6 | §4.5 每个 replacement 严格缩减 | 逐节点守卫 `estimateTokens(message) >= shadowedTokens → continue`（等价/涨价连事件都不写）；裁剪侧 `charsAfter > threshold \|\| charsAfter >= totalChars` 抛错 | `src/internal/compaction/tool-group-replacement.ts:106-115`、`src/tool-result-pruner.ts:165-179,262-270` |
| E7 | §4.5 操作①本轮 notEligible/oversized/failed 的原始结果交给操作② | 摘要落盘即获得 audit 或影子价格身份 → 非 original；未落盘节点保持 original → 仍进入裁剪候选（逐结果粒度成立） | `src/compaction-basic.ts:476-477,511-513`、`source-index.ts:98-110` |
| E8 | §4.5 禁止扫描全 surface | 3 个 `pruneSession` 调用点全部传显式 `candidateSeqs`（工具区、遗忘区、overflow），且 `pruneSession` 先做成员校验（缺一即抛） | `src/compaction-basic.ts:478-485,514-518,1021`、`src/tool-result-pruner.ts:304-311,319` |
| E9 | §4.5 禁止裁剪已摘要/已裁剪/unknown replacement | 候选集只取 `isOriginalToolResult`；`unknown-replacement` 在 `canCompactHistory` 中永不放开 | `source-index.ts:81-84,85-95`、`tests/compaction-three-zone.spec.ts:952-969` |
| E10 | §3.7 已提交 replacement 的长期身份由 Session provenance 恢复 + §6.1 紧邻 shadow event | `compaction/prune`（含 `shadowedRange/shadowedSeqs/shadowedTokenCount`）+ 紧邻 `surfaceOp:{op:'replace'}` + `sourceEventSeqs`；`wasPruned()` 用「紧邻前驱 + 单节点 + 单来源」重建身份，纯 Session 可恢复 | `src/internal/compaction/tool-group-replacement.ts:120-131`、`src/tool-result-pruner.ts:350-361`、`source-index.ts:119-126`、`tests/compaction-tool-group-replacement.spec.ts:60-93` |

---

## 2. 逐条差异矩阵

| ID | 理想要求（章节） | 当前设计 | 分类 | 证据（路径:符号:行） | 影响 | 需实验 |
|---|---|---|---|---|---|---|
| **A1** | §3.7 每轮操作①返回 `ToolGroupOutcome{groupId,sourceSeqs,status∈五值,resultOutcomes[],replacementSeqs[],retryable,reasonCode}` | 无该类型、无返回值。`summarizeToolGroups(...): Promise<void>`；唯一的「接近分类」是 `ToolStageDebt='none'\|'inert'\|'actionable'`（服务于是否推迟语义压缩，不进入操作②） | 确认差异 | `src/compaction-basic.ts:106`（ToolStageDebt）、`:1080-1088`（签名）、`:757-803`（debt 判定）、`:1127-1189`（逐组处理无返回值）；`src/internal/compaction/tool-group-replacement.ts:28-31`（结果仅 replacementSeqs/sourceSeqs） | 轮内分类不可观测、不可下传；操作②候选只能由持久索引反推；`groupId` 仅以指纹形式存在于 audit（`toolGroupFingerprint`，`compaction-basic.ts:737-747`） | 否 |
| **A2** | §6.1「replacement 的来源类型不能只存在独立 storage audit」+ §8 不变量第 5 条「provenance 可仅凭 Session 日志恢复；删除 audit 不改变 surface 类型判断」 | `classifyEvent` 判定顺序为 **audit 成功 → 影子价格 → unknown**；`tool-summary` 身份**仅**来自 audit 表（`servedReplacementSeqs`）。引擎每次重建索引都注入 audit 结果 | 确认差异 | §8 不变量第 5 条。`src/internal/compaction/source-index.ts:98-110`（尤其 `:107`）、`src/compaction-basic.ts:726-734`；测试固化该依赖：`tests/compaction-served-provenance.spec.ts:210-211`（无 audit ⇒ `tool-pruned`）、`tests/compaction-three-zone.spec.ts:918-935` | 删除/损坏 audit 域（`layout:'single'` 单文档）⇒ 语义摘要降级为 `tool-pruned`，若影子价格邻接也缺失则降为 `unknown-replacement`（永久不可重入）。**不误判为 original**（理想底线满足），但「删除 audit 不改变类型」不成立 | 是（删 audit 后的重入行为只在测试内验证，需真实会话回放量化降级比例） |
| **A3** | §6.2「独立 audit 只用于诊断和合规，不决定当前 surface 的类型，也不作为第二份 stable」 | audit 表同时是**诊断存储**与**类型权威**；且是 `layout:'single'` 的 storage-domain 文档，行按 `requestId` 键存放、无清理/保留策略；`recordsForSession` 在 `createdAt===undefined` 时静默返回空集 | 确认差异 | `src/internal/compaction/tool-group-domain.ts:27-34,36-44`、`src/internal/compaction/tool-group-audit-store.ts:19-22`、`src/compaction-basic.ts:730-733` | audit 既是必需运行时依赖又是单文档后端：损坏/清空即改变 surface 类型判断；无 `createdAt` 的 header 会让全部记录静默失配（DSH 类型上 `createdAt` 必填，故当前安全，但无显式断言/告警） | 是（多实例/单文档覆盖属总审范围；本专题只记录「类型权威落在 audit」） |
| **A4** | §4.4「事实只能来自输入」——含未决问题保持来源引用 | `assertFactsBelongToInput` 校验 `facts/files/identifiers/errors`，**不校验** `item.unresolved`，也不校验 `groupErrors`/`unresolved`（顶层）与 `groupSummary` 的来源引用 | 确认差异（校验覆盖缺口） | `src/internal/compaction/tool-group-summary.ts:94-102`（claims 列表缺 `unresolved`）、`:48-57`（顶层字段仅类型校验） | 未决问题/组级错误可被模型编造而不触发 `source` 失败；`INSTRUCTION` 虽要求「Keep unknown information in unresolved」，但无强制。影响面限于摘要文本可信度，不改变 surface 结构 | 否（可选：补一条 `assertFactsBelongToInput` 覆盖 `unresolved` 即可闭环） |
| **A5** | §6.1 需持久化的「操作编号和维护轮 id」「recovery level」「本轮排除和最早重入 turn」 | Session 内**无**这些字段：官方 `compaction/prune` 只带 `shadowedRange/shadowedSeqs/shadowedTokenCount`；round-local 集合与 veto reason 仅存内存；overflow 等级仅 `logger.warn` 文本 | 确认差异 | `src/internal/compaction/tool-group-replacement.ts:120-124`、`src/tool-result-pruner.ts:350-354`、`src/compaction-basic.ts:456,645-657,1060`（`logger.warn(... level ${level} ...)`） | 重启后无法从 Session 判断某 replacement 属于哪一级恢复、哪一轮维护；§3.8「每次跨级必须持久记录等级」不成立。审计侧 `requestId` 提供了部分替代（`ToolGroupAuditRecord.requestId`，`tool-group-audit.ts:18`），但不是 Session 事实 | 否 |
| **A6** | §3.7「轮内分类用于精确构造操作②候选」；§4.5「候选来源：操作①本轮 notEligible/oversized/failed 的原始结果」 | 操作②候选**不由操作①的分类构造**，而由重建后的 provenance 索引推导（`isOriginalToolResult`）。互斥成立，但路径不同 | 确认差异（等价安全的替代机制） | `src/compaction-basic.ts:471-488,509-521`；`tests/compaction-tool-group-replacement.spec.ts`（落盘/不落盘守卫）、`tests/compaction-served-provenance.spec.ts:145-158` | 结果安全（互斥、部分落盘可恢复），但「分类」不可审计：无法回答「某组本轮为什么没被摘要」；与 A1 同源 | 否 |
| **B1** | §3.8 第 5 条「近区原始工具结果只有超过 `hardLimitChars` 或已进入 `overflow-recent` 才可处理」 | overflow 第 1 级 `prune.pruneSession(session, {candidateSeqs: 全部 original})` **不传 `olderRange`** ⇒ `olderSpan = {0, len-1}` ⇒ 近区结果按普通 `thresholdChars` 被裁剪 | 参数差异（+区域授权过宽） | `src/compaction-basic.ts:1019-1022`；`src/tool-result-pruner.ts:296-302,320-323,334-336`；`src/internal/compaction/pruner-types.ts:74-81`（无 `olderRange` 即全域 ordinary） | 溢出时近区大结果在 8192 码点门槛即被头尾截断，绕过 `hardLimitChars`。与 §3.8 的分级放宽顺序「先遗忘/工具区、最后近区」不一致；`overflow-recent` 语义被 `thresholdChars` 替代 | 是（需真实溢出会话确认近区被截断的频次与信息损失） |
| **B2** | §7「`hardLimitChars` 必须配置模型安全值」 | `hardLimitChars` 默认 `undefined`（该上限关闭），`presets/contextual/agent.cordis.yml` **未配置**；仅在显式配置时启用，且校验放宽为 `>= thresholdChars` | 参数差异 | `src/tool-result-pruner.ts:73-80`、`src/internal/compaction/pruner-config.ts:64,79-87`；`audit/01` §4.2 已证静态默认 `undefined` | 默认近区原始结果永不被裁剪（保护充分），但 §7 的「模型安全值」基线缺失；同时 ideal 的 `hardLimitChars > thresholdChars`（严格）被实现放宽为 `>=`，取等时 `pruneRecentContent` 与普通阈值行为重合 | 否（取值问题） |
| **B3** | §4.4「失败只影响该组」的实现细节（重试次数、终态判定） | 逐组独立 try/catch，失败 `continue`；瞬时判定 `reason∈{stream,failure}` → audit `failure`（最多 2 条后拒绝）；终态 `{route,empty,json,schema,source}` → `fallback`（永久拒绝该指纹） | 参数差异 | `src/compaction-basic.ts:1127-1168`、`src/internal/compaction/tool-group-audit.ts:132-144`、`src/internal/compaction/tool-group-summarizer.ts:30-36` | 与 §4.4 无冲突；与调查资料 12 §Q6 一致。风险点：`fallback` 为**永久**终态且无过期，同一内容指纹一旦落入 `schema/source`（含模型偶发格式错误）即不再尝试 —— 属策略取值而非结构差异 | 是（`schema` 类模型偶发错误导致永久放弃的频次需轨迹量化） |
| **B4** | §4.4「满足最小结果数、字符、token」的度量口径 | 字符按 **Unicode 码点**跨消息全部 text block（含嵌套 tool-result 文本）；token 用 `tokenMeter.estimateMessage`；组预算再被信封 `inputCapTokens` 收窄 | 参数差异 | `src/internal/compaction/tool-groups.ts:344-373`、`src/compaction-basic.ts:951-953`、`src/internal/compaction/envelope-budget.ts:101-104` | 与 §4.5 裁剪门槛同口径（码点）——这是实现增强：避免「选组用块数、裁剪用码点」的双口径。不构成风险 | 否 |
| **C1** | §3.7「不要求永久保存五种终态」——轮内分类用于精确构造操作②候选 | 未找到任何 `status: summarized\|notEligible\|oversized\|failed\|unchanged` 的类型或返回值（全仓 `src` 无该词汇） | 未找到（已由 A1 的等价机制承接） | grep `notEligible\|oversized\|unchanged\|classification` 在 `src/` 仅命中 `tool-groups.ts:187,200` 的分割函数注释；`summarizeToolGroups(): Promise<void>`（`compaction-basic.ts:1080-1088`） | 见 A1/A6 | 否 |
| **D1** | §8 静态/单元：操作①失败后操作②继续；操作①成功集合与操作②候选互斥 | 有逐节点/逐组落盘证据与 provenance 互斥证据，但**没有**端到端用例（现有引擎 harness 把 `summarizeToolGroups` 与 `hasPendingToolIntermediateWork` 打桩） | 需实验确认（测试覆盖缺口，非实现缺陷） | `tests/compaction-three-zone.spec.ts:340-353,560-573`（打桩）、`tests/compaction-tool-groups.spec.ts`（仅选择，无裁剪）、`tests/compaction-tool-group-replacement.spec.ts:95-139`（逐节点守卫） | 静态推演成立，缺少「一次 invocation 内先摘要再裁剪、候选集不含新 replacement」的观测断言 | 是 |
| **D2** | §4.4「整组完全位于工具压缩区」（跨界不得拆组） | 实现为位置区间全包含过滤：`startPosition < positions.start \|\| endPosition > positions.end → continue`（整组排除、绝不截断）。等价机制存在，但**无定向用例** | 需实验确认（测试覆盖缺口） | `src/internal/compaction/tool-groups.ts:68-76,331-342`；边界加宽见 `src/internal/compaction/zones.ts:350-367` | 跨界工具组既不属操作①也不属操作②（「无人区」），需轨迹统计确认其是否总会老化进单一区间（与调查资料 12 §5 Q4 同题） | 是 |
| **D3** | §4.5/§3.5「同轮 replacement 不进入操作③」 | 三重拦截：`roundReplacements`（4 处写入）+ 维护批次 `break` + 压力跨度回退 veto `same-pass-tool-replacement`；且压力跨度 `allowImmediateReentry=true` 会放开**历史 invocation** 的已知 replacement | 需实验确认（策略边界） | `src/compaction-basic.ts:456,554,645-659,1173,484,486,517,519`；`src/internal/compaction/source-index.ts:85-95`；正/反例测试 `tests/compaction-three-zone.spec.ts:530-582,610-644,705-794` | 70% 维护批次的 `break`（`:554`）**无定向用例**；`allowImmediateReentry` 允许同一 turn 内（`minReentryTurns=1` 未满足）把上一 invocation 的工具摘要立即折叠进历史摘要——与 §6.1「最早重入 turn」的字面约束不同，代码注释自述为有意放宽。需轨迹确认是否出现「摘要未送达即被二次压缩」 | 是 |
| **Z1** | — | `lib/compaction-basic.js` 与 `lib/tool-result-pruner.js` 承载与 `src` 同一设计（`isServedAudit`/`servedReplacementSeqs`/`isOriginalToolResult`/`roundReplacements`/`same-pass-tool-replacement`/`tool-stage-deferred`/`overflow-tool-zone` 均在 bundle 中） | 等价实现（构建产物核对通过） | `lib/compaction-basic.js:7160-7173,8671,9000-9021,9097,9119,9362-9386` | 无需实验；未发现 `lib` 与 `src` 设计分叉 | 否 |

---

## 3. 按任务清单逐项结论

| 清单项 | 结论 | 主证据 |
|---|---|---|
| 工具组完整性 | **等价实现**：配对平衡 + 同 turn burst + 最长平衡跨度 + 阈值 + 整组区间包含 | `tool-segments.ts:117-163`、`tool-groups.ts:55-184` |
| 区域约束 | **等价实现 + 参数差异**：普通维护严格用 `zones.tool`/`zones.forget`（`olderRange` 位置语义）；溢出不传 `olderRange`（B1） | `compaction-basic.ts:458-521,1019-1022` |
| 轮次五值 outcome | **确认差异（A1）；等价机制并存；`oversized/failed/unchanged` 未找到（C1）** | `compaction-basic.ts:1080-1088` |
| provenance 是否可仅凭 Session 恢复 | **确认差异（A2/A3）**：`history-summary`/`tool-pruned` 可；`tool-summary` 必须依赖 audit。安全底线（不误判 original、unknown 永不放开）满足 | `source-index.ts:98-110`、`served-provenance.spec.ts:210-211` |
| 摘要校验 | **部分等价 + 确认差异（A4）**：一一覆盖/callId/事实来源/严格缩减/落盘 provenance 全部实现；`unresolved` 与顶层 `groupErrors/unresolved/groupSummary` 不校验来源 | `tool-group-summary.ts:75-102` |
| 操作①/②互斥 | **等价实现（E7）**：靠 provenance 而非分类；逐结果粒度成立 | `compaction-basic.ts:471-488` |
| 失败降级 | **等价实现 + 参数差异（B3）**：audit 不可用 ⇒ 仅剩确定性裁剪；逐组隔离；落盘后失败绝不写终态；瞬时重试上限 1 次 | `compaction-basic.ts:245-250,1091-1093,1143-1188,1208-1237` |
| 裁剪候选 | **等价实现（E8/E9）**：3 个调用点显式候选集 + 成员校验；无「遍历全部 tool/result」常规路径 | `tool-result-pruner.ts:304-324` |
| hard limit | **参数差异（B2）**：默认关闭；`>= thresholdChars` 校验 | `pruner-config.ts:79-87` |
| audit 丢失语义 | **确认差异（A2/A3）**：不误判 original（符合 §3.7 底线），但类型判断确实改变（违反 §8 不变量第 5 条） | 同上 |
| 同轮排除 | **等价实现 + 需实验确认（D3）** | `compaction-basic.ts:456,554,645-659` |

---

## 4. Baseline Drift 检查

基准：`审计资料/工作区基线.json`（2026-09-11 10:00+08:00）。

| 项 | baseline | 本轮实测 | 判定 |
|---|---|---|---|
| HEAD commit | `cf034b4bce6141bb95b590f5ed7fa66f8727daa2` | 同 | 未漂移 |
| Modified 数 | 102 | 102 | 未漂移 |
| Untracked 数 | 31 | 32 | **+1，来源为新增审计产物目录 `审计资料/`，非源码/配置漂移** |
| 总 dirty 条目 | 133 | 134 | 同上（新增审计文档不计 drift） |
| `审计资料/` 内容 | `00/01/工作区基线.json` | 同 + 本轮 `11` + 同期并行专题 `10`、`13` | 均由审计会话新增；`git status` 只把该目录计为 1 项，故计数不受影响 |
| `package.json` | `20454835…02E9` | 同 | 未漂移 |
| `pnpm-lock.yaml` | `385431F7…35F4` | 同 | 未漂移 |
| `cordis.patch.yml` | `95E8C9C0…68FE` | 同 | 未漂移 |
| `理想化落地方案.md` | `12734F96…2435` | 同 | 未漂移 |
| `dsh-context-enhancement-0.1.10.tgz` | `BE9DE3DE…7E40` | 同 | 未漂移 |
| `src/index.ts` | `5F53AEAB…F20B` | 同 | 未漂移 |
| `src/compaction-basic.ts` | `5585C60D…653` | 同 | 未漂移 |
| `src/task-state-basic.ts` | `93F18075…F3C3` | 同 | 未漂移 |
| `src/tool-result-pruner.ts` | `BF126FF0…5DE1` | 同 | 未漂移 |
| `src/internal/compaction/config.ts` | `C8801BA4…F8E` | 同 | 未漂移 |
| `lib/index.js` | `921F8F66…0510` | 同 | 未漂移 |
| `lib/compaction-basic.js` | `4718F3AD…C0D1` | 同 | 未漂移 |
| `lib/task-state-basic.js` | `6BF1AD71…825A` | 同 | 未漂移 |
| `lib/tool-result-pruner.js` | `5E8D5CB5…1F1C9` | 同 | 未漂移 |

**结论：无源码/配置漂移。**唯一 delta 是本轮新落的审计文档，属允许的新增审计产物。
未执行任何修改/格式化/构建/测试/安装/还原操作；未派生子代理；未做运行观测（运行实例、watcher、用户 settings 均不在本轮范围）。

---

## 5. 供综合矩阵引用的差异 ID

- 确认差异：`TG-A1`（五值 outcome 缺失）、`TG-A2`（provenance 依赖 audit）、`TG-A3`（audit 兼作类型权威 + 单文档 + 无保留策略）、`TG-A4`（unresolved/groupErrors 来源校验缺口）、`TG-A5`（操作编号/轮 id/recovery level 未入 Session）、`TG-A6`（操作②候选不由本轮分类构造）。
- 参数差异：`TG-B1`（overflow 近区按普通门槛裁剪）、`TG-B2`（`hardLimitChars` 未配置、校验放宽为 `>=`）、`TG-B3`（`fallback` 永久终态无过期）、`TG-B4`（码点口径与信封收窄，实现增强）。
- 等价实现：`TG-E1`…`TG-E10`（见 §1），另 `TG-Z1`（lib/src 设计一致）。
- 需实验确认：`TG-D1`（①②接续无端到端用例）、`TG-D2`（跨界组「无人区」轨迹统计）、`TG-D3`（70% 批次 break 无用例 + `allowImmediateReentry` 同 turn 放宽）。
- 未找到：`TG-C1`（五值词汇不存在，由 A1 承接）。
- 构建产物差异：无。

术语与理想方案一致：以 §3.7/§4.4/§4.5/§6 为对照基准；对《压缩方案4》原文的偏差（如 U≥70% 时操作①②也处理遗忘区）属区域边界条款，已在其他专题登记，本文件只在 `olderRange` 层面引用其结果。
