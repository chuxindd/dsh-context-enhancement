# 01 插件装配与配置生效链（正式审计第一批）

> 范围：只建立"哪些代码和配置实际装配"的事实。不审业务算法（compaction/task-state 算法、存储内容、测试质量）。
> 证据标记：【已证实】= 已读具体文件与行号；【待运行确认】= 需真实 `$DSH_HOME` 装载或进程观测；【未找到】= 在限定范围内不存在。
> 工作区：`C:\Users\chuxi\Documents\trae_projects\code\dsh-context-enhancement`（git 有 133 项未提交变更）。
> 本轮未构建、未启动/未修改任何服务、未改源码、未派生子代理；只新增本文件。
> 已检查的实现/配置文件约 18 个（插件侧）+ DSH base/profile 一层配置（§3 的覆盖顺序证据）。
> 前置材料已读：`理想化落地方案.md` §7/§10、`调查资料/10-插件架构与DSH集成地图.md`、`调查资料/06-DSH插件钩子与可变更边界.md`（未重新调查 DSH）。

---

## 0. 结论速览

1. 运行时只引用 `lib/*.js`；`src/*.ts` 只在构建期进入（tsdown 输入是 `lib/types/*.js`，不是 `src/`）。【已证实】
2. 六个能力分两种平面：`task-state-basic` / `task-state-control` / effect projection 在**宿主平面**；`compaction-basic` / `tool-result-pruner` / `task-state-prompt` 在 **`contextual` preset 的 isolate realm**；client view 走 `./client` 子路径。【已证实】
3. **当前工作区 `lib/client.js` 的模块 id 是脚手架 id `"@deepseek-ai/dsh-client-ui-jobs"`**（`patch-client-id.mjs` 那一步的结果没落盘），而 HEAD 提交版与 `$DSH_HOME` 安装副本都是 `"dsh-context-enhancement"`。【已证实】这是第一批里最直接的一处静态装配事实。
4. **工作区 lib ≠ 已打好的 tgz ≠ `$DSH_HOME` 里的安装副本**，三者是三次不同构建：工作区用 `tool-groups-CJkqYVVc.js`，tgz 用 `tool-groups-l8pJ_H0h.js`，安装副本两者都没有（自包含旧构建）。【已证实-文件层】
5. 插件所有事件监听**都不带 `prepend`**，因此 `compaction-basic` 与 `llm-retry` 对 `agent/request-error` 的相对顺序无法由静态配置判定 → 见 §5，标【待运行确认】。
6. 本轮**不声称 GUI 实际加载已验证**：静态声明完整（`exports`/`dsh.client`/preset roots），但真实装载对象与进程状态未观测。

---

## 1. 入口事实：package export / bin / plugin / client

### 1.1 声明的事实【已证实】（`package.json`）

| 项 | 位置 | 值 |
|---|---|---|
| `main` | `package.json:25` | `./lib/index.js` |
| `types` | `package.json:26` | `./lib/types/index.d.ts` |
| `exports` | `package.json:27-62` | `.`、`./task-state`、`./task-state-basic`、`./task-state-control`、`./task-state-prompt`、`./compaction-basic`、`./tool-result-pruner`、`./client`、`./cordis.patch.yml`、`./package.json` |
| 每个子路径的运行时条件 | 同上 | 一律 `default: ./lib/<name>.js`；`types: ./lib/types/<name>.d.ts` |
| `files` | `package.json:63-84` | 逐一列出 `lib/*.js` 根 bundle + `lib/*.js` 通配 + `lib/types/**/*.d.ts` + `assets`、`cordis.patch.yml`、`presets`、`scripts/install-desktop-preset.mjs` |
| `sideEffects` | `package.json:85-92` | 六个 bundle：`task-state(-basic/-control/-prompt)`、`compaction-basic`、`tool-result-pruner` |
| `dsh.bundle.patch` | `package.json:107-110` | `./cordis.patch.yml` |
| `dsh.client` | `package.json:111-121` | `platform: web`，`inject` 6 个 client 包（locale / ui-conversation / ui-renderer / ui-session / ui-slots / api-gateway） |
| `build` / `build:lib` | `package.json:98-99` | `tsc -p tsconfig.build.json && tsdown --config-loader tsx && node scripts/patch-client-id.mjs` |

- `bin`：【未找到】`package.json` 无 `bin` 字段，插件不是 CLI 可执行包。
- npm `dependencies`（`package.json:123-138`）与 `peerDependencies`（139-153）对 12 个 `@deepseek-ai/*` 包同版本声明；`devDependencies`（154-200）额外含 `dsh-app-boot`、`dsh-agent-presets`、`dsh-tools`、`dsh-scope` 等，仅测试/构建用。

### 1.2 src 与 lib 谁被运行时引用【已证实】

- 运行时解析路径 = `exports` 的 `default` → **`lib/*.js` 全部**；`src/**` 不出现在任何 export 条件里。
- `src/*.ts` 的唯一引用点都在构建期：`tsdown.config.ts:7-15`（打包输入列表指向 `lib/types/*.js`）、`tsconfig.build.json`、`lib/client.js.map` 的 `sources`。
- 因此：**构建链是 `src/*.ts` --tsc--> `lib/types/*.js` --tsdown--> `lib/*.js`（+ `lib/client.js`）**。`lib/types/**/*.js` 不是运行时产物（见 §7.3）。

### 1.3 client 入口与一处静态不一致【已证实】

- 声明：`exports["./client"] → lib/client.js`（`package.json:56-59`）+ `dsh.client.platform: web`（111-121）。
- 形态：`lib/client.js:1-4` 是浏览器模块装载协议的 `window.__ModuleLoader__.load({ id, factory })`，不是 Node 模块（无相对 import；`require('react')`、`require('react/jsx-runtime')` 由宿主提供）。
- **不一致**：
  - 工作区 `lib/client.js:2` → `id: "@deepseek-ai/dsh-client-ui-jobs"`
  - `git show HEAD:lib/client.js:2` → `id: "dsh-context-enhancement"`
  - `$DSH_HOME/profiles/web/node_modules/dsh-context-enhancement/lib/client.js:2` → `id: "dsh-context-enhancement"`
  - `scripts/patch-client-id.mjs:5-8` 正是把 `@deepseek-ai/dsh-client-ui-jobs` 改写为 `dsh-context-enhancement` 的那一步；工作区当前产物中该改写未生效（`dsh-...-jobs` 出现 1 次，`dsh-context-enhancement` 另有 9 处，来自远程名与文案，不是 loader id）。
  - 影响面：若把当前工作区产物打包/装载，client 插件会以脚手架 id 注册；本批次不判定 GUI 行为，只记录静态事实。

---

## 2. 六个能力的注册方式与依赖

### 2.1 compaction（`compaction-basic`）【已证实】

- 装载点：`presets/contextual/agent.cordis.yml:168-186` 的 `compaction` group（`cordis:group`，`isolate: { compaction: true, toolResultPruner: true }`），行 `id: compaction-basic`、`name: dsh-context-enhancement/compaction-basic`（:175-176）。该行**没有 `config`**。
- 模块：`lib/compaction-basic.js` ← `src/compaction-basic.ts`。
  - 类 `BasicCompactionEngine extends CompactionEngine`（`src/compaction-basic.ts:207`），继承官方 rc.1 服务定义以被 Loader 识别为 `compaction` 服务提供者。
  - 依赖声明 `static inject = ['llm','tokenMeter','sessions','storageDomain']`（:214）。
  - `static Config` 为 schemastery `z.object`（:216-229）：`...policyFields` + `modelPolicies` + `auto` + `toolGroupSummarizer{}`。
  - 构造期 `resolveConfig(config)`（:244）并异步打开工具组摘要 audit 的 storage domain（:245-255，`ctx.effect` 上挂 disposer）。
  - `if (this.config.auto) this._registerAutomaticCompaction()`（:256）。
- 监听注册（`_registerAutomaticCompaction`，:264-351，**全部 `ctx.on(...)` 无 `prepend`**）：
  - `agent/pre-step`（:274）→ `compactIfNeeded(agent,'pressure')`，失败仅 warn 后 `next()`；
  - `agent/status`（:294）→ idle 清 overflow 计数；
  - `session/event`（:300）→ `assistant/message` 清 overflow 计数；
  - `agent/request-error`（:306）→ 仅当 `failure.code === CONTEXT_WINDOW_EXCEEDED_CODE` 时尝试恢复，成功后返回 `{kind:'retry'}`（:334/:349），否则 `next()`。
- 对 pruner 的依赖：`this.ctx.get('toolResultPruner')`（:437），并在 `zones.tool`/`zones.forget` 内调用 `prune.pruneSession(...)`（:478、:514）。**要求 pruner 与 compaction-basic 同 realm**（`agent.cordis.yml:171-174` 的 isolate 同时列出两个服务键）。

### 2.2 tool pruner（`tool-result-pruner`）【已证实】

- 装载点：同一个 `compaction` group 内（`presets/contextual/agent.cordis.yml:181-186`），显式 config：`thresholdChars: 8192`、`headChars: 4096`、`tailChars: 1024`（**没有 `hardLimitChars`**）。
- 模块：`lib/tool-result-pruner.js` ← `src/tool-result-pruner.ts`。
  - `class ToolResultPruner extends Service`，`super(ctx,'toolResultPruner')`（:68、:86）→ 以服务身份注册，**不注册任何事件监听**。
  - `static inject = ['tokenMeter']`（:71）（每个被遮蔽节点要写 shadow-price 事件）。
  - `static Config`（:73-80）：4 字段，均带 schemastery `.default(...)`；`hardLimitChars` 默认显式 `undefined` 以保留"未配置"语义。
  - 它是官方 rc.1 pruner 的重实现（模块头注释 :1-22 说明官方包无可扩展 provider 入口）。

### 2.3 task-state basic（provider）【已证实】

- 装载点：**宿主平面**，插件自身 patch `cordis.patch.yml:43-56`，行 `id: task-state-basic`，`name: dsh-context-enhancement/task-state-basic`，config **11 个字段全部显式给出**（provider/model/minEvents/maxEvents/maxInputBytes/maxOutputTokens/timeoutMs/maxInfraRetries/maxEntriesPerKind/maxEntryBytes/maxListItems）。
- 模块：`lib/task-state-basic.js` ← `src/task-state-basic.ts`（默认导出 `TaskStateBasicService`，:18-32）← `src/internal/task-state/basic/service.ts`。
  - `static inject = ['storageDomain','sessions','llm']`（`service.ts:99`）；`static Config` 11 键全部 `.required()`（:102-114）。
  - 构造期 `resolveTaskStateBasicConfig(config)`（:135）走独立校验（`internal/task-state/basic/config.ts:43-72`，拒绝未知键 :49-51）。
  - `[Service.init]`（:139-179）：先以 `session/created` **global** 观察者抓取开域期间创建的 Session（:145），`ctx.storageDomain.open(taskStateDomainSpec)`（:149）；开域失败即 `disabled`，不调用模型（:155-164）；`ctx.effect` 注册关闭时 drain worker + close domain（:167-179）。
  - 其余监听：`session/created`（:403）、`session/event`（:409）、`session/disposed`（:427）。
- 契约模块：`lib/task-state.js`（`exports["./task-state"]`）只提供 `TaskStateService` 服务定义与 durable schema/audit 助手（`src/task-state.ts:17-73`），**不注册任何监听、不装载任何行**；它是被 prompt consumer 与 control 共享的类型/模式词汇。

### 2.4 task-state prompt / control【已证实】

- **prompt consumer**：`presets/contextual/agent.cordis.yml:58-61`，`name: dsh-context-enhancement/task-state-prompt`，`config: { maxBytes: 8000 }`。
  - 模块 `lib/task-state-prompt.js` ← `src/task-state-prompt.ts`：**function 插件**（具名 `name`/`inject`/`Config`/`apply`，刻意无 default 导出，:9-11、:27、:34、:37-39）。
  - `inject = ['systemPrompt']`（:34）——**不注入 `taskState`**，因此没有 provider 也能干净装载；用 `ctx.get('taskState')?.getStable(sessionId)` 在每次 assembly 时实时读取（:69-71），provider 缺席即渲染空串。
  - 注册 `ctx.systemPrompt.context({ name:'task-state:snapshot', order:125, text:'{{task_state_snapshot}}' })`（:56-63）与同变量 provider（:64-72）。
- **control（浏览器桥）**：宿主平面 row `cordis.patch.yml:57-58`，**无 config**。
  - 模块 `lib/task-state-control.js` ← `src/task-state-control.ts`（2 行再导出）← `src/internal/task-state/control/service.ts`：`class TaskStateControlService extends TypertRemoteService`，`super(ctx,'taskStateControl',{namespace:'taskState'})`（:70-74），`static inject = ['sessions','taskState']`（:71）。
  - 依赖 host plane 的 provider：`ctx.effect` 内 `ctx.taskState.subscribeCommitted?.(...)`（:75-88），`@Remote({mode:'stream'}) control`（:93-96）、`@Remote('edit')`（:99-106）。
  - 浏览器端通过远程名 `dsh-context-enhancement/task-state-control` 打开流（`src/client/index.ts:37`）。

### 2.5 effect projection【已证实】

- 装载点：宿主平面 row `cordis.patch.yml:41-42`，`id: context-enhancement-ui`，`name: dsh-context-enhancement`（根导出）。
- 模块 `lib/index.js` ← `src/index.ts:7-11`：`export const inject = ['sessionProjections']`，`apply(ctx)` 只做一件事 —— `ctx.sessionProjections.register(contextEnhancementProjectionDefinition)`。
- 定义在 `src/effect-projection.ts:49-63`：`key:'contextEnhancement'`、`stateVersion:1`、zod `stateSchema`（strict，5 个非负整数）、`apply` 折叠 `request/header` / `compaction/prune` / `compaction/end`、`wire.view` 原样暴露。
- 该行没有 `config`；它与 client 端 `conversation.view`（§2.6）配对：projection 是浏览器可读的计数视图。

### 2.6 client view【已证实】

- 入口 `lib/client.js` ← `src/client/index.ts`：`inject = ['locale','slots','remote']`（:19），`apply`（:21-117）依次：
  1. `ctx.locale.register(NS,{zh,en})`（:22）；
  2. `ctx.effect` 内 `ctx.remote.$mount(taskStateRemoteContribution)`（:62）→ `ctx.inject(['remote.taskState'], ...)`（:67）→ 成功后 `mirror.start()`（:82），disposer 逆序卸载（:88-96）；
  3. `ctx.slots.inject('conversation.view', ...)` 注册 view `id:'contextEnhancement'`、`order:5`、`locale:NS`（:99-116），组件 `ContextEnhancementView`。
- 依赖：`remote` 名字面量 `dsh-context-enhancement/task-state-control`（:37）必须与 §2.4 的 control 服务对应；`TaskStateControlMirror` 与 `createTaskStateControlSessionSource` 来自 `src/client/task-state-control-store.ts`。

### 2.7 注册/依赖关系图（静态）

```text
宿主平面（profile patch 插入，cordis.patch.yml:40-58）
  context-enhancement-ui ──→ lib/index.js  ──→ sessionProjections.register(key=contextEnhancement)
  task-state-basic        ──→ lib/task-state-basic.js ──→ Service(ctx.taskState) + domain context_enhancement_task_state
  task-state-control      ──→ lib/task-state-control.js ──→ remote taskState{control,edit}；inject taskState

contextual preset 平面（agent.cordis.yml）
  task-state-prompt ──→ lib/task-state-prompt.js ──→ systemPrompt.context(order 125) ；ctx.get('taskState')
  compaction group [isolate compaction + toolResultPruner]
    compaction-basic     ──→ lib/compaction-basic.js ──→ Service(ctx.compaction)；hooks 4 个
    tool-result-pruner   ──→ lib/tool-result-pruner.js ──→ Service(ctx.toolResultPruner)；无 hook
    （compaction-basic 通过 ctx.get('toolResultPruner') 消费，故必须同 realm）
  command-compact ──→ 官方 @deepseek-ai/dsh-command-compact（未替换）

浏览器平面
  lib/client.js (./client, dsh.client) ──→ conversation.view + remote.taskState + locale
```

---

## 3. 默认配置来源、schema 归一化与覆盖顺序

### 3.1 装载顺序的直接一层证据【已证实】

- `$DSH_HOME/profiles/web/package.json:9-18`：`dsh.profile.bundles = ["@deepseek-ai/dsh-base", "@deepseek-ai/dsh-web-app", "dsh1024", "dsh-context-enhancement"]`，`patchReload: "live"`；依赖里插件是 `file:C://.../dsh-context-enhancement-0.1.10.tgz`（:5）。
- `$DSH_HOME/profiles/web/cordis.yml:1-4`：profile 根是空数组，树由 "每个 bundle 的 patch → `cordis.patch.yml` → 任意 `--patch` 覆盖" 组成。
- `$DSH_HOME/profiles/web/cordis.patch.yml:1-4`：**空数组**，即本部署没有额外的用户 patch 层。
- 因此静态顺序 = base 层 → web-app 层 → dsh1024 层 → 插件层 → （空 profile patch） → （未知 CLI --patch）。`dsh1024` 的 patch 仅 50 字节且不触及本插件的 seam【已证实】。
- web-app 层确实禁用了 base 的官方压缩行，与插件 patch 的注释一致：`@deepseek-ai/dsh-web-app/cordis.patch.yml:387-388`（`compaction-basic`）、`:390-391`（`command-compact`）、`:393-394`（`tool-result-pruner`）；官方行来源见 `@deepseek-ai/dsh-base/cordis.patch.yml:326-332`（token-meter/compaction-basic/command-compact）、`:400-401`（官方 tool-result-pruner）。

### 3.2 `agent-presets` 行的三层写入【已证实】

| 层 | 位置 | 内容 |
|---|---|---|
| 提供行 | `dsh-web-app/cordis.patch.yml:440-444` | `id: agent-presets`，`config.default: standard` |
| 插件覆盖 | `cordis.patch.yml:62-71` | `default: contextual`、`includeShippedRoot: true`、`includeUserRoot: true`、两个 `roots`（`dshHomePath('profiles','node_modules','dsh-context-enhancement','presets')` 与基于 `ctx.baseUrl` 的 profile 局部 `node_modules`，均 `trust: system`） |
| 用户设置 | `$DSH_HOME/settings.yaml:127-128` | `agent-presets: { default: contextual }`（settings-file 行热加载） |

- 两个 roots 中，本部署实测**只有第二个存在**：`$DSH_HOME/profiles/node_modules/dsh-context-enhancement` **不存在**【已证实】，插件实际装在 `$DSH_HOME/profiles/web/node_modules/dsh-context-enhancement`。第一个 root 在本部署解析为不存在的路径。
- patch 语义补充（来自 DSH 配置注释，非本轮新调查）：补丁替换整行 `config` 而不是合并（`dsh-base/cordis.patch.yml:6-7`）。

### 3.3 schema 与归一化方式【已证实】

- `compaction-basic`：schemastery `z.object`，**所有字段可选且无 `.default()`**（`src/compaction-basic.ts:216-229`）→ 未配置键保持 `undefined`，由 `resolveConfig` 内部常量兜底（`src/internal/compaction/config.ts:53-80`、`126-176`）。额外做未知键拒绝（:271）、比值/预算关系校验（:194-215）、`TargetPressureConfigError` 分类错误（:47-49）。
- `task-state-basic`：schemastery 11 键全 `.required()`（`service.ts:102-114`）+ 直接构造时的 `resolveTaskStateBasicConfig`（未知键/正整数/`maxEvents >= minEvents`，`config.ts:43-72`）。**没有任何仓库默认值**（模块头注释 :1-4 明示"每个字段都由 composition 显式给出"）。
- `task-state-prompt`：仅 `maxBytes`，`.required()`（`src/task-state-prompt.ts:37-39`）。
- `tool-result-pruner`：4 键均 `.default(DEFAULTS.x)`（`src/tool-result-pruner.ts:73-80`），`resolveConfig` 再做 `head+marker+tail <= threshold` 与 `hardLimitChars >= threshold` 校验（`src/internal/compaction/pruner-config.ts:66-84`）。
- 根导出与 projection 无 config。

### 3.4 值级覆盖顺序（静态）

```text
插件代码内常量默认（resolveConfig / DEFAULTS）
  ↑ 被 preset 行 config 覆盖（task-state-prompt.maxBytes=8000；tool-result-pruner 三个 chars）
  ↑ 被插件 bundle patch 行 config 覆盖（task-state-basic 11 字段；agent-presets.default）
  ↑ 被 profile cordis.patch.yml 覆盖（本部署：空）
  ↑ 被 CLI --patch / 启动参数覆盖（静态不可知 → §9）
  ↑ 被 $DSH_HOME/settings.yaml 覆盖（仅对支持 settings 的行；本文件含 agent-presets、llm-deepseek、llm-pi-ai、agent-default-model 等段）
```

注：`settings.yaml` 作用于哪些行、以何优先级覆盖 patch，属 DSH `settings-file` 行为；本轮只证实该文件存在对应段并且**与插件 patch 同值**（`default: contextual`），没有观察到冲突值。

---

## 4. 静态可证的最终默认值

### 4.1 compaction-basic【已证实】

`compaction-basic` 行无 config（`agent.cordis.yml:175-176`），插件 patch 也未对 `compaction-basic` 行做任何 config 写入（`cordis.patch.yml` 仅覆盖 `agent-presets`）⇒ 生效的是 `resolveConfig({})` 结果：

| 字段 | 静态默认 | 来源 |
|---|---|---|
| `auto` | `true` | `config.ts:69` |
| `pressureRatio`（= `thresholdRatio`） | `0.80` | `config.ts:16`、`:134` |
| `recentRatio`（=`retainRatio`） | `0.20` | `config.ts:17`、`:135` |
| `forgetBoundaryRatio` | `0.50` | `config.ts:18` |
| `toolMaintenanceRatio` | `0.40` | `config.ts:19` |
| `forgetMaintenanceRatio` | `0.70` | `config.ts:20` |
| `targetBatchTokens` / `maxBatchTokens` | `16000` / `24000` | `config.ts:21-22` |
| `maxMaintenanceBatches` | `1` | `config.ts:23` |
| `maxPressureBatches` | `2` | `config.ts:24`（`compactionRetries` 未设时） |
| `minReentryTurns` | `1` | `config.ts:25` |
| `responseReserveTokens` / `safetyMarginTokens` | `8192` / `2048` | `config.ts:26-27` |
| `maxTokens`（摘要输出） | `8192` | `config.ts:167` |
| `compactionRetries` / `maxOverflowRetries` | `1` / `1` | `config.ts:168-169` |
| `summarizationProvider` / `summarizationModel` | `''` / `''`（空对 ⇒ 复用对话自身 route） | `config.ts:165-166` |
| `modelPolicies` | `[]` | `config.ts:218`、`:62` |
| `toolGroupSummarizer.enabled` | `true` | `config.ts:71` |
| `minGroupResults` / `minGroupChars` / `minGroupTokens` | `2` / `12000` / `2000` | `config.ts:72-74` |
| `maxGroupTokens` / `maxGroupsPerPass` / `maxSummaryTokens` | `12000` / `2` / `1200` | `config.ts:75-77` |

### 4.2 tool-result-pruner【已证实】

preset 显式：`thresholdChars 8192`、`headChars 4096`、`tailChars 1024`（`agent.cordis.yml:184-186`）；`hardLimitChars` **未配置 ⇒ `undefined`（该上限关闭）**（`pruner-config.ts:26`、`:64`；`src/tool-result-pruner.ts:137-138` 在 undefined 时直接返回 `null`）。

### 4.3 task-state-basic【已证实】

`cordis.patch.yml:45-56` 的显式值即静态最终值：`provider deepseek-official`、`model deepseek-v4-flash`、`minEvents 20`、`maxEvents 200`、`maxInputBytes 60000`、`maxOutputTokens 4000`、`timeoutMs 120000`、`maxInfraRetries 2`、`maxEntriesPerKind 50`、`maxEntryBytes 4000`、`maxListItems 40`。

### 4.4 与《理想化落地方案》§7 基线的字面比对（仅静态事实，不做算法裁决）

- 一致项：`recentRatio 0.20`、`forgetBoundaryRatio 0.50`、`toolMaintenanceRatio 0.40`、`forgetMaintenanceRatio 0.70`、`pressureRatio 0.80`、`targetBatchTokens 16000`、`maxBatchTokens 24000`、`maxMaintenanceBatches 1`、`maxPressureBatches 2`、`minReentryTurns 1`、`thresholdChars/headChars/tailChars 8192/4096/1024`、task-state 的 `minEvents 20 / maxEvents 200 / maxInputBytes 60000 / maxOutputTokens 4000 / timeoutMs 120000 / maxInfraRetries 2`。
- 【未找到】在插件 schema / 默认常量 / preset 配置中：`minNetReleaseTokens`、`minNetReleaseRatio`、`stableInjectionMaxTokens`、`urgentDebounceMs`、`startupBacklogUpdate`、`multiInstanceMode`。另外方案 §7 的 `hardLimitChars`（"必须配置模型安全值"）在当前 preset 中**未配置**。
- 【待运行确认】`contextWindow`：`resolveCompactSpec` 要求正整数的 adapter `contextWindow`（`config.ts:92-96`），否则抛 `TargetPressureConfigError`。它来自运行时 routed model 的 adapter metadata；`settings.yaml` 中 `llm-deepseek` 的模型声明了 `contextWindow: 1000000`（:135/142/147），而 `agent-default-model` 指向 `deepseek/meta/muse-spark-1.3-contributor`（:3-5），该 provider 段未声明 contextWindow。**真实会话落到哪个 route、metadata 取值必须运行观测**。

---

## 5. hooks 的实际注册模块与顺序（重点：`agent/request-error`）

### 5.1 插件内 hooks 全清单【已证实】

| 模块 | 事件 | 注册方式 | 行 |
|---|---|---|---|
| `src/compaction-basic.ts` | `agent/pre-step` | `ctx.on(...)`，无 prepend | :274 |
| `src/compaction-basic.ts` | `agent/status` | `ctx.on`，无 prepend | :294 |
| `src/compaction-basic.ts` | `session/event` | `ctx.on`，无 prepend | :300 |
| `src/compaction-basic.ts` | `agent/request-error` | `ctx.on`，无 prepend | :306 |
| `src/internal/task-state/basic/service.ts` | `session/created`（`{global:true}`） | `ctx.on` | :145 |
| 同上 | `session/created` | `ctx.on` | :403 |
| 同上 | `session/event` | `ctx.on` | :409 |
| 同上 | `session/disposed` | `ctx.on` | :427 |
| `src/task-state-prompt.ts` | 非事件：`systemPrompt.context` / `.variable` | :56、:64 |
| `src/tool-result-pruner.ts`、`src/internal/task-state/control/service.ts`、`src/index.ts`、`src/client/index.ts` | 无事件监听（Services / projection register / `@Remote` / slots） | — |

- 全仓库 `src` 内检索 `prepend`：**0 处**【已证实】。即插件不主动占据 waterfall 外层。

### 5.2 `compaction-basic` 与 `llm-retry` 对 `agent/request-error` 的顺序

事实分层（避免把静态与运行混谈）：

1. 【已证实】`llm-retry` 由 base bundle 提供：`dsh-base/cordis.patch.yml:84-85`，行 `id: llm-retry`、`name: @deepseek-ai/dsh-llm-retry`，**该行没有 config**。web-app 层与插件层都没有触碰该行；`settings.yaml` 中也**没有** `llm-retry` 段【已证实】。
2. 【已证实】插件的 `compaction-basic` 行位于 `contextual` preset 的 isolate group 内（`agent.cordis.yml:168-176`），而 preset 由 `agent-presets` roster 挂载（web-app 行 + 插件 patch 把 default 指向 `contextual`）。
3. 【已证实】两者注册 `agent/request-error` 时都**不带 `prepend`**：插件侧见 §5.1；DSH 侧见 `调查资料/06` Q4（`compaction/compaction-basic/src/index.ts:180`、`llm/llm-retry/src/index.ts:243`）。
4. 【已证实】base patch 明确声明"**行顺序不携带装载语义（activation is service-availability driven）**"（`dsh-base/cordis.patch.yml:12-13`）。因此**不能用 yml 行序或 bundle 顺序推出二者相对位置**。
5. 由此：
   - 默认 profile（无人为覆盖）下的实际先后 → 【待运行确认】。Cordis `waterfall` 的语义是"先注册者在外层；外层不调 `next()` 即否决内层与内建"（`调查资料/06` Q4）。
   - 静态可证的安全性质：默认 `retryableCodes` **不含** `CONTEXT_WINDOW_EXCEEDED`（`调查资料/06` Q4 引用 `llm/llm/src/retry-policy.ts:18-24`），故 normal 模式下无论谁在外层，`llm-retry` 都会 `next()`，插件的 overflow 恢复仍能被调用。
   - 静态可证的失败模式：**若**部署把 `CONTEXT_WINDOW_EXCEEDED` 配进 `retryableCodes` **且** `llm-retry` 位于外层，则同一超限请求会被直接重试而永不进入插件的恢复路径（`调查资料/06` Q4 已给结论）。本轮未发现任何把该 code 配入的证据（base 行无 config、web-app/插件/dsh1024 未改该行、`settings.yaml` 无该段）→ 记为【未找到 · 待运行确认】，因为 CLI `--patch`、settings 热加载后的边界层不在静态可见范围。
   - 同时注意：宿主行与 preset 行可能在不同时刻激活（roster 何时挂载 `contextual` 未在限定范围内证实），这本身也会改变先后。

---

## 6. HMR / reload 与 client 热更新（只陈述已知 DSH 事实 + 当前静态配置）

- 服务端模块级 HMR 行存在但**默认关闭**：`dsh-base/cordis.patch.yml:21-25`，`id: hmr`、`name: @deepseek-ai/cordis-plugin-hmr`、`disabled: true`；在 web-app 层与插件层**未检索到重新启用该行**【已证实-未找到】。
- 配置层热加载是启用的：`$DSH_HOME/profiles/web/package.json:17` `patchReload: "live"`；base 注释说明该模式"使用 launcher 的 watch-only 回退，不要求 hmr 行"（`dsh-base/cordis.patch.yml:19-20`）。
- 卸载/重装的机制（DSH 已知事实，本轮未重新调查）：监听器由 `fiber.effect` 承载、随 owning fiber 卸载注销；`Service` 构造即 `provide`、随 fiber 注销；已开始的 waterfall 链无法被卸载撤回（`调查资料/06` Q7）。
- 插件自身**没有**任何 `restart()`/`update()`/自建 reload 入口【未找到】；卸载重装入口只可能在 DSH loader / profile patch reload / 宿主重启一层。`cordis.patch.yml` 是纯声明式 patch，不含钩子代码。
- client plugin 静态事实：入口 `exports["./client"] → lib/client.js`（`package.json:56-59`）、`dsh.client.platform: "web"` 与 inject 列表（:111-121）、文件形态为 `window.__ModuleLoader__.load(...)`（`lib/client.js:1-4`）。
- 已知 DSH 事实（来自本会话环境说明，非本轮新调查）：client-plugin 变更只有在 `pnpm run dev:web` 同时运行时才免刷新热更新；否则必须重建对应 Web 产物并刷新页面；`apps/web` 的 Vite 入口不是独立应用，只有 `dsh web` 注入 `window.__DSH_BOOT__`。
- 本轮**没有**做进程观测（是否有 dev:web watcher、宿主启动时间、实际加载的 bundle realpath）⇒ 未验证 GUI 实际加载，见 §9。

---

## 7. `lib` 的引用链与残留 chunk 判定

### 7.1 工作区 lib 引用图（逐条来自各文件首行 import）【已证实】

```text
exports "."                 → lib/index.js              → ./schemas-B9RBVgB9.js → ./rolldown-runtime-8H4AJuhK.js
exports "./task-state"      → lib/task-state.js         → ./contract-BuVHI3zF.js → ./schemas-B9RBVgB9.js
exports "./task-state-basic"→ lib/task-state-basic.js   → ./contract-BuVHI3zF.js, ./lib-Bj3jGSND.js,
                                                            ./lib-DXy8Ramy.js, ./lib-biAw7Hvg.js
exports "./task-state-control" → lib/task-state-control.js → ./lib-DkAdRvjl.js
exports "./task-state-prompt"  → lib/task-state-prompt.js  → ./lib-Bj3jGSND.js
exports "./compaction-basic"   → lib/compaction-basic.js   → ./rolldown-runtime-8H4AJuhK.js,
                                  ./schemas-B9RBVgB9.js, ./lib-Bj3jGSND.js, ./lib-DXy8Ramy.js,
                                  ./lib-biAw7Hvg.js, ./tool-groups-CJkqYVVc.js
exports "./tool-result-pruner" → lib/tool-result-pruner.js → ./lib-Bj3jGSND.js, ./lib-DXy8Ramy.js,
                                  ./tool-groups-CJkqYVVc.js
exports "./client"             → lib/client.js             → （无相对 import；自包含浏览器 bundle）
普通 chunk: lib-DXy8Ramy.js → ./lib-Bj3jGSND.js, ./lib-DkAdRvjl.js
            lib-biAw7Hvg.js → ./lib-Bj3jGSND.js
            lib-Bj3jGSND.js、lib-DkAdRvjl.js、schema/runtime chunk：无内部 chunk 依赖（仅外部包/node:）
```

### 7.2 残留 chunk 判定（按引用链，不按时间）【已证实】

- `lib/tool-groups-l8pJ_H0h.js`（25231 B）：**工作区内零入边** —— `lib/*.js` 中没有任何文件 import 它；除自身外唯一命中是 `调查资料/11-...md:148` 的文字描述。**工作区视角：孤儿残留**。
- 但同一个 chunk 在**已打包产物**里是活的：`dsh-context-enhancement-0.1.10.tgz` 中 `package/lib/compaction-basic.js:6` 与 `package/lib/tool-result-pruner.js:3` 都 import `./tool-groups-l8pJ_H0h.js`，且 tgz 内**不含** `tool-groups-CJkqYVVc.js`。**tgz 视角：l8pJ_H0h 是唯一被引用的共享 chunk**。
- 两个 chunk 在 git 中都是**未跟踪**（`?? lib/tool-groups-CJkqYVVc.js`、`?? lib/tool-groups-l8pJ_H0h.js`），因此 git 不能裁决哪个属于"当前构建"；只有 import 图能裁决：**工作区当前构建引用 `CJkqYVVc`，`l8pJ_H0h` 在工作区已无入边**。
- `files: "lib/*.js"`（`package.json:72`）会把**两者都**打进包（tgz 清单已确认 `l8pJ_H0h` 在包内）。即：发布物会把工作区的孤儿 chunk 一并发布。

### 7.3 `lib/types/**` 的可达性【已证实】

- `exports` 的 `types` 条件一律指向 `lib/types/*.d.ts`；**没有任何 export 条件指向 `lib/types/**/*.js`**。`files` 只发布 `lib/types/**/*.d.ts`（`package.json:73`）。
- 实测安装副本 `$DSH_HOME/profiles/web/node_modules/dsh-context-enhancement/lib/types` 内**只有 54 个 `.d.ts`，没有 `.js`** —— 证明 `lib/types/**/*.js` 只是 tsc 中间产物（且是 tsdown 的输入：`tsdown.config.ts:7-15`），运行时不可达。
- 因此 Q7 的答案是：**多份 chunk 中只有工作区的 `tool-groups-l8pJ_H0h.js` 无入边；`lib/types/**/*.js` 整体运行时不可达（构建输入而非运行产物）**。

### 7.4 三份"lib"的差异（重要装配事实）【已证实-文件层】

| 对象 | 位置 / 时间 | 与工作区差异 |
|---|---|---|
| 工作区构建 | `.` 仓库 `lib/`，`lib/client.js` 2026/9/11 9 时 | 基准；client loader id = 脚手架 id |
| 已打包 artifact | `dsh-context-enhancement-0.1.10.tgz`（仓库根，2026/9/10 20:51，git 标记 M） | 打包时构建：用 `tool-groups-l8pJ_H0h.js`；profile patch/presets/lib/index.js 与工作区一致 |
| 安装副本（宿主实际用） | `$DSH_HOME/profiles/web/node_modules/dsh-context-enhancement`（目录 2026/9/8 15:12，真实目录非 symlink；来自 `profiles/web/package.json:5` 的 `file:` tgz 依赖） | `lib/compaction-basic.js`、`lib/client.js`、`lib/task-state-basic.js`、`lib/tool-result-pruner.js` 与 `package.json` **哈希不同**；`lib/` 内**没有** `tool-groups-*.js`（自包含旧构建）；`cordis.patch.yml`、`presets/contextual/*`、`lib/index.js`、`lib/types/index.d.ts` 哈希相同；client loader id = `dsh-context-enhancement` |

- 静态可得结论：**当前仓库工作区并不是宿主装配的字节来源**；宿主读取的是 profile 局部 `node_modules` 里的 tgz 安装副本，而该副本早于工作区 tgz 的打包时间（2026/9/8 < 2026/9/10）。
- 但"宿主当前进程加载的是哪一份、是否在 9/8 之后重新 install/restart"不能由文件推断 ⇒ 【待运行确认】（观测 loader 解析到的 realpath 与宿主启动时间）。
- 附带【已证实】：`scripts/patch-client-id.mjs:3` 用的相对路径是 `../lib/client.js`，"工作区产物缺 id 改写"与"安装副本有改写"这两件事同时成立，说明安装副本来自一次 patch 步骤成功的构建。

---

## 8. 配置字段 → schema/default → profile override → 最终静态默认 → 消费模块

| 配置字段 | schema / 默认（代码） | preset 行 override（`agent.cordis.yml`） | 插件 bundle patch（`cordis.patch.yml`） | 用户/家目录（`settings.yaml`、profile patch） | 最终静态默认 | 消费模块（消费点行号） |
|---|---|---|---|---|---|---|
| `compaction.*`（20 policy 键） | schemastery 全可选、无 `.default`（`src/compaction-basic.ts:216-219`）；常量兜底 `config.ts:16-27,134-171` | 不动（`compaction-basic` 行无 config，:175-176） | 不动 | 未发现 | §4.1 表（0.80/0.20/0.50/0.40/0.70、16000/24000、1/2、1、8192/2048、maxTokens 8192、retries 1/1、summarizationProvider/Model `''`） | `compactIfNeeded`（`src/compaction-basic.ts:425-717`）、`envelopeBudget`（:929-944）、`zones`（:886-902） |
| `compaction.modelPolicies[]` | 数组，元素需 provider+model 必填（`src/compaction-basic.ts:188-192`、`config.ts:217-233`） | 不动 | 不动 | 未发现 | `[]` | `resolveTargetPolicy`（`config.ts:82-90`，被 :314/:432/:371 调用） |
| `compaction.auto` | boolean，缺省 `true`（`config.ts:69`） | 不动 | 不动 | 未发现 | `true`（四个 hook 因此注册） | `_registerAutomaticCompaction`（`src/compaction-basic.ts:256`） |
| `compaction.toolGroupSummarizer.{enabled,minGroupResults,minGroupChars,minGroupTokens,maxGroupTokens,maxGroupsPerPass,maxSummaryTokens}` | 7 键，缺省 `true/2/12000/2000/12000/2/1200`（`config.ts:70-78`） | 不动 | 不动 | 未发现 | 同上 | `toolGroupSelectionOptions`（`src/compaction-basic.ts:853-883`）、`hasPendingToolIntermediateWork`（:787） |
| `toolResultPruner.thresholdChars` | `.default(8192)`（`src/tool-result-pruner.ts:74`） | `8192`（`agent.cordis.yml:184`） | 不动 | 未发现 | `8192` | `pruneContent`（:124）、`hasPendingToolIntermediateWork`（`compaction-basic.ts:782`） |
| `toolResultPruner.headChars` / `tailChars` | `.default(4096)` / `.default(1024)`（:75-76） | `4096` / `1024`（:185-186） | 不动 | 未发现 | `4096` / `1024` | `resolveConfig`（`pruner-config.ts:61-63`） |
| `toolResultPruner.hardLimitChars` | `.default(undefined)`（:79） | **未配置** | 不动 | 未发现 | `undefined`（近区硬上限关闭） | `pruneRecentContent`（:136-139） |
| `taskStateBasic` 11 键 | schemastery 全 `.required()`（`service.ts:102-114`）+ `resolveTaskStateBasicConfig`（`config.ts:43-72`） | 不适用（宿主平面行） | **全部显式**（`cordis.patch.yml:45-56`） | 未发现 | `deepseek-official/deepseek-v4-flash`、20/200/60000/4000/120000/2/50/4000/40 | `TaskStateBasicService`（`service.ts:133-179`、:403-427） |
| `taskStatePrompt.maxBytes` | `.required()`（`src/task-state-prompt.ts:38`） | `8000`（`agent.cordis.yml:61`） | 不适用 | 未发现 | `8000` | `renderTaskStateSnapshot`（`src/task-state-prompt.ts:71`） |
| `agentPresets.default` | 由 DSH 行定义 | 不适用（roster 行在 host） | `contextual`（`cordis.patch.yml:64`） | `settings.yaml:128` 亦为 `contextual`（同值） | `contextual`（两层一致） | `@deepseek-ai/dsh-agent-presets`（决定 `contextual` preset 是否挂载） |
| `agentPresets.includeShippedRoot/includeUserRoot/roots[2]` | 由 DSH 行定义 | 不适用 | `true/true` + 两个 `trust: system` root（:65-71） | 未发现 | 两 root 声明存在；本部署**第一个 root 路径不存在**、第二个（`ctx.baseUrl` 相对）存在 | `dsh-agent-presets` 的 preset 发现 |
| `client`（`platform`/`inject`） | `package.json` 静态声明（:111-121） | 不适用 | 不适用 | 未发现 | `platform: web` + 6 个 inject 包 | `lib/client.js` → `ctx.locale/slots/remote`（`src/client/index.ts:19-22`） |
| projection（`key/stateVersion/stateSchema`） | 代码常量（`src/effect-projection.ts:49-63`） | 不适用 | 不适用 | 未发现 | `contextEnhancement` v1 | `sessionProjections.register`（`src/index.ts:10`） |

---

## 9. 必须到真实 `$DSH_HOME` / 进程观测才能确定的项（留给后续实验）

1. **宿主实际加载的插件字节**：loader 解析到的 realpath 是 `$DSH_HOME/profiles/web/node_modules/dsh-context-enhancement/lib/*.js`（旧构建）还是其它副本；宿主启动时间是否晚于 2026/9/10 的 tgz 重打包（§7.4）。
2. **`agent/request-error` 的实际 listener 顺序**：`llm-retry` 与插件 `compaction-basic` 谁在外层（§5.2；静态不可判）。以及部署是否把 `CONTEXT_WINDOW_EXCEEDED` 配进 `llm-retry.retryableCodes`（含 CLI `--patch` 与 settings 热加载后的最终值）。
3. **`contextual` preset 的实际挂载时机**：roster 是否在启动期即挂载（影响上述顺序与 prompt consumer 的出现时间）。
4. **路由模型与 `contextWindow`**：会话实际 route（provider/model）与该 adapter 的 `contextWindow` metadata，决定 `resolveCompactSpec` 是否抛 `TargetPressureConfigError`（`config.ts:92-96,445-447`）。
5. **`settings.yaml` 与 patch 的最终合并结果**：`agent-presets.default` 当前两层同值，但热加载后的权威值、以及是否还有 `--patch`/项目级覆盖，需要看进程内行配置（例如配置目录或 `/cordis` 类诊断面）。
6. **client plugin 是否真的被 Web 装载**：浏览器侧 `__ModuleLoader__` 是否注册了 `dsh-context-enhancement`（工作区产物当前 id 错误，若装载该产物会以 `@deepseek-ai/dsh-client-ui-jobs` 注册）。
7. **`pnpm run dev:web` watcher 是否在跑**：决定 client-plugin 变更能否免刷新；以及宿主是否已重启以消费新构建。
8. **profile patch 层是否仍为空**：本轮读到的 `profiles/web/cordis.patch.yml` 是空数组，需在运行中确认没有其他 patch 叠加（`patchReload: live` 意味着它可能被热改）。

---

## 10. 本轮证据清单（文件级）

- 插件：`package.json`、`cordis.patch.yml`、`presets/contextual/{agent.cordis.yml,preset.yml}`、`src/index.ts`、`src/effect-projection.ts`、`src/compaction-basic.ts`、`src/tool-result-pruner.ts`、`src/internal/compaction/config.ts`、`src/internal/compaction/pruner-config.ts`（grep）、`src/task-state.ts`、`src/task-state-basic.ts`、`src/task-state-prompt.ts`、`src/task-state-control.ts`、`src/internal/task-state/basic/{service.ts,config.ts}`、`src/internal/task-state/control/service.ts`、`src/client/index.ts`、`tsdown.config.ts`、`scripts/patch-client-id.mjs`、`lib/` 全部根 bundle（首行 import / loader id / 哈希）。
- DSH 一层：`$DSH_HOME/profiles/web/{package.json,cordis.yml,cordis.patch.yml}`、`$DSH_HOME/profiles/node_modules/@deepseek-ai/dsh-base/cordis.patch.yml`（:12-13、:19-25、:84-85、:326-332、:400-401）、`.../dsh-web-app/cordis.patch.yml`（:387-394、:440-444）、`.../dsh1024/cordis.patch.yml`、`$DSH_HOME/settings.yaml`。
- 产物：`dsh-context-enhancement-0.1.10.tgz` 成员清单与 `package/lib/{compaction-basic,tool-result-pruner,index,task-state-basic,client}.js` 首行 import。

**未做的事**：未构建、未启动服务、未修改源码或任何既有文件（仅新增本文件）、未派生子代理、未做进程观测、未重新调查 DSH。因此本文件不声称 GUI 实际加载已验证。
