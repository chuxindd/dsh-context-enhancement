# B3 主代理验收

## 结论

B3 单实例 pressure 修复验收通过。该结论不包含 B1 多实例安全；B1 继续为 `blocked-upstream`，E10 继续为 `reproduced`。

## 验收范围

- pressure exit line；
- 普通 pressure 同 invocation 有界多批次；
- low-yield 双门槛与 typed stop；
- same-pass replacement 隔离；
- prior checkpoint 有条件 reentry；
- retained tail、step/tool pairing、envelope cap；
- 修复后 E03 抖动对照。

## 接受的生产与测试改动

主要生产文件：

- `src/compaction-basic.ts`
- `src/internal/compaction/config.ts`
- `src/internal/compaction/types.ts`
- `src/internal/compaction/zones.ts`（继承半成品；最终 B3.2 未继续修改）

新增/更新测试：

- `tests/compaction-pressure-exit.spec.ts`
- `tests/compaction-pressure-low-yield.spec.ts`
- `tests/compaction-pressure-reentry.spec.ts`
- `tests/compaction-envelope-budget.spec.ts`
- `tests/compaction-three-zone.spec.ts`
- `tests/compaction-reentry-liveness.spec.ts`

实施证据：

- `审计资料/B3-变更清单.md`
- `审计资料/B3-并发写入事故记录.md`
- `审计资料/B3.1-实施记录.md`
- `审计资料/B3.2-实施记录.md`
- `审计资料/实验结果/B3-E03修复后回归/result.json`
- `审计资料/实验结果/B3-E03修复后回归/ledger.json`

## 主代理独立验证

- `pnpm run typecheck`：exit 0；
- B3 相关 6 suite：97/97 通过；
- 子代理最终全量：427/428 通过；唯一失败为既有 artifact parity；
- 修复后 E03：1/1 通过，verdict=`fixed`，sourceDrift=false。

## E03 前后对比

| 指标 | 修复前 | 修复后 |
|---|---:|---:|
| thrashStreak | 5 | 1 |
| headroomGapStreak | 5 | 1 |
| pressure after | 78K–79K | 68K–69K |
| pressure headroom | 1K–2K | 11K–12K |
| pressure typed stop | 无 | `pressure-exit` |
| 每次新增一个 tool result 即压缩 | 连续复现 | 不再连续复现 |

修复后 fixture 通常需要累计 2–3 个典型 tool result 才重新达到 pressure tier；原判据 `thrashStreak>=3` 不再成立。

## 保留限制

- token 仍为 fixture token，不等于 provider token；
- 未在真实 GUI/生产会话统计发生率；
- maintenance 70%–80% 档的策略仍需与后续 Goal/Stable 预算共同观察；
- production ledger 的长期持久化与 Web 展示不在本批；
- artifact parity 未修；
- 多实例 storage P0 未修。

## 最终状态

```text
B3 = completed (single-instance pressure semantics)
E03 controlled symptom = fixed
B1 = blocked-upstream
E10 = reproduced
artifact parity = failing, unchanged
```
