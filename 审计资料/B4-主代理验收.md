# B4 主代理验收

## 结论

B4 验收通过（单实例安全语义）：startup backlog、Goal/TODO 权威替换、显式清空、urgent trigger、infeasible-window 终态与 recovery provenance 均已实现并有修复后对照证据。B4.4 采用 clean-break v2 domain，使无 stable 的 terminal/quarantine 也能持久化且不伪造 stable。

## 主代理独立验证

- authority + startup + restart + worker：55/55 通过；
- 全部 `tests/task-state-*.spec.ts`：23 文件，253/253 通过；
- `pnpm run typecheck`：exit 0。

子代理全量验证：471/472 通过，唯一失败为既有 artifact parity。

## B4.1

验收记录：`审计资料/B4.1-主代理验收.md`。

E07 修复后：

- 无新事件时 startup wave 自动启动；
- 首次观察样本约 100 ms；
- backlog `[6,7,8]` 单独折叠；
- trigger=`startup`；
- baseline rev1 与 startup rev2 分离发布。

## B4.2

实现记录：`审计资料/B4.2-实施记录.md`。

接受的不变量：

- Goal/TODO 为 Host 拥有的权威命名视图；
- 最新 Goal/TODO 事实替换旧视图；
- `todo/write({todos: []})` 是合法 cleared 事实；
- Goal clear/TODO clear 不会被旧 candidate/reference 复活；
- 实质权威变化低于 `minEvents` 也触发异步 urgent wave；
- audit 可区分 `startup|threshold|urgent|trailing|manual`；
- 同步 session/event 栈零模型/零存储；
- 缺 schema v2 authority views 的旧记录 fail closed，不迁移、不 fallback。

E05/E06 修复后同 fixture 对照：`fixed`。历史缺陷断言翻转，新的生产路径测试覆盖真实 service/storage/domain。

## B4.3/B4.4 终态与恢复

- 普通单事件不可行时只隔离被测量出的安全 culprit prefix；
- terminal provenance 与 cursor 位于同一 sessions record 的一次 put 原子边界；
- 无 stable 时也可写 terminal，绝不伪造 revision/stable；
- restart 恢复 effective cursor，不重复付费；
- authority 超限不 skip，记录 `blockAuthorityFact` 并等待 generation 变化；
- base-only 超限不归咎任何 event，记录 `blockBaseOverBudget`；
- transient/aborted 不推进、不写终态；
- generation 仅由 base/filter/budget 变化重开；
- 新域 `context_enhancement_task_state_v2` version 2，旧 v1 文档不读、不迁移、不重写，错戳 fail closed。

主代理独立验证：新增 B4.3 12/12、B4.4 10/10、最终全部 task-state 275/275、typecheck 通过。

## 保留风险

- B1 多实例写者仲裁仍 `blocked-upstream`；
- B5 Stable 固定槽位注入未实现；
- ordinary facts 里若含旧 objective 文本，事实级过滤仍未做；
- artifact parity 未修；
- 未做真实 provider/GUI 与高并发 urgent 压测。

```text
B4.1 = completed
B4.2 = completed
B4 = completed-clean-break-single-instance
E05 controlled symptom = fixed
E06 controlled symptom = fixed
E07 controlled symptom = fixed
B1 = blocked-upstream
```
