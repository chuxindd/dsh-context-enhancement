# B6 主代理验收

## 结论

B6 验收通过（单实例安全语义）：replacement provenance 已独立写入 Session 邻接事件并可在 audit 缺失时恢复；audit 状态机可恢复、降级和诊断。

## B6.1 provenance

- tool-summary / tool-pruned provenance 写入官方 `compaction/prune` shadow-price event；
- replacement 自身保持 DSH rewrite contract，不附加非法 data；
- sourceEventSeqs、coveredSeqs、groupId、generation、digest 严格校验；
- provenance 缺失、损坏、外来或冲突时为 `unknown-replacement`，不当作 original；
- JSONL restart 后不依赖 audit 即可分类；
- audit 只能收紧判断，不能创建 replacement 类型。

主代理复验：provenance replay + replacement 17/17，typecheck 通过。

## B6.2 audit recovery

- 状态机覆盖 `open/success/fallback/failure/aborted/repaired`；
- 重启后无人拥有的 open 行转为 `aborted`，不伪造成功；
- `repaired` 只能由 Session provenance 证明；
- audit 丢失但 provenance 完整时继续正确分类并避免重复处理；
- audit 损坏/冲突时 fail closed；
- audit 写入失败有界重试，归约不被错误取消；
- op1/op2 互斥在重启后仍成立；
- 多实例 LWW/CAS 不可检测继续保留 B1 blocker。

主代理复验：B6.2 相关 6 files、43/43，typecheck 通过。

## 保留风险

- B1 多实例仍 `blocked-upstream`；
- B7 fork/resume cursor/bootstrap 未实现；
- B8 artifact parity 未修；
- 历史 E01 未重跑、不覆盖；
- 未测真实 provider usage。

```text
B6.1 = completed
B6.2 = completed
B6 = completed-single-instance
B1 = blocked-upstream
```
