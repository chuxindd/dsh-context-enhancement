# B4.1 主代理验收

## 结论

B4.1 startup backlog 修复验收通过。B4 整体尚未完成；Goal/TODO 权威语义留给 B4.2。

## 主代理独立验证

- `tests/task-state-startup-backlog.spec.ts`
- `tests/task-state-restart-recovery.spec.ts`
- `tests/task-state-worker.spec.ts`

合计 26/26 通过。

`pnpm run typecheck`：exit 0。

## E07 修复后对照

证据目录：`审计资料/实验结果/B4.1-E07修复后回归/`。

- 修复前：2010 ms 窗口内 0 请求，revision 恒为 1；
- 修复后：首个 startup wave 在 100 ms 样本出现；
- backlog `[6,7,8]` 被单独折叠；
- audit trigger 为 `startup`；
- 发布顺序为 `rev1(cursor3) -> rev2(cursor8)`；
- 后续 seq10 独立进入下一更新，不再把历史 backlog 携带进去。

## 接受的不变量

- hydration 完成后按真实 filter 计算 cursor 上方 backlog；
- 达到 `minEvents` 时只调度一个 startup wave；
- 不依赖新事件唤醒；
- 同步 session/event 栈上不运行模型或存储；
- lifecycle、runtime identity、admission 与 disabled 四重围栏；
- 空 backlog、低于阈值、domain open 失败或 lifecycle 不匹配时不调度；
- durable audit 区分 `startup | threshold | trailing | manual`。

## 保留风险

- B1 多实例安全仍为 `blocked-upstream`；
- B4.2 Goal/TODO 权威语义未实现；
- artifact parity 仍失败；
- 未做真实 provider/GUI 时延实验。

```text
B4.1 = completed
E07 controlled symptom = fixed
B4 overall = in-progress
```
