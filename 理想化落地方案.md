# DSH 长会话分层压缩与稳定任务状态：理想化落地方案

> 本文基于《压缩方案4》的大方向，以及 `调查资料/00`–`21` 对 DSH 与当前插件的源码调查，固定一套可用于后续审计、实现与验收的理想化方案。
>
> 本文不是对当前插件代码的修补清单。后续应先用本文与当前实现做差异矩阵，再形成开发 PLAN。

---

## 1. 方案定位

系统由两个彼此独立、通过 Session 事实关联的子系统组成：

1. **主线程分层压缩器**：控制当前模型可见 surface 的体积与粒度。
2. **稳定任务状态服务（Stable Task State）**：在后台维护当前任务事实、目标、决策、约束、风险和继续执行信息。

两个系统不得混为一条摘要链：

- 主线程压缩通过 Session replacement 改变当前 surface；
- 稳定任务状态不替换主线程历史，只按受控预算注入主 Agent；
- 主线程摘要不能成为稳定任务状态的二手事实来源；稳定任务状态以原始 Session 事件和权威 Goal/TODO 事件为输入；
- 两者失败互不阻塞。

---

## 2. 保留《压缩方案4》的核心原则

以下方向保持不变：

1. 内容随时间经历“近区 → 工具压缩区 → 遗忘区”的生命周期；
2. 近区保存当前工作现场，普通维护不处理近区；
3. 工具信息先形成可验证、可纠偏的中间记忆，再进入遗忘压缩；
4. 工具要点化、工具裁剪、遗忘压缩是独立操作；
5. 同一内容不得在同一维护轮连续经历工具 replacement 和遗忘压缩；
6. 遗忘压缩从最老端选择有限批次，不一次吞掉整个遗忘区；
7. replacement 只改变 surface，原始 Session 事件保留；
8. 每项操作提交后重新计量和划区；
9. overflow 逐级放宽保护，不反向污染普通维护；
10. 稳定任务状态独立增量更新，失败时主 Agent继续读取最近成功版本。

---

## 3. 相对《压缩方案4》的修订点

### 3.1 比例作用于“可用输入预算”，不是直接作用于模型窗口 `C`

原文以 `C` 作为区域和水位的统一分母。实际 DSH 请求还包含 system、tools、runtime context、provider framing 和输出预留，因此直接用 `C` 会让 surface 区域与真实请求压力错位。

固定定义：

```text
C       模型 contextWindow
O       本次请求输出预留；pre-step 不可得时采用策略保守值
F       provider framing 安全余量
E       非 surface 输入成本：system + tools + 当前非 surface 信封
I       Stable Task State 注入预算上限
G       可供 Session surface 使用的安全预算

G = max(0, C - O - F - E - I)
```

- 调度压力使用完整请求投影 `T` 与安全总输入预算 `C-O-F`；
- 三区边界使用 surface 节点 token 与 `G`；
- 不能用累计 Session 日志 token、事件条数或 UI 百分比划区。

### 3.2 比例是初始策略，不是不可校准常量

保留默认 `20%/50%/40%/70%/80%` 作为首版策略，但它们必须：

- 以目标模型、输出策略和工具信封校准；
- 在审计日志中记录最终绝对 token 边界；
- 可按模型策略覆盖；
- 通过运行时净释放、维护频率和任务质量指标验证。

因此验收基于“最终绝对边界和行为”，不只检查配置值是否存在。

### 3.3 压力维护不允许 whole-zone 一次压缩

原文要求“有限多个遗忘批次，每批后重计量”。固定采用这一规则：

- 压力维护仍使用与普通遗忘维护相同的有界批次选择器；
- 每批不得越过遗忘区；
- 每批后重算请求压力、`G` 和三区；
- 达到明确释放目标、批次 guard 或无安全候选时停止；
- 不允许仅因摘要比输入少 1 token 就视为有效压力释放。

### 3.4 增加明确的释放目标与最小净收益

原文提到“回落到安全水位”，但未固定安全目标和低收益处理。

固定定义：

```text
U = projectedTotalInput / safeTotalInput

普通工具维护目标：完成有限工具候选后结束，不追水位
普通遗忘维护目标：最多一个批次
压力维护目标：U <= pressureExitRatio
默认 pressureExitRatio = forgetMaintenanceRatio = 0.70
```

每个语义批次必须同时满足：

```text
netReleasedTokens = beforeProjectedTotal - afterProjectedTotal
netReleaseRatio = netReleasedTokens / selectedRouteTokens

netReleasedTokens >= minNetReleaseTokens
或 netReleaseRatio >= minNetReleaseRatio
```

建议首版：

```text
minNetReleaseTokens = 1024
minNetReleaseRatio  = 0.15
```

若摘要无法满足，批次不提交并记录 `low-yield`；压力状态转向后续安全候选或在 guard 后结束，不反复处理同一范围。

### 3.5 区域隔离是普通维护的硬约束

固定采用原文的严格含义：

- 操作①、②仅处理工具压缩区；
- 操作③仅处理遗忘区；
- `70%+` 不得先在遗忘区执行工具摘要或工具裁剪；
- pressure 只增加遗忘批次数量，不扩大操作③边界；
- 跨入工具区和近区只属于 overflow 恢复，并必须记录恢复等级。

### 3.6 Step 安全边界从事件结构重建，不依赖 message payload 猜测

DSH 的部分 `user/message` 不带 turn/step 字段。固定要求：

- 通过 `turn/start|end`、`step/start|end` 和 surface 节点对应 event seq 建立位置级 step span 索引；
- 工具调用/结果使用 call id 建立配对索引；
- 边界调整使用 span 索引，不使用某一种 message payload 是否携带 turn/step 来判断；
- 找不到完整安全 span 时宁可本轮不处理，不截断步骤。

### 3.7 五值分类改为“持久 provenance + 轮次结果”，不要求永久保存五种终态

原文要求逐组/逐结果五值分类。理想落地保留其调度价值，但不把所有瞬时分类当作长期权威状态。

每轮操作①返回：

```text
ToolGroupOutcome {
  groupId
  sourceSeqs
  status: summarized | notEligible | oversized | failed | unchanged
  resultOutcomes[]
  replacementSeqs[]
  retryable
  reasonCode
}
```

- 轮内分类用于精确构造操作②候选；
- 已提交 replacement 的长期身份由 Session 日志中的 provenance 事实恢复；
- 独立 audit 只保存模型请求与诊断，不作为判断 replacement 类型的唯一来源；
- audit 丢失不能让已提交 tool-summary 被误认成 original。

### 3.8 Overflow 的工具裁剪也要分区逐级放宽

原文“裁剪全部尚未处理的原始大工具结果”容易直接触及近区。固定改为：

1. 裁剪遗忘区和工具区中的原始大工具结果；
2. 分批压缩遗忘区；
3. 遗忘区无安全候选后，压缩工具区最老端；
4. 工具区耗尽后，才允许裁剪/压缩近区；
5. 近区原始工具结果只有超过 `hardLimitChars`，或已进入 `overflow-recent`，才可处理。

每次跨级必须证明前一级“无安全候选或已达到本次 guard”，并持久记录等级。

### 3.9 Checkpoint 政名为 Stable Task State，并定义与 Goal/TODO 的权威关系

为避免与主线程 compaction checkpoint 混淆，本文统一称 **Stable Task State**。

权威关系：

- DSH Goal 是当前目标和 phase 的权威；
- DSH TODO 是当前行动清单的权威；
- Stable Task State 是事实、决策、约束、风险、证据和继续执行叙事的权威；
- Stable Task State 可以引用 Goal/TODO 快照，但不能形成第二份可独立漂移的目标或 TODO 权威。

### 3.10 Goal/TODO 权威变更使用高优先级触发

原文只有有效事件计数触发。固定增加事件优先级：

```text
urgent:
  goal/change（create/edit/pause/resume/complete/blocked/clear）
  todo/write（包括空表）
  用户明确纠正目标或撤销既有决策

normal:
  其他有效进展事件
```

- urgent 事件在当前 worker 完成后立即合并为下一 wave，不等待 `minEvents`；
- 多个连续 urgent 事件可短时间 debounce/coalesce；
- 空 TODO 表必须被采集，表达“当前 TODO 已清空”；
- Stable Task State 中的目标视图必须按最新 goal revision 替换，而不是追加多个目标叙事。

### 3.11 稳定状态存储不使用 JSON `single` 作为多实例权威后端

若产品支持两个 DSH 访问同一 `$DSH_HOME`，整文档 LWW 不满足要求。

固定支持级别：

- **单实例模式**：JSON storage 可作为开发/兼容模式，但启动时必须声明 `singleWriterOnly`；
- **受支持的多实例模式**：使用记录级后端（优先 SQLite）并实现 revision CAS、busy retry 和重新读取；
- 如果暂时不能实现多实例协调，必须 fail closed：同一 home 获取实例租约失败时禁止第二写者启动 Stable Task State 和工具审计写服务，不能静默 LWW。

Stable record 的 key 至少包含：

```text
sessionId + lifecycleIdentity(createdAt,cwd,isSeeded,inheritedEventCount)
```

### 3.12 Stable 与 audit 的提交顺序增加 CAS 和幂等提交协议

固定协议：

1. 创建 `open audit(requestId, baseRevision, windowEnd, lifecycle)`；
2. 调用模型并校验 candidate；
3. 用 `expectedRevision=baseRevision` CAS 写 stable；
4. stable 写成功后发布内存版本；
5. 完成 audit；完成 audit 失败只需 repair；
6. 重启时按 stable revision 与 audit request id 修复，不重跑模型。

stable 是权威；audit 永远不能覆盖或回滚 stable。不同进程不能同时从同一 baseRevision 提交两个 `N+1`。

### 3.13 Stable 注入采用“固定槽位 replacement”，不无限追加 runtime context 快照

DSH runtime context 文本变化会追加 user-role snapshot。固定要求：

- 为 Stable Task State 保留一个可识别的注入节点；
- stable 变更时，通过 Session replacement 替换上一个注入节点，而不是永久追加新快照；
- replacement 保留 stable revision、sourceCursor 和生命周期引用；
- 若本 step 尚无 stable 或文本未变化，不写新节点；
- 注入 token 受独立预算 `I` 限制，并进入请求预算账本；
- Stable 注入节点不得成为 Stable Task State 的输入事件，避免自反馈；
- Fork 时明确选择：默认基于子 Session 的事件重建，不直接共享父 stable；可用父 stable 作为一次性 bootstrap 候选，但必须标记 inherited 并在子第一批成功后替换。

### 3.14 可观测性从“影子 token”升级为完整维护账本

“压缩 N 条、约 N tokens”不足以证明释放成功。每次维护必须记录：

```text
model / contextWindow / outputReserve / framingReserve
before: total, surface, envelope, stableInjection
absolute zone boundaries
trigger and operation
candidate range, source kinds, routeTokens, heuristicTokens
summary input tokens, output tokens
replacement seqs and source seqs
before/after projected total and surface
netReleasedTokens / netReleaseRatio
stopReason / guard / recoveryLevel
surface generation and lifecycle identity
```

UI 至少区分：tool-summary、tool-pruned、history-summary、overflow；展示净释放，不把 `shadowedTokenCount` 称为释放量。

---

## 4. 主线程压缩详细设计

### 4.1 数据模型

为当前 surface 构建只读维护快照：

```text
MaintenanceSnapshot {
  sessionId
  lifecycleIdentity
  surfaceGeneration
  nodes[]: {
    position
    eventSeq
    messageKind
    routeTokens
    heuristicTokens
    sourceKind
    sourceEventSeqs[]
    turnSpanId?
    stepSpanId?
    toolCallIds[]
  }
  requestBudget
  zones
}
```

来源类型：

```text
original
stable-state-injection
tool-summary
tool-pruned
history-summary
unknown-replacement
```

`unknown-replacement` 默认不可参与普通语义重压缩，除非有明确兼容策略。

### 4.2 预算和三区

```text
safeTotalInput = C - O - F
projectedTotal = tokenMeter.totalTokens + explicitStableInjectionAdjustment
G = safeTotalInput - E - I

recentTailTokens = recentRatio * G
forgetTailTokens = forgetBoundaryRatio * G
```

从 surface 尾部向前累计 route token：

- `[recentStart, end)` 为近区；
- `[forgetEnd, recentStart)` 为工具压缩区；
- `[0, forgetEnd)` 为遗忘区。

随后：

1. 应用 open turn + 最近完整 turn 地板；
2. 用 step span 和 tool pairing 向历史方向调整边界；
3. 三区必须互不重叠；
4. 将绝对边界写入维护账本。

### 4.3 普通调度状态机

```text
U < 0.40
  stop(below-tool-maintenance)

0.40 <= U < 0.70
  op1(tool zone)
  remeasure
  op2(tool zone, explicit outcomes)
  stop

0.70 <= U < 0.80
  op1(tool zone)
  remeasure
  op2(tool zone)
  remeasure
  op3(oldest bounded forget batch, max 1)
  stop

U >= 0.80
  op1(tool zone)
  remeasure
  op2(tool zone)
  remeasure
  while U > pressureExitRatio and batches < maxPressureBatches:
      op3(oldest bounded forget batch)
      remeasure
      if no-progress/low-yield/no-safe-candidate: break
  stop(with explicit reason)
```

压力维护绝不跨出遗忘区。达到 batch guard 后即结束本 step，下一 step 重新规划；不能把工具区当普通 pressure 的备用遗忘区。

### 4.4 操作①：工具要点化

候选必须满足：

- 完整工具调用与结果配对；
- 同一安全 step/turn burst；
- 整组完全位于工具压缩区；
- 来源为 original；
- 满足最小结果数、字符、token；
- 完整摘要请求不超过输入预算；
- 每轮组数有限。

模型输出按 result 结构化，校验：

- 所有调用 id 和结果一一覆盖；
- 事实只能来自输入；
- 文件、标识符、错误、未决问题保持来源引用；
- 每个 replacement 严格缩减；
- replacement provenance 随 Session 事件持久。

失败只影响该组；返回轮次分类供操作②使用。

### 4.5 操作②：工具裁剪

候选来源：

- 操作①本轮 `notEligible/oversized/failed` 的原始结果；
- 未形成完整组的孤立原始大结果；
- 已在工具区、超过字符阈值的原始结果。

禁止：

- 扫描全 surface；
- 裁剪已摘要、已裁剪或 unknown replacement；
- 普通维护处理近区或遗忘区。

裁剪保留 head/tail、原始体积、调用 id 和 source refs，并执行严格缩减断言。

### 4.6 操作③：遗忘批次

选择器：

1. 固定从 position 0 开始；
2. 终点不得超过当前遗忘区边界；
3. 在 `targetBatchTokens` 附近选首个完整安全边界；
4. 超过 `maxBatchTokens` 时向前回退；
5. 第一个完整片段即超上限则返回 `first-safe-span-oversized`，不发模型请求；
6. 排除本轮 replacement、未经过 `minReentryTurns` 的 tool/history summary、unknown replacement。

摘要内容保留阶段工作、决策、约束、纠偏、错误、当前进展和可溯源事实。提交必须通过生命周期、generation、source node、step/tool 边界和最小净收益校验。

### 4.7 Overflow 状态机

```text
request-error CONTEXT_WINDOW_EXCEEDED
→ capture generation
→ level 0: prune original large results in forget+tool zones
→ remeasure; recovered => retry
→ level 1: bounded forget batches
→ remeasure; recovered => retry
→ only when no safe forget candidate:
     level 2: bounded oldest tool-zone history batches
→ only when tool zone exhausted:
     level 3: recent recovery
       - hard-limit tool pruning first
       - then bounded oldest recent span as final action
→ generation advanced => retry, else propagate original error
```

每一级都有独立 batch guard、stop reason 和持久 recovery level。近区处理不是普通 replacement 的永久授权。

---

## 5. Stable Task State 详细设计

### 5.1 权威数据结构

```text
StableTaskState {
  schemaVersion
  revision
  lifecycleIdentity
  sourceCursor
  coveredRange
  digest

  goalView {
    goalId?
    goalRevision?
    phase?
    objective?
    status: current | cleared | none
  }

  todoView {
    sourceSeq?
    items[]
    status: current | cleared | none
  }

  facts[]
  decisions[]
  constraints[]
  risks[]
  evidenceRefs[]
  continuation {
    currentFocus
    openWork[]
    nextActions[]
  }
}
```

Goal/TODO view 由最新权威事件确定，不交给模型自由累积。模型只合并事实、决策、约束、风险和 continuation；Host 在提交时覆盖 goalView/todoView。

### 5.2 输入与触发

事件分两类：

- Authority events：goal/change、todo/write、用户明确撤销/纠正；
- Progress events：assistant、tool、turn、决策相关 command、模型选择和压缩生命周期等。

worker 状态：

```text
pendingHighWater
pendingEligible
urgentRequested
active
followUpRequested
chain
lifetimeToken
```

触发：

- normal：达到 `minEvents`；
- urgent：短 debounce 后立即 wave；
- startup/resume：若 stable cursor 后存在 eligible backlog，主动 schedule 一次；
- active 期间新需求合并为一个 follow-up wave；
- infeasible 单事件进入确定性降级投影，不允许永久卡住 cursor。

### 5.3 后台生命周期

- 在 `agent/created` 上为每个 live Session 创建 runtime；
- 用 `agent.ctx.effect` 持有 worker 和 AbortController；
- 事件 observer 同步只抬水位并 queue microtask；
- 模型更新优先使用受监管 job 或自持单飞 runtime；
- dispose 关闭 admission、abort、等待 chain；
- 每次提交前检查 live agent/session identity、lifecycle、base revision 和 worker token；
- HMR 新 runtime 不接受旧 runtime 的完成回调。

更新失败不注入错误状态，继续使用上一 stable；有限基础设施重试后等待后续 wave。

### 5.4 持久化

首选记录级 SQLite domain：

```text
stable(sessionLifecycleKey PRIMARY KEY, revision, cursor, digest, payload)
audit(requestId PRIMARY KEY, sessionLifecycleKey, baseRevision, targetRevision, status, payload)
lease/session writer metadata（若多实例支持）
```

必要约束：

- stable CAS：`WHERE revision = expectedRevision`；
- busy 有界退避；
- 冲突后重新读 stable、重新折叠未处理窗口；
- audit 与 stable 尽量单事务；若基础设施不提供跨表事务，仍按 stable 权威 + audit repair；
- JSON 兼容模式只允许单写者，并明确拒绝第二实例写服务。

### 5.5 Fork/Resume

- Resume：相同 SessionId 和 lifecycle identity 时继续 stable；identity 不匹配视为新生命周期，不覆盖旧记录。
- Fork：默认创建独立 stable，cursor 起点为子 own events 边界；父 stable 可作为带来源标记的 bootstrap 输入，但不是子 stable 的已提交版本。
- 子第一批更新必须读取继承前缀和 own events，形成自己的 revision 1。
- 不以 `firstLiveSeq` 判断继承边界，使用 `inheritedEventCount`/`ownEvents()`。

### 5.6 注入

渲染优先级：

1. 当前 Goal；
2. 当前 TODO 与 focus/next actions；
3. constraints/risks；
4. decisions/facts；
5. evidence refs。

按 token 预算截断，记录 `revision/sourceCursor/digest/staleness`。如果 stable cursor 落后当前 eligible high-water，显示明确 stale marker。

注入采用固定节点 replacement，避免每次更新永久追加快照。该节点在来源索引中标为 `stable-state-injection`，普通工具操作不处理；当其老化进入遗忘区时，也只允许最新节点存在，旧节点已由 replacement 遮蔽。

---

## 6. 持久 provenance 与审计

### 6.1 Session 内必须可恢复的事实

以下信息不能只存在独立 storage audit：

- replacement 的来源类型；
- source event seqs；
- replacement range；
- 操作编号和维护轮 id；
- recovery level；
- stable injection revision/cursor；
- 本轮排除和最早重入 turn。

这些应通过 DSH 可兼容的 Session event metadata 或紧邻 shadow event 持久化。插件卸载后，DSH 仍能安全忽略未知诊断事件并正确折叠 replacement。

### 6.2 独立审计用途

独立 audit 保存：

- 模型请求与原始输出；
- schema/semantic failure；
- token 账本；
- storage commit 和 repair；
- 调度候选与排除原因。

它用于诊断和合规，不决定当前 surface 的类型，也不作为第二份 stable。

---

## 7. 配置基线

保留原文默认值，但新增以下配置：

```text
预算
  outputReserveTokens         模型策略值
  providerFramingReserve      模型策略值
  stableInjectionMaxTokens    2000

区域
  recentRatio                 0.20
  forgetBoundaryRatio         0.50

水位
  toolMaintenanceRatio        0.40
  forgetMaintenanceRatio      0.70
  pressureRatio               0.80
  pressureExitRatio           0.70

语义批次收益
  minNetReleaseTokens         1024
  minNetReleaseRatio          0.15

工具摘要
  minGroupResults             2
  minGroupChars               12000
  minGroupTokens              2000
  maxGroupTokens              12000
  maxGroupsPerPass            2
  maxSummaryTokens            1200

工具裁剪
  thresholdChars              8192
  headChars                   4096
  tailChars                   1024
  hardLimitChars              必须配置模型安全值

遗忘
  targetBatchTokens           16000
  maxBatchTokens              24000
  maxMaintenanceBatches       1
  maxPressureBatches          2
  minReentryTurns             1

Stable Task State
  minEvents                   20
  urgentDebounceMs            200
  maxEvents                   200
  maxInputBytes               60000
  maxOutputTokens             4000
  timeoutMs                   120000
  maxInfraRetries             2
  startupBacklogUpdate        true
  multiInstanceMode           sqlite | single-writer-json
```

校验关系：

```text
0 < recentRatio < forgetBoundaryRatio < forgetMaintenanceRatio
forgetMaintenanceRatio <= pressureExitRatio < pressureRatio <= 1
maxMaintenanceBatches >= 1
maxPressureBatches >= 1
hardLimitChars > thresholdChars
```

`pressureExitRatio` 可等于 forget maintenance 水位，但不得接近 pressure trigger 到只留一次工具调用的空间。

---

## 8. 验收不变量

除保留《压缩方案4》第 14 节外，增加：

1. 区域比例基于 `G`，触发水位基于完整请求安全预算；两种口径在账本中可对账。
2. pressure 不执行 whole-zone 压缩，不跨遗忘区。
3. 每个语义批次满足最小净收益，否则不提交。
4. step 边界由结构事件索引恢复，不依赖 message payload 是否有 turn/step。
5. provenance 可仅凭 Session 日志恢复；删除 audit 不改变 surface 类型判断。
6. overflow 近区处理只能发生在 `overflow-recent` 或 hard-limit 条件。
7. Goal/TODO urgent 变更不等待 normal `minEvents`。
8. 空 TODO 表推进 stable 并清空 todoView。
9. stable CAS 冲突不能产生两个相同 revision 的不同内容。
10. 两进程同 home 不允许静默整文档覆盖。
11. resume 能处理 stable cursor 后 backlog，无需等待新事件。
12. fork 不错误共享父 stable 权威版本。
13. stable 注入更新不会无限增加可见快照节点。
14. UI 展示净释放与 stop reason，不能把 shadowed heuristic token 当净释放。
15. 所有后台任务在 dispose 后不能提交到新 lifecycle/runtime。

---

## 9. 验证计划要求

正式实现前后必须具备以下验证：

### 静态/单元

- surface seq 非单调但位置分区正确；
- step/tool pairing 边界；
- 操作①失败后操作②继续；
- 操作①成功集合与操作②候选互斥；
- 同轮 replacement 不进入操作③；
- pressure 多批次回落到 exit ratio；
- low-yield 不提交；
- overflow 跨级证明；
- Goal edit、clear、TODO 空表 immediate wave；
- stable CAS 和 audit repair；
- fork/resume identity。

### 运行时回放

- 大量工具调用在 40%–70% 阶段提前释放；
- 80% 压力维护后可容纳多次正常工具调用，不形成“一工具一压缩”；
- 记录每批 before/after/net release；
- 注入 stable 多次更新后 surface 中仅保留最新可见节点。

### 多实例

- 同 home 双进程交替提交，不丢 unrelated sessions；
- 同一 session 同 revision 竞争只允许一方提交；
- 外部更新可在约定时机重新读取；
- 不支持的单写 JSON 模式第二实例 fail closed。

### 崩溃恢复

- stable 已提交、audit 未完成；
- audit open、模型未完成；
- Session replacement bracket 中断；
- projection/cache stale；
- restart 时存在 cursor backlog。

---

## 10. 后续审计使用方式

后续对当前插件的评审按三层比较：

```text
《压缩方案4》大方向
        ↕
本文固定的 DSH 理想化落地方案
        ↕
当前插件实现与运行证据
```

差异分类：

1. 当前实现违反必须不变量；
2. 当前实现与本文不同，但有等价安全机制；
3. 当前实现受 DSH 限制，需要基础设施改造；
4. 《压缩方案4》原文需要按本文修订；
5. 只有静态风险，仍需运行实验确认；
6. 纯参数问题，可通过校准解决。

只有完成这一差异矩阵后，才编制文件级开发 PLAN。
